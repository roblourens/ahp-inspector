---
phase: 02
plan: 03
type: execute
wave: 1
depends_on: ["02-00"]
autonomous: true
requirements: [TIME-02, TIME-03]
requirements_addressed: [TIME-02, TIME-03]
files_modified:
  - packages/ui/src/components/timeline/cells/DirectionGlyph.tsx
  - packages/ui/src/components/timeline/cells/DirectionGlyph.test.tsx
  - packages/ui/src/components/timeline/cells/KindTag.tsx
  - packages/ui/src/components/timeline/cells/KindTag.test.tsx
  - packages/ui/src/components/timeline/cells/ActionDot.tsx
  - packages/ui/src/components/timeline/cells/ActionDot.test.tsx
  - packages/ui/src/components/timeline/cells/StatusCell.tsx
  - packages/ui/src/components/timeline/cells/StatusCell.test.tsx
  - packages/ui/src/components/timeline/cells/LatencyCell.tsx
  - packages/ui/src/components/timeline/cells/LatencyCell.test.tsx
  - packages/ui/src/components/timeline/cells/PayloadPreview.tsx
  - packages/ui/src/components/timeline/cells/PayloadPreview.test.tsx
must_haves:
  truths:
    - "DirectionGlyph returns → (color --dir-c2s) for c2s, ← (--dir-s2c) for s2c, · for unknown"
    - "KindTag renders the §5.2 short label inside a pill with background color-mixed from --kind-{kind}, uppercase, with a tooltip"
    - "ActionDot returns null when actionFamily is null; otherwise a 6px circle colored from --action-{family}"
    - "StatusCell maps status → label per §5.5: ok→'2xx' (success), error→'ERR' (destructive), pending→'…', orphan→'ORPHAN' pill (warning), unmatched→'TIMEOUT' (warning), n/a→'—'"
    - "LatencyCell renders {ms}ms for <1000, {(ms/1000).toFixed(1)}s for ≥1000, '—' for null; band background uses --latency-{band}; tabular-nums"
    - "PayloadPreview renders the text in --color-text-muted mono with text-overflow: ellipsis; white-space: nowrap; overflow: hidden"
    - "Every cell file references only var(--*) tokens (no hex literals)"
  artifacts:
    - path: "packages/ui/src/components/timeline/cells/DirectionGlyph.tsx"
      provides: "Direction arrow renderer"
      exports: ["DirectionGlyph"]
    - path: "packages/ui/src/components/timeline/cells/KindTag.tsx"
      provides: "Kind pill renderer"
      exports: ["KindTag"]
    - path: "packages/ui/src/components/timeline/cells/ActionDot.tsx"
      provides: "Action-family dot renderer"
      exports: ["ActionDot"]
    - path: "packages/ui/src/components/timeline/cells/StatusCell.tsx"
      provides: "Status label renderer per §5.5"
      exports: ["StatusCell"]
    - path: "packages/ui/src/components/timeline/cells/LatencyCell.tsx"
      provides: "Latency text + band-colored bar renderer"
      exports: ["LatencyCell"]
    - path: "packages/ui/src/components/timeline/cells/PayloadPreview.tsx"
      provides: "Truncated payload preview renderer"
      exports: ["PayloadPreview"]
  key_links:
    - from: "packages/ui/src/components/timeline/cells/*.tsx"
      to: "packages/ui/src/styles/tokens.css"
      via: "var(--dir-*) / var(--kind-*) / var(--action-*) / var(--latency-*) / var(--color-*)"
      pattern: "var\\(--"
    - from: "packages/ui/src/components/timeline/cells/*.tsx"
      to: "@ahp-inspector/core"
      via: "import type { LatencyBand, ActionFamily, KindTag, DirGlyph, Status }"
      pattern: "from \"@ahp-inspector/core\""
---

<objective>
Build the six hand-rolled timeline cells (DirectionGlyph, KindTag, ActionDot,
StatusCell, LatencyCell, PayloadPreview), each in its own file with a sibling
Vitest. These are dependency-free leaves consumed by `EventRow` in Plan 02-04.

Purpose: Splitting the cells into their own plan keeps Plan 02-04 (which wires
EventRow + ParseErrorRow + TimelineList + TimelineRegion + states + App.tsx
routing) under the 15-file quality threshold.

Output: 12 files under `packages/ui/src/components/timeline/cells/`, all green
under `pnpm vitest run packages/ui/src/components/timeline/cells`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-vertical-slice-cli-server-timeline/02-RESEARCH.md
@.planning/phases/02-vertical-slice-cli-server-timeline/02-VALIDATION.md
@.planning/phases/02-vertical-slice-cli-server-timeline/02-UI-SPEC.md
@.planning/phases/02-vertical-slice-cli-server-timeline/02-00-PLAN.md
@packages/core/src/row-projection.ts

<interfaces>
From @ahp-inspector/core (Plan 02-00):
```ts
export type Direction = "c2s" | "s2c" | "unknown";
export type DirGlyph = "→" | "←" | "·";
export type KindTag = "REQ" | "RES" | "NTF" | "ACT" | "BAD" | "LOG";
export type ActionFamily = "text" | "tool-call" | "tool-result" | "status" | "unknown";
export type LatencyBand = "fast" | "normal" | "slow" | "critical";
export type Status = "ok" | "error" | "pending" | "unmatched" | "orphan" | "n/a";
```

UI-SPEC §5 (lines 153–222) — visual encoding rules.
UI-SPEC §7 (lines 270–318) — column widths for context (used by Plan 02-04 EventRow).
UI-SPEC §12 (lines 490–567) — token names this plan references.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DirectionGlyph + KindTag + ActionDot (+ tests)</name>
  <files>packages/ui/src/components/timeline/cells/DirectionGlyph.tsx, packages/ui/src/components/timeline/cells/DirectionGlyph.test.tsx, packages/ui/src/components/timeline/cells/KindTag.tsx, packages/ui/src/components/timeline/cells/KindTag.test.tsx, packages/ui/src/components/timeline/cells/ActionDot.tsx, packages/ui/src/components/timeline/cells/ActionDot.test.tsx</files>
  <read_first>
    - .planning/phases/02-vertical-slice-cli-server-timeline/02-UI-SPEC.md (§5.1 direction lines 156–169, §5.2 kinds lines 171–187, §5.4 actions lines 200–210)
    - packages/core/src/row-projection.ts (Plan 02-00 — DirGlyph, KindTag, ActionFamily types)
  </read_first>
  <behavior>
    - `DirectionGlyph({ direction })`: c2s → `→` color `var(--dir-c2s)`; s2c → `←` color `var(--dir-s2c)`; unknown → `·` color `var(--color-text-muted)`. Width 16, inline-block, text-align center.
    - `KindTag({ kind })`: uppercase pill, width 44 (height 16 line), padding 0 4px, font-size `var(--text-ui-muted-size)`, background `color-mix(in srgb, var(--kind-{kind-var}) 20%, transparent)`, color `var(--kind-{kind-var})`. Mapping: `REQ→request, RES→response, NTF→notification, ACT→action, BAD→parse-error, LOG→action`. Tooltip via `title` attr: `Request`, `Response`, `Notification`, `Action`, `Parse error`, `Log`.
    - `ActionDot({ family })`: returns `null` when family is null; otherwise `<span title="Action family: {family}" style={{ display: "inline-block", width: 6, height: 6, borderRadius: "50%", background: actionVar(family) }} />`. `actionVar` maps text→--action-text, tool-call→--action-tool-call, tool-result→--action-tool-result, status→--action-status, unknown→--action-unknown.
  </behavior>
  <action>
**1. Create `packages/ui/src/components/timeline/cells/DirectionGlyph.tsx`:**
```tsx
import type { Direction } from "@ahp-inspector/core";

const GLYPH: Record<Direction, string> = { c2s: "→", s2c: "←", unknown: "·" };
const COLOR: Record<Direction, string> = {
  c2s: "var(--dir-c2s)",
  s2c: "var(--dir-s2c)",
  unknown: "var(--color-text-muted)",
};

export function DirectionGlyph({ direction }: { direction: Direction }): JSX.Element {
  return (
    <span
      data-testid="dir-glyph"
      data-direction={direction}
      style={{ display: "inline-block", width: 16, textAlign: "center", color: COLOR[direction] }}
    >
      {GLYPH[direction]}
    </span>
  );
}
```

**2. Create `packages/ui/src/components/timeline/cells/DirectionGlyph.test.tsx`** — render each direction; assert glyph text and `data-direction` attribute. Cases: c2s→"→", s2c→"←", unknown→"·".

**3. Create `packages/ui/src/components/timeline/cells/KindTag.tsx`:**
```tsx
import type { KindTag as KindTagValue } from "@ahp-inspector/core";

const VAR: Record<KindTagValue, string> = {
  REQ: "--kind-request",
  RES: "--kind-response",
  NTF: "--kind-notification",
  ACT: "--kind-action",
  BAD: "--kind-parse-error",
  LOG: "--kind-action",
};
const TITLE: Record<KindTagValue, string> = {
  REQ: "Request", RES: "Response", NTF: "Notification",
  ACT: "Action", BAD: "Parse error", LOG: "Log",
};

export function KindTag({ kind }: { kind: KindTagValue }): JSX.Element {
  const v = VAR[kind];
  return (
    <span
      data-testid="kind-tag"
      title={TITLE[kind]}
      style={{
        display: "inline-block",
        width: 44,
        textAlign: "center",
        padding: "0 4px",
        fontSize: "var(--text-ui-muted-size)",
        textTransform: "uppercase",
        background: `color-mix(in srgb, var(${v}) 20%, transparent)`,
        color: `var(${v})`,
        borderRadius: 2,
      }}
    >
      {kind}
    </span>
  );
}
```

**4. Create `packages/ui/src/components/timeline/cells/KindTag.test.tsx`** — assert that for each of the six KindTag values: `screen.getByTestId("kind-tag").textContent === kind` and `.title === TITLE[kind]`. Loop the six values via `it.each`.

**5. Create `packages/ui/src/components/timeline/cells/ActionDot.tsx`:**
```tsx
import type { ActionFamily } from "@ahp-inspector/core";

const VAR: Record<NonNullable<ActionFamily>, string> = {
  text: "--action-text",
  "tool-call": "--action-tool-call",
  "tool-result": "--action-tool-result",
  status: "--action-status",
  unknown: "--action-unknown",
};

export function ActionDot({ family }: { family: ActionFamily | null }): JSX.Element | null {
  if (family == null) return null;
  return (
    <span
      data-testid="action-dot"
      data-family={family}
      title={`Action family: ${family}`}
      style={{
        display: "inline-block",
        width: 6,
        height: 6,
        borderRadius: "50%",
        background: `var(${VAR[family]})`,
      }}
    />
  );
}
```

**6. Create `packages/ui/src/components/timeline/cells/ActionDot.test.tsx`** — `null` family returns null (assert `container.firstChild === null`); each non-null family renders span with matching `data-family` attribute and a `title` of `Action family: {family}`.
  </action>
  <acceptance_criteria>
    - All 6 files exist
    - `pnpm vitest run packages/ui/src/components/timeline/cells/DirectionGlyph.test.tsx packages/ui/src/components/timeline/cells/KindTag.test.tsx packages/ui/src/components/timeline/cells/ActionDot.test.tsx` exits 0
    - `rg -n "#[0-9a-fA-F]{3,8}" packages/ui/src/components/timeline/cells/` returns 0 hits
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/ui/src/components/timeline/cells/DirectionGlyph.test.tsx packages/ui/src/components/timeline/cells/KindTag.test.tsx packages/ui/src/components/timeline/cells/ActionDot.test.tsx</automated>
  </verify>
  <done>Three glyph/pill/dot cell components and tests green; only var(--*) tokens referenced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatusCell + LatencyCell + PayloadPreview (+ tests)</name>
  <files>packages/ui/src/components/timeline/cells/StatusCell.tsx, packages/ui/src/components/timeline/cells/StatusCell.test.tsx, packages/ui/src/components/timeline/cells/LatencyCell.tsx, packages/ui/src/components/timeline/cells/LatencyCell.test.tsx, packages/ui/src/components/timeline/cells/PayloadPreview.tsx, packages/ui/src/components/timeline/cells/PayloadPreview.test.tsx</files>
  <read_first>
    - .planning/phases/02-vertical-slice-cli-server-timeline/02-UI-SPEC.md (§5.5 status lines 211–219, §5.6 latency lines 220–222, §7.2 columns lines 282–315)
    - packages/core/src/row-projection.ts (Plan 02-00 — Status, LatencyBand types)
  </read_first>
  <behavior>
    - `StatusCell({ status })` renders one of:
      - ok → text `2xx` color `var(--color-success)`
      - error → text `ERR` color `var(--color-destructive)`
      - pending → text `…` color `var(--color-text-muted)`
      - orphan → uppercase pill `ORPHAN` color `var(--color-warning)` background color-mixed
      - unmatched → uppercase pill `TIMEOUT` color `var(--color-warning)` background color-mixed
      - n/a → text `—` color `var(--color-text-muted)`
    - `LatencyCell({ ms, band })`: text `{ms}ms` for ms < 1000; `{(ms/1000).toFixed(1)}s` for ms ≥ 1000; `—` for null. Wrap in 72px-wide cell with a 4px-tall background bar at the bottom using `var(--latency-{band})` when band is non-null. Class `latency` (so global `tabular-nums` applies).
    - `PayloadPreview({ text })` renders `<span className="preview" style={{ color: "var(--color-text-muted)", textOverflow: "ellipsis", whiteSpace: "nowrap", overflow: "hidden", display: "inline-block", maxWidth: "100%" }}>{text}</span>`
  </behavior>
  <action>
**1. Create `packages/ui/src/components/timeline/cells/StatusCell.tsx`:**
```tsx
import type { Status } from "@ahp-inspector/core";

interface Spec { text: string; color: string; pill?: boolean }
const SPEC: Record<Status, Spec> = {
  ok:        { text: "2xx",     color: "var(--color-success)" },
  error:     { text: "ERR",     color: "var(--color-destructive)" },
  pending:   { text: "…",       color: "var(--color-text-muted)" },
  orphan:    { text: "ORPHAN",  color: "var(--color-warning)", pill: true },
  unmatched: { text: "TIMEOUT", color: "var(--color-warning)", pill: true },
  "n/a":     { text: "—",       color: "var(--color-text-muted)" },
};

export function StatusCell({ status }: { status: Status }): JSX.Element {
  const s = SPEC[status];
  if (s.pill) {
    return (
      <span
        data-testid="status-cell"
        data-status={status}
        style={{
          display: "inline-block",
          padding: "0 4px",
          fontSize: "var(--text-ui-muted-size)",
          textTransform: "uppercase",
          color: s.color,
          background: `color-mix(in srgb, ${s.color} 20%, transparent)`,
          borderRadius: 2,
        }}
      >
        {s.text}
      </span>
    );
  }
  return (
    <span data-testid="status-cell" data-status={status} style={{ color: s.color }}>
      {s.text}
    </span>
  );
}
```

**2. Create `packages/ui/src/components/timeline/cells/StatusCell.test.tsx`** — `it.each` over the 6 statuses; assert `getByTestId("status-cell").textContent` matches expected (`2xx`, `ERR`, `…`, `ORPHAN`, `TIMEOUT`, `—`) and `data-status` matches.

**3. Create `packages/ui/src/components/timeline/cells/LatencyCell.tsx`:**
```tsx
import type { LatencyBand } from "@ahp-inspector/core";

function fmt(ms: number | null): string {
  if (ms == null) return "—";
  if (ms < 1000) return `${ms}ms`;
  return `${(ms / 1000).toFixed(1)}s`;
}

export function LatencyCell({ ms, band }: { ms: number | null; band: LatencyBand | null }): JSX.Element {
  return (
    <div
      data-testid="latency-cell"
      className="latency"
      style={{ position: "relative", width: 72, height: "var(--row-height)", display: "flex", alignItems: "center" }}
    >
      <span>{fmt(ms)}</span>
      {band && (
        <span
          data-testid="latency-bar"
          style={{
            position: "absolute",
            left: 0,
            right: 0,
            bottom: 0,
            height: 4,
            background: `var(--latency-${band})`,
          }}
        />
      )}
    </div>
  );
}
```

**4. Create `packages/ui/src/components/timeline/cells/LatencyCell.test.tsx`** — assert formatting:
- `<LatencyCell ms={12} band="fast" />` → text `12ms`; bar present
- `<LatencyCell ms={1500} band="slow" />` → text `1.5s`; bar present
- `<LatencyCell ms={null} band={null} />` → text `—`; no bar (`queryByTestId("latency-bar")` is null)

**5. Create `packages/ui/src/components/timeline/cells/PayloadPreview.tsx`:**
```tsx
export function PayloadPreview({ text }: { text: string }): JSX.Element {
  return (
    <span
      data-testid="payload-preview"
      className="preview"
      style={{
        color: "var(--color-text-muted)",
        textOverflow: "ellipsis",
        whiteSpace: "nowrap",
        overflow: "hidden",
        display: "inline-block",
        maxWidth: "100%",
      }}
    >
      {text}
    </span>
  );
}
```

**6. Create `packages/ui/src/components/timeline/cells/PayloadPreview.test.tsx`** — assert text rendered verbatim and inline style includes `text-overflow: ellipsis`.
  </action>
  <acceptance_criteria>
    - All 6 files exist under `packages/ui/src/components/timeline/cells/`
    - `pnpm vitest run packages/ui/src/components/timeline/cells/StatusCell.test.tsx packages/ui/src/components/timeline/cells/LatencyCell.test.tsx packages/ui/src/components/timeline/cells/PayloadPreview.test.tsx` exits 0
    - `pnpm vitest run packages/ui/src/components/timeline/cells` exits 0 (full cell suite)
    - `pnpm -F @ahp-inspector/ui build` exits 0
    - `pnpm typecheck` exits 0
    - `rg -n "#[0-9a-fA-F]{3,8}" packages/ui/src/components/timeline/cells/` returns 0 hits
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/ui/src/components/timeline/cells && pnpm -F @ahp-inspector/ui build && pnpm typecheck</automated>
  </verify>
  <done>All six timeline cells implemented + tested; ready for Plan 02-04 to compose them into EventRow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EventRow data → cell text | Server-supplied row text rendered as React text children |

## STRIDE Threat Register

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|-----------|
| T-02-03-01 | Tampering / XSS | StatusCell / LatencyCell / PayloadPreview | mitigate | All values rendered as React text children (auto-escaped); never `dangerouslySetInnerHTML` |
| T-02-03-02 | Tampering | design tokens | mitigate | `no-hex-in-components.test.ts` (Plan 02-02) rejects hex literals in cell sources |
| T-02-03-03 | Information Disclosure | PayloadPreview | mitigate | Truncation enforced by core projection (`payloadPreview` capped at 120 chars in Plan 02-00); CSS overflow:hidden prevents long-text leaks beyond cell bounds |
</threat_model>

<verification>
- `pnpm vitest run packages/ui/src/components/timeline/cells` exits 0
- `pnpm typecheck` exits 0
- `pnpm -F @ahp-inspector/ui build` exits 0
- `rg -n "#[0-9a-fA-F]{3,8}" packages/ui/src/components/timeline/cells/` returns 0 hits
</verification>

<success_criteria>
- All six cell components present, each in its own file with a sibling Vitest
- DirectionGlyph / KindTag / ActionDot / StatusCell / LatencyCell / PayloadPreview render per UI-SPEC §5
- Cells consume only design-token CSS variables — zero hex literals
- Plan 02-04 can import these cells directly into EventRow with no further refactoring
</success_criteria>

<output>
After completion, create `.planning/phases/02-vertical-slice-cli-server-timeline/02-03-SUMMARY.md`
</output>
