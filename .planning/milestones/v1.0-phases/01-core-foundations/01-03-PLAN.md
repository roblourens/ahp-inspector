---
phase: 01
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/core/src/index.ts
  - packages/core/src/event-store.ts
  - packages/core/src/correlator.ts
  - packages/core/src/event-store.test.ts
  - packages/core/src/correlator.test.ts
  - packages/host-node/src/index.ts
  - packages/host-node/src/host-adapter.ts
  - packages/host-node/src/tail-reader.ts
  - packages/host-node/src/discovery.ts
  - packages/host-node/src/host-adapter.test.ts
  - packages/server/src/index.ts
  - packages/server/src/health-server.ts
  - packages/server/src/health-server.test.ts
  - packages/server/package.json
  - packages/cli/src/index.ts
  - packages/cli/src/cli.smoke.test.ts
  - packages/cli/package.json
  - test/security.test.ts
autonomous: true
requirements: [FOUND-01, EVENT-03, FOUND-04]
must_haves:
  truths:
    - "Developer can run `pnpm exec ahp-viewer ./test/fixtures/tiny.jsonl` and the CLI boots a local app shell on 127.0.0.1, reports the opened log, and exits cleanly when sent SIGINT."
    - "The local server binds 127.0.0.1 ONLY — never 0.0.0.0 or a non-loopback interface — and a test fails if the bind address regresses."
    - "EventStore appends AhpEvents into columnar arrays and notifies subscribers of (from,to) ranges."
    - "Correlator pairs c2s requests with s2c responses (and vice versa) using `(session, requestDir, idType, id)`; same numeric id `1` from a c2s request does NOT collide with the same numeric id `1` from an s2c request, and `1` ≠ \"1\"."
    - "Notifications, actions, protocol-notifications, and parse-errors are NEVER added to the correlation map."
    - "host-node implements HostAdapter using `node:fs` + chokidar; UI/shared/parser/core remain Node-free (boundary test still green)."
  artifacts:
    - path: "packages/core/src/event-store.ts"
      provides: "Append-only columnar EventStore with subscribe()"
      contains: "class EventStore"
    - path: "packages/core/src/correlator.ts"
      provides: "Correlator that pairs requests/responses on append"
      contains: "class Correlator"
    - path: "packages/host-node/src/host-adapter.ts"
      provides: "NodeHostAdapter implements HostAdapter"
      contains: "class NodeHostAdapter"
    - path: "packages/server/src/health-server.ts"
      provides: "Hono server bound to 127.0.0.1 with GET /health"
      contains: "127.0.0.1"
    - path: "packages/cli/src/index.ts"
      provides: "ahp-viewer CLI entry — argv → openLog + start health server"
      contains: "ahp-viewer"
  key_links:
    - from: "packages/cli/src/index.ts"
      to: "packages/host-node/src/host-adapter.ts"
      via: "imports NodeHostAdapter and calls openLog(file)"
      pattern: "NodeHostAdapter"
    - from: "packages/cli/src/index.ts"
      to: "packages/server/src/health-server.ts"
      via: "imports startHealthServer and binds 127.0.0.1"
      pattern: "startHealthServer"
    - from: "packages/core/src/correlator.ts"
      to: "packages/shared/src/correlation.ts"
      via: "uses correlationKeyForRequest / correlationKeyForResponse"
      pattern: "correlationKeyFor(Request|Response)"
---

<objective>
Wire the canonical event flow end-to-end: an in-memory `EventStore` + `Correlator` (consuming AhpEvents from Plan 02), a Node `HostAdapter` (file open + incremental tail-read + chokidar watch), a minimal Hono health server bound to `127.0.0.1`, and a `commander`-based CLI that boots the local app shell.

Purpose: Satisfy FOUND-01 (CLI boots local app shell), EVENT-03 (correlator with bidirectional JSON-RPC-safe key), and the FOUND-04 bind-address half (loopback-only). Per RESEARCH Pattern 5 (lines 321-358), Pattern 6 (lines 362-407), §"CLI scaffold" (lines 601-628), §"Pitfall 7 — Discoverable port".

Output: `pnpm exec ahp-viewer ./test/fixtures/tiny.jsonl` opens the log, ingests its events into the EventStore, pairs request/response, prints a summary, starts the health server on `http://127.0.0.1:5173/health`, and exits cleanly on SIGINT.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-foundations/01-RESEARCH.md
@.planning/phases/01-core-foundations/01-VALIDATION.md
@.planning/phases/01-core-foundations/01-01-SUMMARY.md
@.planning/phases/01-core-foundations/01-02-SUMMARY.md
@.github/copilot-instructions.md

<interfaces>
<!-- From @ahp-viewer/shared (Plan 02): -->
import type { AhpEvent, EventKind, Direction, IdType, CorrelationKey, HostAdapter, HostClient, HostMessage, LogHandle, LogCandidate, Disposable } from '@ahp-viewer/shared';
import { makeCorrelationKey, correlationKeyForRequest, correlationKeyForResponse } from '@ahp-viewer/shared';

<!-- From @ahp-viewer/parser (Plan 02): -->
import { LineSplitter, parseLine, normalize } from '@ahp-viewer/parser';

<!-- Status semantics (RESEARCH Pattern 5 line 327): -->
export type Status = 'ok' | 'error' | 'pending' | 'unmatched' | 'orphan' | 'n/a';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EventStore (columnar) + Correlator (bidirectional JSON-RPC-safe pairing)</name>
  <read_first>
    - .planning/phases/01-core-foundations/01-RESEARCH.md (Pattern 5 lines 321-358, Pattern 4 lines 277-319, Pitfall 2 lines 444-447)
    - packages/shared/src/event.ts (Plan 02)
    - packages/shared/src/correlation.ts (Plan 02)
  </read_first>
  <files>
    packages/core/src/index.ts,
    packages/core/src/event-store.ts,
    packages/core/src/correlator.ts,
    packages/core/src/event-store.test.ts,
    packages/core/src/correlator.test.ts
  </files>
  <behavior>
    - **EventStore** (event-store.test.ts):
      - `new EventStore()` is empty: `size() === 0`.
      - `append(ev)` returns the new index (0, 1, 2, ...) and pushes to all columns (`seq`, `ts`, `kind`, `dir`, `method`, `id`, `idType`, `sessionId`, `actionType`, `serverSeq`, `byteOffset`, `byteLength`).
      - `at(idx)` returns the original AhpEvent (object reference preserved — events array stores the full event for round-trip).
      - `subscribe(fn)` invokes `fn({from, to})` after each append (or batch). Returns an unsubscribe function. After unsubscribe, no more callbacks.
      - `byKind`, `byDir`, `byMethod` side-indices map values → array of indices and stay in sync with appends.
    - **Correlator** (correlator.test.ts):
      - On append of a request (kind='request'), publishes `correlationKeyForRequest(ev) → idx`.
      - On append of a response (kind='response'), looks up `correlationKeyForResponse(ev)` and on hit: records `pairIdx[responseIdx] = requestIdx` and `pairIdx[requestIdx] = responseIdx`, plus `latencyMs[requestIdx] = response.ts - request.ts`, plus `status[requestIdx] = response.error ? 'error' : 'ok'` (`status` derived by inspecting `response.raw`).
      - Out-of-order: if a response arrives BEFORE its request (rare but possible in legacy adapter / re-played logs), the response is parked in a `pendingResponses` map keyed by `correlationKeyForResponse(response)`; when the matching request arrives, pairing completes immediately.
      - **Direction asymmetry test (Pitfall 2):** c2s request `{id:1,method:"listSessions"}` followed by s2c response `{id:1,result:{}}` MUST pair. c2s request `{id:1}` followed by another c2s message with `id:1` MUST NOT pair.
      - **idType test:** c2s request `{id:1}` followed by s2c response `{id:"1"}` MUST NOT pair (different idType).
      - **Notifications never correlate:** appending events with `kind in {client-notification, server-notification, action, protocol-notification, parse-error, log}` MUST NOT touch the correlation map (no entries published, no lookups performed). Verify via a stub Map with spy methods.
      - **Status for unmatched requests:** after N appends, `status[requestIdx]` for a request with no matching response is `'pending'`. After a `flush(now)` call advances time past a configurable timeout (default 30s), `status` becomes `'unmatched'`.
  </behavior>
  <action>
    1. **`packages/core/src/event-store.ts`:** Implement `EventStore` as RESEARCH Pattern 5 (lines 329-357). Columns: parallel arrays (`number[]`, `string[]` etc.) for fast scans + a `events: AhpEvent[]` for round-trip via `at()`. Side indices: `byKind: Map<EventKind, number[]>`, `byDir: Map<Direction, number[]>`, `byMethod: Map<string, number[]>`. `append(ev)` pushes to every column AND every applicable side-index, then notifies subscribers with `{from: idx, to: idx+1}`. `subscribe(fn)` stores callbacks in a Set; returns an unsubscribe closure. No batching/coalescing in Phase 1 (rAF coalescing is Phase 2 — RESEARCH line 356).

    2. **`packages/core/src/correlator.ts`:** Implement `Correlator` that hooks an `EventStore` via `subscribe()`. Internal state:
       - `pendingRequests: Map<CorrelationKey, number>` — request idx by key.
       - `pendingResponses: Map<CorrelationKey, number>` — response idx by key (out-of-order handling).
       - `pairIdx: number[]` — indexed by event idx; defaults to `-1`.
       - `latencyMs: number[]` — indexed by event idx; defaults to `-1`.
       - `status: Status[]` — indexed by event idx; defaults to `'n/a'` for non-pairable kinds, `'pending'` for unpaired requests.
       Public surface: `pairOf(idx): number | null`, `latencyOf(idx): number | null`, `statusOf(idx): Status`, `flush(nowMs, timeoutMs = 30_000): void`, `dispose(): void`.
       Skip pairing entirely when `kind` is not `'request'` or `'response'`. For `'response'` events, derive error/ok status by inspecting `(ev.raw as any)?.error` (defensive — never throws).

    3. **`packages/core/src/index.ts`:** Barrel exporting `EventStore`, `Correlator`, and a `Status` type alias re-exported from a local `types.ts` (or inline).

    4. **`packages/core/src/event-store.test.ts`:** Cover every behavior bullet above. Use small synthetic AhpEvents constructed inline (do NOT depend on `@ahp-viewer/parser` here — tests of parser-driven flow live in Task 2/4).

    5. **`packages/core/src/correlator.test.ts`:** Cover every Correlator behavior bullet above. Include explicit tests for:
       - `(c2s id:1) then (s2c id:1 result)` → pair, `latency > 0`, `status === 'ok'`.
       - `(c2s id:1) then (s2c id:1 error)` → pair, `status === 'error'`.
       - `(c2s id:1) then (c2s id:1 method:"foo")` → NO pair (same direction; second is treated as new request key).
       - `(c2s id:1) then (s2c id:"1" result)` → NO pair (idType differs).
       - `(s2c id:5 result)` arriving BEFORE `(c2s id:5 method:"x")` → pair completes when request arrives.
       - Append a server-notification — assert `pairIdx`, `latencyMs`, `status` for that idx remain at defaults (no correlation work).
       - `flush(now=initialTs+31_000)` after a lone request → `statusOf(requestIdx) === 'unmatched'`.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/event-store.test.ts packages/core/src/correlator.test.ts &amp;&amp; pnpm vitest run test/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both core test files pass.
    - `test/boundary.test.ts` still passes (core has no Node imports).
    - `grep -E "class EventStore" packages/core/src/event-store.ts` matches.
    - `grep -E "class Correlator" packages/core/src/correlator.ts` matches.
    - `grep -E "correlationKeyForRequest|correlationKeyForResponse" packages/core/src/correlator.ts` matches both.
    - `pnpm --filter @ahp-viewer/core typecheck` exits 0.
  </acceptance_criteria>
  <done>EventStore and Correlator implement columnar storage and bidirectional JSON-RPC-safe pairing per RESEARCH Patterns 4-5; all behavior bullets pass; boundary test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NodeHostAdapter — fs/chokidar implementation behind HostAdapter interface</name>
  <read_first>
    - .planning/phases/01-core-foundations/01-RESEARCH.md (Pattern 6 lines 362-407, §"Don't Hand-Roll" lines 419-431, Pitfall 7 lines 468-470, §"Security Domain / Path traversal" line 779)
    - packages/shared/src/host-protocol.ts (Plan 02)
    - packages/host-node/package.json (Plan 01 stub)
  </read_first>
  <files>
    packages/host-node/src/index.ts,
    packages/host-node/src/host-adapter.ts,
    packages/host-node/src/tail-reader.ts,
    packages/host-node/src/discovery.ts,
    packages/host-node/src/host-adapter.test.ts
  </files>
  <behavior>
    - `NodeHostAdapter implements HostAdapter` from `@ahp-viewer/shared`.
    - `openLog(path)`:
      - Resolves `path` via `path.resolve(process.cwd(), path)`. **No chroot** (locked decision: viewer reads user-chosen paths anywhere on disk).
      - Asserts `fs.statSync(resolved).isFile()` and the file is readable (`fs.accessSync(resolved, fs.constants.R_OK)`); rejects with a sanitized error message (do NOT echo user-supplied path beyond its basename in error messages — defensive).
      - Returns `{ path: resolved, size: stat.size }`.
    - `watchLog(handle, onChunk)`:
      - Reads existing bytes from offset 0 to current size into `Uint8Array` chunks (max 256 KiB per chunk) and emits via `onChunk`.
      - Then uses `chokidar.watch(handle.path, { persistent: true, ignoreInitial: true, awaitWriteFinish: false })` to watch for `'change'` events.
      - On change: opens an `fs.createReadStream(handle.path, { start: lastOffset })`, pipes Buffers → `Uint8Array` chunks via `onChunk`, advances `lastOffset` to new file size on stream end.
      - Returns a `Disposable` with `dispose()` that closes the chokidar watcher and any pending stream.
    - `close(handle)`: no-op for now (each watch returns its own Disposable).
    - `discoverLogs()`: returns `[]` in Phase 1 — full implementation in Phase 4. Document as TODO referencing INGEST-02.
    - `host-adapter.test.ts`:
      - `openLog('./test/fixtures/tiny.jsonl')` returns `{ path: <absolute>, size: > 0 }`.
      - `openLog('./does-not-exist')` rejects (Promise rejection or thrown error).
      - `watchLog` against tiny.jsonl emits one or more chunks whose concatenation equals the file's bytes.
      - Append simulation: write to a `tmp/test-watch.jsonl` (use `os.tmpdir()`), open + watch, append additional bytes via `fs.appendFileSync`, assert the next chunk arrives within 1500 ms and contains the appended bytes. Cleanup tmp file in `afterAll`.
      - Test setting timeout to 3000 ms.
  </behavior>
  <action>
    1. **`packages/host-node/src/tail-reader.ts`:** Implement `class TailReader { constructor(path: string); readInitial(onChunk): Promise<void>; startWatch(onChunk): Disposable; dispose(): void; }`. Internal `lastOffset` tracks consumed bytes. `readInitial` uses `fs.createReadStream(path, { highWaterMark: 256 * 1024 })`; on `'data'` (Buffer) call `onChunk(new Uint8Array(buf.buffer, buf.byteOffset, buf.byteLength))`; on `'end'` set `lastOffset = file size at start`. `startWatch` constructs a chokidar watcher and on `'change'` reads from `lastOffset` to new size via another `createReadStream` and updates `lastOffset` on end.

    2. **`packages/host-node/src/host-adapter.ts`:** Implement `NodeHostAdapter implements HostAdapter`. Compose a `TailReader` per `watchLog`. Path-resolution as described; error messages sanitized to `${path.basename(resolvedPath)}` only.

    3. **`packages/host-node/src/discovery.ts`:** Export `async function discoverVsCodeLogs(): Promise<LogCandidate[]> { return []; }` with a JSDoc TODO referencing INGEST-02 / Phase 4.

    4. **`packages/host-node/src/index.ts`:** Barrel exporting `NodeHostAdapter`, `TailReader`, `discoverVsCodeLogs`.

    5. **`packages/host-node/src/host-adapter.test.ts`:** Cover all behavior bullets. For the append test, generate a unique tmp path per test run with `path.join(os.tmpdir(), 'ahp-viewer-watch-${randomUUID()}.jsonl')`.

    6. Confirm `chokidar` is in `packages/host-node/package.json` dependencies (added in Plan 01); add `@types/node` if not already inherited.
  </action>
  <verify>
    <automated>pnpm vitest run packages/host-node/src/host-adapter.test.ts &amp;&amp; pnpm vitest run test/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/host-node/src/host-adapter.test.ts` passes (within 5 s).
    - `test/boundary.test.ts` still passes (host-node IS allowed to use Node; shared/parser/core remain Node-free).
    - `grep -E "class NodeHostAdapter" packages/host-node/src/host-adapter.ts` matches.
    - `grep -E "implements HostAdapter" packages/host-node/src/host-adapter.ts` matches.
    - `grep -E "from 'chokidar'" packages/host-node/src/tail-reader.ts` matches.
    - `pnpm --filter @ahp-viewer/host-node typecheck` exits 0.
  </acceptance_criteria>
  <done>NodeHostAdapter implements HostAdapter end-to-end (open + initial-read + watch-and-tail) using fs+chokidar; tested against tiny.jsonl and a tmp append; boundary test still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hono health server (127.0.0.1) + CLI entrypoint + smoke test</name>
  <read_first>
    - .planning/phases/01-core-foundations/01-RESEARCH.md (§"CLI scaffold" lines 601-628, Assumption A7 line 661, Pitfall 7 lines 468-470, §"Security Domain" lines 757-791)
    - packages/host-node/src/host-adapter.ts (Task 2)
    - packages/core/src/event-store.ts (Task 1)
    - packages/core/src/correlator.ts (Task 1)
    - packages/parser/src/index.ts (Plan 02)
    - test/security.test.ts (Plan 01)
  </read_first>
  <files>
    packages/server/package.json,
    packages/server/src/index.ts,
    packages/server/src/health-server.ts,
    packages/server/src/health-server.test.ts,
    packages/cli/package.json,
    packages/cli/src/index.ts,
    packages/cli/src/cli.smoke.test.ts,
    test/security.test.ts
  </files>
  <behavior>
    - `startHealthServer({ port }): Promise<{ url: string; close(): Promise<void> }>`:
      - Binds to `127.0.0.1` ONLY (NEVER `0.0.0.0`, NEVER `::`).
      - Exposes `GET /health` returning `200` with `{"status":"ok","version":"<pkg.version>"}`.
      - Returns `{ url: 'http://127.0.0.1:${port}', close }`.
    - `health-server.test.ts`:
      - Starts the server on an ephemeral port (`port: 0` if Hono/Node-server supports it, else port `45100`).
      - `fetch('http://127.0.0.1:${port}/health')` returns 200 with `status:'ok'`.
      - **Bind-address regression test:** `fetch('http://0.0.0.0:${port}/health')` from the same machine MUST fail OR connect to a different listener (NOT to our server). Pragmatic implementation: assert that the server's address (`server.address()`) equals `127.0.0.1`. Fail if it equals `0.0.0.0`, `::`, or any non-loopback string.
      - `close()` releases the port.
    - `cli.smoke.test.ts`:
      - Spawns `node dist/index.js ./test/fixtures/tiny.jsonl --port 45101 --no-server` (or equivalent flag) as a child process; asserts stdout contains `[ahp-viewer] opened` and the file's basename within 3 s; sends SIGINT; asserts exit code 0.
      - Second test: spawns `node dist/index.js ./test/fixtures/tiny.jsonl --port 45102` (server enabled), waits for stdout `listening on http://127.0.0.1:45102`, fetches `/health`, asserts 200, sends SIGINT, asserts clean exit.
      - If pre-build is required, the test calls `execSync('pnpm --filter @ahp-viewer/cli build')` in `beforeAll` (or runs `tsx packages/cli/src/index.ts ...` directly to skip the build step — preferred for speed).
  </behavior>
  <action>
    1. **Update root `test/security.test.ts` allow-list** to include `hono` and `@hono/node-server`. The security test must continue to pass.

    2. **`packages/server/package.json`:** Add `"dependencies": { "hono": "^4.12.18", "@hono/node-server": "^1.x" }` (verify @hono/node-server latest; research notes Hono 4.12.18). Run `pnpm install`.

    3. **`packages/server/src/health-server.ts`:** Build a minimal Hono app with `GET /health` → JSON `{status:'ok', version: <from package.json>}`. Use `@hono/node-server`'s `serve({ fetch: app.fetch, port, hostname: '127.0.0.1' })`. Capture the returned `Server` instance; export `close()` that calls `server.close()` wrapped in a Promise. Read `pkg.version` via `import { readFileSync } from 'node:fs'` + `JSON.parse` of `packages/cli/package.json` (or pass version as a param — preferred: pass as param from CLI to avoid host-node-style fs use here; the server is Node anyway, so it's fine, but param-passing is cleaner). **Hard-code `hostname: '127.0.0.1'`**; do NOT read from env or argv.

    4. **`packages/server/src/index.ts`:** Barrel exporting `startHealthServer`.

    5. **`packages/server/src/health-server.test.ts`:** Cover all behavior bullets. Use `node:net` `Server.address()` introspection to assert `address === '127.0.0.1'`. (If `@hono/node-server` does not expose the underlying server, return it from `startHealthServer` for testing — internal symbol or `_server`.)

    6. **`packages/cli/src/index.ts`:** Implement per RESEARCH §"CLI scaffold" lines 604-625, extended:
       ```ts
       #!/usr/bin/env node
       import { Command } from 'commander';
       import { NodeHostAdapter } from '@ahp-viewer/host-node';
       import { LineSplitter, parseLine, normalize } from '@ahp-viewer/parser';
       import { EventStore, Correlator } from '@ahp-viewer/core';
       import { startHealthServer } from '@ahp-viewer/server';
       import { readFileSync } from 'node:fs';
       import { fileURLToPath } from 'node:url';
       import { dirname, join } from 'node:path';

       const pkg = JSON.parse(readFileSync(join(dirname(fileURLToPath(import.meta.url)), '../package.json'), 'utf8'));

       const program = new Command()
         .name('ahp-viewer')
         .version(pkg.version)
         .argument('[file]', 'AHP JSONL log file path')
         .option('--port <n>', 'local server port', '5173')
         .option('--no-server', 'skip starting the local health server (smoke-test mode)')
         .action(async (file, opts) => {
           const host = new NodeHostAdapter();
           const store = new EventStore();
           const correlator = new Correlator(store);
           let serverHandle;
           if (opts.server !== false) {
             serverHandle = await startHealthServer({ port: Number(opts.port), version: pkg.version });
             console.log(`[ahp-viewer] listening on ${serverHandle.url}`);
           }
           if (file) {
             const handle = await host.openLog(file);
             console.log(`[ahp-viewer] opened ${handle.path} (${handle.size} bytes)`);
             const splitter = new LineSplitter();
             let seq = 0;
             let byteOffset = 0;
             const watcher = host.watchLog(handle, (chunk) => {
               const text = new TextDecoder('utf-8').decode(chunk);
               for (const line of splitter.push(text)) {
                 const byteLen = Buffer.byteLength(line, 'utf8');
                 const parsed = parseLine(line, byteOffset, byteLen);
                 const ts = Date.now();
                 const ev = parsed.error
                   ? { /* call makeParseErrorEvent — exported via shared barrel */ } as any
                   : normalize(parsed.raw, { dir: 'c2s', ts, tsRaw: String(ts), seq, byteOffset, byteLength: byteLen });
                 store.append(ev);
                 seq += 1;
                 byteOffset += byteLen + 1; // +1 for newline (approx)
               }
             });
             // Note: dir='c2s' is a Phase-1 placeholder. Real direction inference comes from log envelope (Phase 2 transport).
             process.on('SIGINT', async () => { watcher.dispose(); if (serverHandle) await serverHandle.close(); process.exit(0); });
           } else {
             console.log('[ahp-viewer] no file specified; UI not yet wired (Phase 2)');
             if (serverHandle) {
               process.on('SIGINT', async () => { await serverHandle.close(); process.exit(0); });
             }
           }
         });
       program.parse();
       ```
       Make sure the file starts with the shebang and `packages/cli/package.json` declares `"bin": { "ahp-viewer": "./dist/index.js" }` (Plan 01 already did this).

    7. **`packages/cli/src/cli.smoke.test.ts`:** Use `node:child_process.spawn` to launch `tsx packages/cli/src/index.ts ./test/fixtures/tiny.jsonl --port 45101 --no-server` (add `tsx` as a devDependency at the root to allow running TS directly from tests — update test/security.test.ts allow-list). Collect stdout for 2 s; assert `opened` line; send SIGINT; assert exit code 0. Second test with port 45102 and server enabled: assert `listening on http://127.0.0.1:45102` line, fetch `http://127.0.0.1:45102/health`, assert 200 + `{status:'ok'}`, SIGINT, clean exit.

    8. **Update `test/security.test.ts` allow-list:** add `hono`, `@hono/node-server`, `tsx`. Re-run security test.
  </action>
  <verify>
    <automated>pnpm vitest run packages/server/src/health-server.test.ts packages/cli/src/cli.smoke.test.ts test/security.test.ts test/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/server/src/health-server.test.ts` passes; bind-address introspection confirms `127.0.0.1`.
    - `pnpm vitest run packages/cli/src/cli.smoke.test.ts` passes; both smoke scenarios green.
    - `pnpm vitest run test/security.test.ts` passes (allow-list now includes `hono`, `@hono/node-server`, `tsx`, plus everything from Plans 01-02).
    - `pnpm vitest run test/boundary.test.ts` still passes.
    - `grep -E "127\\.0\\.0\\.1" packages/server/src/health-server.ts` matches; `grep -E "0\\.0\\.0\\.0" packages/server/src/health-server.ts` returns NOTHING.
    - `pnpm exec ahp-viewer ./test/fixtures/tiny.jsonl --no-server` (after `pnpm --filter @ahp-viewer/cli build`) prints `[ahp-viewer] opened` and exits cleanly on SIGINT — manual smoke OK to skip if smoke test passes.
    - Full suite: `pnpm vitest run` exits 0 in <10 s (per VALIDATION.md target).
  </acceptance_criteria>
  <done>FOUND-01 demo works (`pnpm exec ahp-viewer ./test/fixtures/tiny.jsonl` opens log + boots local server on 127.0.0.1); EVENT-03 wiring is live in CLI; FOUND-04 bind-address regression test guards loopback-only; full Phase 1 test suite passes in under 10 s.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN → local server | Other machines on the same network must NEVER reach the viewer's server. |
| User CLI argv → host filesystem | User-supplied paths are resolved and read; no chroot but read-only and per-locked-decision intentional. |
| EventStore append → subscribers | Subscriber callbacks must not be able to crash the append path or DoS later events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Local server bound to non-loopback (LAN exposure) | mitigate | `health-server.ts` hard-codes `hostname: '127.0.0.1'`; `health-server.test.ts` introspects `server.address()` and fails if address ≠ `127.0.0.1`; CLI does not expose a `--host` flag (RESEARCH Pitfall 7). |
| T-03-02 | Tampering | Mispairing JSON-RPC requests/responses across directions or id types | mitigate | Correlator uses `correlationKeyForResponse` (inverts wire dir); key includes `idType`; tests cover `(c2s 1) ↮ (c2s 1)`, `(c2s 1) ↮ (s2c "1")`, `(c2s 1) ↔ (s2c 1)` (RESEARCH Pitfall 2). |
| T-03-03 | Information Disclosure | openLog error messages echoing full user paths into logs | mitigate | NodeHostAdapter sanitizes errors to `path.basename(resolvedPath)`; never includes parent directory in error text. |
| T-03-04 | DoS | Subscriber callback throwing or blocking inside `EventStore.append` | mitigate | `EventStore.append` wraps subscriber invocations in `try/catch` and logs via `console.warn` (one-shot — does not echo payload); a misbehaving subscriber cannot block ingest. |
| T-03-05 | Information Disclosure | New runtime dep (hono, @hono/node-server, tsx) bypassing the allow-list | mitigate | `test/security.test.ts` allow-list extended in Task 3 step 1; any further dep additions require updating the allow-list and re-running security tests. |
| T-03-06 | Tampering | Path traversal in user-supplied log path | accept | Locked decision: viewer intentionally reads user-chosen paths anywhere on disk; no chroot. Mitigation limited to read-only access + readability check + sanitized error messages (RESEARCH §"Security Domain / Path traversal", line 779). |
</threat_model>

<verification>
- `pnpm vitest run` (full suite) → all tests pass in under 10 s (per VALIDATION.md sampling-rate target).
- `pnpm typecheck` (root) → 0 errors.
- `pnpm lint` → 0 errors.
- `pnpm exec ahp-viewer ./test/fixtures/tiny.jsonl` → prints `[ahp-viewer] listening on http://127.0.0.1:5173` AND `[ahp-viewer] opened ...tiny.jsonl ...`; SIGINT exits cleanly with code 0.
- `curl http://127.0.0.1:5173/health` while CLI is running returns `{"status":"ok","version":"0.1.0"}`.
- `curl http://0.0.0.0:5173/health` from another machine on the LAN should connection-refuse (impractical to assert in CI; bind-address introspection in `health-server.test.ts` is the automated proxy).
</verification>

<success_criteria>
- FOUND-01: `pnpm exec ahp-viewer <file>` boots a local app shell on 127.0.0.1 with no outbound network calls or CDN assets.
- EVENT-03: Correlator pairs requests/responses using the bidirectional JSON-RPC-safe key from Plan 02.
- FOUND-04 (bind half): server is loopback-only; regression test guards it.
- Full Phase 1 suite green in <10 s.
- All Plan 01 boundary, security, and fixture-scrub tests still green after extending the allow-list.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundations/01-03-SUMMARY.md` with: CLI demo command + transcript, full test suite runtime, list of allow-listed deps (final), Correlator semantics + edge cases covered, and a readiness checklist for Phase 2 (HostClient over SSE, Hono routes for streaming events, virtualized timeline UI).
</output>
