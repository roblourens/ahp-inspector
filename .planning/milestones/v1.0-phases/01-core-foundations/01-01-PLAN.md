---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - biome.json
  - tsconfig.base.json
  - vitest.config.ts
  - .gitignore
  - .nvmrc
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/parser/package.json
  - packages/parser/tsconfig.json
  - packages/parser/src/index.ts
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/src/index.ts
  - packages/host-node/package.json
  - packages/host-node/tsconfig.json
  - packages/host-node/src/index.ts
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/src/index.ts
  - packages/cli/package.json
  - packages/cli/tsconfig.json
  - packages/cli/src/index.ts
  - test/boundary.test.ts
  - test/security.test.ts
  - test/fixture-scrub.test.ts
  - test/fixtures/generate.ts
  - test/fixtures/tiny.jsonl
  - test/fixtures/malformed.jsonl
  - test/fixtures/crlf.jsonl
  - test/fixtures/bom.jsonl
  - test/fixtures/legacy.sample.log
autonomous: true
requirements: [FOUND-02, FOUND-04, VERIFY-04]
must_haves:
  truths:
    - "Repo installs with `pnpm install` against the declared workspace and produces no warnings about unresolved workspace packages."
    - "`pnpm vitest run` boots and executes test/boundary.test.ts, test/security.test.ts, and test/fixture-scrub.test.ts (all green)."
    - "`packages/shared`, `packages/parser`, and `packages/core` cannot import `node:*`, `fs`, `path`, `chokidar`, `react`, or any DOM type — and a test fails if they ever do."
    - "Committed fixtures contain no real-token patterns (Bearer, sk-, ghp_, JWT prefix, password=, api_key=)."
    - "Sample log path `~/agenthost.*.log` is gitignored to prevent accidental copy."
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package list"
      contains: "packages/*"
    - path: "biome.json"
      provides: "Lint+format with per-package noRestrictedImports for shared/parser/core"
      contains: "noRestrictedImports"
    - path: "tsconfig.base.json"
      provides: "Strict TS 5.x base config"
      contains: "\"strict\": true"
    - path: "vitest.config.ts"
      provides: "Vitest 4.x root config"
    - path: "test/fixtures/generate.ts"
      provides: "Synthesizes all fixtures from canonical envelope shapes"
      min_lines: 40
    - path: "test/boundary.test.ts"
      provides: "AST/regex scan asserting portable packages have no forbidden imports"
    - path: "test/security.test.ts"
      provides: "package.json dependency allow-list assertion"
    - path: "test/fixture-scrub.test.ts"
      provides: "Secret-pattern scanner for committed fixtures"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "packages/*/package.json"
      via: "workspace include glob"
      pattern: "packages/\\*"
    - from: "test/boundary.test.ts"
      to: "packages/{shared,parser,core}/src"
      via: "scans every .ts file for forbidden import specifiers"
      pattern: "node:|from ['\"]fs['\"]|chokidar|react"
---

<objective>
Stand up the pnpm workspace, TypeScript 5.x base config, Biome import-restriction guardrails, Vitest 4.x harness, and Wave 0 fixture/security/boundary tests that every later plan depends on.

Purpose: Lock the layer boundaries (UI cannot import Node; portable packages cannot import Node or DOM) and the fixture-scrub posture before any feature code lands. Per RESEARCH §"Recommended Project Structure", §"Anti-Patterns", §"Security Domain / Fixture scrubbing".

Output: Empty-but-installable workspace with six package skeletons, a passing test harness, and committed scrubbed fixtures.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-foundations/01-RESEARCH.md
@.planning/phases/01-core-foundations/01-VALIDATION.md
@.github/copilot-instructions.md

<interfaces>
<!-- This plan creates the workspace shell only. No types are produced; later plans add types in packages/shared/src. -->
<!-- Reference: RESEARCH §"Recommended Project Structure" lines 86-126 for the exact directory layout. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — Workspace + tooling scaffold</name>
  <read_first>
    - .planning/phases/01-core-foundations/01-RESEARCH.md (§"Standard Stack" lines 38-72, §"Recommended Project Structure" lines 86-131)
    - .planning/phases/01-core-foundations/01-VALIDATION.md (Test Infrastructure table)
    - .github/copilot-instructions.md
  </read_first>
  <files>
    package.json, pnpm-workspace.yaml, biome.json, tsconfig.base.json, vitest.config.ts, .gitignore, .nvmrc,
    packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts,
    packages/parser/package.json, packages/parser/tsconfig.json, packages/parser/src/index.ts,
    packages/core/package.json, packages/core/tsconfig.json, packages/core/src/index.ts,
    packages/host-node/package.json, packages/host-node/tsconfig.json, packages/host-node/src/index.ts,
    packages/server/package.json, packages/server/tsconfig.json, packages/server/src/index.ts,
    packages/cli/package.json, packages/cli/tsconfig.json, packages/cli/src/index.ts
  </files>
  <action>
    Create a pnpm workspace with TypeScript 5.6+ (NOT TS 6 — locked decision; research Assumption A1 flags TS 6 as too new), Vitest 4.1.5, Biome 2.4.14, tsup 8.5.1, commander 14.0.3, chokidar 5.0.0, @types/node, and Node 22 LTS (.nvmrc = `22`).

    1. **Root `package.json`:**
       - `"name": "ahp-inspector"`, `"private": true`, `"type": "module"`, `"packageManager": "pnpm@9.15.0"`, `"engines": { "node": ">=22" }`.
       - Scripts: `"test": "vitest run"`, `"test:changed": "vitest run --changed"`, `"lint": "biome check ."`, `"format": "biome format --write ."`, `"build": "pnpm -r build"`, `"typecheck": "pnpm -r typecheck"`.
       - devDependencies: `typescript@^5.6.0`, `@biomejs/biome@^2.4.14`, `vitest@^4.1.5`, `tsup@^8.5.1`, `@types/node@^22.0.0`.

    2. **`pnpm-workspace.yaml`:** `packages: ["packages/*"]`.

    3. **`tsconfig.base.json`:** `"target": "ES2022"`, `"module": "ESNext"`, `"moduleResolution": "Bundler"`, `"strict": true`, `"noUncheckedIndexedAccess": true`, `"exactOptionalPropertyTypes": true`, `"verbatimModuleSyntax": true`, `"isolatedModules": true`, `"skipLibCheck": true`, `"declaration": true`, `"sourceMap": true`.

    4. **`biome.json`:** Enable formatter and linter. Configure `linter.rules.style.noRestrictedImports` per-package via `overrides`:
       - For `packages/shared/**` and `packages/core/**`: forbid specifiers `node:*` (pattern), `fs`, `fs/promises`, `path`, `chokidar`, `react`, `react-dom`, `vite`, `hono`, and any path starting with `packages/host-node`.
       - For `packages/parser/**`: same as above EXCEPT allow no Node/DOM imports either; legacy adapter is portable.
       - Use Biome 2.x `overrides[].linter.rules.correctness.noRestrictedImports` with `paths` map. If Biome 2.x's `noRestrictedImports` cannot match `node:*` glob, also rely on `test/boundary.test.ts` (Task 2) as the authoritative check; mark that in a comment in `biome.json`.

    5. **`vitest.config.ts`:** `defineConfig({ test: { include: ["test/**/*.test.ts", "packages/*/src/**/*.test.ts", "packages/*/test/**/*.test.ts"], reporters: ["default"], pool: "forks" } })`.

    6. **`.gitignore`:** include `node_modules`, `dist`, `.turbo`, `coverage`, and an explicit line `~/agenthost.*.log` plus a top-level `agenthost.*.log` entry (defensive — research §"Fixture scrubbing").

    7. **Each `packages/<name>/package.json`:**
       - `"name": "@ahp-inspector/<name>"`, `"version": "0.1.0"`, `"private": true`, `"type": "module"`, `"main": "./src/index.ts"`, `"types": "./src/index.ts"`, `"exports": { ".": "./src/index.ts" }`.
       - Scripts: `"typecheck": "tsc --noEmit"`, `"build": "tsup src/index.ts --format esm --dts"` (cli adds `--shims` and bin entry; server omits build for now).
       - For `packages/cli`: add `"bin": { "ahp-inspector": "./dist/index.js" }` and dependency on `commander@^14.0.3`, `@ahp-inspector/host-node@workspace:*`, `@ahp-inspector/server@workspace:*`.
       - For `packages/host-node`: dependency `chokidar@^5.0.0`.
       - For `packages/server`: no runtime deps yet (Hono added in Plan 03).
       - For `packages/parser` and `packages/core`: dependency `@ahp-inspector/shared@workspace:*`.

    8. **Each `packages/<name>/tsconfig.json`:** `{ "extends": "../../tsconfig.base.json", "include": ["src/**/*"], "compilerOptions": { "outDir": "dist", "rootDir": "src" } }`.

    9. **Each `packages/<name>/src/index.ts`:** Empty stub — `export {};` — so workspace resolves and typecheck passes. No `packages/ui` is created (per locked decision: skip until package-graph requires it).

    10. Run `pnpm install` to materialize `node_modules` and `pnpm-lock.yaml` (commit the lockfile).

    NOTE on TypeScript major: research recommends 5.6+ explicitly; do NOT install `typescript@6.x` even if available.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false &amp;&amp; pnpm typecheck &amp;&amp; pnpm vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0 and creates `pnpm-lock.yaml`.
    - `pnpm typecheck` (running `tsc --noEmit` in every package) exits 0.
    - `pnpm vitest run --passWithNoTests` exits 0 (harness boots; no tests yet).
    - `node -e "console.log(require('./package.json').devDependencies.typescript)"` prints a string starting with `^5.` (NOT `^6.`).
    - `grep -E '"@ahp-inspector/(shared|parser|core|host-node|server|cli)"' packages/*/package.json | wc -l` ≥ 6.
    - `cat .gitignore | grep -E '^agenthost\.\*\.log'` matches.
    - `cat pnpm-workspace.yaml | grep -E 'packages/\*'` matches.
  </acceptance_criteria>
  <done>Workspace installs cleanly, all six packages typecheck, Vitest harness boots with zero tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 — Boundary, security, and fixture-scrub tests + synthesized fixtures</name>
  <read_first>
    - .planning/phases/01-core-foundations/01-RESEARCH.md (§"Anti-Patterns" lines 409-416, §"Security Domain" lines 757-791, §"Validation Architecture" referenced from 01-VALIDATION.md)
    - .planning/phases/01-core-foundations/01-VALIDATION.md (Wave 0 Requirements section + Per-Task Verification Map rows 01-W0-01, 01-W0-02, 01-FOUND-02, 01-FOUND-04)
    - package.json (created in Task 1)
  </read_first>
  <files>
    test/boundary.test.ts,
    test/security.test.ts,
    test/fixture-scrub.test.ts,
    test/fixtures/generate.ts,
    test/fixtures/tiny.jsonl,
    test/fixtures/malformed.jsonl,
    test/fixtures/crlf.jsonl,
    test/fixtures/bom.jsonl,
    test/fixtures/legacy.sample.log
  </files>
  <behavior>
    - boundary.test.ts: scanning every `.ts` under `packages/{shared,parser,core}/src/**` MUST find ZERO matches for `from ['"](node:|fs|fs/promises|path|chokidar|react|react-dom|vite|hono)['"]` and ZERO matches for `from ['"]@ahp-inspector/host-node['"]`. Scanning under `packages/ui/**` (if it ever exists) MUST find zero `node:` / `fs` / `host-node` imports. Test fails with a clear message naming the offending file + import.
    - security.test.ts: declares an allow-list of root + per-package devDependencies and runtime dependencies. Reading every `package.json` in the workspace, no dependency or devDependency outside the allow-list is permitted. Allow-list for Phase 1: `typescript`, `@biomejs/biome`, `vitest`, `tsup`, `@types/node`, `commander`, `chokidar`, `@ahp-inspector/shared`, `@ahp-inspector/parser`, `@ahp-inspector/core`, `@ahp-inspector/host-node`, `@ahp-inspector/server`, `@ahp-inspector/cli`. Test fails with the rogue dep name. (Hono is added in Plan 03 — that plan extends the allow-list.)
    - fixture-scrub.test.ts: reads every file under `test/fixtures/**`. For each, asserts NONE of these regexes match: `/Bearer\s+\S+/`, `/sk-[A-Za-z0-9]{20,}/`, `/ghp_[A-Za-z0-9]{20,}/`, `/eyJ[A-Za-z0-9_\-]{20,}\./`, `/password\s*[:=]\s*\S+/i`, `/api[_-]?key\s*[:=]\s*\S+/i`. Fails with offending file + matched pattern.
    - generate.ts: pure TS module exporting `generateAll()` that writes all four .jsonl fixtures + the legacy.sample.log from in-memory canonical shapes. Idempotent. The fixtures it produces MUST satisfy fixture-scrub.test.ts.
  </behavior>
  <action>
    1. **`test/fixtures/generate.ts`:** Export `generateAll(outDir = "test/fixtures")`. Build canonical objects in-code (do NOT read any file from `~`). Shapes:

       - **tiny.jsonl** — 8 lines covering each EventKind once: a c2s request `{"jsonrpc":"2.0","id":1,"method":"listSessions","params":{}}`, an s2c response `{"jsonrpc":"2.0","id":1,"result":{"sessions":[]}}`, a c2s notification `{"jsonrpc":"2.0","method":"dispatch","params":{"action":{"type":"noop"}}}`, an s2c action notification `{"jsonrpc":"2.0","method":"action","params":{"action":{"type":"rootState.onDidChange","payload":{}},"serverSeq":1,"origin":"server"}}`, an s2c protocol notification `{"jsonrpc":"2.0","method":"notification","params":{"notification":{"type":"log","message":"hello"}}}`, an s2c response with error `{"jsonrpc":"2.0","id":2,"error":{"code":-32000,"message":"boom"}}`, a c2s request with string id `{"jsonrpc":"2.0","id":"abc","method":"authenticate","params":{}}`, a c2s request with null id `{"jsonrpc":"2.0","id":null,"method":"ping","params":{}}`. Prefix each line with a synthetic ISO timestamp prefix? NO — keep raw JSONL pure (timestamps live INSIDE payloads in real logs; for Phase 1 the normalizer derives `ts` from a synthesized field — see Plan 02 Task 1). Ensure every payload field uses synthetic placeholder data: session ids like `"session-0001"`, no email/name/token-shaped strings.

       - **malformed.jsonl** — 5 lines: 1 valid (copy line 1 of tiny), 1 truncated `{"jsonrpc":"2.0","id":3,"method":"listSes`, 1 not-JSON `this is not json`, 1 empty line, 1 valid (copy line 2 of tiny).

       - **crlf.jsonl** — same as tiny but joined with `\r\n` and ending with a trailing `\r\n`.

       - **bom.jsonl** — leading U+FEFF, then 3 valid lines from tiny joined with `\n`.

       - **legacy.sample.log** — synthesized blocks demonstrating each header marker without copying any real payload. Format:
         ```
         [2026-01-01T00:00:00.000Z] >> listSessions
           {"jsonrpc":"2.0","id":1,"method":"listSessions","params":{}}
         [2026-01-01T00:00:00.010Z] << listSessions
           {"jsonrpc":"2.0","id":1,"result":{"sessions":[]}}
         [2026-01-01T00:00:00.020Z] >> dispatch
           {"jsonrpc":"2.0","method":"dispatch","params":{"action":{"type":"noop"}}}
         [2026-01-01T00:00:00.030Z] ** rootState.onDidChange
           {"jsonrpc":"2.0","method":"action","params":{"action":{"type":"rootState.onDidChange","payload":{}},"serverSeq":1,"origin":"server"}}
         [2026-01-01T00:00:00.040Z] ** onDidNotification
           {"jsonrpc":"2.0","method":"notification","params":{"notification":{"type":"log","message":"hi"}}}
         [2026-01-01T00:00:00.050Z] !! authenticate
           {"jsonrpc":"2.0","id":2,"error":{"code":-32000,"message":"unauthorized"}}
         ```

    2. Run `generateAll()` once via a Vitest setup or a small `tsx test/fixtures/generate.ts` step, then commit the resulting fixture files. Add a Vitest `beforeAll` in `test/fixtures.regen.test.ts` (optional helper) that re-runs generate and asserts the on-disk bytes match — OR include this assertion inside `fixture-scrub.test.ts` (preferred: keep one test file).

    3. **`test/boundary.test.ts`:** Use `node:fs` + `node:path` (test files MAY use Node) to walk `packages/shared/src`, `packages/parser/src`, `packages/core/src`. For each `.ts`, regex-match `import .* from ['"](?<spec>[^'"]+)['"]` and `from ['"](?<spec2>[^'"]+)['"]`. For each spec, fail if it matches any forbidden pattern: `^node:`, `^fs$`, `^fs/`, `^path$`, `^chokidar$`, `^react`, `^vite`, `^hono`, `^@ahp-inspector/host-node`. Empty package source counts as pass.

    4. **`test/security.test.ts`:** Read root `package.json` and every `packages/*/package.json`. Build the union of `dependencies` and `devDependencies` keys. Assert each key is in ALLOW = `["typescript","@biomejs/biome","vitest","tsup","@types/node","commander","chokidar","@ahp-inspector/shared","@ahp-inspector/parser","@ahp-inspector/core","@ahp-inspector/host-node","@ahp-inspector/server","@ahp-inspector/cli"]`. Fail with the offending dep name. Document inline that Plan 03 will extend the allow-list with `hono`.

    5. **`test/fixture-scrub.test.ts`:** Walk `test/fixtures/**`, read each as UTF-8, run each forbidden regex, fail with `${file}: matched ${pattern}` if any match.
  </action>
  <verify>
    <automated>pnpm vitest run test/boundary.test.ts test/security.test.ts test/fixture-scrub.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run test/boundary.test.ts` reports 1+ tests, all pass.
    - `pnpm vitest run test/security.test.ts` reports 1+ tests, all pass.
    - `pnpm vitest run test/fixture-scrub.test.ts` reports 1+ tests, all pass.
    - `ls test/fixtures/*.jsonl test/fixtures/legacy.sample.log` lists exactly: tiny.jsonl, malformed.jsonl, crlf.jsonl, bom.jsonl, legacy.sample.log.
    - `wc -l test/fixtures/tiny.jsonl` reports 8.
    - `head -c 3 test/fixtures/bom.jsonl | xxd | grep -i 'efbbbf'` matches (BOM present).
    - `file test/fixtures/crlf.jsonl` (or `grep -U $'\r' test/fixtures/crlf.jsonl`) confirms CRLF endings.
    - `grep -RE 'Bearer |sk-|ghp_|eyJ' test/fixtures/` returns NOTHING.
    - Inserting a deliberate `Bearer abc123def456ghi789jkl` line into a fixture and re-running fixture-scrub.test.ts FAILS (manual smoke; revert after).
  </acceptance_criteria>
  <done>All three test files pass, all five fixture files exist, fixtures are demonstrably scrubbed, and the boundary test would catch a forbidden import (verified by adding a temporary bad import and confirming failure, then reverting).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source repo → public registry | Committed code/fixtures could be exfiltrated by anyone reading the repo. |
| Developer machine → test harness | Tests read filesystem; harness must not inadvertently read or echo `~/agenthost.*.log`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Committed fixtures (test/fixtures/**) | mitigate | `test/fixture-scrub.test.ts` rejects Bearer/sk-/ghp_/JWT/password/api_key patterns; `generate.ts` synthesizes from in-memory shapes (never reads `~/agenthost.*.log`); `.gitignore` excludes `agenthost.*.log` defensively. |
| T-01-02 | Information Disclosure | Outbound dependency added later (analytics/CDN/telemetry) | mitigate | `test/security.test.ts` enforces a strict per-package dependency allow-list; any new dep requires a follow-up plan to extend the list (Plan 03 adds `hono` only). |
| T-01-03 | Tampering | Portable packages importing Node/DOM (breaks future webview portability) | mitigate | `test/boundary.test.ts` regex-walks `packages/{shared,parser,core}/src` and rejects `node:*`, `fs`, `path`, `chokidar`, `react`, `vite`, `hono`, `@ahp-inspector/host-node` import specifiers; Biome `noRestrictedImports` provides editor-time guard. |
| T-01-04 | Repudiation | TS major drift (TS 6 silently adopted) | accept-with-control | `package.json` pins `typescript@^5.6.0`; locked decision documented; security.test.ts allow-list will fail if anyone adds a non-allowed alternative. |
</threat_model>

<verification>
- `pnpm install` → 0 errors.
- `pnpm typecheck` → 0 errors across all six packages.
- `pnpm vitest run` → all three Wave 0 tests pass.
- `pnpm lint` → 0 errors (Biome config valid).
</verification>

<success_criteria>
- Workspace + tooling scaffolded per RESEARCH §"Recommended Project Structure".
- Boundary + security + fixture-scrub guardrails are LIVE before any feature code lands.
- Fixtures synthesized, scrubbed, and committed.
- TS pinned to 5.x per locked decision.
- Server binds 127.0.0.1 — deferred to Plan 03 (allow-list will grow then).
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundations/01-01-SUMMARY.md` with: scaffolded packages, tooling versions, Wave 0 tests passing, fixture inventory, and the dependency allow-list (so Plan 03 knows to extend it).
</output>
