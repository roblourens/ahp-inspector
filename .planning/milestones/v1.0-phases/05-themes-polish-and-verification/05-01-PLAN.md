---
phase: 05-themes-polish-and-verification
plan: 01
type: execute
wave: 1
depends_on: ["05-00"]
autonomous: true
requirements: [THEME-05, VERIFY-02]
files_modified:
  - packages/ui/src/components/shell/AppShell.tsx
  - packages/ui/src/components/shell/AppShell.test.tsx
  - packages/ui/src/components/detail/DetailPanel.tsx
  - packages/ui/src/components/detail/DetailResizeHandle.tsx
  - packages/ui/src/components/detail/detail-layout.ts
  - packages/ui/src/styles/global.css
  - packages/ui/src/state/store.ts

must_haves:
  truths:
    - "Below 1400px, selected event details open as an overlay drawer."
    - "At 1400px and above, details remain a right side rail with 360px-720px resize bounds."
    - "The laptop/narrow drawer has a visible `Close details` control with a 44px minimum target."
    - "Timeline width is not squeezed by the detail panel below 1400px."
    - "Timeline horizontal overflow stays inside the timeline scroll container, not the page body."
---

<objective>
Implement the responsive layout contract from the approved UI-SPEC.

Layout stability comes before theme polish. The detail panel must become an overlay drawer at laptop/narrow widths so the fixed-width timeline remains readable.
</objective>

<context>
@.planning/phases/05-themes-polish-and-verification/05-UI-SPEC.md
@.planning/phases/05-themes-polish-and-verification/05-VALIDATION.md
@packages/ui/src/components/shell/AppShell.tsx
@packages/ui/src/components/detail/DetailPanel.tsx
@packages/ui/src/components/detail/DetailResizeHandle.tsx
@packages/ui/src/components/timeline/TimelineList.tsx
@packages/ui/src/components/filters/FilterBar.tsx
@packages/ui/src/state/store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Add shared detail layout constants</name>
  <files>packages/ui/src/components/detail/detail-layout.ts, packages/ui/src/state/store.ts, packages/ui/src/components/detail/DetailResizeHandle.tsx</files>
  <action>
Create `packages/ui/src/components/detail/detail-layout.ts` exporting:
- `DETAIL_MIN_WIDTH = 360`
- `DETAIL_DEFAULT_WIDTH = 420`
- `DETAIL_MAX_WIDTH = 720`
- `DETAIL_DESKTOP_BREAKPOINT = 1400`
- `DETAIL_KEYBOARD_STEP = 16`

Use these constants in `store.ts` and `DetailResizeHandle.tsx` instead of duplicated magic numbers.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui test src/components/detail/DetailPanel.test.tsx src/state/persistence.test.ts</automated>
  </verify>
  <done>
    - Shared constants exist and are imported by touched files.
    - Detail width defaults to 420 and clamps between 360 and 720.
    - Keyboard resize step remains 16px.
    - Detail panel and persistence tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Implement side rail vs overlay drawer</name>
  <files>packages/ui/src/components/shell/AppShell.tsx, packages/ui/src/components/shell/AppShell.test.tsx, packages/ui/src/components/detail/DetailPanel.tsx, packages/ui/src/styles/global.css</files>
  <action>
In `AppShell.tsx`, add a small media-query helper for `(min-width: 1400px)` with deterministic test behavior.

Desktop mode:
- Keep the existing right side rail in the main split.
- Keep `data-testid="detail-panel-wrapper"`.
- Keep resize handle visible.

Drawer mode:
- Timeline pane fills available width.
- Do not reserve `detailWidth` in the main flex split.
- If `selectedIdx !== null`, render a tokenized backdrop and right drawer.
- Include a close button whose text/accessible name is exactly `Close details`.
- Escape closes the drawer.
- Focus moves to the close button or drawer container on open.
- Resize handle is hidden.

Update detail retry copy from `Retry` to `Retry event details`.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui test src/components/shell/AppShell.test.tsx src/components/detail/DetailPanel.test.tsx</automated>
  </verify>
  <done>
    - Desktop mode renders the existing rail and resize handle.
    - Below 1400px, selected details render in an overlay drawer.
    - Drawer close and Escape behavior are tested.
    - Retry button copy is exactly `Retry event details`.
  </done>
</task>

<task type="auto">
  <name>Add responsive CSS for timeline overflow and filter wrapping</name>
  <files>packages/ui/src/styles/global.css, packages/ui/src/components/shell/AppShell.tsx, packages/ui/src/components/filters/FilterBar.tsx, packages/ui/src/components/timeline/TimelineList.tsx</files>
  <action>
Add semantic class names and CSS for `.app-shell`, `.app-main`, `.timeline-pane`, `.detail-rail`, `.detail-drawer-backdrop`, `.detail-drawer`, `.filter-bar`, and `.timeline-list-shell`.

Implement UI-SPEC breakpoints:
- `<1400px`: drawer overlay, no side rail width reservation.
- `<768px`: drawer width `100vw`.
- `768px-1399px`: drawer width `min(88vw, 720px)`.
- `>=1200px`: filter bar stays one row when space permits.
- `768px-1199px`: filter controls wrap to two rows; search min width 240px.
- `<768px`: filter chips may horizontally scroll inside the filter bar.

Keep timeline grid/header/rows aligned and put horizontal overflow inside `data-testid="timeline-list"`, not on `body`.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui test src/components/timeline/TimelineList.virt.test.tsx src/components/filters/FilterBar.test.tsx</automated>
  </verify>
  <done>
    - Shell/timeline/drawer/filter semantic classes exist.
    - Breakpoint rules match UI-SPEC.
    - Timeline overflow is localized to the timeline scroll container.
    - FilterBar and TimelineList tests pass.
  </done>
</task>

</tasks>

<verification>
Run:
1. `pnpm -F @ahp-inspector/ui test src/components/shell/AppShell.test.tsx src/components/detail/DetailPanel.test.tsx`
2. `pnpm -F @ahp-inspector/ui test src/components/timeline/TimelineList.virt.test.tsx src/components/filters/FilterBar.test.tsx`
3. `pnpm -F @ahp-inspector/ui build`
</verification>

<success_criteria>
- Responsive behavior matches 05-UI-SPEC.
- Overlay drawer is implemented.
- Timeline remains primary at laptop/narrow widths.
- Existing keyboard navigation and virtualized timeline tests remain green.
</success_criteria>

<commit_boundary>
Commit after this plan:
`feat(05-01): add responsive detail drawer layout`
</commit_boundary>

<output>
After completion, create `.planning/phases/05-themes-polish-and-verification/05-01-SUMMARY.md`.
</output>
