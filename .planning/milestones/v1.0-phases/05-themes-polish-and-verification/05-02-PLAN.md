---
phase: 05-themes-polish-and-verification
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, THEME-04, VERIFY-02]
files_modified:
  - packages/ui/src/styles/tokens.css
  - packages/ui/src/styles/global.css
  - packages/ui/src/styles/theme-tokens.test.ts
  - packages/ui/src/styles/reduced-motion-css.test.ts
  - packages/ui/src/styles/no-hex-in-components.test.ts
  - packages/ui/src/components/shell/HeaderBar.tsx
  - packages/ui/src/components/timeline/EventRow.tsx
  - packages/ui/src/components/timeline/TimelineList.tsx
  - packages/ui/src/components/detail/DetailPanel.tsx

must_haves:
  truths:
    - "Dark, light, and hacker themes are all polished token-driven product surfaces."
    - "Light theme is intentional and high contrast, not a washed-out inversion."
    - "Hacker mode has CRT scanlines/grid/noise/glow/retrowave accents."
    - "Hacker effects respect prefers-reduced-motion and do not affect virtualization measurements."
    - "No component source contains raw hex/rgb/rgba/hsl/hsla color literals."
---

<objective>
Polish all three themes through design tokens, including a distinctive hacker CRT aesthetic.

This plan delivers the visual substance of THEME-01 through THEME-04 while preserving token-first architecture and accessibility safeguards.
</objective>

<context>
@.planning/phases/05-themes-polish-and-verification/05-UI-SPEC.md
@.planning/phases/05-themes-polish-and-verification/05-VALIDATION.md
@packages/ui/src/styles/tokens.css
@packages/ui/src/styles/global.css
@packages/ui/src/components/shell/HeaderBar.tsx
@packages/ui/src/components/timeline/EventRow.tsx
@packages/ui/src/components/timeline/TimelineList.tsx
@packages/ui/src/components/detail/DetailPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Complete Phase 5 theme tokens</name>
  <files>packages/ui/src/styles/tokens.css, packages/ui/src/styles/theme-tokens.test.ts</files>
  <action>
Expand `REQUIRED_THEME_TOKENS` in `theme-tokens.test.ts`, then update `tokens.css` so dark, light, and hacker theme blocks each define the full Phase 5 token set from `05-UI-SPEC.md`, including shadows, focus ring, row hover/selection, drawer/overlay, effect opacity/glow, and text selection tokens.

Theme direction:
- Dark: keep calm matte surfaces and subtle shadows.
- Light: use crisp paper-like surfaces with high-contrast text and readable JSON colors.
- Hacker: use phosphor green base, cyan/yellow/purple/red accents, terminal borders, glow tokens, and bounded effect opacity values.

Do not add component-specific tokens unless they map to a semantic UI-SPEC role.
  </action>
  <verify>
    <automated>pnpm -F @ahp-viewer/ui test src/styles/theme-tokens.test.ts</automated>
  </verify>
  <done>
    - Full Phase 5 token list is enforced by `theme-tokens.test.ts`.
    - Dark, light, and hacker theme blocks define the same required token set.
    - Hacker effect opacity values stay within UI-SPEC bounds.
  </done>
</task>

<task type="auto">
  <name>Tokenize component polish surfaces</name>
  <files>packages/ui/src/components/shell/HeaderBar.tsx, packages/ui/src/components/timeline/EventRow.tsx, packages/ui/src/components/timeline/TimelineList.tsx, packages/ui/src/components/detail/DetailPanel.tsx, packages/ui/src/styles/no-hex-in-components.test.ts</files>
  <action>
Replace raw/generic visual values in touched components with Phase 5 tokens:
- Replace HeaderBar raw menu shadow with `var(--shadow-menu)`.
- Make active theme menu item distinguishable using tokenized selected/chip/accent styling.
- Use `--row-selected-bg` and `--row-hover-bg` for row selection/hover.
- Keep timeline header and rows aligned with tokenized surfaces.
- Use `--shadow-panel`, `--shadow-drawer`, and `--drawer-border` where appropriate.

Keep dense timeline text free of heavy text-shadow in all themes.
  </action>
  <verify>
    <automated>pnpm -F @ahp-viewer/ui test src/styles/no-hex-in-components.test.ts src/components/shell/HeaderBar.test.tsx src/components/timeline/TimelineList.virt.test.tsx src/components/detail/DetailPanel.test.tsx</automated>
  </verify>
  <done>
    - HeaderBar no longer contains raw `rgba(` menu shadow.
    - Selected and hover row colors use dedicated row tokens.
    - Raw color guard passes.
    - HeaderBar, TimelineList, and DetailPanel tests pass.
  </done>
</task>

<task type="auto">
  <name>Add hacker CRT effects with reduced-motion safeguards</name>
  <files>packages/ui/src/styles/global.css, packages/ui/src/styles/tokens.css, packages/ui/src/styles/reduced-motion-css.test.ts</files>
  <action>
Implement hacker-only CRT styling in `global.css` using `[data-theme="hacker"]` selectors and tokenized pseudo-elements:
- static scanlines
- faint grid
- bounded noise/vignette
- phosphor glow
- terminal-like borders
- tokenized selection styling

All overlays must use `pointer-events: none`. Effects must not change row height, column width, scroll position, transforms of virtualized rows, or virtualizer measurements. Add `@media (prefers-reduced-motion: reduce)` rules disabling flicker, pulsing glow, scanline drift, and animated noise/grid movement.

Add `packages/ui/src/styles/reduced-motion-css.test.ts` or equivalent static CSS test proving `global.css` contains a `prefers-reduced-motion: reduce` block that disables hacker CRT animations/effects introduced by this plan.
  </action>
  <verify>
    <automated>pnpm -F @ahp-viewer/ui test src/styles/reduced-motion-css.test.ts</automated>
    <automated>pnpm -F @ahp-viewer/ui build</automated>
  </verify>
  <done>
    - Hacker mode has visible CRT/terminal styling beyond palette changes.
    - Reduced-motion CSS test proves motion safeguards exist.
    - Effects are non-interactive and do not affect timeline measurement.
    - UI build passes.
  </done>
</task>

</tasks>

<verification>
Run:
1. `pnpm -F @ahp-viewer/ui test src/styles/theme-tokens.test.ts src/styles/no-hex-in-components.test.ts src/styles/reduced-motion-css.test.ts`
2. `pnpm -F @ahp-viewer/ui test src/components/shell/HeaderBar.test.tsx src/components/timeline/TimelineList.virt.test.tsx src/components/detail/DetailPanel.test.tsx`
3. `pnpm -F @ahp-viewer/ui build`
</verification>

<success_criteria>
- Dark, light, and hacker token blocks are complete.
- Raw-color guard is green.
- Hacker theme implements CRT styling with reduced-motion safeguards.
- No visual polish bypasses token architecture.
</success_criteria>

<commit_boundary>
Commit after this plan:
`feat(05-02): polish token themes and hacker crt effects`
</commit_boundary>

<output>
After completion, create `.planning/phases/05-themes-polish-and-verification/05-02-SUMMARY.md`.
</output>
