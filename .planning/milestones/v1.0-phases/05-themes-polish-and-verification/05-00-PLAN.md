---
phase: 05-themes-polish-and-verification
plan: 00
type: execute
wave: 0
depends_on: []
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, THEME-04, THEME-05, VERIFY-03]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - playwright.config.ts
  - packages/ui/src/styles/no-hex-in-components.test.ts
  - packages/ui/src/styles/theme-tokens.test.ts

must_haves:
  truths:
    - "Project has committed Playwright browser E2E infrastructure using @playwright/test as a dev dependency."
    - "Components cannot introduce raw color literals or raw shadow colors outside token files."
    - "Dark, light, and hacker theme blocks expose the complete token family required by 05-UI-SPEC."
    - "Playwright E2E discovery does not depend on global playwright-cli."
  artifacts:
    - path: "package.json"
      provides: "root devDependency @playwright/test and pnpm e2e script"
    - path: "playwright.config.ts"
      provides: "local-only Playwright configuration"
    - path: "packages/ui/src/styles/no-hex-in-components.test.ts"
      provides: "static guard rejecting raw component colors"
    - path: "packages/ui/src/styles/theme-tokens.test.ts"
      provides: "token completeness guard for all themes"
---

<objective>
Create Phase 5 guardrails before visual polish starts.

This plan adds repeatable browser E2E infrastructure and tightens token-first tests so later theme/layout work cannot drift away from the approved UI-SPEC.
</objective>

<context>
@.planning/phases/05-themes-polish-and-verification/05-RESEARCH.md
@.planning/phases/05-themes-polish-and-verification/05-VALIDATION.md
@.planning/phases/05-themes-polish-and-verification/05-UI-SPEC.md
@package.json
@packages/ui/src/styles/tokens.css
@packages/ui/src/styles/no-hex-in-components.test.ts
</context>

<tasks>

<task type="auto">
  <name>Add committed Playwright setup</name>
  <files>package.json, pnpm-lock.yaml, playwright.config.ts</files>
  <action>
Add `@playwright/test` as a root dev dependency. Add root scripts:
- `"e2e": "playwright test"`
- `"e2e:headed": "playwright test --headed"` if consistent with existing package script style.

Create `playwright.config.ts` with `defineConfig`, `testDir: "./e2e"`, Chromium-only project, local-only reporters, `screenshot: "only-on-failure"`, and `trace: "retain-on-failure"`. Do not configure any remote service, telemetry upload, or external reporter.
  </action>
  <verify>
    <automated>pnpm install --lockfile-only</automated>
    <automated>pnpm exec playwright test --list</automated>
  </verify>
  <done>
    - `package.json` includes root `e2e` script and devDependency `@playwright/test`.
    - `pnpm-lock.yaml` is updated by pnpm.
    - `playwright.config.ts` exists, uses `defineConfig`, and has no remote reporter/service.
    - `pnpm exec playwright test --list` passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Expand raw color guard</name>
  <files>packages/ui/src/styles/no-hex-in-components.test.ts</files>
  <action>
Rewrite `packages/ui/src/styles/no-hex-in-components.test.ts` to scan component source files and reject:
- raw `#RGB/#RRGGBB/#RRGGBBAA`
- `rgb(...)` / `rgba(...)`
- `hsl(...)` / `hsla(...)`
- raw shadow strings that contain those color forms.

Keep CSS variables, `color-mix(...)`, and tokenized shadows allowed. Include `{ file, line, pattern, text }` in failures. Do not scan `tokens.css`.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui test src/styles/no-hex-in-components.test.ts</automated>
  </verify>
  <done>
    - Guard scans component source files and excludes tests/token CSS.
    - Guard rejects raw hex, rgb/rgba, hsl/hsla, and raw shadow color forms.
    - Failure output includes file, line, pattern, and text.
    - Test passes after any existing raw component color is either excluded as token CSS or tokenized in this plan.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Add theme token completeness guard</name>
  <files>packages/ui/src/styles/theme-tokens.test.ts, packages/ui/src/styles/tokens.css</files>
  <action>
Create `packages/ui/src/styles/theme-tokens.test.ts` that reads `tokens.css` and validates that `:root, [data-theme="dark"]`, `[data-theme="light"]`, and `[data-theme="hacker"]` are parseable and expose the existing shared semantic token baseline without failing on Phase 5 tokens that are not yet implemented.

Structure the test with an explicit `REQUIRED_THEME_TOKENS` export so Plan 05-02 can expand that list to the full UI-SPEC token set in the same file when the tokens are added. Plan 05-00 must finish green.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui test src/styles/theme-tokens.test.ts</automated>
  </verify>
  <done>
    - Test file exists and passes in Plan 05-00.
    - It validates all three theme blocks are present and share the current baseline tokens.
    - It exposes a clear token list that Plan 05-02 will expand to the full Phase 5 token set.
  </done>
</task>

</tasks>

<verification>
Run:
1. `pnpm install --lockfile-only`
2. `pnpm exec playwright test --list`
3. `pnpm -F @ahp-inspector/ui test src/styles/no-hex-in-components.test.ts src/styles/theme-tokens.test.ts`
</verification>

<success_criteria>
- Playwright is committed as a dev dependency.
- `pnpm e2e` is available.
- Guardrail tests exist before visual changes and pass at the end of this plan.
</success_criteria>

<commit_boundary>
Commit after this plan:
`test(05-00): add phase 5 guardrails and playwright setup`
</commit_boundary>

<output>
After completion, create `.planning/phases/05-themes-polish-and-verification/05-00-SUMMARY.md`.
</output>
