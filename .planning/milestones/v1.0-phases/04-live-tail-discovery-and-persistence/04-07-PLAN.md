---
phase: 04-live-tail-discovery-and-persistence
plan: 07
type: execute
wave: 6
depends_on: ["04-06"]
autonomous: false
requirements: [INGEST-02, INGEST-03, INGEST-04, INGEST-05, SEARCH-05]
files_modified:
  - test/phase4-vertical-slice.test.ts
  - USER_GUIDE.md
  - .planning/phases/04-live-tail-discovery-and-persistence/04-UAT.md

must_haves:
  truths:
    - "Vertical-slice integration test exercises: discover → open by candidateId → snapshot → tail append → pause → resume → reload (new manager) restores per-log prefs."
    - "Test asserts no absolute paths are present in any HTTP response or SSE frame body — D-05 privacy gate."
    - "Test asserts SwitchLog mid-stream emits log-reset, then a fresh snapshot-begin/end."
    - "USER_GUIDE.md documents: launching with no file, the picker UI, pause/resume, switch log, persistence scope."
    - "UAT checklist captures the 10 visual states from UI-SPEC §Test Checklist with screenshot evidence."
  artifacts:
    - path: "test/phase4-vertical-slice.test.ts"
      provides: "End-to-end integration test for Phase 4 capabilities"
      contains: "describe(\"phase 4 vertical slice\""
    - path: ".planning/phases/04-live-tail-discovery-and-persistence/04-UAT.md"
      provides: "Manual UAT checklist with screenshot references"
      min_lines: 60
    - path: "USER_GUIDE.md"
      provides: "Updated user-facing docs for Phase 4 features"
      contains: "## Live tail and the log picker"
  key_links:
    - from: "test/phase4-vertical-slice.test.ts"
      to: "packages/server, packages/host-node, packages/ui transports"
      via: "spawned server + simulated browser fetch + EventSource"
      pattern: "startLogServer|connectLogStream|fetchCandidates"
---

<objective>
Lock Phase 4 with a single horizontal integration test that verifies all five requirements together, then capture screenshots for UAT and update USER_GUIDE.md. This is the phase's verification gate; it must run green before checker hand-off.

Includes one human checkpoint to capture screenshots and confirm visual fidelity to UI-SPEC.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-live-tail-discovery-and-persistence/04-CONTEXT.md
@.planning/phases/04-live-tail-discovery-and-persistence/04-UI-SPEC.md
@.planning/phases/04-live-tail-discovery-and-persistence/04-VALIDATION.md
@USER_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase4-vertical-slice integration test</name>
  <files>test/phase4-vertical-slice.test.ts</files>
  <read_first>
    .planning/phases/04-live-tail-discovery-and-persistence/04-VALIDATION.md,
    .planning/phases/04-live-tail-discovery-and-persistence/04-RESEARCH.md,
    test/boundary.test.ts
  </read_first>
  <action>
Create a vitest integration test at the workspace root in `test/phase4-vertical-slice.test.ts` (matches existing top-level `test/` directory). The test:

1. **Setup** — write two temporary jsonl files into a `tmpdir/agent/`-shaped fixture tree to look like a "discoverable" launch dir (use the same fixture helpers as Plan 04-01 if exposed; otherwise inline). Each file: a few session/request rows.
2. **Boot server with no active log** — call `startLogServer({ port: 0, hostAdapter, sessions: new LogSessionManager(...) })`. Probe `/api/log/meta` → expect 204.
3. **Discover** — `GET /api/sessions/discover` → expect `candidates.length >= 2`. Assert NO field of any candidate contains `/Users/`, `\\Users\\`, or any absolute path string. Assert each candidate has only the SafeCandidate shape.
4. **Open by candidateId** — `POST /api/sessions/open {candidateId}`. Expect 200. Then `GET /api/log/meta` → 200 with `filename` (basename only).
5. **Subscribe to SSE** — open `/api/log/stream`; collect frames. Expect snapshot-begin → snapshot-chunk(s) → snapshot-end. Assert payload bodies contain no abs paths.
6. **Tail append** — append a new line to the underlying file; expect an `append` SSE frame within 500 ms.
7. **Pause** — simulate pause: just verify the SSE frames keep arriving (server is unaware). Append more lines; assert frames received.
8. **Switch log** — `POST /api/sessions/open {candidateId: <other id>}`. Expect a `log-reset` SSE frame on the existing stream, then a new `snapshot-begin` for the new log. Assert the rotation banner is NOT triggered (log-reset is distinct from rotation).
9. **Rotation simulation** — truncate or rename the active file; expect a `rotation` SSE frame.
10. **Persistence sanity (in-memory)** — out of scope for this server-only test; persistence is jsdom-only and covered by `persist-effect.test.ts`. Note this in a comment.

Use `@hono/node-server` style integration where possible; otherwise spawn the existing test harness used elsewhere. Total test time should stay under 5 seconds.

Privacy assertion helper:

```ts
function assertNoAbsPath(payload: string): void {
  expect(payload).not.toMatch(/\/Users\//);
  expect(payload).not.toMatch(/\\Users\\/);
  expect(payload).not.toMatch(/\/home\//);
  expect(payload).not.toMatch(/[A-Za-z]:\\\\/);
}
```

Apply this to every HTTP response body and every SSE frame `event.data` string.
  </action>
  <verify>
    <automated>pnpm vitest run test/phase4-vertical-slice.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `test/phase4-vertical-slice.test.ts` exists.
    - `grep -E "phase 4 vertical slice" test/phase4-vertical-slice.test.ts` returns one match.
    - `grep -E "/api/sessions/discover" test/phase4-vertical-slice.test.ts` returns ≥ 1 match.
    - `grep -E "log-reset|rotation" test/phase4-vertical-slice.test.ts` returns ≥ 2 matches.
    - `grep -E "assertNoAbsPath" test/phase4-vertical-slice.test.ts` returns ≥ 5 matches (applied broadly).
    - `pnpm vitest run test/phase4-vertical-slice.test.ts` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: USER_GUIDE.md update</name>
  <files>USER_GUIDE.md</files>
  <read_first>USER_GUIDE.md</read_first>
  <action>
Append (or insert into the appropriate section) a Phase 4 capabilities section. Required headings + content:

```markdown
## Launching without a file

You can run the viewer without specifying a log file:

```sh
ahp-viewer
```

The browser opens to a picker showing logs the viewer discovered automatically under `~/Library/Application Support/Code` (macOS), `%APPDATA%\Code` (Windows), and `~/.config/Code` (Linux). Pick a log to begin streaming, or paste an absolute path under "or open manually".

## The log picker

- **Confidence dot** — green (JSONL), amber (Legacy heuristic match), grey (unknown).
- **Origin badge** — VS Code, VS Code Insiders, or Manual.
- Each row shows the file basename, an origin chip, the time since last modification, and the file size.
- Click "Refresh List" to rescan.

## Live tail and pause

The viewer streams new events as the file grows. Use the **Pause** button in the header to freeze the timeline; new events accumulate and a "<N> new events" pill appears at the bottom. Click the pill to flush the buffer and resume. Pause is local to the browser — the server keeps reading.

## Switching logs

Click **Switch log…** at any time to open the same picker over the current view. Selecting a different log resets the timeline.

## Persistence

Filter selections, column visibility, expanded groups, and the currently-selected event are remembered per-log in your browser's localStorage. Reloading the page restores them. Up to 50 logs are remembered (least-recently-used logs are evicted).

## Banners

- **Log file rotated.** — Appears once when the active file rotates (truncate/rename). The viewer continues from the new file.
- **Lost watch on log file.** — Appears if the OS dropped the file watcher. Click **Retry** to reconnect.
```
  </action>
  <verify>
    <automated>grep -nE "Launching without a file|The log picker|Live tail and pause|Switching logs" USER_GUIDE.md | wc -l | awk '{ if ($1 < 4) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "## Launching without a file" USER_GUIDE.md` returns one match.
    - `grep -E "## The log picker" USER_GUIDE.md` returns one match.
    - `grep -E "## Live tail and pause" USER_GUIDE.md` returns one match.
    - `grep -E "## Switching logs" USER_GUIDE.md` returns one match.
    - `grep -E "## Persistence" USER_GUIDE.md` returns one match.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual UAT — capture 10 screenshots from UI-SPEC test checklist</name>
  <files>screenshots/phase4/, .planning/phases/04-live-tail-discovery-and-persistence/04-UAT.md</files>
  <what-built>
    All Phase 4 UI surfaces: NoActiveLogState, CandidateRow variants (high/medium/low confidence + manual origin), NoCandidatesHint, ManualOpenInput error states, AppShell with rows + LivePauseButton, NewEventsPill, RotationBanner, WatchErrorBanner, LogPickerPanel.
  </what-built>
  <action>Capture 10 screenshots per the verification steps below; save into `screenshots/phase4/`; index them in `.planning/phases/04-live-tail-discovery-and-persistence/04-UAT.md`.</action>
  <verify>Manual visual review of screenshots against UI-SPEC §1–§4 copy strings and layouts.</verify>
  <done>10 PNGs in screenshots/phase4/; 04-UAT.md filled with status per row; reviewer types "approved".</done>
  <how-to-verify>
**Setup:**
1. Run `pnpm dev:mock-server` (uses the in-repo mock log fixture) in one terminal.
2. Run `pnpm dev:cli` in another to launch the viewer with no file.
3. Open the URL printed by the CLI in a browser at 1440×900.

**Capture and save the following PNGs into `screenshots/phase4/`** using either OS screen capture or the playwright-cli skill if available:

1. `01-no-active-log.png` — Initial picker with ≥3 candidates of mixed confidence.
2. `02-no-candidates-hint.png` — Picker with no candidates (rename fixtures temporarily).
3. `03-manual-open-error-not-found.png` — Type a non-existent path, click Open Log; banner shows "File not found. Check the path and try again."
4. `04-manual-open-error-too-long.png` — Paste a 5000-char path; "Path is too long."
5. `05-app-shell-with-rows.png` — Picker → select log → AppShell with timeline populated.
6. `06-live-pause-button-paused.png` — Pause button activated; aria-pressed=true; appearance distinct.
7. `07-new-events-pill.png` — Append rows while paused; pill shows "5 new events".
8. `08-switch-log-panel.png` — Click Switch log; LogPickerPanel overlay open.
9. `09-rotation-banner.png` — Rotate file via mock; banner: "Log file rotated. Reading new file from start."
10. `10-watch-error-banner.png` — Drop watch via mock; banner: "Lost watch on log file. Live updates paused." with Retry.

**Verify visually:**
- Copy strings match UI-SPEC Copywriting Contract VERBATIM.
- No raw colors that look mismatched (everything from token palette).
- No absolute paths visible anywhere.
- Layouts match UI-SPEC §1–§4.

**Save the index to `.planning/phases/04-live-tail-discovery-and-persistence/04-UAT.md`** with one entry per screenshot:

```markdown
# Phase 4 UAT

| # | Screenshot | UI-SPEC Reference | Status |
|---|------------|--------------------|--------|
| 1 | screenshots/phase4/01-no-active-log.png | UI-SPEC §1 | ✅ |
| 2 | screenshots/phase4/02-no-candidates-hint.png | UI-SPEC §1.NoCandidates | ✅ |
| ... | ... | ... | ... |
```

For each row, write a one-line note on visual fidelity ("matches", "minor delta — column gap 1px wider than spec, accepted", etc.).
  </how-to-verify>
  <resume-signal>Type "approved" once 04-UAT.md is filled and all 10 screenshots are saved. Describe any visual deltas accepted.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration test ↔ filesystem | test writes to tmpdir; cleans up |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Information Disclosure | screenshots saved to repo | mitigate | Use mock fixture data only — never real user logs. Reviewer instruction: do NOT capture screens of real Copilot logs. |
| T-04-07-02 | Information Disclosure | integration test bodies | mitigate | `assertNoAbsPath` applied to every response body and SSE frame; failure surfaces immediately. |
| T-04-07-03 | Test flakiness | timer-driven SSE assertions | mitigate | Use waitFor/poll with explicit timeouts; cap test at 5s. |
</threat_model>

<verification>
- `pnpm vitest run test/phase4-vertical-slice.test.ts` — green.
- USER_GUIDE.md contains all five new sections.
- `.planning/phases/04-live-tail-discovery-and-persistence/04-UAT.md` exists with 10 entries; `screenshots/phase4/` has 10 PNGs.
- Phase verification gate: `pnpm vitest run` (full workspace) — all Phase 4 work green.
- `pnpm lint && pnpm typecheck` — green.
</verification>

<success_criteria>
1. Vertical-slice test exercises discover → open → tail → switch → rotation, all with privacy assertions.
2. USER_GUIDE.md documents launching without a file, picker, pause, switch, persistence, banners.
3. 10 UAT screenshots captured and indexed in 04-UAT.md.
4. Full workspace test suite green.
5. Phase 4 deliverables match UI-SPEC visually.
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-tail-discovery-and-persistence/04-07-SUMMARY.md`.
</output>
