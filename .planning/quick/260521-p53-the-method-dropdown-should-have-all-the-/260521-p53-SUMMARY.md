---
quick_id: 260521-p53
status: complete
date: 2026-05-22
commit: 377b891
---

# Quick Task 260521-p53 Summary

## Result

The Method facet now behaves as a checked visibility list: discovered methods are checked unless they are hidden, and `ping` is hidden by default through the normal app filter state.

## Changes

- Added `APP_DEFAULT_FILTERS` with `method: ["ping"]` for initial app state, clear/reset flows, and log-switch resets.
- Changed method filtering from inclusion to exclusion while preserving match-all behavior for `EMPTY_FILTERS` and allowing null-method rows through method exclusions.
- Derived Method popover checked values from available methods minus hidden methods, preserving hidden methods that are not currently present in the option list.
- Rendered method chips as `Hidden method: ...`, with dismiss showing that method again.
- Added focused selector, FilterBar, active-chip, and persistence reset coverage.

## Verification

- `pnpm -F @ahp-inspector/ui test -- FilterBar.test.tsx selectors.test.ts` passed.
- `pnpm -F @ahp-inspector/ui typecheck` passed.
- VS Code diagnostics reported no errors in touched files.
