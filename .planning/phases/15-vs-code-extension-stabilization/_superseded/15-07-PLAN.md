---
phase: 15
plan: 07
type: execute
wave: 4
depends_on: [06]
files_modified: []
autonomous: false
requirements: [STAB-15-04]
must_haves:
  truths:
    - "In a fresh VS Code (Code OSS or Insiders) session with the built extension loaded, opening an AHP JSONL log shows the timeline."
    - "Clicking a row in the timeline opens the detail panel with event content — no 'Failed to load event: 403' or any HTTP-style failure appears."
    - "Typing a query in the search input highlights matching rows — no 403."
    - "Opening the State Inspector from the detail panel populates state data with a confidence badge — no 403."
    - "Switching the active log via the picker swaps the timeline to the new log — no 403."
    - "After a simulated disconnect (or natural disconnect), clicking Reconnect re-opens the stream — no 403."
  artifacts:
    - path: ".planning/phases/15-vs-code-extension-stabilization/15-07-SUMMARY.md"
      provides: "UAT outcome log with timestamps, screenshots-or-notes per scenario, and explicit pass/fail per scenario"
  key_links: []
---

<objective>
Manual end-to-end verification of the five webview interaction surfaces fixed in Plans 02–05. CONTEXT.md flags this as required because Phase 11 deferred its real-extension UAT.

Purpose: Automated tests + boundary rule confirm the code shape is right; this checkpoint confirms the bug is actually gone for a human exercising the real extension.

Output: A UAT outcome log in 15-07-SUMMARY.md.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-vs-code-extension-stabilization/15-CONTEXT.md
@.planning/phases/15-vs-code-extension-stabilization/15-02-SUMMARY.md
@.planning/phases/15-vs-code-extension-stabilization/15-03-SUMMARY.md
@.planning/phases/15-vs-code-extension-stabilization/15-04-SUMMARY.md
@.planning/phases/15-vs-code-extension-stabilization/15-05-SUMMARY.md
@.planning/phases/15-vs-code-extension-stabilization/15-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Webview end-to-end UAT</name>
  <files>.planning/phases/15-vs-code-extension-stabilization/15-07-SUMMARY.md, screenshots/phase15/</files>
  <action>
Build the extension (`pnpm -F @ahp-inspector/ui build && pnpm -F @ahp-inspector/extension build`), launch a Code OSS / VS Code Insiders dev session with the extension loaded, open an AHP JSONL log from `test/fixtures/`, then walk through the six scenarios in `<how-to-verify>`. Record pass/fail per scenario in 15-07-SUMMARY.md and save screenshots for any failures to `screenshots/phase15/` in the workspace.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-vs-code-extension-stabilization/15-07-SUMMARY.md</automated>
  </verify>
  <done>
15-07-SUMMARY.md records explicit pass/fail for each of the six scenarios. All scenarios pass OR follow-up plans/reverts are documented.
  </done>
  <what-built>
Wave 2 routed five UI components (DetailPanel, StateInspectorPanel, useSearch, AppShell, TimelineRegion) through the injected `AhpViewerClient` so the VS Code webview uses postMessage instead of HTTP `fetch`. Wave 3 added a boundary test that prevents future runtime imports of `transport/*-client.js` from `packages/ui/src/components/**`.
  </what-built>
  <how-to-verify>
**Setup (executor runs these before handing off):**

1. Build both packages:
   ```
   pnpm -F @ahp-inspector/ui build && pnpm -F @ahp-inspector/extension build
   ```
2. Confirm `packages/extension/ui-dist/` contains the freshly built UI bundle.
3. Launch a Code OSS (or VS Code Insiders) dev session with the extension loaded — either via `Extension Development Host` from this repo, or by packaging with `vsce package` and `code --install-extension <vsix>` in a clean profile.

**Scenarios (user runs each manually and records pass/fail in 15-07-SUMMARY.md):**

Open any sample AHP JSONL log from `test/fixtures/` (e.g. `long-realistic-ahp.jsonl`) inside the dev session and run the AHP Inspector command from the Command Palette.

| # | Scenario | Expected behavior | Anti-signal (FAIL) |
|---|----------|-------------------|--------------------|
| 1 | Timeline render | Rows appear, top-of-log selected | Blank timeline / spinner that never resolves |
| 2 | Row click → detail | Clicking any row opens the detail panel with event JSON, summary, and field strip | "Failed to load event: 403" anywhere in the webview |
| 3 | Search | Typing a substring into the search box highlights matching rows; Enter / Shift+Enter navigates between matches | No matches when matches should exist; 403 toast/error |
| 4 | State Inspector | From a detail panel, open the State Inspector — state resources render with a confidence badge | "Failed to fetch state" / 403 |
| 5 | Log switch | Use the log picker to switch to a different log — timeline replaces, detail clears | Picker hangs; timeline stuck on old log; 403 |
| 6 | Reconnect | If the disconnected banner appears (or simulate by editing the JSONL externally and back), clicking Reconnect re-opens the stream | Banner persists after click; 403 |

Capture **screenshots saved to `screenshots/phase15/` in the workspace** for each scenario (the user requires workspace screenshots for verification, not /tmp). At minimum: one screenshot per failing scenario. Passing scenarios may be confirmed by note alone.

Open the webview Developer Tools (right-click webview → "Open Webview Developer Tools" or via `Developer: Open Webview Developer Tools` command) and verify the Console contains NO `fetch` errors and NO `/api/` URLs being hit.
  </how-to-verify>
  <resume-signal>
Reply "approved — all 6 scenarios pass" OR list the failing scenarios with the screenshot paths. If any scenario fails, the phase is incomplete — open a follow-up plan or revert the offending refactor.
  </resume-signal>
</task>

</tasks>

<verification>
- 15-07-SUMMARY.md records explicit pass/fail per scenario
- `screenshots/phase15/` exists with at least one screenshot OR a note that no failures occurred (so no screenshots needed beyond confirmation)
- No new automated failures since Plan 06
</verification>

<success_criteria>
All six scenarios pass in the VS Code webview. The bug class described in CONTEXT.md is verifiably gone.
</success_criteria>

<output>
After completion, create `.planning/phases/15-vs-code-extension-stabilization/15-07-SUMMARY.md`.
</output>
