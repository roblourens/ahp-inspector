---
phase: 15
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/ui/src/components/filters/useSearch.ts
  - packages/ui/src/components/filters/FilterBar.test.tsx
autonomous: true
requirements: [STAB-15-01, STAB-15-02]
must_haves:
  truths:
    - "useSearch calls searchEvents through the injected AhpViewerClient, not the raw search-client module."
    - "In the VS Code webview, typing a search query yields highlighted results (no 403)."
    - "FilterBar.test.tsx covers the search flow through a fake AhpViewerClient (no vi.mock of transport/search-client)."
  artifacts:
    - path: "packages/ui/src/components/filters/useSearch.ts"
      provides: "Uses useAhpViewerClient() for searchEvents"
      contains: "useAhpViewerClient"
    - path: "packages/ui/src/components/filters/FilterBar.test.tsx"
      provides: "Search-flow test driven by injected fake client"
  key_links:
    - from: "packages/ui/src/components/filters/useSearch.ts"
      to: "packages/ui/src/transport/transport-context.tsx"
      via: "useAhpViewerClient() hook"
      pattern: "useAhpViewerClient\\(\\)"
---

<objective>
Route `useSearch` through the injected `AhpViewerClient`. Add coverage in `FilterBar.test.tsx` that exercises the search effect through a fake client so the behavior is locked in.

Purpose: Search currently 403s in the webview because `useSearch` imports `searchEvents` from `transport/search-client.js` directly. The fix is a single hook call swap; the small test addition guards against regression now that the original transport-module mock pattern is gone.

Output: 1 hook refactored, 1 test file extended with search assertions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-vs-code-extension-stabilization/15-CONTEXT.md
@.planning/phases/15-vs-code-extension-stabilization/15-01-SUMMARY.md

@packages/ui/src/components/filters/useSearch.ts
@packages/ui/src/components/filters/FilterBar.test.tsx
@packages/ui/src/transport/client.ts
@packages/ui/src/transport/transport-context.tsx

<interfaces>
From `AhpViewerClient`:
```typescript
searchEvents(q: string, signal?: AbortSignal): Promise<SearchResult>;
```

Shared test helper (Plan 01):
```typescript
import { fakeAhpViewerClient, renderWithClient } from "../../test-utils/fakeAhpViewerClient.js";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor useSearch.ts</name>
  <files>packages/ui/src/components/filters/useSearch.ts</files>
  <action>
- Remove `import { searchEvents } from "../../transport/search-client.js";`.
- Add `import { useAhpViewerClient } from "../../transport/transport-context.js";`.
- Inside the `useSearch` hook body (before the `useEffect`), add: `const client = useAhpViewerClient();`.
- In the debounced timer, replace `searchEvents(query, ctrl.signal)` with `client.searchEvents(query, ctrl.signal)`.
- Add `client` to the `useEffect` dependency array (alongside `query`, `clearSearchResults`, etc.).
- Keep the abort/debounce/error-handling logic identical otherwise. `AbortError` short-circuit, timer cleanup, and the volatile-results clear-on-empty branch all remain.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui typecheck && pnpm -F @ahp-inspector/ui test -- useSearch FilterBar</automated>
  </verify>
  <done>
    `useSearch.ts` has no value import from `transport/search-client.js`, uses `useAhpViewerClient()`, all existing tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add search coverage in FilterBar.test.tsx</name>
  <files>packages/ui/src/components/filters/FilterBar.test.tsx</files>
  <action>
Add a new `describe("useSearch via injected client", ...)` block (or extend an existing block) with at least two cases:

1. **Happy path** — Build `const searchEvents = vi.fn().mockResolvedValue({ matches: [{ idx: 0 }], total: 1, truncated: false });` (use a `SearchResult`-shaped value; copy minimal shape from `transport/search-client.ts`). Render the parent component that mounts `useSearch` (FilterBar itself if it mounts the hook, else a small inline test harness component) with `renderWithClient(...)`. Type a query into the search input (`fireEvent.change` or RTL `userEvent.type`), advance fake timers past 150ms (use `vi.useFakeTimers()` / `vi.advanceTimersByTime(150)`), and assert `searchEvents` was called with `(query, expect.any(AbortSignal))`.

2. **Empty query** — Verify that clearing the query does NOT call `searchEvents` (it should call `clearSearchResults` on the store instead — that store interaction is observable via `useAppStore.getState().searchMatches` being reset, or via a spy on the store action if the test already does that).

Use `vi.useFakeTimers()` in the `beforeEach` for this block and `vi.useRealTimers()` in `afterEach`. Per repo convention (faked timers in tests), do NOT use real setTimeout.

Do NOT add `vi.mock("../../transport/search-client.js")`. The whole point is to drive search through the injected fake.

If FilterBar.test.tsx already covers some of this via store-level assertions, augment rather than duplicate — but the call-to-`client.searchEvents` assertion is new and required.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui test -- FilterBar.test.tsx</automated>
  </verify>
  <done>
    New tests in `FilterBar.test.tsx` pass; they call `client.searchEvents` and assert the call shape; no `vi.mock("../../transport/search-client.js")` is added.
  </done>
</task>

</tasks>

<verification>
- `pnpm -F @ahp-inspector/ui typecheck` clean
- `pnpm -F @ahp-inspector/ui test -- filters` green
- `pnpm -F @ahp-inspector/ui test` full UI suite green
- `grep -E "from \"\\.\\./\\.\\./transport/search-client" packages/ui/src/components/filters/useSearch.ts | grep -v "import type"` returns nothing
</verification>

<success_criteria>
Search runs through `useAhpViewerClient().searchEvents` in both browser and webview runtimes. New tests prove the wiring in CI.
</success_criteria>

<output>
After completion, create `.planning/phases/15-vs-code-extension-stabilization/15-03-SUMMARY.md`.
</output>
