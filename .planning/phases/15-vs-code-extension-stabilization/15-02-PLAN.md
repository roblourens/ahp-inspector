---
phase: 15
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/transport/api-base.ts
  - packages/ui/src/transport/api-base.test.ts
  - packages/ui/src/transport/http-client.ts
  - packages/ui/src/transport/sse-client.ts
  - packages/ui/src/transport/search-client.ts
  - packages/ui/src/transport/state-client.ts
  - packages/ui/src/transport/sessions-client.ts
  - packages/ui/src/transport/browser-client.ts
autonomous: true
requirements: [STAB-15-B, STAB-15-API-BASE]
must_haves:
  truths:
    - "A new apiUrl(path) helper reads window.__AHP_API_BASE__ (with type-safe fallback to empty string) and prepends it to the given path."
    - "All five transport client modules (http, sse, search, state, sessions) plus probeLogMeta in browser-client route their URLs through apiUrl(...)."
    - "When window.__AHP_API_BASE__ is unset (standalone CLI build), apiUrl('/api/log/meta') === '/api/log/meta' — relative URLs resolve against the page origin exactly as today."
    - "When window.__AHP_API_BASE__ is 'http://localhost:51234', apiUrl('/api/log/meta') === 'http://localhost:51234/api/log/meta'."
    - "All existing transport tests still pass (the helper is a pure prefix; behavior under default base is unchanged)."
  artifacts:
    - path: "packages/ui/src/transport/api-base.ts"
      provides: "apiUrl(path) helper + global typing"
      exports: ["apiUrl"]
    - path: "packages/ui/src/transport/api-base.test.ts"
      provides: "Unit tests for apiUrl with and without window.__AHP_API_BASE__"
  key_links:
    - from: "packages/ui/src/transport/http-client.ts"
      to: "packages/ui/src/transport/api-base.ts"
      via: "fetch(apiUrl('/api/log/event/...'))"
      pattern: "apiUrl\\("
    - from: "packages/ui/src/transport/sse-client.ts"
      to: "packages/ui/src/transport/api-base.ts"
      via: "EventSource(apiUrl('/api/log/stream'))"
      pattern: "apiUrl\\("
---

<objective>
Introduce a tiny `apiUrl(path)` helper that prefixes every transport URL with `window.__AHP_API_BASE__` (set by the extension HTML in Plan 03), defaulting to empty string for standalone. Route all five `*-client.ts` callsites and the `probeLogMeta` `fetch("/api/log/meta")` in `browser-client.ts` through it.

Purpose: The webview is hosted on `vscode-webview://...` but needs to hit `http://localhost:{port}/api/...`. Relative URLs like `fetch("/api/...")` would resolve to `vscode-webview://.../api/...`, which doesn't exist. This helper makes the prefix configurable at runtime without affecting the standalone CLI (where `__AHP_API_BASE__` is undefined → empty prefix → relative URL → page origin → loopback server, exactly as today).

Output: 1 new helper, 1 new test, 5 transport modules edited (one-line URL changes each).
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-vs-code-extension-stabilization/15-CONTEXT.md
@.planning/phases/15-vs-code-extension-stabilization/15-RESEARCH.md

@packages/ui/src/transport/http-client.ts
@packages/ui/src/transport/sse-client.ts
@packages/ui/src/transport/search-client.ts
@packages/ui/src/transport/state-client.ts
@packages/ui/src/transport/sessions-client.ts
@packages/ui/src/transport/browser-client.ts

<interfaces>
Existing callsites (exact lines):
- `packages/ui/src/transport/http-client.ts:76` — `await fetch(\`/api/log/event/${idx}\`, init);`
- `packages/ui/src/transport/search-client.ts:19` — `await fetch(\`/api/log/search?${params.toString()}\`, init);`
- `packages/ui/src/transport/state-client.ts:99` — `await fetch(\`/api/state-at?${params.toString()}\`, init);`
- `packages/ui/src/transport/sessions-client.ts:41` — `await fetch("/api/sessions/discover");`
- `packages/ui/src/transport/sessions-client.ts:89` — `await fetch("/api/sessions/open", { ... });`
- `packages/ui/src/transport/sse-client.ts:44,51` — `opts.url ?? "/api/log/stream"`, then `new Ctor(url, ...)`. The DEFAULT URL is what needs prefixing; an explicit `opts.url` override is honored as-is (tests inject custom URLs).
- `packages/ui/src/transport/browser-client.ts:21` — `resp = await fetch("/api/log/meta");` (in `probeLogMeta`)

Note: sessions-client.ts likely has additional `fetch` calls beyond lines 41 and 89 (e.g. `openSessionByPath`). Sweep ALL `fetch("/api/...")` and `fetch(\`/api/...\`)` calls in that file; do not stop at the two grep matched.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create apiUrl helper + tests</name>
  <files>packages/ui/src/transport/api-base.ts, packages/ui/src/transport/api-base.test.ts</files>
  <action>
Create `packages/ui/src/transport/api-base.ts`:

```typescript
// API URL prefixing for the UI bundle. The extension webview needs to hit
// the loopback server on a different origin (http://localhost:{port}); the
// standalone CLI build serves the UI from the same origin and uses relative
// URLs. The extension HTML injects `window.__AHP_API_BASE__` to opt in.
//
// Phase 15: this is the single seam for cross-origin HTTP/SSE access.

declare global {
  interface Window {
    __AHP_API_BASE__?: string;
  }
}

/** Prefix `path` (which must start with `/`) with the configured API base
 *  URL, or return `path` unchanged when no base is set. */
export function apiUrl(path: string): string {
  const base = (typeof window !== "undefined" ? window.__AHP_API_BASE__ : undefined) ?? "";
  if (!base) return path;
  // Tolerate trailing slash on base; require leading slash on path.
  const trimmedBase = base.endsWith("/") ? base.slice(0, -1) : base;
  return `${trimmedBase}${path}`;
}

export {};
```

Create `packages/ui/src/transport/api-base.test.ts` with vitest cases:
1. Without `window.__AHP_API_BASE__` set, `apiUrl("/api/x")` returns `"/api/x"`.
2. With `window.__AHP_API_BASE__ = "http://localhost:51234"`, `apiUrl("/api/x")` returns `"http://localhost:51234/api/x"`.
3. With trailing slash `"http://localhost:51234/"`, `apiUrl("/api/x")` returns `"http://localhost:51234/api/x"` (no double slash).
4. With base set, `apiUrl("/api/log/stream")` returns the prefixed URL (sanity for the SSE callsite).

Use `beforeEach`/`afterEach` to reset `window.__AHP_API_BASE__` so tests don't leak between cases.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui test -- api-base</automated>
  </verify>
  <done>
    `api-base.ts` exists and exports `apiUrl`. `api-base.test.ts` passes all 4 cases. `pnpm -F @ahp-inspector/ui typecheck` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route all transport callsites through apiUrl</name>
  <files>packages/ui/src/transport/http-client.ts, packages/ui/src/transport/sse-client.ts, packages/ui/src/transport/search-client.ts, packages/ui/src/transport/state-client.ts, packages/ui/src/transport/sessions-client.ts, packages/ui/src/transport/browser-client.ts</files>
  <action>
For each of the six files, add `import { apiUrl } from "./api-base.js";` and rewrite the URL arguments:

**`http-client.ts:76`** — `fetch(\`/api/log/event/${idx}\`, init)` → `fetch(apiUrl(\`/api/log/event/${idx}\`), init)`.

**`search-client.ts:19`** — `fetch(\`/api/log/search?${params.toString()}\`, init)` → `fetch(apiUrl(\`/api/log/search?${params.toString()}\`), init)`.

**`state-client.ts:99`** — `fetch(\`/api/state-at?${params.toString()}\`, init)` → `fetch(apiUrl(\`/api/state-at?${params.toString()}\`), init)`.

**`sessions-client.ts:41,89` AND ANY OTHER `fetch("/api/...")` CALLS IN THIS FILE** — read the whole file and wrap every `fetch("/api/..." | \`/api/...\`)` in `apiUrl(...)`. Likely there are 3–4 callsites total (discover, open, close, active). Sweep all of them.

**`sse-client.ts:51`** — `const url = opts.url ?? "/api/log/stream";` → `const url = opts.url ?? apiUrl("/api/log/stream");`. Important: only the DEFAULT is prefixed. If `opts.url` is provided (tests do this), it is used verbatim. This means tests that pass an absolute or fake URL keep working unchanged.

**`browser-client.ts:21`** — `resp = await fetch("/api/log/meta");` → `resp = await fetch(apiUrl("/api/log/meta"));`. Sweep the rest of `browser-client.ts` for any other `/api/` callsites (likely none — most logic is delegated to the per-route clients above).

Run typecheck and the full UI test suite. Existing tests should pass without modification because `apiUrl` returns the input unchanged when `window.__AHP_API_BASE__` is unset (vitest doesn't set it).

If any test sets `window.location` and expects a particular URL, those tests are unaffected because they don't set `__AHP_API_BASE__`.

Do NOT delete the existing `vi.mock(\"../../transport/...\")` patterns in component tests — they still mock at the module level. The `apiUrl` change is invisible to those tests.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui test</automated>
  </verify>
  <done>
    Full UI test suite green with no expectation changes. `grep -c "apiUrl(" packages/ui/src/transport/*.ts` returns ≥ 6 (one per callsite minimum). `grep "fetch(\"/api/\\|fetch(\\\`/api/" packages/ui/src/transport/*.ts | grep -v test | grep -v api-base` returns nothing (every transport call is prefixed).
  </done>
</task>

</tasks>

<verification>
- `pnpm -F @ahp-inspector/ui test` green
- `pnpm -F @ahp-inspector/ui typecheck` clean
- `pnpm -F @ahp-inspector/ui build` succeeds
- `grep -rE "fetch\\([\"\\\`]/api/" packages/ui/src --include='*.ts'` returns nothing outside test files
- `grep -c "apiUrl" packages/ui/src/transport/api-base.ts` ≥ 1
</verification>

<success_criteria>
The UI bundle is now configurable via `window.__AHP_API_BASE__` for cross-origin API access. Standalone CLI behavior is preserved (helper returns input unchanged when global is unset). Plan 03 can now inject the global from the extension HTML.
</success_criteria>

<output>
After completion, create `.planning/phases/15-vs-code-extension-stabilization/15-02-SUMMARY.md`.
</output>
