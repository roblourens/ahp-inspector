---
phase: 06-protocol-reducer-sync-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - packages/protocol/src/reducers.test.ts
  - packages/protocol/src/source-info.test.ts
  - packages/protocol/src/fixture-privacy.test.ts
  - packages/protocol/src/source-info.ts
  - packages/protocol/test-cases/reducers/*.json
autonomous: true
requirements:
  - SYNC-03
  - VERIFY-01
must_haves:
  truths:
    - "The synced AHP source commit is exported from `@ahp-viewer/protocol` and verified by tests."
    - "Reducer fixture parity passes locally for root, session, and terminal reducers using copied upstream reducer JSON fixtures."
    - "Reducer parity is deterministic because `Date.now()` is controlled to the upstream fixture value `9999`."
    - "Copied fixtures remain synthetic reducer fixtures and do not include real `.jsonl` or log data."
  artifacts:
    - path: "packages/protocol/src/reducers.test.ts"
      provides: "Vitest reducer fixture parity harness"
      contains: "const MOCK_NOW = 9999"
    - path: "packages/protocol/src/source-info.test.ts"
      provides: "Source commit diagnostics verification"
      contains: "AHP_SOURCE_COMMIT"
    - path: "packages/protocol/src/fixture-privacy.test.ts"
      provides: "Generated fixture privacy guard"
      contains: "test-cases/reducers"
    - path: "packages/protocol/.ahp-version"
      provides: "Synced upstream commit hash visible to tests"
      contains: "^[0-9a-f]+$"
  key_links:
    - from: "packages/protocol/src/reducers.test.ts"
      to: "packages/protocol/test-cases/reducers/*.json"
      via: "fixture directory load"
      pattern: "test-cases/reducers"
    - from: "packages/protocol/src/reducers.test.ts"
      to: "packages/protocol/src/reducers.ts"
      via: "root/session/terminal reducer imports"
      pattern: "rootReducer.*sessionReducer.*terminalReducer"
    - from: "packages/protocol/src/source-info.test.ts"
      to: "packages/protocol/.ahp-version"
      via: "commit equality check"
      pattern: "AHP_SOURCE_COMMIT"
---

<objective>
Add reducer parity fixture tests, source-commit diagnostics tests, fixture privacy checks, and full Phase 6 validation for the generated protocol package.

Purpose: The generated package is only safe if reducer behavior matches upstream fixtures, the source commit is visible to diagnostics, and validation catches nondeterminism or stale protocol sources.
Output: Vitest parity harness, source commit tests, privacy fixture guard, and green focused/full validation commands.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-protocol-reducer-sync-foundation/06-RESEARCH.md
@.planning/phases/06-protocol-reducer-sync-foundation/06-VALIDATION.md
@.planning/research/PITFALLS.md
@packages/protocol/package.json
@packages/protocol/src/index.ts
@packages/protocol/src/source-info.ts
@packages/protocol/src/reducers.ts
@packages/protocol/src/state.ts
@packages/protocol/src/actions.ts
@packages/protocol/src/action-origin.generated.ts
@packages/protocol/.ahp-version
@/Users/roblou/code/agent-host-protocol/types/reducers.test.ts

<interfaces>
Upstream parity strategy from `/Users/roblou/code/agent-host-protocol/types/reducers.test.ts`:
```ts
const MOCK_NOW = 9999;
Date.now = () => MOCK_NOW;
state = rootReducer(state as RootState, action as any);
state = terminalReducer(state as TerminalState, action as any);
state = sessionReducer(state as SessionState, action as any);
```

Generated source metadata contract from Plan 06-01:
```ts
export const AHP_SOURCE_COMMIT = "<short git hash>" as const;
export const AHP_SOURCE_REPOSITORY = "agent-host-protocol" as const;
export const AHP_SOURCE_TYPES_PATH = "../agent-host-protocol/types" as const;
export const AHP_GENERATED_FILES = [...] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-03-01: Add deterministic reducer fixture parity harness</name>
  <read_first>
    packages/protocol/src/reducers.ts
    packages/protocol/src/state.ts
    packages/protocol/src/actions.ts
    packages/protocol/test-cases/reducers
    /Users/roblou/code/agent-host-protocol/types/reducers.test.ts
    .planning/phases/06-protocol-reducer-sync-foundation/06-RESEARCH.md
    .planning/phases/06-protocol-reducer-sync-foundation/06-VALIDATION.md
  </read_first>
  <files>packages/protocol/src/reducers.test.ts</files>
  <behavior>
    - Loads every copied `packages/protocol/test-cases/reducers/*.json` fixture in sorted order.
    - Converts JSON `null` to `undefined` before comparing expected state.
    - Mocks `Date.now()` to `9999` for every fixture and restores it afterward.
    - Applies `rootReducer`, `sessionReducer`, and `terminalReducer` according to fixture `reducer`.
  </behavior>
  <action>Create `packages/protocol/src/reducers.test.ts` as a Vitest adaptation of the upstream Node test. Use `describe`, `it`, `beforeEach`, `afterEach`, and `expect` from `vitest`; use `readFileSync` and `readdirSync` from `node:fs`, `resolve` and `dirname` from `node:path`, and `fileURLToPath` from `node:url`. Import `rootReducer`, `sessionReducer`, and `terminalReducer` from `./reducers.js`; import `RootState`, `SessionState`, and `TerminalState` types from `./state.js`. Define `const fixtureDir = resolve(root, "../test-cases/reducers")` because the test file is under `src`. Define `const MOCK_NOW = 9999`, store `originalDateNow`, assign `Date.now = () => MOCK_NOW` in `beforeEach`, and restore in `afterEach`. Implement recursive `nullToUndefined` exactly so expected optional fields match reducer output. For each fixture, reduce all actions and `expect(state).toEqual(fixture.expected)`. Include assertions that fixture files are non-empty and include at least one `root`, one `session`, and one `terminal` reducer fixture.</action>
  <verify>
    <automated>pnpm test -- packages/protocol/src/reducers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'const MOCK_NOW = 9999' packages/protocol/src/reducers.test.ts`
    - `grep -q 'Date.now = () => MOCK_NOW' packages/protocol/src/reducers.test.ts`
    - `grep -q 'nullToUndefined' packages/protocol/src/reducers.test.ts`
    - `grep -q 'rootReducer' packages/protocol/src/reducers.test.ts`
    - `grep -q 'sessionReducer' packages/protocol/src/reducers.test.ts`
    - `grep -q 'terminalReducer' packages/protocol/src/reducers.test.ts`
  </acceptance_criteria>
  <done>Reducer parity tests pass deterministically against copied upstream root/session/terminal fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-03-02: Add source commit diagnostics and fixture privacy tests</name>
  <read_first>
    packages/protocol/src/source-info.ts
    packages/protocol/src/index.ts
    packages/protocol/.ahp-version
    packages/protocol/test-cases/reducers
    scripts/sync-agent-host-protocol.ts
    .planning/phases/06-protocol-reducer-sync-foundation/06-RESEARCH.md
  </read_first>
  <files>packages/protocol/src/source-info.test.ts, packages/protocol/src/fixture-privacy.test.ts, packages/protocol/src/source-info.ts, packages/protocol/test-cases/reducers/*.json</files>
  <behavior>
    - Tests prove `AHP_SOURCE_COMMIT` is not `unsynced`, equals `.ahp-version`, and looks like a git hash.
    - Tests prove the package barrel exports source metadata.
    - Tests prove reducer fixtures are JSON-only synthetic fixtures and no real log files were copied.
  </behavior>
  <action>Create `packages/protocol/src/source-info.test.ts` importing `AHP_SOURCE_COMMIT`, `AHP_SOURCE_REPOSITORY`, `AHP_SOURCE_TYPES_PATH`, and `AHP_GENERATED_FILES` from `./source-info.js`; read `../.ahp-version` from the package root and assert `AHP_SOURCE_COMMIT === versionFile.trim()`, it matches `/^[0-9a-f]{7,40}$/`, it is not `"unsynced"`, repository is `"agent-host-protocol"`, source path is `"../agent-host-protocol/types"`, and `AHP_GENERATED_FILES` contains `"reducers.ts"` and `"version/registry.ts"`. Create `packages/protocol/src/fixture-privacy.test.ts` that recursively scans `packages/protocol/test-cases/reducers`, asserts every file ends with `.json`, parses each fixture, and asserts each fixture has `description`, `reducer`, `initial`, `actions`, and `expected`. Also assert no file name ends with `.jsonl`, `.log`, `.txt`, and no fixture contains obvious real-log transport keys `jsonrpc`, `method`, `params`, or `timestamp` at the top level. Do not add real log data; use only copied upstream reducer fixtures.</action>
  <verify>
    <automated>pnpm test -- packages/protocol/src/source-info.test.ts packages/protocol/src/fixture-privacy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'AHP_SOURCE_COMMIT' packages/protocol/src/source-info.test.ts`
    - `grep -q '.ahp-version' packages/protocol/src/source-info.test.ts`
    - `grep -q 'AHP_GENERATED_FILES' packages/protocol/src/source-info.test.ts`
    - `grep -q 'test-cases/reducers' packages/protocol/src/fixture-privacy.test.ts`
    - `grep -q 'jsonrpc' packages/protocol/src/fixture-privacy.test.ts`
    - `grep -q 'method' packages/protocol/src/fixture-privacy.test.ts`
  </acceptance_criteria>
  <done>Source commit diagnostics are test-visible and fixture privacy checks prevent accidental real-log fixture inclusion.</done>
</task>

<task type="auto">
  <name>Task 06-03-03: Run Phase 6 focused and full validation gate</name>
  <read_first>
    package.json
    packages/protocol/package.json
    packages/protocol/src/reducers.test.ts
    packages/protocol/src/source-info.test.ts
    packages/protocol/src/fixture-privacy.test.ts
    packages/parser/src/normalizer.ts
    packages/shared/src/ahp/index.ts
    test/security.test.ts
    .planning/phases/06-protocol-reducer-sync-foundation/06-VALIDATION.md
  </read_first>
  <files>packages/protocol/src/reducers.test.ts, packages/protocol/src/source-info.test.ts, packages/protocol/src/fixture-privacy.test.ts</files>
  <action>Run the exact Phase 6 validation commands without watch mode: `pnpm sync:ahp`, `pnpm test -- packages/protocol/src`, `pnpm -F @ahp-viewer/protocol build`, `pnpm test`, `pnpm typecheck`, and `pnpm lint`. If failures occur, fix only Phase 6 files and generated-package integration issues. Do not change reducer semantics manually; regenerate via `pnpm sync:ahp` if generated protocol code is stale. Confirm `grep -R 'agent-host-protocol/types' -n packages test` returns no matches and `grep -R '"agent-host-protocol": "file:' -n package.json packages/*/package.json` returns no matches.</action>
  <verify>
    <automated>pnpm sync:ahp && pnpm test -- packages/protocol/src && pnpm -F @ahp-viewer/protocol build && pnpm test && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm sync:ahp` exits 0
    - `pnpm test -- packages/protocol/src` exits 0
    - `pnpm -F @ahp-viewer/protocol build` exits 0
    - `pnpm test` exits 0
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
    - `! grep -R 'agent-host-protocol/types' -n packages test`
    - `! grep -R '"agent-host-protocol": "file:' -n package.json packages/*/package.json`
  </acceptance_criteria>
  <done>All Phase 6 focused and full validation commands pass, stale file dependency checks are clean, and reducer parity is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated protocol source → reducer parity harness | Tests execute generated reducer code copied from upstream. |
| generated fixture files → repository history | Copied fixture data becomes committed project test data and must stay synthetic. |
| generated metadata → developer diagnostics | Source commit metadata must be accurate enough for developers/tests to identify upstream origin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | `packages/protocol/src/source-info.ts`, `.ahp-version` | mitigate | `source-info.test.ts` asserts exported commit equals `.ahp-version`, matches a git-hash pattern, is not `unsynced`, and generated files include required protocol files. |
| T-06-03 | Tampering | dependency/import graph | mitigate | Final validation greps for `agent-host-protocol/types` and stale `file:` dependency; Plan 06-02 removed them and this plan rechecks under full suite. |
| T-06-04 | Denial of Service / Tampering | `packages/protocol/src/reducers.test.ts` | mitigate | Test harness sets `Date.now = () => 9999` in `beforeEach` and restores it in `afterEach`, matching upstream fixture generation and preventing nondeterministic reducer timestamps. |
| T-06-05 | Information Disclosure | `packages/protocol/test-cases/reducers` | mitigate | `fixture-privacy.test.ts` asserts fixtures are JSON-only reducer cases with expected fixture keys and no `.jsonl`, `.log`, `.txt`, or top-level real-log transport keys. |
</threat_model>

<verification>
Run the full Phase 6 validation sequence:

```bash
pnpm sync:ahp
pnpm test -- packages/protocol/src
pnpm -F @ahp-viewer/protocol build
pnpm test
pnpm typecheck
pnpm lint
```
</verification>

<success_criteria>
- `AHP_SOURCE_COMMIT` is exported and equals `packages/protocol/.ahp-version`.
- Reducer parity fixtures pass for root, session, and terminal reducers.
- Reducer tests mock `Date.now()` to `9999`.
- Fixture privacy tests confirm no real log files are copied.
- All required validation commands pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-protocol-reducer-sync-foundation/06-03-SUMMARY.md`.
</output>
