---
phase: 11-vs-code-extension-command-palette-custom-editor
plan: 4
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
  - 11-03
files_modified:
  - package.json
  - test/boundary.test.ts
  - test/security.test.ts
  - packages/extension/src/extension.test.ts
  - packages/extension/README.md
  - USER_GUIDE.md
  - SECURITY.md
autonomous: true
requirements:
  - EXT-05
  - EXT-06
  - EXT-07
must_haves:
  truths:
    - "Root build/typecheck/test commands include the extension package."
    - "Automated tests guard extension command behavior and local-only constraints."
    - "User-facing docs describe the VS Code webview command accurately."
  artifacts:
    - path: "packages/extension/src/extension.test.ts"
      provides: "Command/webview lifecycle tests"
    - path: "test/boundary.test.ts"
      provides: "Package boundary guard for vscode imports"
    - path: "test/security.test.ts"
      provides: "No outbound/CDN/local-only extension guardrails"
    - path: "packages/extension/README.md"
      provides: "Extension package usage and packaging notes"
  key_links:
    - from: "package.json"
      to: "packages/extension/package.json"
      via: "workspace recursive scripts"
      pattern: "pnpm -r"
    - from: "test/boundary.test.ts"
      to: "packages/extension/src"
      via: "import boundary assertion"
      pattern: "vscode"
---

<objective>
Add packaging verification, security guardrails, and documentation for the VS Code extension webview.

Purpose: Make the Phase 11 extension reviewable and publishable-shaped while proving the existing standalone viewer still works.
Output: Focused extension tests, boundary/security tests, root script coverage, and docs updates.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-vs-code-extension-command-palette-custom-editor/11-CONTEXT.md
@.planning/phases/11-vs-code-extension-command-palette-custom-editor/11-RESEARCH.md
@.planning/phases/11-vs-code-extension-command-palette-custom-editor/11-01-SUMMARY.md
@.planning/phases/11-vs-code-extension-command-palette-custom-editor/11-02-SUMMARY.md
@.planning/phases/11-vs-code-extension-command-palette-custom-editor/11-03-SUMMARY.md
@test/boundary.test.ts
@test/security.test.ts
@USER_GUIDE.md
@SECURITY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add extension command and lifecycle tests</name>
  <files>packages/extension/src/extension.test.ts</files>
  <behavior>
    - Activation registers `ahpViewer.open`.
    - Command creates a webview panel and assigns HTML.
    - Panel disposal disposes the viewer session bridge.
    - Active-log initial state is passed to the bridge when detection succeeds.
  </behavior>
  <action>Add unit tests around extension activation using a small fake VS Code API wrapper or dependency injection seam introduced by Plan 01/03. Keep tests in Vitest so they run with the repo's existing test command. Do not require a real VS Code window for these unit tests.</action>
  <verify>
    <automated>pnpm test packages/extension/src/extension.test.ts</automated>
  </verify>
  <done>Command registration and panel lifecycle are covered without launching VS Code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend boundary and security guardrails</name>
  <files>test/boundary.test.ts, test/security.test.ts</files>
  <behavior>
    - `vscode` imports are allowed only in `packages/extension`.
    - UI/core/server/shared packages do not import extension code.
    - Extension webview HTML contains a restrictive CSP and no CDN URLs.
    - Extension runtime does not call `startLogServer` or `open` from the CLI path.
  </behavior>
  <action>Extend existing boundary/security tests with Phase 11 invariants. Use regex gates that ignore comments where counting matters. Preserve current no-CDN and local-only tests for UI source.</action>
  <verify>
    <automated>pnpm test test/boundary.test.ts test/security.test.ts</automated>
  </verify>
  <done>Security tests fail if VS Code APIs leak into portable packages, if the extension starts the loopback server, or if webview assets use remote URLs.</done>
</task>

<task type="auto">
  <name>Task 3: Update package scripts and docs</name>
  <files>package.json, packages/extension/README.md, USER_GUIDE.md, SECURITY.md</files>
  <action>Ensure root `build`, `typecheck`, and `test` workflows include the extension through existing pnpm recursive behavior. Add extension package docs covering local development, command behavior, and build output. Update the user guide with the Command Palette flow and active-log default behavior. Update the security doc with the webview `postMessage` local-only model and CSP/no-outbound expectations.</action>
  <verify>
    <automated>pnpm build && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>Docs match implemented behavior and the full repo verification suite passes with the extension package included.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build/package metadata -> VS Code | Manifest controls activation and command exposure. |
| Test guardrails -> source tree | Tests enforce local-only and package-boundary assumptions. |
| Documentation -> user expectations | Docs describe where log data flows and what is local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Information Disclosure | docs/security | mitigate | Document that absolute paths remain extension-host-local and log content is not sent outbound. |
| T-11-04-02 | Tampering | package manifest | mitigate | Test command contribution and activation behavior so accidental command id drift is caught. |
| T-11-04-03 | Repudiation | tests | mitigate | Add automated tests that prove local-only invariants and make regressions review-visible. |
</threat_model>

<verification>
Run focused extension tests, boundary/security tests, then `pnpm build`, `pnpm typecheck`, and `pnpm test`.
</verification>

<success_criteria>
Phase 11 finishes with a verified, documented, publishable-shaped VS Code extension webview and no regression to standalone browser mode.
</success_criteria>

<output>
After completion, create `.planning/phases/11-vs-code-extension-command-palette-custom-editor/11-04-SUMMARY.md`.
</output>