---
phase: 31-improvements-to-the-filter-pickers-dir-kind-method-action-et
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - e2e/phase31.spec.ts
  - e2e/phase25.spec.ts
  - screenshots/phase31/01-dark-method-desktop.png
  - screenshots/phase31/02-light-method-desktop.png
  - screenshots/phase31/03-hacker-method-desktop.png
  - screenshots/phase31/04-dark-method-narrow.png
  - screenshots/phase31/05-dark-group-session.png
autonomous: true
requirements: []
must_haves:
  truths:
    - "A fixture-only browser workflow proves the default Method picker offers one `Select all`, flips to one `Uncheck all`, never shows `Close`, and keeps complete-set behavior under local query filtering."
    - "Desktop and narrow browser geometry proves the searchable input, contextual command, labels, counts, and selected Group: Session background remain inside their picker surfaces."
    - "Fixture-only Dark, Light, and Hacker screenshots provide reviewable evidence without any real log content or absolute path leakage."
    - "The historical Phase 25 browser workflow continues to pass after only its superseded dual-action and visible-Close assertions are updated to the Phase 31 contract."
    - "No new network, dependency, server, host-adapter, persistence, protocol, Escape, or ARIA behavior is added by verification work."
  artifacts:
    - path: "e2e/phase31.spec.ts"
      provides: "Synthetic browser proof for contextual behavior, deterministic order, responsive geometry, theme safety, screenshots, and path privacy"
    - path: "e2e/phase25.spec.ts"
      provides: "Historical Phase 25 workflow with only superseded visible-contract assertions revised"
    - path: "screenshots/phase31/01-dark-method-desktop.png"
      provides: "Fixture-only Dark desktop Method-picker evidence"
    - path: "screenshots/phase31/02-light-method-desktop.png"
      provides: "Fixture-only Light desktop Method-picker evidence"
    - path: "screenshots/phase31/03-hacker-method-desktop.png"
      provides: "Fixture-only Hacker desktop Method-picker evidence"
    - path: "screenshots/phase31/04-dark-method-narrow.png"
      provides: "Fixture-only narrow Method-picker containment evidence"
    - path: "screenshots/phase31/05-dark-group-session.png"
      provides: "Fixture-only selected Group: Session containment evidence"
  key_links:
    - from: "e2e/phase31.spec.ts"
      to: "packages/ui/src/test-fixtures/phase5-log"
      via: "temporary synthetic-only JSONL served by the local CLI"
      pattern: "PHASE5_BASE_JSONL|phase31"
    - from: "e2e/phase31.spec.ts"
      to: "screenshots/phase31"
      via: "Playwright screenshots taken only after interaction, geometry, theme, and path-leak assertions"
      pattern: "SCREENSHOT_DIR|assertNoPathLeak"
    - from: "e2e/phase25.spec.ts"
      to: "packages/ui/src/components/filters/FacetPopover.tsx"
      via: "revised expectations for one contextual command and no visible Close"
      pattern: "Select all|Uncheck all|Close"
---

<objective>
Prove the approved picker contract in a real browser over synthetic data and keep the historical Phase 25 workflow compatible with the superseding visible contract.

Purpose: Component tests lock logic; this plan proves actual desktop/narrow containment, all-theme rendering, fixture-only privacy, and full-suite browser compatibility.
Output: A Phase 31 Playwright scenario, five reviewable synthetic screenshots, and narrowly revised Phase 25 assertions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-RESEARCH.md
@.planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-PATTERNS.md
@.planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-VALIDATION.md
@.planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-UI-SPEC.md
@.planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-01-SUMMARY.md
@.planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-04-SUMMARY.md
@e2e/phase25.spec.ts
@e2e/phase5.spec.ts
@packages/ui/src/components/filters/FacetPopover.tsx
@packages/ui/src/components/filters/GroupToggleChip.tsx

<interfaces>
<!-- Established fixture-only browser harness and selectors. -->

From e2e/phase25.spec.ts:
```typescript
function spawnCli(args: string[]): CliProc;
function waitForPort(proc: CliProc, timeoutMs?: number): Promise<number>;
async function killCli(proc: CliProc | undefined): Promise<void>;
async function assertNoPathLeak(page: Page): Promise<void>;
```

Existing browser selectors:
```typescript
page.getByRole("button", { name: /^Method/ })
page.getByPlaceholder("Filter…")
page.getByRole("listbox")
page.getByRole("button", { name: /Group:/i })
page.getByRole("radio", { name: "Session" })
page.getByRole("button", { name: /Theme picker/ })
page.getByRole("menuitemradio", { name: "Dark" | "Light" | "Hacker" })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove picker behavior, responsive geometry, themes, and privacy with synthetic browser evidence</name>
  <files>e2e/phase31.spec.ts, e2e/phase25.spec.ts, screenshots/phase31/01-dark-method-desktop.png, screenshots/phase31/02-light-method-desktop.png, screenshots/phase31/03-hacker-method-desktop.png, screenshots/phase31/04-dark-method-narrow.png, screenshots/phase31/05-dark-group-session.png</files>
  <read_first>
    - e2e/phase25.spec.ts
    - e2e/phase5.spec.ts
    - e2e/phase23.spec.ts
    - packages/ui/src/components/filters/FacetPopover.tsx
    - packages/ui/src/components/filters/FilterBar.tsx
    - packages/ui/src/components/filters/GroupToggleChip.tsx
    - packages/ui/src/test-fixtures/phase5-log.ts
    - .github/copilot-instructions.md
    - .planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-UI-SPEC.md
    - .planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-VALIDATION.md
  </read_first>
  <behavior>
    - Over a temporary JSONL built only from repository-owned synthetic fixture text plus clearly synthetic in-test rows, default Method shows `ping` unchecked, exactly one `Select all`, no `Uncheck all`, and no `Close`; clicking the contextual command selects the complete Method set, keeps the picker open, and flips to exactly one `Uncheck all`.
    - While a local Method `Filter…` query hides nonmatching rows, the contextual command label remains derived from complete selection and the action changes hidden options outside the rendered query subset.
    - Method labels appear in case-insensitive visible-label order with visible counts; the checked list remains ordered after a count-changing synthetic append or equivalent fixture count variation.
    - On desktop and a 600x900 narrow viewport, the open picker is inside the viewport and the searchable input, footer command, labels, and counts are inside the picker bounding box.
    - With Group set to Session, the checked Session row/background bounding box is inside the grouping popover bounding box.
    - Dark, Light, and Hacker picker screenshots are generated from the same synthetic workflow after the corresponding `data-theme` attribute is asserted; every screenshot is path-leak checked before saving.
    - Phase 25 retains Search/Filter rows independence and checked-visible row visibility assertions while expecting exactly one contextual command and no visible `Close`.
  </behavior>
  <action>
Create `e2e/phase31.spec.ts` by reusing the Phase 25 local CLI harness shape: import `PHASE5_BASE_JSONL`, write a temporary `phase31-browser-safe.jsonl`, append only clearly synthetic rows needed to produce scrambled/case-varied Method values and enough option/count variation for ordering assertions, launch `packages/cli/src/index.ts` with `--port 0 --no-open`, and clean up the temporary directory/process. Never read or save a real AHP log, home-directory path, or external resource. Keep `assertNoPathLeak()` rejecting `/Users/`, `/home/`, and Windows absolute paths.

At 1440x900, open Method under the default hidden-ping state and assert: `ping` is unchecked; the option-label sequence is case-insensitive alphabetical; counts are visible; exactly one `Select all` exists; `Uncheck all` and `Close` do not exist. Compare the `Filter…` input bounding box with the `listbox` bounding box and require the input's left/right/top/bottom edges to stay within the popover. Fill `Filter…` with a query that hides at least one complete option, click `Select all`, and prove the hidden nonmatching option becomes selected when the query is cleared, the picker remains open, and exactly one `Uncheck all` is now visible. Capture `01-dark-method-desktop.png` only after these assertions and `assertNoPathLeak()`.

Use the existing Theme picker selectors to choose Light and Hacker, assert `html[data-theme="light"]` and `html[data-theme="hacker"]` respectively, reopen Method as needed, rerun picker/input/footer containment plus no-`Close` assertions, call `assertNoPathLeak()`, and capture `02-light-method-desktop.png` and `03-hacker-method-desktop.png`. Return to Dark, set the viewport to 600x900, open Method, require both the listbox and searchable input/footer to remain within the viewport/picker bounds, call `assertNoPathLeak()`, and capture `04-dark-method-narrow.png`. Set Group to Session, reopen the Group picker, compare the checked Session label/background bounding box against its containing raised surface, require every edge to be contained, call `assertNoPathLeak()`, and capture `05-dark-group-session.png`.

Revise only the stale visible-contract assertions/action sequence in `e2e/phase25.spec.ts`: where partial/default-hidden Method currently expects both bulk commands, expect exactly one `Select all`, no `Uncheck all`, and no `Close`; after clicking `Select all`, expect `ping` checked and exactly one `Uncheck all`; after clicking `Uncheck all`, expect known Method-backed rows hidden and exactly one `Select all`. Keep the historical Phase 25 screenshot names/content generation, Search versus Filter rows assertions, narrow viewport containment, and path-leak checks intact; do not treat Phase 25 screenshots as Phase 31 evidence.

After the Playwright run regenerates the five Phase 31 files, inspect every image with the image viewer before completion. Confirm each image is fixture-only and has no clipping, overlap, input-border overflow, footer-command overflow, count overflow, or selected-background bleed; Dark, Light, and Hacker must each have readable labels/borders/check states using their existing theme treatment.
  </action>
  <verify>
    <automated>pnpm --filter @ahp-inspector/ui build &amp;&amp; pnpm exec playwright test e2e/phase31.spec.ts e2e/phase25.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/phase31.spec.ts` imports repository-owned synthetic fixture data, launches only the local CLI, contains `assertNoPathLeak`, contains no real-log root or hard-coded home path, and asserts contextual complete-set behavior under a local query.
    - `e2e/phase31.spec.ts` asserts case-insensitive Method label DOM order, visible counts, no `Close`, desktop/narrow viewport containment, searchable input containment, contextual footer containment, and checked Group: Session row/background containment.
    - `e2e/phase31.spec.ts` switches through Dark, Light, and Hacker with Theme picker selectors and asserts each corresponding `data-theme` value before capturing the named Phase 31 screenshots.
    - `e2e/phase25.spec.ts` no longer expects `Select all`, `Uncheck all`, and `Close` simultaneously; it retains its Search/Filter rows, row-visibility, narrow-containment, screenshot, and path-leak assertions.
    - All five `screenshots/phase31/*.png` files exist, were generated by `e2e/phase31.spec.ts` after synthetic-data/path-leak assertions, and have been inspected for clipping, overlap, selected-background bleed, readable all-theme treatment, and sensitive-content absence.
    - The focused UI build plus Phase 31 and stale-contract Phase 25 Chromium specs exit 0; full repository test, typecheck, and lint remain the overall phase gate.
  </acceptance_criteria>
  <done>Real-browser fixture evidence proves the new visible contract, responsive geometry, all-theme safety, and privacy while the superseded Phase 25 assertions remain historically useful and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic fixture source -> local CLI/browser -> saved screenshots | Saved visual evidence could disclose sensitive log content or local paths if a real source enters the harness. |
| Browser-visible picker surface -> screenshot/review artifacts | Theme or geometry regressions can remain hidden unless assertions run before evidence capture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Information Disclosure | `e2e/phase31.spec.ts` and `screenshots/phase31/*` | mitigate | Build the temporary log exclusively from checked-in/synthetic strings, retain absolute-path leak assertions, capture only after those assertions, and inspect every saved image before completion. |
| T-31-05 | Denial of Service | Browser option rendering under synthetic high-cardinality data | mitigate | Preserve the product's 100-row cap and keep fixture cardinality bounded to the smallest set that proves query/cap behavior; run the focused specs with one Playwright worker under existing config. |
| T-31-06 | Tampering | Historical `e2e/phase25.spec.ts` regression contract | mitigate | Revise only expectations superseded by Phase 31; retain independent Search/Filter rows, row visibility, responsive containment, screenshot, and privacy assertions. |
| T-31-07 | Information Disclosure / privacy regression | Network/dependency behavior | accept | The plan uses the existing loopback-only CLI and installed test stack; no new network, dependency, server, host, persistence, or protocol behavior is introduced. |
</threat_model>

<verification>
- Run `pnpm --filter @ahp-inspector/ui build && pnpm exec playwright test e2e/phase31.spec.ts e2e/phase25.spec.ts --project=chromium` and require all browser assertions/screenshots to complete.
- Inspect all five generated files under `screenshots/phase31/` with the image viewer before completion; reject any clipping, overlap, selected-background bleed, unreadable theme treatment, or sensitive content.
- Run `pnpm test && pnpm typecheck && pnpm lint` as the phase gate.
</verification>

<success_criteria>
- Fixture-only browser evidence proves contextual complete-set behavior, no visible Close, deterministic rows, desktop/narrow containment, and selected Group-row containment.
- Dark, Light, and Hacker screenshots are generated and inspected without any absolute path or real log content.
- Phase 25's historical E2E remains green after only its superseded footer expectations are revised.
- Full repository test, typecheck, and lint gates pass with no new network/dependency/protocol behavior.
</success_criteria>

<source_coverage>
| Source | ID | Acceptance item | Task | Status |
|--------|----|-----------------|------|--------|
| GOAL | - | Browser-visible proof of contextual picker polish and corrected layout | 1 | COVERED |
| REQ | - | Phase requirement IDs are explicitly none/TBD; no IDs invented | - | EXCLUDED |
| RESEARCH | - | Fixture-only desktop/narrow geometry, screenshots, all-theme support, path privacy, and Phase 25 assertion revision | 1 | COVERED |
| UI-SPEC | - | Desktop Method evidence, narrow containment, Group: Session containment, Dark/Light/Hacker safety, exact copy, and privacy | 1 | COVERED |
| VALIDATION | 31-02-01 | Fixture-only desktop/narrow interaction, geometry, visual proof, and path privacy | 1 | COVERED |
| CONTEXT | - | No CONTEXT.md by explicit user choice | - | EXCLUDED |
| DEFERRED | - | Escape-to-close, ARIA/keyboard redesign, count sorting, global box-sizing, and new network/dependency/protocol behavior | - | EXCLUDED |
</source_coverage>

<output>
After completion, create `.planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-02-SUMMARY.md`.
</output>