---
phase: 31-improvements-to-the-filter-pickers-dir-kind-method-action-et
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/filters/FacetPopover.tsx
  - packages/ui/src/components/filters/FilterBar.tsx
  - packages/ui/src/components/filters/GroupToggleChip.tsx
  - packages/ui/src/components/filters/FilterBar.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every categorical picker shows exactly one complete-facet bulk command: `Uncheck all` only when every complete option is selected, otherwise `Select all`."
    - "The contextual command acts on every complete option even while the picker query filters rows or more than 100 options exceed the rendered cap; the default-hidden `ping` state initially offers `Select all`."
    - "No categorical picker renders visible `Close`; trigger-toggle and click-outside closure remain intact, while Escape and picker ARIA/keyboard behavior remain unchanged."
    - "Dir, Kind, Method, Action, Channel, Turn, and Status options stay in case-insensitive visible-label order with raw-value tie-breaking, regardless of counts or arrival order."
    - "The searchable facet input and selected Group row use local border-box sizing so their border/background remains inside the raised picker surface."
    - "Checked-visible semantics, hidden-value persistence, default-hidden `ping`, local query behavior, the 100-row cap, semantic tokens, and all themes remain unchanged."
  artifacts:
    - path: "packages/ui/src/components/filters/FacetPopover.tsx"
      provides: "Single contextual complete-set command, no visible Close action, and contained searchable input"
    - path: "packages/ui/src/components/filters/FilterBar.tsx"
      provides: "One deterministic visible-label ordering boundary shared by all categorical facets"
    - path: "packages/ui/src/components/filters/GroupToggleChip.tsx"
      provides: "Contained selected Group row/background geometry"
    - path: "packages/ui/src/components/filters/FilterBar.test.tsx"
      provides: "Focused regressions for contextual bulk behavior, ordering, closure, preserved semantics, and local geometry styles"
  key_links:
    - from: "packages/ui/src/components/filters/FilterBar.tsx"
      to: "packages/ui/src/components/filters/FacetPopover.tsx"
      via: "sorted complete options plus checked-visible selected values"
      pattern: "mapToOptions|visibleSelectionFromHidden|hiddenValuesFromSelection"
    - from: "packages/ui/src/components/filters/FacetPopover.tsx"
      to: "packages/ui/src/components/filters/FilterBar.tsx"
      via: "contextual onChange payload containing either every complete option value or no selected values"
      pattern: "options\.every|options\.map|onChange"
    - from: "packages/ui/src/components/filters/GroupToggleChip.tsx"
      to: "packages/ui/src/components/filters/FilterBar.test.tsx"
      via: "component style contract for the full-width selected row"
      pattern: "boxSizing|border-box"
---

<objective>
Make every categorical facet picker predictable and contained while preserving the existing checked-visible filter model.

Purpose: Replace the crowded footer and arrival-order rows with one truthful complete-facet action, stable alphabetical targets, and root-cause local geometry fixes.
Output: Updated shared picker components plus focused component regressions that lock the approved Phase 31 UI contract.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-RESEARCH.md
@.planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-PATTERNS.md
@.planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-VALIDATION.md
@.planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-UI-SPEC.md
@.planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-03-SUMMARY.md
@packages/ui/src/components/filters/FacetPopover.tsx
@packages/ui/src/components/filters/FilterBar.tsx
@packages/ui/src/components/filters/GroupToggleChip.tsx
@packages/ui/src/components/filters/FilterBar.test.tsx

<interfaces>
<!-- Existing contracts to preserve. No new store, persistence, server, host, protocol, dependency, Escape, or ARIA contract is introduced. -->

From packages/ui/src/components/filters/FacetPopover.tsx:
```typescript
interface FacetOption {
  value: string;
  label: string;
  count: number;
}

interface FacetPopoverProps {
  options: FacetOption[];
  selected: string[];
  onChange(values: string[]): void;
  onClose(): void;
  searchable?: boolean;
  align?: "start" | "end";
}

const MAX_VISIBLE = 100;
```

From packages/ui/src/components/filters/FilterBar.tsx:
```typescript
function mapToOptions(
  m: Map<string, number>,
  labelFor: (value: string) => string = (value) => value,
): { value: string; label: string; count: number }[];

function visibleSelectionFromHidden(options, hiddenValues): string[];
function hiddenValuesFromSelection(options, selectedValues, previousHiddenValues): string[];
```

From packages/ui/src/state/filters.ts:
```typescript
export const APP_DEFAULT_FILTERS: FilterState = {
  ...EMPTY_FILTERS,
  method: ["ping"],
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make bulk behavior contextual and categorical ordering deterministic</name>
  <files>packages/ui/src/components/filters/FacetPopover.tsx, packages/ui/src/components/filters/FilterBar.tsx, packages/ui/src/components/filters/FilterBar.test.tsx</files>
  <read_first>
    - packages/ui/src/components/filters/FacetPopover.tsx
    - packages/ui/src/components/filters/FilterBar.tsx
    - packages/ui/src/components/filters/FilterBar.test.tsx
    - packages/ui/src/state/filters.ts
    - packages/ui/src/state/selectors.ts
    - .planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-UI-SPEC.md
    - .planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-PATTERNS.md
  </read_first>
  <behavior>
    - With every complete option checked, the picker renders only enabled `Uncheck all`; clicking it calls the existing selected-value callback with `[]`, keeps the picker open, and flips the only command to `Select all`.
    - With any complete option unchecked, including default-hidden Method value `ping`, the picker renders only enabled `Select all`; clicking it forwards every value from complete `options`, keeps the picker open, and flips the only command to `Uncheck all`.
    - With zero complete options, the picker renders disabled `Select all`, retains `No options`, and clicking the disabled command cannot change state.
    - A picker-local query and the `MAX_VISIBLE = 100` cap change only rendered option rows; they never change the command label or the complete-set action target.
    - No categorical picker renders visible `Close`; clicking the open facet trigger again or mousing down outside the popover closes it. Escape behavior, roles, aria attributes, and focus management are unchanged.
    - Every categorical facet is sorted by visible label ascending case-insensitively, then raw `value` ascending for a deterministic tie; formatted Channel labels participate after formatting, and counts/row-arrival order never participate.
    - Existing hidden arrays remain exclusions, checked remains visible, default-hidden `ping` remains hidden until selected, and newly discovered non-hidden values remain checked.
  </behavior>
  <action>
First extend `FilterBar.test.tsx` with focused regressions for the complete contract: default Method with `ping` hidden shows only `Select all`; selecting all clears the hidden Method array and flips the one command to `Uncheck all`; unchecking all stores every complete Method value and flips back; the command remains visible and the popover stays open after each click; no button named `Close` exists; same-trigger toggle and document click-outside still close. Add a zero-options case asserting disabled `Select all` plus `No options`. Add a searchable Method case with a local `Filter…` query and more than 100 synthetic methods where the command label is derived from the complete selection and `Select all` clears hidden values outside the query/rendered subset.

In `FacetPopover.tsx`, compute `allSelected` exactly as `options.length > 0 && options.every((option) => selected.includes(option.value))`. Replace the three footer buttons with exactly one left-aligned text button. Its exact label is `Uncheck all` when `allSelected` is true and `Select all` otherwise; disable it when `options.length === 0`; on click call `onChange(allSelected ? [] : options.map((option) => option.value))`; do not call `onClose`. Preserve the footer divider, `var(--space-1) var(--space-2)` padding, current text-button token styles/focus treatment, local literal case-insensitive query, `MAX_VISIBLE = 100`, `…and {N} more`, native controls, click-outside effect, roles/aria, and trigger-owned closure. Remove the visible `Close` button only. Do not add Escape handling or change picker semantics/keyboard ownership.

In `FilterBar.tsx`, sort only at `mapToOptions` after `labelFor(value)` has produced visible labels. Compare `a.label.toLocaleLowerCase()` with `b.label.toLocaleLowerCase()` using `localeCompare`, then use `a.value.localeCompare(b.value)` as the only tie-breaker. Keep Direction, Kind, Method, Action, Channel, Turn, and Status on this shared projection; do not sort by count, mutate the source maps, change the hidden-value adapters, or change filter persistence/state. Add DOM-order regressions with scrambled row insertion for every categorical picker, a formatted Channel-label case, a raw-value tie-break case for equal/case-colliding labels, and a count-change case proving existing options do not reorder.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/components/filters/FilterBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/components/filters/FacetPopover.tsx` contains one contextual footer button whose exact visible copy is either `Select all` or `Uncheck all`, contains no rendered `Close` button, and derives state/action from complete `options` rather than `filtered` or `visible`.
    - `packages/ui/src/components/filters/FilterBar.tsx` sorts projected options after label formatting with a case-insensitive visible-label comparison followed only by raw-value comparison; count is rendered but absent from comparator logic.
    - `packages/ui/src/components/filters/FilterBar.test.tsx` asserts default-hidden `ping`, contextual label flips, complete-set action under an active query and over-100-option cap, disabled empty-state `Select all`, absence of `Close`, retained trigger-toggle/click-outside closure, all seven categorical facets in DOM order, formatted Channel ordering, raw-value tie-breaking, and count-change stability.
    - `pnpm exec vitest run packages/ui/src/components/filters/FilterBar.test.tsx` exits 0.
    - Source inspection confirms no new store field, persistence migration, Escape listener, ARIA/role change, dependency, server call, or host/protocol change.
  </acceptance_criteria>
  <done>Every categorical facet has one truthful complete-set command and stable visible-label ordering without changing checked-visible state or closure/keyboard boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contain the searchable input and selected Group row locally</name>
  <files>packages/ui/src/components/filters/FacetPopover.tsx, packages/ui/src/components/filters/GroupToggleChip.tsx, packages/ui/src/components/filters/FilterBar.test.tsx</files>
  <read_first>
    - packages/ui/src/components/filters/FacetPopover.tsx
    - packages/ui/src/components/filters/GroupToggleChip.tsx
    - packages/ui/src/components/filters/FilterBar.test.tsx
    - packages/ui/src/components/filters/SearchPopover.tsx
    - packages/ui/src/styles/global.css
    - packages/ui/src/styles/tokens.css
    - .planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-UI-SPEC.md
  </read_first>
  <behavior>
    - The searchable Method, Channel, and Turn input keeps `width: 100%` while its border and horizontal padding are included inside that width through local `boxSizing: "border-box"`.
    - Each Group mode label keeps `width: 100%` while its padding and selected `var(--color-chip-bg-active)` background are included inside that width through local `boxSizing: "border-box"`.
    - Group modes remain `None`, `Session`, and `Session + Turn` in the same order; selecting one still closes the grouping picker.
    - No global box-sizing reset, width arithmetic, JavaScript geometry measurement, raw component color, theme-specific rule, or token change is introduced.
  </behavior>
  <action>
Extend `FilterBar.test.tsx` before the product change: open searchable Method and assert the `Filter…` input keeps `width: 100%` and has `box-sizing: border-box`; select grouping `Session`, reopen the Group picker, locate the checked `Session` radio's parent label, and assert it keeps `width: 100%`, `box-sizing: border-box`, and background `var(--color-chip-bg-active)`. Retain existing assertions that selecting grouping updates the store and closes the picker.

In `FacetPopover.tsx`, add only `boxSizing: "border-box"` beside the searchable input's existing `width: "100%"`. In `GroupToggleChip.tsx`, add only `boxSizing: "border-box"` beside each mode label's existing `width: "100%"`. Preserve all current dimensions, spacing tokens, borders, backgrounds, labels, mode order, selection behavior, and native controls. Do not modify `global.css`, `tokens.css`, or add width calculations/measurement code.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/components/filters/FilterBar.test.tsx &amp;&amp; pnpm --filter @ahp-inspector/ui typecheck &amp;&amp; pnpm --filter @ahp-inspector/ui build</automated>
  </verify>
  <acceptance_criteria>
    - The searchable input style in `packages/ui/src/components/filters/FacetPopover.tsx` contains both `width: "100%"` and `boxSizing: "border-box"`.
    - The Group option label style in `packages/ui/src/components/filters/GroupToggleChip.tsx` contains both `width: "100%"` and `boxSizing: "border-box"`, retains `var(--color-chip-bg-active)`, and retains modes `None`, `Session`, and `Session + Turn` in their existing order.
    - `packages/ui/src/components/filters/FilterBar.test.tsx` asserts both local border-box style contracts and preserved grouping selection/closure.
    - `git diff -- packages/ui/src/styles/global.css packages/ui/src/styles/tokens.css` is empty for Phase 31 execution.
    - The focused Vitest command, UI typecheck, and UI build all exit 0.
  </acceptance_criteria>
  <done>The two reported overflow defects are corrected at their local full-width controls without changing application-wide geometry or theme contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local row-derived option text -> React picker rendering and literal query matching | Potentially unusual or numerous method/channel/turn labels enter client-side display, search, sorting, and bulk-selection logic. |
| Picker interaction -> durable hidden-value filters | Contextual commands must preserve complete-set checked-visible semantics rather than silently acting on a rendered subset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | `FacetPopover` complete-set command and `FilterBar` hidden-value adapters | mitigate | Derive label and action only from complete `options`, preserve the existing hidden-value adapter, and test active-query plus over-100-option cases so rendered subsets cannot change only part of durable visibility state. |
| T-31-02 | Denial of Service | `mapToOptions` and `FacetPopover` local query/rendering | mitigate | Keep literal case-insensitive `includes` matching and `MAX_VISIBLE = 100`; sort once at the display projection boundary and never inside each option row or by live-changing counts. |
| T-31-03 | Tampering / XSS | Row-derived visible labels | accept | Existing React text rendering remains unchanged; the phase adds no HTML injection, regex interpretation, network input, or protocol behavior. |
</threat_model>

<verification>
- Run `pnpm exec vitest run packages/ui/src/components/filters/FilterBar.test.tsx` after each task.
- Run `pnpm --filter @ahp-inspector/ui typecheck && pnpm --filter @ahp-inspector/ui build` before advancing to Wave 2.
- Inspect the Phase 31 product diff and confirm it is limited to the three picker components plus their existing integration test owner; `global.css`, `tokens.css`, state, persistence, server, host, protocol, and dependency manifests remain unchanged.
</verification>

<success_criteria>
- Every categorical picker renders exactly one contextual complete-set command and no visible `Close` action.
- Complete-set commands remain correct under default-hidden `ping`, local query filtering, empty options, and the 100-row cap.
- All seven categorical facets render in deterministic case-insensitive visible-label order with raw-value tie-breaking and count-independent stability.
- Searchable facet inputs and selected Group rows use local border-box geometry while preserving all themes, existing labels, and closure/selection behavior.
</success_criteria>

<source_coverage>
| Source | ID | Acceptance item | Task | Status |
|--------|----|-----------------|------|--------|
| GOAL | - | Contextual bulk toggle, no visible Close, deterministic alphabetical ordering, input fix, and Group background fix | 1, 2 | COVERED |
| REQ | - | Phase requirement IDs are explicitly none/TBD; no IDs invented | - | EXCLUDED |
| RESEARCH | - | Complete-options contextual command, visible-label/raw-value ordering, literal/capped query, local border-box fixes, preserved state/boundaries | 1, 2 | COVERED |
| UI-SPEC | - | Exact copy, one footer command, closure contract, all seven facets, local geometry, tokens/themes, checked-visible/default-ping persistence | 1, 2 | COVERED |
| VALIDATION | 31-01-01 | Contextual command, no Close, deterministic order | 1 | COVERED |
| VALIDATION | 31-01-02 | Search input and Group selected-row containment style contract | 2 | COVERED |
| CONTEXT | - | No CONTEXT.md by explicit user choice | - | EXCLUDED |
| DEFERRED | - | Escape-to-close, ARIA/keyboard redesign, count sorting, global box-sizing, state/server/protocol changes | - | EXCLUDED |
</source_coverage>

<output>
After completion, create `.planning/phases/31-improvements-to-the-filter-pickers-dir-kind-method-action-et/31-01-SUMMARY.md`.
</output>