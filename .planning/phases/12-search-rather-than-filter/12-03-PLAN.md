---
phase: 12-search-rather-than-filter
plan: 3
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - packages/ui/src/state/persistence.ts
  - packages/ui/src/state/persistence.test.ts
  - packages/ui/src/persistence/persist-effect.test.ts
  - e2e/phase12.spec.ts
  - USER_GUIDE.md
  - README.md
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-03
  - SEARCH-04
  - SEARCH-05
  - EXT-04
  - VERIFY-02
  - VERIFY-03
must_haves:
  truths:
    - Reloading or reopening the same log restores the search query without restoring stale match indexes.
    - Browser E2E proves search preserves timeline context and navigation reaches matches.
    - User-facing docs describe search as find/highlight/navigation and filters as row narrowing.
  artifacts:
    - path: packages/ui/src/state/persistence.ts
      provides: Persisted search query/filter preferences without stale result metadata.
    - path: e2e/phase12.spec.ts
      provides: Browser regression for search-rather-than-filter behavior.
    - path: USER_GUIDE.md
      provides: Updated user-facing search/filter semantics.
  key_links:
    - from: packages/ui/src/state/persistence.ts
      to: packages/ui/src/components/filters/useSearch.ts
      via: query restoration triggers fresh search fetch
      pattern: searchQuery
    - from: e2e/phase12.spec.ts
      to: packages/ui/src/components/timeline/EventRow.tsx
      via: data-search-match assertion
      pattern: data-search-match
---

<objective>
Lock the new search semantics across persistence, verification, and docs.

Purpose: The behavioral change should hold across reloads, local browser usage, and the Phase 11 extension transport surface.
Output: Persistence tests, E2E coverage, and docs that make search-versus-filter behavior explicit.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-vs-code-extension-command-palette-custom-editor/11-03-PLAN.md
@.planning/phases/12-search-rather-than-filter/12-01-SUMMARY.md
@.planning/phases/12-search-rather-than-filter/12-02-SUMMARY.md

<interfaces>
From packages/ui/src/state/persistence.ts:
```typescript
// Existing persistence stores UI preferences such as searchQuery and filters per active log.
```

From e2e/phase5.spec.ts:
```typescript
// Existing Playwright tests launch the app, open fixtures, fill the search input, and assert UI state.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist only durable search inputs</name>
  <files>packages/ui/src/state/persistence.ts, packages/ui/src/state/persistence.test.ts, packages/ui/src/persistence/persist-effect.test.ts</files>
  <behavior>
    - Persisted preferences include `searchQuery` and facet filters.
    - Persisted preferences do not include search match indexes, result counts, truncation, searching status, or error state.
    - Hydrating a search query leaves match metadata empty until `useSearch` refetches against the active log/transport.
  </behavior>
  <action>Update persistence types/tests to keep query persistence but exclude volatile search result metadata added in plan 12-01. Preserve existing per-log persistence behavior from SEARCH-05 and ensure log switch/rotation still clears match metadata through store reset paths.</action>
  <verify>
    <automated>pnpm --filter @ahp-viewer/ui test -- persistence.test.ts persist-effect.test.ts store.test.ts</automated>
  </verify>
  <done>Reloading can restore the query, but stale row indexes cannot be restored from storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add vertical browser verification for search-rather-than-filter</name>
  <files>e2e/phase12.spec.ts</files>
  <behavior>
    - Opening a fixture and searching for a known needle marks at least one matching row.
    - Nonmatching context rows remain visible in the timeline after the query is applied.
    - Next/previous match navigation changes the selected row to a marked match.
    - Applying a facet filter still narrows visible rows independently from search.
  </behavior>
  <action>Create a focused Playwright spec following existing `e2e/phase5.spec.ts` launch/open patterns. Save screenshots under `screenshots/phase12/` when the spec captures artifacts. If Phase 11 extension E2E harness exists by execution time, add a small shared assertion or fixture helper so the same search semantics are exercised through the extension webview transport without duplicating the browser test body.</action>
  <verify>
    <automated>pnpm e2e -- e2e/phase12.spec.ts</automated>
  </verify>
  <done>E2E proves search highlights/navigates without filtering context rows and facets still narrow rows.</done>
</task>

<task type="auto">
  <name>Task 3: Update user-facing search/filter docs</name>
  <files>USER_GUIDE.md, README.md</files>
  <action>Update the search/filter sections to describe search as case-insensitive find/highlight/navigation across methods, ids, sessions, turns, errors, and payloads; describe filters as the controls that narrow the timeline by direction, kind, method, action, session, turn, status, and time. Keep local-only privacy language and avoid adding unsupported saved-search/preset claims.</action>
  <verify>
    <automated>pnpm test -- --run test/vertical-slice.test.ts</automated>
  </verify>
  <done>Docs match the implemented behavior and do not call search a filter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local storage -> UI state | Persisted local preferences hydrate into runtime state. |
| fixture log -> browser E2E | Test fixture payloads render through the same local-only viewer pipeline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | T | persistence.ts | mitigate | Do not persist volatile search match indexes because row indexes are log/session-specific. |
| T-12-03-02 | I | docs | mitigate | Keep documentation explicit that logs and queries remain local; do not introduce telemetry or remote search language. |
| T-12-03-03 | D | e2e/phase12.spec.ts | mitigate | Keep the E2E fixture small and focused so verification does not become a slow full-suite gate. |
</threat_model>

<verification>
Run persistence tests, focused E2E, UI typecheck, and a final full `pnpm test` if the focused gates pass.
</verification>

<success_criteria>
Search-rather-than-filter behavior is covered by persistence tests, browser E2E, and docs. The Phase 11 shared transport remains compatible because the search result contract stays `matches`, `total`, and `truncated` with volatile match metadata recomputed per active log.
</success_criteria>

<output>
After completion, create `.planning/phases/12-search-rather-than-filter/12-03-SUMMARY.md`.
</output>