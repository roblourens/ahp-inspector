---
phase: 10-pinned-comparison-and-milestone-verification
plan: "10-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/detail/StateInspectorPanel.tsx
  - packages/ui/src/components/detail/StateInspectorPanel.test.tsx
  - packages/ui/src/components/detail/PinnedStatePanel.tsx
  - packages/ui/src/components/detail/PinnedStatePanel.test.tsx
  - packages/ui/src/components/detail/state-pins.ts
  - packages/ui/src/styles/global.css
autonomous: true
requirements:
  - COMPARE-01
  - COMPARE-03
must_haves:
  truths:
    - "Pin action appears only after selected full resource state exists."
    - "Users can hold exactly two active pinned reconstructed state points with event metadata, resource context, confidence, diagnostics, and replay metadata."
    - "Pinned state points are browser-memory only, use no storage/network APIs, and reset on log switch."
  artifacts:
    - path: "packages/ui/src/components/detail/state-pins.ts"
      provides: "Memory-only pinned state model and helper functions"
    - path: "packages/ui/src/components/detail/PinnedStatePanel.tsx"
      provides: "Pinned state list with remove and clear controls"
    - path: "packages/ui/src/components/detail/StateInspectorPanel.tsx"
      provides: "Pin state point action wired to selected reconstructed resource state"
    - path: "packages/ui/src/styles/global.css"
      provides: "Theme-token styling for pinned state surfaces"
  key_links:
    - from: "packages/ui/src/components/detail/StateInspectorPanel.tsx"
      to: "packages/ui/src/components/detail/state-pins.ts"
      via: "createPinnedStatePoint and upsertPinnedStatePoint capture selected resource state"
    - from: "packages/ui/src/components/detail/StateInspectorPanel.tsx"
      to: "packages/ui/src/components/detail/PinnedStatePanel.tsx"
      via: "React memory state passes pins plus remove and clear handlers"
    - from: "packages/ui/src/components/detail/DetailPanel.tsx"
      to: "packages/ui/src/components/detail/StateInspectorPanel.tsx"
      via: "event label/timestamp props provide pin context when needed"
---

<objective>
Add memory-only pinned reconstructed state points to the existing state inspector.

Purpose: satisfy COMPARE-01 by allowing users to pin two selected full resource states with event metadata and resource context, while preserving COMPARE-03 local-only privacy by keeping pins in browser memory only.

Output: pin model/helper, pin action in StateInspectorPanel, pinned-state panel/list, reset-on-log-change behavior, focused tests, and theme-safe styling.
</objective>

<must_haves>
- Pin action appears only after `selectedResource` full state exists.
- Each pin contains target event index, logKey, event label/timestamp context, resource kind, resource URI, confidence, diagnostic count, replay metadata, and selected resource state.
- The UI supports exactly two active pinned points for Phase 10 and can remove individual pins or clear both.
- Pins are memory-only React/UI state; no localStorage/sessionStorage/IndexedDB/export/network behavior.
- Pins reset when `logKey` changes.
- UI remains inside the existing detail drawer/rail and uses existing theme tokens.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pin model and memory-only pin helpers</name>
  <read_first>
    - packages/ui/src/components/detail/StateInspectorPanel.tsx
    - packages/ui/src/components/detail/StateInspectorPanel.test.tsx
    - packages/ui/src/transport/state-client.ts
    - packages/ui/src/components/detail/StateConfidenceBadge.tsx
    - packages/ui/src/components/detail/StateDiagnosticsPanel.tsx
    - .planning/phases/10-pinned-comparison-and-milestone-verification/10-RESEARCH.md
  </read_first>
  <files>packages/ui/src/components/detail/state-pins.ts, packages/ui/src/components/detail/PinnedStatePanel.test.tsx</files>
  <action>
    Create `state-pins.ts` exporting:
    - `PinnedStatePoint` with fields: `id`, `logKey`, `targetIndex`, `eventLabel`, `eventTimestamp`, `resourceKind`, `resourceUri`, `confidence`, `diagnosticCount`, `baselineEventIdx`, `lastAppliedEventIdx`, `baselineFromSeq`, `lastServerSeq`, `state`.
    - `MAX_PINNED_STATE_POINTS = 2`.
    - `createPinnedStatePoint(args)` that builds `id` as `${logKey}:${targetIndex}:${resourceKind}:${resourceUri}` and copies metadata from `StateAtSelectedResource`.
    - `upsertPinnedStatePoint(existing, next)` that replaces an existing pin with the same `id`, appends new pins, and when the list exceeds 2 drops the oldest pin.
    - `removePinnedStatePoint(existing, id)` and `clearPinnedStatePoints()`.
    Add unit coverage in `PinnedStatePanel.test.tsx` or a new colocated helper test if preferred. Tests must prove duplicate IDs replace, a third pin drops the oldest, and no helper reads or writes browser storage.
  </action>
  <acceptance_criteria>
    - `packages/ui/src/components/detail/state-pins.ts` exports `MAX_PINNED_STATE_POINTS = 2`.
    - `state-pins.ts` contains no `localStorage`, `sessionStorage`, `indexedDB`, `fetch(`, or `EventSource`.
    - Focused test command exits 0: `pnpm test -- packages/ui/src/components/detail/PinnedStatePanel.test.tsx test/boundary.test.ts`.
    - Boundary test exits 0 and still rejects UI imports from server/Hono/Node modules.
  </acceptance_criteria>
  <done>
    Done when the pin helper exports the required model/functions, duplicate pins replace deterministically, a third pin drops the oldest, storage/network APIs are absent, and the focused test command exits 0.
  </done>
  <verify>
    <automated>pnpm test -- packages/ui/src/components/detail/PinnedStatePanel.test.tsx test/boundary.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pinned-state panel and pin action to StateInspectorPanel</name>
  <read_first>
    - packages/ui/src/components/detail/StateInspectorPanel.tsx
    - packages/ui/src/components/detail/StateInspectorPanel.test.tsx
    - packages/ui/src/components/detail/StateCopyMenu.tsx
    - packages/ui/src/components/detail/StateConfidenceBadge.tsx
    - packages/ui/src/components/detail/StateDiagnosticsPanel.tsx
    - packages/ui/src/styles/global.css
    - packages/ui/src/components/detail/state-pins.ts
  </read_first>
  <files>packages/ui/src/components/detail/PinnedStatePanel.tsx, packages/ui/src/components/detail/StateInspectorPanel.tsx, packages/ui/src/components/detail/StateInspectorPanel.test.tsx, packages/ui/src/components/detail/PinnedStatePanel.test.tsx, packages/ui/src/styles/global.css</files>
  <action>
    Create `PinnedStatePanel.tsx` that renders:
    - Heading text `Pinned state points`.
    - Empty text `Pin two state points to compare before/after reducer state.` when there are no pins.
    - For each pin: `#${targetIndex}`, `eventLabel`, resource kind, resource URI, `StateConfidenceBadge`, and diagnostic count.
    - Buttons labelled `Remove pinned state` per pin and `Clear pinned states` when at least one pin exists.
    - Text `Pinned ${count}/2`.
    Update `StateInspectorPanel.tsx` to hold `const [pinnedPoints, setPinnedPoints] = useState<readonly PinnedStatePoint[]>([])`. When `logKey` changes, reset pinned points to `[]`. When selected resource is loaded, render a button labelled `Pin state point` beside `StateCopyMenu`. Clicking it must call `createPinnedStatePoint` with current `targetIndex`, `logKey`, `eventLabel`, `eventTimestamp`, and selected resource metadata/state, then `upsertPinnedStatePoint`. Use current event context from props; if needed, extend `StateInspectorPanelProps` with `eventLabel` and `eventTimestamp` and pass values from `DetailPanel.tsx` using `event.method ?? event.actionType ?? event.kind` and `event.ts`.
    Render `PinnedStatePanel` below the state view/diagnostics inside the inspector. Do not add persistence or server calls.
  </action>
  <acceptance_criteria>
    - `StateInspectorPanel.tsx` contains the button label `Pin state point`.
    - `PinnedStatePanel.tsx` contains `Pinned state points`, `Pinned ${`, `Remove pinned state`, and `Clear pinned states`.
    - Tests assert the pin button is absent before selected resource state exists and present after `selectedResource` loads.
    - Tests assert two pinned points render with event indexes/resource URIs, and the third pin leaves exactly two visible points.
    - Tests assert changing `logKey` clears pinned points.
    - Focused test command exits 0: `pnpm test -- packages/ui/src/components/detail/StateInspectorPanel.test.tsx packages/ui/src/components/detail/PinnedStatePanel.test.tsx test/boundary.test.ts`.
  </acceptance_criteria>
  <done>
    Done when the state inspector shows `Pin state point` only for loaded selected resources, pinned points render with metadata/resource context, remove/clear and two-pin cap behavior work, pins reset on `logKey` change, and focused tests pass.
  </done>
  <verify>
    <automated>pnpm test -- packages/ui/src/components/detail/StateInspectorPanel.test.tsx packages/ui/src/components/detail/PinnedStatePanel.test.tsx test/boundary.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Apply token-based styling for pinned points</name>
  <read_first>
    - packages/ui/src/styles/global.css
    - packages/ui/src/styles/tokens.css
    - packages/ui/src/components/detail/PinnedStatePanel.tsx
    - packages/ui/src/components/detail/StateConfidenceBadge.tsx
  </read_first>
  <files>packages/ui/src/styles/global.css, packages/ui/src/components/detail/PinnedStatePanel.tsx</files>
  <action>
    Add CSS classes under the existing state inspector section in `global.css`: `.pinned-state-panel`, `.pinned-state-header`, `.pinned-state-list`, `.pinned-state-card`, `.pinned-state-meta`, `.pinned-state-actions`, and `.pinned-state-empty`. Use only existing CSS variables such as `--color-surface`, `--color-surface-raised`, `--color-border`, `--color-border-strong`, `--color-text`, `--color-text-muted`, `--color-accent`, `--space-*`, `--font-sans`, `--font-mono`, and `--shadow-menu`. Do not add raw hex literals in TSX or component-local inline colors.
  </action>
  <acceptance_criteria>
    - `global.css` contains `.pinned-state-panel`, `.pinned-state-card`, and `.pinned-state-empty`.
    - `packages/ui/src/components/detail/PinnedStatePanel.tsx` contains no raw `#` hex color literals.
    - `pnpm lint` exits 0.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>
    Done when pinned state UI classes are implemented with existing theme tokens only, no raw TSX hex colors are introduced, and typecheck plus lint pass.
  </done>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
</task>

</tasks>

<threat_model>

| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-10-01-01 | Information Disclosure | Pinned state storage | Keep pins in React memory only. Do not use localStorage/sessionStorage/IndexedDB/export or outbound network. |
| T-10-01-02 | Spoofing | Cross-log pins | Reset pins on `logKey` change and include `logKey` in pin identity. |
| T-10-01-03 | DoS | Pin list growth | Cap active pins at `MAX_PINNED_STATE_POINTS = 2`. |
| T-10-01-04 | Boundary violation | UI imports | Keep pin helpers browser-only and verify with `test/boundary.test.ts`. |

</threat_model>

<verification>
Run:

```bash
pnpm test -- packages/ui/src/components/detail/StateInspectorPanel.test.tsx packages/ui/src/components/detail/PinnedStatePanel.test.tsx test/boundary.test.ts
pnpm typecheck
pnpm lint
```
</verification>

<success_criteria>
COMPARE-01 is complete when a user can pin two selected reconstructed state points from the state inspector and see each pinned point's event metadata, resource context, confidence, and diagnostic count. COMPARE-03 remains protected because pins are browser-memory only and reset on log switch.
</success_criteria>
