---
phase: 10-pinned-comparison-and-milestone-verification
plan: "10-03"
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - e2e/phase10.spec.ts
  - test/sse-integration.test.ts
  - packages/ui/src/test-fixtures/phase10-state-log.ts
  - USER_GUIDE.md
  - screenshots/phase10/
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - VERIFY-03
  - VERIFY-04
  - COMPARE-03
must_haves:
  truths:
    - "Browser E2E covers opening a log, selecting rows, inspecting state, pinning two points, comparison, confidence, diagnostics, and theme screenshots."
    - "Large-log verification proves `/api/state-at` remains lazy/responsive and timeline SSE rows are not inflated with replay payloads."
    - "User guide documents reducer-backed state inspection, pinning, comparison, and local-only privacy using fresh Phase 10 screenshots."
    - "Milestone tracking documents mark Phase 10 requirements and plans complete only after implementation validation passes."
  artifacts:
    - path: "e2e/phase10.spec.ts"
      provides: "End-to-end browser coverage for the full state inspection and pinned comparison flow"
    - path: "packages/ui/src/test-fixtures/phase10-state-log.ts"
      provides: "Synthetic replayable JSONL fixture for Phase 10 browser and large-log tests"
    - path: "test/sse-integration.test.ts"
      provides: "Large-log state-at responsiveness and SSE non-inflation verification"
    - path: "USER_GUIDE.md"
      provides: "User-facing walkthrough for state inspection, pinning, and comparison"
    - path: "screenshots/phase10/"
      provides: "Fresh dark, light, and hacker mode screenshots for documentation"
  key_links:
    - from: "e2e/phase10.spec.ts"
      to: "packages/ui/src/test-fixtures/phase10-state-log.ts"
      via: "Synthetic fixture drives browser state inspection and comparison flow"
    - from: "test/sse-integration.test.ts"
      to: "packages/server/src/state-routes.ts"
      via: "Large-log `/api/state-at` request verifies lazy reducer replay endpoint behavior"
    - from: "USER_GUIDE.md"
      to: "screenshots/phase10/"
      via: "Guide embeds fresh synthetic-fixture screenshots"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 10 completion marks COMPARE/VERIFY requirements and plan checkboxes consistently"
---

<objective>
Add milestone-level verification for the reducer-backed state workflow and refresh user-facing docs/screenshots.

Purpose: satisfy VERIFY-03 and VERIFY-04 by covering the full browser flow from log open through state inspection, pinning, comparison, and diagnostics, and by proving large-log state-at lookup stays lazy without SSE payload inflation.

Output: Playwright E2E, synthetic Phase 10 fixtures, large-log/SSE tests, updated user guide screenshots, final validation commands, and tracking document updates.
</objective>

<must_haves>
- Browser E2E covers opening a log, selecting rows, viewing state, pinning two points, seeing confidence/diagnostics, and seeing changed top-level paths.
- E2E asserts no absolute local path leakage in browser-visible text.
- Large-log test confirms `/api/state-at` remains lazy and SSE rows do not include `resources`, `diagnostics`, `intents`, `cache`, or `state`.
- `USER_GUIDE.md` describes state inspection, pinning, comparison, and screenshots using `screenshots/phase10/`.
- Full validation runs `pnpm test`, `pnpm typecheck`, `pnpm lint`, and `pnpm e2e` unless a pre-existing environment issue blocks E2E and is documented.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 10 browser E2E fixture and flow</name>
  <read_first>
    - e2e/phase5.spec.ts
    - packages/ui/src/test-fixtures/phase5-log.ts
    - packages/ui/src/components/detail/StateInspectorPanel.tsx
    - packages/ui/src/components/detail/PinnedStatePanel.tsx
    - packages/server/src/state-routes.ts
    - packages/server/src/app-state.test.ts
  </read_first>
  <files>e2e/phase10.spec.ts, packages/ui/src/test-fixtures/phase10-state-log.ts, screenshots/phase10/</files>
  <action>
    Create `packages/ui/src/test-fixtures/phase10-state-log.ts` exporting a synthetic JSONL string with:
    - initialize request.
    - initialize response containing root/session/terminal snapshots.
    - at least one server action that changes a top-level session state field or root active session count.
    - at least one unknown server action diagnostic for the state inspector diagnostics panel.
    Create `e2e/phase10.spec.ts` following `e2e/phase5.spec.ts` helper patterns: spawn CLI with `BROWSER=none`, write fixture to temp dir, use `--port 0 --no-open`, wait for the printed port, clean up process and temp dir.
    The test must:
    1. open the viewer,
    2. select the first state-bearing row,
    3. click `State at this point`,
    4. select `session copilot:/session/1`,
    5. click `Pin state point`,
    6. select a later row after a state-changing action,
    7. open state and pin the same resource again,
    8. assert `Pinned comparison` is visible,
    9. assert `Changed top-level paths` is visible,
    10. assert confidence and replay diagnostics are visible,
    11. switch dark/light/hacker themes and save screenshots to `screenshots/phase10/`.
    Add `assertNoPathLeak` equivalent from Phase 5.
  </action>
  <acceptance_criteria>
    - `e2e/phase10.spec.ts` contains `Pinned comparison`, `Pin state point`, and `Changed top-level paths`.
    - `packages/ui/src/test-fixtures/phase10-state-log.ts` contains `snapshots`, `copilot:/session/1`, and `mystery.action`.
    - `pnpm e2e -- e2e/phase10.spec.ts` exits 0, or if Playwright CLI does not support file args in this project, `pnpm e2e` exits 0.
    - `screenshots/phase10/` contains at least three screenshots covering dark, light, and hacker modes.
  </acceptance_criteria>
  <done>
    Done when the Phase 10 Playwright flow opens a synthetic log, pins two reconstructed state points, shows comparison/confidence/diagnostics, verifies no path leak, captures dark/light/hacker screenshots, and E2E passes.
  </done>
  <verify>
    <automated>pnpm e2e -- e2e/phase10.spec.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add large-log state-at and SSE non-inflation verification</name>
  <read_first>
    - test/sse-integration.test.ts
    - packages/server/src/state-routes.test.ts
    - packages/server/src/app-state.test.ts
    - packages/server/src/state-replay-index.ts
    - packages/server/src/state-routes.ts
  </read_first>
  <files>test/sse-integration.test.ts, packages/ui/src/test-fixtures/phase10-state-log.ts</files>
  <action>
    Extend `test/sse-integration.test.ts` with a Phase 10 test that builds or writes a synthetic log with at least 1,000 events and replayable snapshots/actions. The test must call `/api/state-at?idx=<late index>` and assert success, then inspect SSE snapshot/append payloads and assert the serialized SSE row payloads do not contain `"resources"`, `"diagnostics"`, `"intents"`, `"cache"`, or `"state":`. Measure the `/api/state-at?idx=<late index>` request duration for the synthetic 1,000+ event log and assert it completes within a generous CI-safe threshold such as `< 2000ms`, or use an existing project performance threshold if one already exists. Keep the assertion focused on preventing obviously unbounded replay/SSE inflation regressions rather than microbenchmarking. Reuse existing SSE helpers and non-inflation assertions from Phase 8/Phase 9 tests. Do not change server state replay implementation unless the test exposes a directly related bug.
  </action>
  <acceptance_criteria>
    - `test/sse-integration.test.ts` contains a test name with `large-log state-at` or `state-at lookup remains lazy`.
    - The test fixture/event generation creates at least 1,000 events.
    - The large-log test asserts `/api/state-at?idx=<late index>` completes within a bounded CI-safe threshold, e.g. `< 2000ms`, for the synthetic 1,000+ event log.
    - The test asserts absence of `"resources"`, `"diagnostics"`, `"intents"`, `"cache"`, and `"state":` in SSE row payload JSON.
    - `pnpm test -- test/sse-integration.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    Done when the SSE integration suite includes a 1,000+ event replayable fixture, proves `/api/state-at` succeeds within a bounded CI-safe threshold, proves SSE row payloads omit replay fields, and the focused integration test passes.
  </done>
  <verify>
    <automated>pnpm test -- test/sse-integration.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Refresh user guide and milestone tracking</name>
  <read_first>
    - USER_GUIDE.md
    - screenshots/phase9/
    - screenshots/phase10/
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/09-state-inspector-ui/09-VERIFICATION.md
  </read_first>
  <files>USER_GUIDE.md, screenshots/phase10/, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    Update `USER_GUIDE.md` to add or refresh a walkthrough section for reducer-backed state inspection:
    - selecting a row,
    - clicking `State at this point`,
    - selecting a resource,
    - reading confidence/diagnostics,
    - pinning two state points,
    - reading `Pinned comparison` and `Changed top-level paths`,
    - local-only privacy note for copied/pinned state.
    Embed fresh screenshots from `screenshots/phase10/` using the existing screenshot markdown style.
    Update `.planning/ROADMAP.md` after Phase 10 execution to mark 10-01/10-02/10-03 complete. Update `.planning/REQUIREMENTS.md` to mark COMPARE-01, COMPARE-02, COMPARE-03, VERIFY-03, and VERIFY-04 done. Update `.planning/STATE.md` to show Phase 10 complete/ready for milestone verification. Do not archive the milestone in this plan; `/gsd-complete-milestone` remains separate unless the user asks.
  </action>
  <acceptance_criteria>
    - `USER_GUIDE.md` contains `State at this point`, `Pin state point`, `Pinned comparison`, and `Changed top-level paths`.
    - `USER_GUIDE.md` references at least one `screenshots/phase10/` image.
    - `.planning/REQUIREMENTS.md` marks COMPARE-01, COMPARE-02, COMPARE-03, VERIFY-03, and VERIFY-04 as `[x]` or `Done`.
    - `.planning/ROADMAP.md` marks 10-01, 10-02, and 10-03 complete after implementation.
    - `pnpm test && pnpm typecheck && pnpm lint` exits 0.
  </acceptance_criteria>
  <done>
    Done when the user guide includes the Phase 10 state inspection/pinning/comparison walkthrough with screenshots, planning trackers mark Phase 10 requirements/plans complete, and full test/typecheck/lint validation passes.
  </done>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm lint</automated>
  </verify>
</task>

</tasks>

<threat_model>

| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-10-03-01 | Information Disclosure | E2E/browser text | Assert no `/Users/`, `/home/`, or Windows drive paths in visible browser text. |
| T-10-03-02 | DoS | Large-log verification | Use synthetic fixture and bounded assertions; do not add state to SSE row payloads. |
| T-10-03-03 | Information Disclosure | Documentation screenshots | Use synthetic fixtures only; screenshots must not contain real user paths or secrets. |
| T-10-03-04 | Scope creep | Milestone verification | Do not archive milestone or add export/AI explanation; only verify and refresh docs. |

</threat_model>

<verification>
Run:

```bash
pnpm test -- test/sse-integration.test.ts
pnpm e2e
pnpm test
pnpm typecheck
pnpm lint
```
</verification>

<success_criteria>
VERIFY-03 is complete when browser E2E covers opening a log, selecting a row, viewing state, pinning two points, comparing them, and seeing confidence diagnostics. VERIFY-04 is complete when large-log tests prove state-at-index lookup remains lazy and SSE row payloads are not inflated. COMPARE-03 remains complete when verification proves no path leakage or outbound behavior.
</success_criteria>
