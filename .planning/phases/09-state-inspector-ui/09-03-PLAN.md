---
phase: 09-state-inspector-ui
plan: "09-03"
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - packages/ui/src/components/detail/StateInspectorPanel.tsx
  - packages/ui/src/components/detail/StateInspectorPanel.test.tsx
  - packages/ui/src/components/detail/StateConfidenceBadge.tsx
  - packages/ui/src/components/detail/StateDiagnosticsPanel.tsx
  - packages/ui/src/components/detail/StateCopyMenu.tsx
  - packages/ui/src/components/detail/StateCopyMenu.test.tsx
  - packages/ui/src/components/detail/clipboard.ts
  - packages/ui/src/components/detail/CopyMenu.tsx
  - packages/ui/src/components/detail/CopyMenu.test.tsx
  - packages/ui/src/styles/global.css
  - packages/ui/src/styles/tokens.css
autonomous: true
requirements:
  - STATE-04
  - STATE-05
validation_commands:
  - pnpm test -- packages/ui/src/components/detail/StateInspectorPanel.test.tsx packages/ui/src/components/detail/StateCopyMenu.test.tsx packages/ui/src/components/detail/CopyMenu.test.tsx test/boundary.test.ts
  - pnpm typecheck
  - pnpm lint
scope_guards:
  - No Phase 10 pinning or comparison UI.
  - No before/after diff or changed-path comparison.
  - No SSE state payload changes.
  - No server replay rewrite.
  - No external network, telemetry, or AI explanation of state.
scope_notes:
  - Plan 09-03 intentionally touches 11 cohesive detail/copy/theme files; keep it as one plan unless execution context becomes tight, in which case split clipboard extraction into a follow-up before coding.
must_haves:
  truths:
    - User sees aggregate and selected-resource confidence adjacent to reconstructed state.
    - Partial or unknown state is visually and textually distinct from complete state.
    - User sees replay diagnostics near reconstructed state, including code, severity, event index, and message.
    - User can copy selected reconstructed state as compact JSON and pretty JSON.
    - User can copy a concise state summary that includes event index, resource identity, confidence, diagnostic count, and replay metadata.
  artifacts:
    - path: packages/ui/src/components/detail/StateConfidenceBadge.tsx
      provides: Complete/partial/unknown trust label UI.
    - path: packages/ui/src/components/detail/StateDiagnosticsPanel.tsx
      provides: Replay diagnostics and context display.
    - path: packages/ui/src/components/detail/StateCopyMenu.tsx
      provides: State JSON and summary copy actions.
    - path: packages/ui/src/components/detail/clipboard.ts
      provides: Shared clipboard helper reused by existing CopyMenu and StateCopyMenu.
  key_links:
    - from: packages/ui/src/components/detail/StateInspectorPanel.tsx
      to: packages/ui/src/components/detail/StateConfidenceBadge.tsx
      via: aggregate and selected-resource confidence display.
      pattern: StateConfidenceBadge
    - from: packages/ui/src/components/detail/StateInspectorPanel.tsx
      to: packages/ui/src/components/detail/StateDiagnosticsPanel.tsx
      via: diagnostics and replay context display near state.
      pattern: StateDiagnosticsPanel
    - from: packages/ui/src/components/detail/StateCopyMenu.tsx
      to: packages/ui/src/components/detail/clipboard.ts
      via: shared explicit clipboard write helper.
      pattern: copyText
---

<objective>
Add confidence, diagnostics, and copy actions to the state inspector.

Purpose: Satisfy STATE-04 and STATE-05 so users understand reconstruction trust level and can copy selected state or a concise summary without confusing partial/unknown state for complete truth.

Output: Confidence UI, diagnostics UI, state copy menu, shared clipboard helper, tests, and theme-safe styling.
</objective>

<context>
Read before implementation:

- `.planning/ROADMAP.md`
- `.planning/REQUIREMENTS.md`
- `.planning/STATE.md`
- `.planning/phases/09-state-inspector-ui/09-RESEARCH.md`
- `.planning/phases/08-server-state-at-index-api-and-cache-integration/08-VERIFICATION.md`
- `.planning/phases/09-state-inspector-ui/09-01-SUMMARY.md`
- `.planning/phases/09-state-inspector-ui/09-02-SUMMARY.md`
- `packages/server/src/state-routes.ts`
- `packages/ui/src/components/detail/StateInspectorPanel.tsx`
- `packages/ui/src/components/detail/CopyMenu.tsx`
- `packages/ui/src/components/detail/CopyToast.tsx`
- `packages/ui/src/styles/tokens.css`
- `packages/ui/src/styles/global.css`

Phase 8 response fields to surface:

- `confidence: "complete" | "partial" | "unknown"`
- `diagnostics[]`
- `selectedResource.confidence`
- `selectedResource.diagnostics[]`
- `intents[]`
- `cache`

Copy pattern from existing `CopyMenu`:

- Copy must be explicit user action only.
- Use `navigator.clipboard.writeText` with textarea `execCommand("copy")` fallback.
- Report feedback through `CopyToast`.
- Do not use `dangerouslySetInnerHTML`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add confidence and diagnostics components</name>
  <files>packages/ui/src/components/detail/StateConfidenceBadge.tsx, packages/ui/src/components/detail/StateDiagnosticsPanel.tsx, packages/ui/src/components/detail/StateInspectorPanel.tsx, packages/ui/src/components/detail/StateInspectorPanel.test.tsx, packages/ui/src/styles/global.css, packages/ui/src/styles/tokens.css</files>
  <behavior>
    - Complete confidence renders as "Complete" and does not include caution text.
    - Partial confidence renders as "Partial" with text that reconstruction may be incomplete.
    - Unknown confidence renders as "Unknown" with text that state cannot be treated as authoritative.
    - StateInspectorPanel shows both aggregate confidence and selected-resource confidence when a selected resource is loaded.
    - Diagnostics panel lists diagnostic code, severity, event index when present, and message.
    - Diagnostics panel distinguishes aggregate diagnostics from selected-resource diagnostics.
    - Intents and cache metadata appear only as concise replay context, not as copied/pinned/comparison features.
  </behavior>
  <action>
    Create `StateConfidenceBadge.tsx` for complete/partial/unknown visual treatment using token classes, with partial/unknown rendered as cautionary text adjacent to reconstructed state. Create `StateDiagnosticsPanel.tsx` that displays aggregate diagnostics and selected-resource diagnostics with code, severity, event index if present, and message. Include concise replay context for client intents count and cache status/count fields when available; keep raw diagnostic details out of default view unless already summarized in the message. Wire both components into `StateInspectorPanel.tsx` near the selected state Summary/Pretty/Raw area so confidence and diagnostics are visible next to reconstructed state. Add or extend token variables/classes for confidence surfaces in dark/light/hacker themes without raw color literals in TSX.
  </action>
  <verify>
    <automated>pnpm test -- packages/ui/src/components/detail/StateInspectorPanel.test.tsx test/boundary.test.ts</automated>
  </verify>
  <done>Users can see complete/partial/unknown confidence and replay diagnostics next to reconstructed state, with partial/unknown impossible to mistake for authoritative complete state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add state copy menu with shared clipboard helper</name>
  <files>packages/ui/src/components/detail/clipboard.ts, packages/ui/src/components/detail/CopyMenu.tsx, packages/ui/src/components/detail/CopyMenu.test.tsx, packages/ui/src/components/detail/StateCopyMenu.tsx, packages/ui/src/components/detail/StateCopyMenu.test.tsx, packages/ui/src/components/detail/StateInspectorPanel.tsx, packages/ui/src/components/detail/StateInspectorPanel.test.tsx</files>
  <behavior>
    - "Copy state raw JSON" writes compact `JSON.stringify(selectedResource.state)`.
    - "Copy state pretty JSON" writes `JSON.stringify(selectedResource.state, null, 2)`.
    - "Copy state summary" writes concise text containing target idx, resource kind, resource URI, confidence, diagnostic count, baseline event idx, last applied event idx, baseline sequence, and last server sequence.
    - Copy failures call `onCopy("Copy failed. Select and copy manually.", false)`.
    - Copy success calls `onCopy` with copied character count and true.
    - Existing `CopyMenu` tests still pass after extracting shared clipboard helper.
    - State copy menu is not rendered until `selectedResource` exists.
  </behavior>
  <action>
    Extract the private `copyText` function from `CopyMenu.tsx` into `clipboard.ts`, export it, and update existing `CopyMenu.tsx` to import it without changing event-copy labels or payloads. Create `StateCopyMenu.tsx` using the same dropdown interaction pattern and explicit user action behavior. Provide three actions: compact state JSON, pretty state JSON, and concise state summary. Build summary text locally from current response/selected-resource metadata; do not include Phase 10 pin IDs, comparison language, diffs, or changed paths. Wire `StateCopyMenu` into `StateInspectorPanel.tsx` beside the state view header and use existing `CopyToast` feedback state. For non-serializable state, return `[Circular or non-serializable value]` consistently with existing Raw/Copy behavior.
  </action>
  <verify>
    <automated>pnpm test -- packages/ui/src/components/detail/StateCopyMenu.test.tsx packages/ui/src/components/detail/CopyMenu.test.tsx packages/ui/src/components/detail/StateInspectorPanel.test.tsx</automated>
  </verify>
  <done>Users can copy selected reconstructed state as compact JSON, pretty JSON, or concise summary, and existing event copy behavior is preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Validate full Phase 9 UI behavior and record browser/screenshot notes</name>
  <files>packages/ui/src/components/detail/StateInspectorPanel.tsx, packages/ui/src/components/detail/StateConfidenceBadge.tsx, packages/ui/src/components/detail/StateDiagnosticsPanel.tsx, packages/ui/src/components/detail/StateCopyMenu.tsx, packages/ui/src/styles/global.css, packages/ui/src/styles/tokens.css</files>
  <action>
    Run focused tests, boundary test, typecheck, and lint. Then perform browser verification with `pnpm start:fixture`: open the local viewer URL printed by the CLI, select a row, open state inspector, select a resource, confirm Summary/Pretty/Raw views, confirm confidence and diagnostics are adjacent to state, use each state copy action, and confirm CopyToast feedback. Check dark/light/hacker themes and record screenshot notes in the summary. If user-facing docs are not updated in Phase 9, explicitly note that full docs/screenshot refresh remains Phase 10 scope. Do not add Phase 10 E2E, pinning, comparison, before/after diff, changed-path UI, SSE state payload changes, server replay changes, or outbound network behavior.
  </action>
  <verify>
    <automated>pnpm test -- packages/ui/src/components/detail/StateInspectorPanel.test.tsx packages/ui/src/components/detail/StateCopyMenu.test.tsx packages/ui/src/components/detail/CopyMenu.test.tsx test/boundary.test.ts && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>STATE-04 and STATE-05 behavior is tested, browser-verified, theme-checked, and scoped to Phase 9 state inspection only.</done>
</task>

</tasks>

<threat_model>

| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-09-03-01 | Information Disclosure | `StateCopyMenu` clipboard actions | Copy only on explicit user click; labels state payload clearly; no automatic clipboard writes. |
| T-09-03-02 | Spoofing | Confidence display | Render partial/unknown with explicit caution text and distinct token styling adjacent to state. |
| T-09-03-03 | Information Disclosure | Diagnostics rendering | Render diagnostics as React text only; do not use `dangerouslySetInnerHTML`; omit opaque raw details from default UI unless included in safe message text. |
| T-09-03-04 | Tampering | Shared clipboard helper extraction | Preserve existing `CopyMenu` tests exactly and add state copy tests for compact/pretty/summary payloads. |
| T-09-03-05 | Repudiation | Copy feedback | Use `CopyToast` status feedback for success/failure so user sees copy result. |
| T-09-03-06 | Information Disclosure | Local-only posture | No outbound network calls, telemetry, AI explanation, export, pinning, or comparison behavior in Phase 9. |

</threat_model>

<verification>
Run:

```bash
pnpm test -- packages/ui/src/components/detail/StateInspectorPanel.test.tsx packages/ui/src/components/detail/StateCopyMenu.test.tsx packages/ui/src/components/detail/CopyMenu.test.tsx test/boundary.test.ts
pnpm typecheck
pnpm lint
```

Browser smoke:

1. Start fixture server with `pnpm start:fixture` and open the local viewer URL printed by the CLI.
2. Select a timeline event and click "State at this point".
3. Select an available resource.
4. Confirm confidence badge/strip is visible near reconstructed state.
5. Confirm diagnostics show code/severity/event/message when fixture data includes diagnostics.
6. Switch Summary, Pretty JSON, and Raw JSON tabs.
7. Use compact JSON, pretty JSON, and summary copy actions.
8. Confirm CopyToast success/failure feedback.
9. Repeat visual check in dark/light/hacker themes and record screenshot notes; if docs are not updated, state that full docs/screenshot refresh remains Phase 10 scope.
10. Confirm no pinning, comparison, before/after diff, changed paths, or E2E additions were introduced.
</verification>

<success_criteria>
STATE-04 and STATE-05 are complete when confidence and diagnostics are visible beside reconstructed state and users can explicitly copy selected state or a concise summary with existing local-only clipboard behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/09-state-inspector-ui/09-03-SUMMARY.md`.
</output>
