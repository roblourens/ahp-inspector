---
phase: 30-please-make-sure-that-we-will-discover-the-latest-logs-maybe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/host-node/src/bounded-log-discovery.ts
  - packages/host-node/src/bounded-log-discovery.test.ts
  - packages/host-node/src/discovery.ts
  - packages/host-node/src/discovery.test.ts
  - packages/host-node/src/find-latest-ahp-log.ts
  - packages/host-node/src/find-latest-ahp-log.test.ts
autonomous: true
requirements: [DISC-30-01, DISC-30-02, DISC-30-03, DISC-30-04]
must_haves:
  truths:
    - "Every configured VS Code log root receives its own per-root time, stat, and immediate-entry bounded scan opportunity, so exhausting an earlier root cannot prevent a later root from contributing."
    - "Discovery streams immediate root entries and selects the newest launch directories from every launch entry examined within the per-root bounds before applying topLaunchDirs."
    - "The picker retains at least the newest ranked candidate from every populated configured root, with an input-bounded effective cap of max(200, populatedRootCount) and quota/fill allocation for remaining capacity."
    - "CLI auto-open globally ranks every fairly gathered non-empty candidate and probes newest-first until the first valid AHP-shaped log, which is therefore the globally newest valid candidate in the bounded gathered set."
    - "Filename and AHP-shape checks, picker confidence ordering, opaque ID resolution, local-only privacy, and the host adapter boundary remain intact."
  artifacts:
    - path: "packages/host-node/src/bounded-log-discovery.ts"
      provides: "Shared host-node-internal bounded per-root path discovery primitive"
      exports: ["scanConfiguredRoots"]
    - path: "packages/host-node/src/bounded-log-discovery.test.ts"
      provides: "Hermetic regressions for streaming bounds, independent root budgets, post-recency launch caps, and ranking-safe gathering"
    - path: "packages/host-node/src/discovery.ts"
      provides: "Fair bounded picker candidate selection and opaque ID mapping"
      exports: ["discoverVsCodeLogs", "resolveCandidateId"]
    - path: "packages/host-node/src/discovery.test.ts"
      provides: "Hermetic picker regressions for noisy-root starvation, launch recency, ranking-safe gathering, and over-200-root fair retention"
    - path: "packages/host-node/src/find-latest-ahp-log.ts"
      provides: "Fair CLI auto-open scan and newest valid AHP log selection"
      exports: ["findLatestAhpLog"]
    - path: "packages/host-node/src/find-latest-ahp-log.test.ts"
      provides: "Hermetic CLI regressions for noisy-root starvation and global newest-valid selection"
  key_links:
    - from: "packages/host-node/src/discovery.ts"
      to: "packages/host-node/src/bounded-log-discovery.ts"
      via: "per-root path scan followed by picker-specific scoring, fair retention, and opaque candidate projection"
      pattern: "scanConfiguredRoots"
    - from: "packages/host-node/src/find-latest-ahp-log.ts"
      to: "packages/host-node/src/bounded-log-discovery.ts"
      via: "per-root path scan followed by one global mtime ordering and newest-first AHP-shape probing across the bounded gathered set"
      pattern: "scanConfiguredRoots"
    - from: "packages/host-node/src/discovery.ts"
      to: "packages/host-node/src/discovery.test.ts"
      via: "opaque IDs remain resolvable without exposing absolute paths in LogCandidate"
      pattern: "resolveCandidateId"
---

<objective>
Make picker discovery and CLI auto-open find fresh logs fairly across every configured VS Code root without surrendering bounded local filesystem traversal.

Purpose: Eliminate the verified root-order starvation and pre-recency launch truncation regressions while preserving the existing discovery contracts and local-only host boundary.
Output: One shared per-root bounded scan primitive, fair picker retention, fair CLI probing, and hermetic regression coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-npx-publishing-and-auto-open-latest-log/13-CONTEXT.md
@.planning/phases/13-npx-publishing-and-auto-open-latest-log/13-01-SUMMARY.md
@.planning/phases/13-npx-publishing-and-auto-open-latest-log/13-VERIFICATION.md
@packages/host-node/src/discovery.ts
@packages/host-node/src/discovery.test.ts
@packages/host-node/src/find-latest-ahp-log.ts
@packages/host-node/src/find-latest-ahp-log.test.ts

<acceptance_context>
Treat these verified live regression facts as locked acceptance context:
- `defaultRoots()` already includes both OSS roots and current OSS logs match the filename rules; do not change root coverage or filename semantics.
- A shared global picker budget returned 200 candidates from earlier roots and zero from a populated later OSS root; putting OSS first restored its fresh logs.
- The CLI walker likewise shares one budget across ordered roots and can choose a stale earlier-root log without reaching a later root.
- Picker discovery slices readdir names to the first 50 before stat/sorting launch directories, so readdir order can hide the newest launch.
- A global 200-candidate slice after sorting can still starve a populated root; bounded output must retain fair representation from every populated configured root.
- Fixed candidate or probe caps applied before consumer ranking can discard the freshest or globally newest valid match; gathering is bounded only by per-root scan bounds and consumers rank the complete gathered set.
- Picker output uses `max(MAX_RESULTS, populatedRootCount)`, seeds one newest-ranked candidate per populated root, then quota/fills remaining capacity; default-root populations still cap at 200.
- `timeBudgetMs`, `maxStats`, and the new immediate-entry bound are per-root options, so aggregate configured work grows linearly with configured root count.
- No CONTEXT.md, RESEARCH.md, REQUIREMENTS.md, PATTERNS.md, or VALIDATION.md exists for Phase 30. The user explicitly skipped research and Nyquist artifacts for this run.
</acceptance_context>

<interfaces>
<!-- Existing contracts plus the planned bounded-enumeration option. -->

From packages/host-node/src/discovery.ts:
```typescript
export interface Root {
  readonly origin: "vscode" | "vscode-insiders" | "vscode-oss-dev";
  readonly dir: string;
}

export interface DiscoverOptions {
  roots?: readonly Root[];
  /** Per configured root; aggregate budget is roots.length * timeBudgetMs, excluding in-flight filesystem call latency. */
  timeBudgetMs?: number;
  /** Per configured root; aggregate stat ceiling is roots.length * maxStats. */
  maxStats?: number;
  /** Per configured root; aggregate immediate-entry ceiling is roots.length * maxImmediateEntries. */
  maxImmediateEntries?: number;
  topLaunchDirs?: number;
  now?: () => number;
}

export async function discoverVsCodeLogs(opts?: DiscoverOptions): Promise<DiscoveryResult>;
export function resolveCandidateId(id: string): string | null;
```

From packages/host-node/src/find-latest-ahp-log.ts:
```typescript
export interface FindLatestAhpLogOptions {
  rootsOverride?: readonly Root[];
}

export async function findLatestAhpLog(opts?: FindLatestAhpLogOptions): Promise<string | null>;
```

From packages/shared/src/host-protocol.ts:
```typescript
export interface LogCandidate {
  readonly id: string;
  readonly label: string;
  readonly mtimeMs: number;
  readonly sizeBytes: number;
  readonly origin: "vscode" | "vscode-insiders" | "vscode-oss-dev";
  readonly confidence: "high" | "medium" | "low";
  readonly contextLabel?: string;
}

export interface DiscoveryResult {
  readonly candidates: readonly LogCandidate[];
  readonly truncated: boolean;
}
```

`probeAhpShape()` in `find-latest-ahp-log.ts` is the existing parser-backed shape check: it reads at most 64 KiB, calls `parseLine()` plus `normalize()`, and accepts only a non-`parse-error` event. Preserve it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the shared bounded per-root path scan</name>
  <files>packages/host-node/src/bounded-log-discovery.ts, packages/host-node/src/bounded-log-discovery.test.ts</files>
  <read_first>
    - packages/host-node/src/discovery.ts
    - packages/host-node/src/discovery.test.ts
    - packages/host-node/src/find-latest-ahp-log.ts
    - packages/host-node/src/find-latest-ahp-log.test.ts
  </read_first>
  <behavior>
    - A noisy first root that exhausts its immediate-entry, stat, or time allowance does not consume the allowance of a later root; every configured root is attempted with fresh counters and a fresh start time.
    - With more than 50 launch directories, a newest launch whose name is observed beyond index 49 in the setup-time `readdir` order is selected when `topLaunchDirs` is capped and that entry falls within the configured immediate-entry bound.
    - A fresh matching file encountered after many older matching files is still gathered; there is no traversal-order candidate cap before consumer ranking.
    - Candidate gathering remains bounded per root by explicit immediate-entry, stat, time, depth, and selected-launch bounds; unreadable, missing, or concurrently removed entries are skipped without aborting other roots.
  </behavior>
  <action>
Create a host-node-internal `scanConfiguredRoots()` helper that returns path-bearing matching files grouped by configured-root input index. Give each root an independent bounded opportunity: reset the immediate-entry counter, stat counter, and time-budget start for every root, always attempt every configured root, and record whether each root truncated. Use only async `node:fs/promises` APIs.

The helper must remove both pre-recency `names.slice(0, MAX_LAUNCH_LIST)` and traversal-order candidate caps. Enumerate immediate root entries with async `opendir(root.dir)` streaming, checking the per-root time, stat, and `maxImmediateEntries` bounds during iteration so a very large root is never fully allocated by `readdir` before bounds apply. Collect and stat only the bounded examined launch entries, sort that examined set newest-first, and only then apply `topLaunchDirs`; wording and tests must claim selection from all entries examined within bounds, not from entries the scan never examined. Walk selected launches with async `opendir` streaming to the existing bounded depth, applying time/stat checks before advancing iteration, and gather every matching file reached within those natural bounds. Do not impose any candidate-count or traversal-order retention cap: per-root stat/time/depth/immediate-entry/selected-launch bounds already bound the gathered set, and consumers must see the complete gathered set to rank correctly. Preserve matching files directly under injected/custom roots. Accept the existing filename predicate as an input so traversal is shared without coupling this helper to picker scoring or CLI parser probing. Keep this module internal to host-node; do not add it to the package barrel.

Write hermetic temp-root tests with `mkdtemp`, `mkdir`, `readdir`, `writeFile`, `utimes`, and `rm` from `node:fs/promises`. Explicitly build a bounded noisy first root plus a fresh later root and prove the later root is attempted after the first truncates. For the greater-than-50 launch regression, create the launch directories, call `readdir` during setup, select an actual directory returned beyond index 49, then make that directory newest and add its qualifying log before invoking the helper; configure `maxImmediateEntries` high enough to examine it and assert `topLaunchDirs` selects it. Add a ranking-safety regression with many older matching files followed by a fresh matching file late in actual enumeration order and assert the fresh match remains in the gathered group. Use small injected bounds/clock seams for deterministic boundedness tests instead of elapsed wall-clock timing.
  </action>
  <verify>
    <automated>pnpm test -- packages/host-node/src/bounded-log-discovery.test.ts &amp;&amp; pnpm --filter @ahp-inspector/host-node typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- packages/host-node/src/bounded-log-discovery.test.ts` passes with assertions that a bounded noisy first root does not block a later root, the actual post-index-49 newest launch is selected from the bounded examined set, and a late fresh match is gathered after many older matches.
    - `pnpm --filter @ahp-inspector/host-node typecheck` passes with the internal grouped-result and per-root bounds contracts fully typed and no unsafe casts.
    - Source inspection confirms immediate root enumeration uses async `opendir`, applies time/stat/entry bounds while iterating, and contains no traversal-order candidate cap.
  </acceptance_criteria>
  <done>The shared helper proves independent per-root budgets, post-recency launch capping, bounded traversal, and resilient async filesystem handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make picker discovery fair across populated roots</name>
  <files>packages/host-node/src/discovery.ts, packages/host-node/src/discovery.test.ts</files>
  <read_first>
    - packages/host-node/src/bounded-log-discovery.ts
    - packages/host-node/src/bounded-log-discovery.test.ts
    - packages/host-node/src/discovery.ts
    - packages/host-node/src/discovery.test.ts
    - packages/shared/src/host-protocol.ts
  </read_first>
  <behavior>
    - A noisy first root cannot prevent a fresh later-root candidate from appearing in picker discovery.
    - A newest candidate under a launch directory observed beyond index 49 in setup-time `readdir` order is discovered when that launch is within the bounded examined set.
    - More than 200 populated custom roots retain at least one ranked candidate each because the effective result cap is `max(MAX_RESULTS, populatedRootCount)`; populations of 200 or fewer remain capped at 200.
    - A freshest candidate encountered after many older matches is retained after consumer ranking rather than discarded by traversal order.
    - `timeBudgetMs`, `maxStats`, and `maxImmediateEntries` give every root an independent opportunity while aggregate configured work is bounded by root count multiplied by each per-root bound.
    - Selected picker results preserve high/medium/low confidence ordering with newest mtime first inside a tier, opaque IDs resolve, and no absolute path appears in `LogCandidate` fields.
  </behavior>
  <action>
Replace `discoverVsCodeLogs()`'s private traversal/global budget with `scanConfiguredRoots()`, passing the existing filename regex semantics and public `DiscoverOptions` seams as per-root bounds. Preserve existing option names and add `maxImmediateEntries` for the new immediate-root streaming bound. In `DiscoverOptions` JSDoc, explicitly document `timeBudgetMs`, `maxStats`, and `maxImmediateEntries` as per-root: configured aggregate budget opportunities are `roots.length * timeBudgetMs` excluding in-flight filesystem call latency, aggregate stat work is at most `roots.length * maxStats`, and aggregate immediate entries examined are at most `roots.length * maxImmediateEntries`. Keep scoring, confidence tiers, launch-relative `contextLabel`, and SHA-256 opaque ID generation in `discovery.ts`.

Rank each configured root's qualifying candidates with the existing confidence-then-mtime comparator. Define `populatedRootCount` from configured-root groups containing at least one qualifying candidate and `effectiveResultCap = max(MAX_RESULTS, populatedRootCount)`, which is bounded by `max(MAX_RESULTS, roots.length)`. First seed the retained set with the single highest-ranked candidate from every populated root. Allocate an equal extra quota from the remaining capacity to every populated root, then fill any unused slots from all still-unretained candidates using the same comparator. Finally sort the retained set with that comparator for existing picker display semantics. Do not reserve capacity for empty/nonexistent roots. Report `truncated: true` when any per-root scan truncates or qualifying candidates are omitted by the effective cap. Populate `idToPath` only for candidates returned to callers so opaque ID resolution remains exact and stale IDs are cleared on each discovery run.

Extend `discovery.test.ts` with hermetic end-to-end regressions that explicitly construct a noisy first root plus a fresh later root; reuse the deterministic greater-than-50 setup rule by calling `readdir`, selecting an actual entry beyond index 49, then making it newest and adding its qualifying log; prove a late-enumerated freshest match survives ranking; prove 201 or more populated custom roots return one candidate from every root with `candidates.length === populatedRootCount`; and prove a population of 200 or fewer remains capped at `MAX_RESULTS`. Add multi-root assertions showing each root gets independent option bounds and total observed stat/immediate-entry work stays within root count multiplied by those per-root bounds. Preserve the existing filename, confidence ordering, nonexistent-root, opaque ID, no-path-leak, and default OSS-root assertions.
  </action>
  <verify>
    <automated>pnpm test -- packages/host-node/src/bounded-log-discovery.test.ts packages/host-node/src/discovery.test.ts &amp;&amp; pnpm --filter @ahp-inspector/host-node typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- packages/host-node/src/bounded-log-discovery.test.ts packages/host-node/src/discovery.test.ts` passes with assertions for independent per-root opportunities, deterministic post-index-49 launch recency, late-fresh-match retention, the 201-plus populated-root guarantee, the default 200 cap, quota/fill ranking, and coherent aggregate-work bounds.
    - `pnpm --filter @ahp-inspector/host-node typecheck` passes with the documented `DiscoverOptions` per-root semantics and `maxImmediateEntries` option.
    - The over-200-root regression asserts every populated configured root contributes its highest-ranked candidate and `candidates.length === max(200, populatedRootCount)` when enough qualifying candidates exist.
  </acceptance_criteria>
  <done>Picker discovery is independently bounded per root, selects launches after recency is known, fairly retains every populated root, and preserves its public privacy/ranking contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make CLI auto-open probe the globally ranked gathered set</name>
  <files>packages/host-node/src/find-latest-ahp-log.ts, packages/host-node/src/find-latest-ahp-log.test.ts</files>
  <read_first>
    - packages/host-node/src/bounded-log-discovery.ts
    - packages/host-node/src/bounded-log-discovery.test.ts
    - packages/host-node/src/discovery.ts
    - packages/host-node/src/find-latest-ahp-log.ts
    - packages/host-node/src/find-latest-ahp-log.test.ts
    - packages/parser/src/index.ts
  </read_first>
  <behavior>
    - A noisy root with more than ten newer invalid or empty filename matches cannot consume the probe opportunity of the globally newest valid log.
    - When multiple roots contain valid logs, CLI auto-open returns the globally newest non-empty AHP-shaped log among all fairly gathered candidates.
    - Empty, malformed, non-AHP-shaped, unreadable, and nonmatching files remain rejected without throwing.
  </behavior>
  <action>
Replace the duplicate `walk()` and shared global stat/time budget in `find-latest-ahp-log.ts` with `scanConfiguredRoots()`. Flatten every configured root's fairly gathered candidates, filter zero-byte files, globally sort the complete non-empty gathered set newest-mtime-first, and probe in that order until the first AHP-shaped candidate passes. Remove `MAX_PROBE_CANDIDATES` and do not add a global or per-root probe-count cap: the gathered set is already bounded by configured root count multiplied by the helper's per-root scan bounds, and stopping at the first valid candidate in global mtime order establishes the globally newest valid gathered log. Preserve `probeAhpShape()`'s 64 KiB read bound and `parseLine()`/`normalize()` behavior exactly; picker confidence has no role in CLI auto-open selection.

Extend `find-latest-ahp-log.test.ts` with hermetic noisy-root/fresh-later starvation coverage, a multi-root global-newest-valid assertion, and a regression placing more than ten newer invalid matching files before the globally newest valid candidate. Assert the valid candidate is still returned after all newer invalid candidates are rejected. Keep the existing newest selection, zero-byte, malformed shape, no-match, and unreadable-file tests. Keep all filesystem setup/cleanup async and ensure custom/injected roots containing matching files directly under the root still work.
  </action>
  <verify>
    <automated>pnpm test -- packages/host-node/src/bounded-log-discovery.test.ts packages/host-node/src/discovery.test.ts packages/host-node/src/find-latest-ahp-log.test.ts &amp;&amp; pnpm --filter @ahp-inspector/host-node typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- packages/host-node/src/find-latest-ahp-log.test.ts` passes with assertions that more than ten newer invalid matches are probed and rejected before the globally newest valid candidate is returned, and that a later configured root remains eligible after an earlier noisy root truncates.
    - `pnpm test -- packages/host-node/src/bounded-log-discovery.test.ts packages/host-node/src/discovery.test.ts packages/host-node/src/find-latest-ahp-log.test.ts &amp;&amp; pnpm --filter @ahp-inspector/host-node typecheck` passes before advancing.
    - Source inspection confirms `MAX_PROBE_CANDIDATES` and all replacement probe-count caps are absent; probing consumes the globally mtime-sorted, non-empty, bounded gathered set until its first valid AHP-shaped candidate.
  </acceptance_criteria>
  <done>CLI auto-open fairly gathers from every configured root, globally probes the bounded gathered set newest-first, and returns the globally newest valid AHP-shaped log without changing existing rejection semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local configured log roots -> host-node discovery | Potentially very large, unreadable, or concurrently changing local directory trees enter bounded traversal. |
| Host-node absolute paths -> picker `LogCandidate` response | Sensitive local paths must be reduced to opaque IDs, basenames, and launch-relative context before leaving the host adapter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Denial of Service | `scanConfiguredRoots()`, picker retention, CLI probing | mitigate | Stream immediate entries with independent per-root time/stat/immediate-entry/depth/selected-launch bounds and skip filesystem errors. Aggregate scan/probe work grows at most linearly with configured root count times per-root bounds; picker output is input-bounded by `max(200, populatedRootCount)`, and CLI probes only the already-bounded gathered set. |
| T-30-02 | Information Disclosure | `discoverVsCodeLogs()` projection | mitigate | Preserve opaque SHA-256 IDs, basename-only labels, launch-relative context labels, and tests that reject absolute path leakage in returned candidates. |
| T-30-03 | Tampering | Local filesystem entries changing during scan | accept | Discovery is read-only and local; stat/read races and permission failures are skipped, with no writes, network calls, or privilege changes. |
</threat_model>

<verification>
- `pnpm test -- packages/host-node/src/bounded-log-discovery.test.ts packages/host-node/src/discovery.test.ts packages/host-node/src/find-latest-ahp-log.test.ts` passes.
- `pnpm --filter @ahp-inspector/host-node typecheck` passes.
- `pnpm test -- test/boundary.test.ts test/security.test.ts` passes, proving the host adapter boundary and local-only privacy checks remain intact.
- The focused tests explicitly include a bounded noisy first root plus a fresh later root; more than 50 launch directories where setup-time `readdir` selects an actual post-index-49 entry before making it newest; a fresh match after many older matches; more than 200 populated custom roots; and more than ten newer invalid CLI matches before the globally newest valid match.
</verification>

<success_criteria>
- DISC-30-01: Every configured root gets an independent streaming, per-root bounded scan, and launch caps apply only after recency is known for every launch entry examined within those bounds.
- DISC-30-02: Picker output is bounded by `max(200, populatedRootCount)` and retains the highest-ranked candidate from every populated configured root before quota/filling remaining capacity.
- DISC-30-03: CLI auto-open globally sorts the complete fairly gathered non-empty set and probes without a count cap until it returns the globally newest valid AHP-shaped log found by the bounded scan.
- DISC-30-04: Existing filename/shape checks, confidence ordering, opaque IDs, privacy posture, and host adapter boundary remain verified.
</success_criteria>

<source_coverage>
| Source | ID | Acceptance item | Task | Status |
|--------|----|-----------------|------|--------|
| GOAL | - | Discover the latest logs fairly across configured roots while staying bounded | 1, 2, 3 | COVERED |
| REQ | DISC-30-01 | Independent per-root bounds and post-recency launch caps | 1, 2, 3 | COVERED |
| REQ | DISC-30-02 | Input-bounded `max(200, populatedRootCount)` picker with one-per-populated-root retention before quota/fill | 2 | COVERED |
| REQ | DISC-30-03 | Fair bounded gathering plus uncapped global-order probing for globally newest valid selection | 3 | COVERED |
| REQ | DISC-30-04 | Preserve checks, ordering, opaque IDs, privacy, and boundaries | 2, 3 | COVERED |
| RESEARCH | - | No Phase 30 RESEARCH.md; user explicitly skipped research | - | EXCLUDED |
| CONTEXT | - | No Phase 30 CONTEXT.md; verified regression evidence is captured in acceptance_context | 1, 2, 3 | COVERED |
</source_coverage>

<output>
After completion, create `.planning/phases/30-please-make-sure-that-we-will-discover-the-latest-logs-maybe/30-01-SUMMARY.md`.
</output>