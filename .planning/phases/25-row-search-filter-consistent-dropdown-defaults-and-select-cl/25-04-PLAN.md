---
phase: 25-row-search-filter-consistent-dropdown-defaults-and-select-cl
plan: 04
type: execute
wave: 3
depends_on: [25-02, 25-03]
files_modified:
  - e2e/phase25.spec.ts
  - screenshots/phase25/01-row-filter-and-facets.png
  - screenshots/phase25/02-row-filter-and-facets-narrow.png
  - USER_GUIDE.md
  - README.md
autonomous: true
requirements: [FILTER-25-01, FILTER-25-02, FILTER-25-03, FILTER-25-04]
must_haves:
  truths:
    - "In a real browser over synthetic fixture data, Search still highlights without hiding context while Filter rows narrows visible events."
    - "In the same browser workflow, categorical defaults and Select all/Uncheck all are discoverable and update visible rows truthfully."
    - "Fixture-backed desktop and narrow evidence show that Search, Filter rows and menu commands remain legible without incoherent overlap."
    - "Documentation explains the difference between raw event Search and displayed-row filtering."
  artifacts:
    - path: "e2e/phase25.spec.ts"
      provides: "Fixture-backed integration scenario for independent search/filter and checkbox menus"
    - path: "screenshots/phase25/01-row-filter-and-facets.png"
      provides: "Reviewable safe-fixture desktop evidence of the Phase 25 control surface"
    - path: "screenshots/phase25/02-row-filter-and-facets-narrow.png"
      provides: "Reviewable safe-fixture narrow-layout evidence of the Phase 25 control surface"
    - path: "USER_GUIDE.md"
      provides: "User workflow guidance for row text and bulk facet controls"
    - path: "README.md"
      provides: "Concise public capability statement"
  key_links:
    - from: "e2e/phase25.spec.ts"
      to: "packages/ui/src/test-fixtures/phase5-log"
      via: "temporary fixture-only JSONL served by the CLI"
      pattern: "PHASE5_BASE_JSONL|phase25"
    - from: "e2e/phase25.spec.ts"
      to: "screenshots/phase25/01-row-filter-and-facets.png"
      via: "Playwright screenshot after proven control interaction"
      pattern: "screenshots/phase25"
    - from: "e2e/phase25.spec.ts"
      to: "screenshots/phase25/02-row-filter-and-facets-narrow.png"
      via: "Playwright narrow-viewport screenshot after proven control interaction"
      pattern: "row-filter-and-facets-narrow"
---

<objective>
Close the phase with browser-visible proof and documentation of the two distinct text workflows and categorical visibility commands.

Purpose: This is an interaction change in a dense inspection surface; fixture-backed evidence protects both truthfulness and usability without exposing real log content.
Output: Playwright regression/evidence at desktop and narrow widths, updated user documentation and a visual verification checkpoint.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-RESEARCH.md
@.planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-VALIDATION.md
@.planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-UI-SPEC.md
@.planning/phases/12-search-rather-than-filter/12-03-SUMMARY.md
@e2e/phase12.spec.ts
@USER_GUIDE.md
@README.md

<interfaces>
Plan 25-03 introduces labels `Search`, `Filter rows`, `Select all`, and `Uncheck all`. Plan 25-01 guarantees that Search does not itself narrow visible rows and that Filter rows uses local EventRow projection text. Screenshots must use fixture JSONL data only per repository instructions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove independent text workflows and facet commands in a fixture-backed browser</name>
  <files>e2e/phase25.spec.ts, screenshots/phase25/01-row-filter-and-facets.png, screenshots/phase25/02-row-filter-and-facets-narrow.png</files>
  <read_first>
    - e2e/phase12.spec.ts
    - packages/ui/src/components/filters/FilterBar.tsx
    - packages/ui/src/components/filters/FacetPopover.tsx
    - packages/ui/src/test-fixtures/phase5-log.ts
    - .github/copilot-instructions.md
    - .planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-UI-SPEC.md
  </read_first>
  <behavior>
    - Entering text in Search highlights/navigates a matching row while a known nonmatching context row stays visible.
    - Entering text in Filter rows hides a known nonmatching row and leaves a matching row visible.
    - Opening a categorical facet shows discovered options checked by default; Method shows `ping` unchecked under application defaults.
    - Clicking Uncheck all narrows/hides known option-backed rows and clicking Select all restores them.
    - Captured desktop and narrow screenshots contain only the synthetic fixture-backed UI and show no control overlap or unreadable menu-command truncation.
  </behavior>
  <action>
    Create `e2e/phase25.spec.ts` following `e2e/phase12.spec.ts`: create a temporary JSONL file exclusively from the repository-owned synthetic `PHASE5_BASE_JSONL`, launch the local CLI with `--port 0 --no-open`, and drive the UI in Playwright. Assert the existing Search query continues to show a nonmatching baseline row while marking/selecting a matching row; then use the new `Filter rows` input to hide the nonmatching row. Open Direction or Kind to assert checked defaults and use `Uncheck all`/`Select all` to prove row visibility changes; open Method to assert the application-default `ping` option is unchecked when present in fixture data (add only synthetic fixture rows in-test if needed). Save a desktop screenshot at `screenshots/phase25/01-row-filter-and-facets.png`, then set a narrow viewport and save `screenshots/phase25/02-row-filter-and-facets-narrow.png` with Search, Filter rows and the bulk commands visible and readable. Never open or screenshot a real user/session JSONL log.
  </action>
  <verify>
    <automated>pnpm --filter @ahp-inspector/ui build &amp;&amp; pnpm exec playwright test e2e/phase25.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/phase25.spec.ts` imports repository-owned synthetic fixture data and contains no path under a home directory or real log root.
    - `e2e/phase25.spec.ts` locates both `Search` and `Filter rows`, asserts row visibility before/after row filtering, and interacts with `Uncheck all` and `Select all`.
    - `screenshots/phase25/01-row-filter-and-facets.png` is produced only after fixture-backed navigation.
    - `screenshots/phase25/02-row-filter-and-facets-narrow.png` is produced from the same fixture-backed flow at a narrow viewport and shows Search, Filter rows and the bulk commands without incoherent overlap.
    - The UI build and targeted Playwright command exit 0.
  </acceptance_criteria>
  <done>A safe browser-level regression demonstrates that Search stays contextual, Filter rows narrows, bulk categorical visibility actions work, and the compact control surface remains readable at desktop and narrow widths.</done>
</task>

<task type="auto">
  <name>Task 2: Document the row filter and visibility-menu behavior</name>
  <files>USER_GUIDE.md, README.md</files>
  <read_first>
    - USER_GUIDE.md
    - README.md
    - .planning/phases/12-search-rather-than-filter/12-03-SUMMARY.md
    - .planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-RESEARCH.md
  </read_first>
  <action>
    Update user-facing documentation where search/filter behavior is already described. State concretely that `Search` scans event content for highlighting and navigation without removing context rows; `Filter rows` performs a case-insensitive filter over the visible timeline row projection and hides nonmatches; categorical menu checkboxes mean checked/visible, with `ping` hidden by default in Method; and `Select all`/`Uncheck all` are available in categorical menus. Keep local-only privacy language and do not claim raw-payload row filtering, because the row filter intentionally operates on projected row text.
  </action>
  <verify>
    <automated>pnpm test &amp;&amp; pnpm --filter @ahp-inspector/ui typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `USER_GUIDE.md` contains `Filter rows`, `Search`, `Select all`, `Uncheck all`, and the documented default-hidden `ping` behavior.
    - `README.md` distinguishes Search highlight/navigation from Filter rows narrowing without promising raw-payload filtering.
    - `pnpm test` and UI typecheck exit 0.
  </acceptance_criteria>
  <done>User guidance matches the tested interface and does not blur raw Search with local row filtering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture source -> saved visual evidence | Screenshots can expose log content unless the data source is controlled. |
| documentation -> user expectations | Incorrect wording could cause users to assume projected filtering searches raw payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | I | `e2e/phase25.spec.ts` screenshots | mitigate | Generate test log data only from checked-in synthetic fixtures and store evidence only under `screenshots/phase25/`. |
| T-25-07 | R | `USER_GUIDE.md` / `README.md` | mitigate | Document the exact split: Search is raw highlight/navigation; Filter rows narrows projected timeline strings. |
</threat_model>

<verification>
Run `pnpm --filter @ahp-inspector/ui build && pnpm exec playwright test e2e/phase25.spec.ts`, `pnpm test`, and `pnpm --filter @ahp-inspector/ui typecheck`. Plan 25-05 performs the separate desktop-and-narrow screenshot review checkpoint after these artifacts exist.
</verification>

<success_criteria>
- Fixture-backed Playwright proves the new workflow and produces safe desktop and narrow visual evidence.
- Docs state the exact filtering/search and checkbox semantics.
- The generated safe-fixture evidence is ready for the dedicated visual review gate in Plan 25-05.
</success_criteria>

<output>
After completion, create `.planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-04-SUMMARY.md`.
</output>
