---
phase: 25-row-search-filter-consistent-dropdown-defaults-and-select-cl
plan: 03
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - packages/ui/src/components/filters/RowFilterInput.tsx
  - packages/ui/src/components/filters/FacetPopover.tsx
  - packages/ui/src/components/filters/FilterBar.tsx
  - packages/ui/src/components/filters/ActiveFilterChips.tsx
  - packages/ui/src/components/filters/FilterBar.test.tsx
autonomous: true
requirements: [FILTER-25-01, FILTER-25-02, FILTER-25-03]
must_haves:
  truths:
    - "Users can enter or clear a `Filter rows` string separately from full-payload Search and immediately see the active filter represented."
    - "Every categorical menu checkbox reads as visible/hidden: all ordinary options start checked, while the application-default hidden `ping` method starts unchecked."
    - "Every categorical menu offers explicit `Select all` and `Uncheck all` actions, and Turn remains usable when all Channels are visible."
  artifacts:
    - path: "packages/ui/src/components/filters/RowFilterInput.tsx"
      provides: "Compact row-string filter input control"
    - path: "packages/ui/src/components/filters/FacetPopover.tsx"
      provides: "Discoverable bulk visibility actions in categorical menus"
    - path: "packages/ui/src/components/filters/FilterBar.tsx"
      provides: "Uniform hidden-to-checked adapters and mounted row filter"
    - path: "packages/ui/src/components/filters/ActiveFilterChips.tsx"
      provides: "Dismissible hidden-option and row-text filter feedback"
  key_links:
    - from: "packages/ui/src/components/filters/RowFilterInput.tsx"
      to: "packages/ui/src/state/filters.ts"
      via: "FilterBar writes `filters.rowText` through patchFilter"
      pattern: "rowText|patchFilter"
    - from: "packages/ui/src/components/filters/FacetPopover.tsx"
      to: "packages/ui/src/components/filters/FilterBar.tsx"
      via: "selected visible values converted into hidden categorical arrays"
      pattern: "Select all|Uncheck all|hidden"
---

<objective>
Expose the new row filter and uniform checkbox visibility model in the filter bar.

Purpose: Make filtering predictable at a glance: checked means shown, bulk actions are explicit, and raw Search remains a separate find/navigation tool.
Output: Compact text-filter UI, categorical bulk controls, active feedback chips and component coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-RESEARCH.md
@.planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-PATTERNS.md
@.planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-VALIDATION.md
@.planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-UI-SPEC.md
@packages/ui/src/components/filters/FilterBar.tsx
@packages/ui/src/components/filters/FacetPopover.tsx
@packages/ui/src/components/filters/ActiveFilterChips.tsx

<interfaces>
Plan 25-01 establishes this rule: `FilterState.direction`, `kind`, `method`, `actionType`, `session`, `turn`, and `status` arrays contain hidden option values; `filters.rowText` is the new local string predicate; `APP_DEFAULT_FILTERS.method` equals `["ping"]`.

The current worktree already contains pending Phase 24 edits in `FilterBar.tsx` and `ActiveFilterChips.tsx`: the visible label is `Channel` and Search status may read `x of y matches`. Preserve those current lines while making Phase 25 changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Turn categorical facet menus into consistent visibility controls</name>
  <files>packages/ui/src/components/filters/FacetPopover.tsx, packages/ui/src/components/filters/FilterBar.tsx, packages/ui/src/components/filters/FilterBar.test.tsx</files>
  <read_first>
    - packages/ui/src/components/filters/FacetPopover.tsx
    - packages/ui/src/components/filters/FilterBar.tsx
    - packages/ui/src/components/filters/FilterBar.test.tsx
    - packages/ui/src/state/filters.ts
    - .planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-PATTERNS.md
    - .planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-UI-SPEC.md
  </read_first>
  <behavior>
    - Direction, Kind, Method, Action, Channel, Turn and Status options are checked when their values are not hidden.
    - Initial application defaults render every discovered categorical option checked except Method option `ping`, which is unchecked.
    - `Select all` removes all currently known hidden values for that facet; `Uncheck all` hides every currently known value.
    - Newly discovered values not in the hidden array appear checked; bulk actions operate on the complete options list even after the popover option-search field narrows visible labels.
    - Turn can be opened with default Channel visibility because an empty hidden Channel array means all channels are shown.
  </behavior>
  <action>
    Generalize the existing Method hidden/selected conversion in `FilterBar.tsx` into typed conversion helpers used by all seven categorical `FacetPopover` instances: selected/checked values are `options` values absent from that facet's hidden array; an update converts selected values back into hidden values while carrying hidden values not present in current options. Leave `TimeRangePopover` unchanged because it is not a checkbox list. Remove the existing Turn disablement based on `filters.session.length === 0`; under exclusion semantics, that state means all channels are visible. In `FacetPopover.tsx`, replace the ambiguous `Clear selection` command with two visible text buttons labelled exactly `Select all` and `Uncheck all`, forwarding the full option value set or an empty selected list respectively; keep Close and the existing maximum-visible/search behavior. Preserve current Phase 24 `Channel` naming and focused Search count changes.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/components/filters/FilterBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/components/filters/FacetPopover.tsx` contains visible controls `Select all` and `Uncheck all` and no visible string `Clear selection`.
    - `packages/ui/src/components/filters/FilterBar.tsx` uses checked-visible conversion for each of Direction, Kind, Method, Action, Channel, Turn and Status and retains `label="Channel"`.
    - `packages/ui/src/components/filters/FilterBar.tsx` no longer disables the Turn chip solely with `filters.session.length === 0`.
    - `packages/ui/src/components/filters/FilterBar.test.tsx` asserts all ordinary options checked by default, `ping` unchecked under app defaults, and both bulk actions changing hidden state.
    - The targeted component test command exits 0.
  </acceptance_criteria>
  <done>All categorical dropdowns consistently present visibility checkboxes and offer functional all/none actions without losing Phase 24 UI changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount a compact Filter rows input and active-filter feedback</name>
  <files>packages/ui/src/components/filters/RowFilterInput.tsx, packages/ui/src/components/filters/FilterBar.tsx, packages/ui/src/components/filters/ActiveFilterChips.tsx, packages/ui/src/components/filters/FilterBar.test.tsx</files>
  <read_first>
    - packages/ui/src/components/filters/SearchInput.tsx
    - packages/ui/src/components/filters/FilterBar.tsx
    - packages/ui/src/components/filters/ActiveFilterChips.tsx
    - packages/ui/src/components/filters/FilterBar.test.tsx
    - packages/ui/src/state/filters.ts
    - .planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-UI-SPEC.md
  </read_first>
  <behavior>
    - Filter bar shows a compact `Filter rows` text control distinct from existing `Search`.
    - Typing in Filter rows updates `filters.rowText`; clearing its icon sets only `filters.rowText` to `""`.
    - A non-empty row text appears as a dismissible active filter chip, and `Clear all filters` clears row text while preserving `searchQuery`.
    - Hidden categorical active chips describe hidden state, including Channel terminology and hidden `ping`.
  </behavior>
  <action>
    Create `RowFilterInput.tsx` as a token-styled compact input using a `Filter` and clear `X` icon from `lucide-react`, with accessible label `Filter rows`, placeholder `Filter visible rows...`, stable bounded sizing that fits beside the existing flexible Search input, and no raw colors. Mount it in `FilterBar.tsx` by reading `filters.rowText` and writing `patchFilter("rowText", value)`/`patchFilter("rowText", "")`; it must not call `setSearchQuery` or dispatch `ahp-search-nav`. Update `ActiveFilterChips.tsx` so a non-empty row text renders `Rows contain: {value}` with dismiss action that clears `rowText`, and each hidden categorical item renders a `Hidden {label}: {value}` chip. Retain `Channel` instead of `Session`, keep Method's default-hidden `ping` visible in feedback, and keep Search out of active filter chips per Phase 12.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/components/filters/FilterBar.test.tsx packages/ui/src/components/shell/AppShell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/components/filters/RowFilterInput.tsx` exports `RowFilterInput` and contains accessible text `Filter rows` and placeholder `Filter visible rows...`.
    - `packages/ui/src/components/filters/FilterBar.tsx` mounts `RowFilterInput` and writes `patchFilter("rowText"` without changing `setSearchQuery` wiring for Search.
    - `packages/ui/src/components/filters/ActiveFilterChips.tsx` contains `Rows contain:` and `Hidden Channel:` behavior while retaining no chip for `searchQuery`.
    - `packages/ui/src/components/filters/FilterBar.test.tsx` asserts typing/clearing Filter rows and preserving Search on Clear all filters.
    - The targeted component test command exits 0.
  </acceptance_criteria>
  <done>Users get an unmistakable independent row filter with accurate active-state feedback, while Search retains its existing find/navigation UX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user interaction -> hidden filter arrays and row-text state | Menu and text input actions convert user intent into visibility rules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06 | T | `FilterBar` facet adapters | mitigate | Use one checked-visible conversion path for all categorical menus and assert both bulk commands in component tests. |
| T-25-02 | I | `RowFilterInput` / chips | mitigate | Render input text as React text/value only and route it to the literal predicate; do not use HTML injection or raw Search transport. |
</threat_model>

<verification>
Run `pnpm exec vitest run packages/ui/src/components/filters/FilterBar.test.tsx packages/ui/src/components/shell/AppShell.test.tsx` followed by `pnpm --filter @ahp-inspector/ui typecheck`.
</verification>

<success_criteria>
- Search and Filter rows are visibly distinct and independently controlled.
- Every categorical popover starts checked-visible under defaults except `ping`, and exposes Select all/Uncheck all.
- Active-filter feedback accurately represents hidden values and row text.
</success_criteria>

<output>
After completion, create `.planning/phases/25-row-search-filter-consistent-dropdown-defaults-and-select-cl/25-03-SUMMARY.md`.
</output>
