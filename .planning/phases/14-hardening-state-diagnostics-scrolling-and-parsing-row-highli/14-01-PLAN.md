---
phase: 14-hardening-state-diagnostics-scrolling-and-parsing-row-highli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/styles/global.css
  - packages/ui/src/components/detail/StateDiagnosticsPanel.tsx
autonomous: true
requirements: [HARDEN-01]
must_haves:
  truths:
    - "When the replay-diagnostics list grows beyond ~240px, the list scrolls inside its panel without pushing other detail content off-screen."
    - "Diagnostic messages display structured fields (severity, code, event index) on their own line with consistent styling, and message text wraps without horizontal overflow."
  artifacts:
    - path: packages/ui/src/components/detail/StateDiagnosticsPanel.tsx
      provides: "Updated diagnostic group rendering with parsed code/severity/event-idx header row and wrapping message body."
    - path: packages/ui/src/styles/global.css
      provides: ".state-diagnostic-group ul scrolls when tall; word-wrap on message text."
  key_links:
    - from: ".state-diagnostic-group ul"
      to: "max-height + overflow-y in global.css"
      via: "CSS rule"
      pattern: "max-height"
---

<objective>
Make replay diagnostics inside the state inspector usable when there are many entries, and split each diagnostic into a clearly parsed meta header + wrapped message body.

Purpose: Long diagnostics lists currently bleed past the visible drawer area with no internal scroll, and message text runs as one wide line that overflows the panel.
Output: Scrollable diagnostic groups with parsed meta and wrapping body text.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/14-hardening-state-diagnostics-scrolling-and-parsing-row-highli/14-CONTEXT.md
@packages/ui/src/components/detail/StateDiagnosticsPanel.tsx
@packages/ui/src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrollable diagnostic groups</name>
  <files>packages/ui/src/styles/global.css</files>
  <action>
In `.state-diagnostic-group ul`: add `max-height: 240px;`, `overflow-y: auto;`, and `padding-right: var(--space-1);` (room for scrollbar). Add `overflow-wrap: anywhere;` and `word-break: break-word;` on `.state-diagnostic-group li` so long codes/messages wrap. Keep existing `gap` and `border-left` rules. Add a `.state-diagnostic-message { white-space: pre-wrap; overflow-wrap: anywhere; color: var(--color-text); }` rule for the body span used in Task 2 (per D-05).
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui lint &amp;&amp; pnpm -F @ahp-inspector/ui typecheck</automated>
  </verify>
  <done>CSS rules added; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Parsed diagnostic row layout</name>
  <files>packages/ui/src/components/detail/StateDiagnosticsPanel.tsx</files>
  <action>
In `DiagnosticGroup`, change each `<li>` to render two stacked elements:
1. The existing `.state-diagnostic-meta` span with `severity`, `code`, optional `event #N`.
2. A new `<span className="state-diagnostic-message">{diagnostic.message}</span>` (replaces the bare sibling `<span>{diagnostic.message}</span>`).
Keep the `key` formula. Do not change props, types, or callers.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui test -- StateInspectorPanel.test.tsx</automated>
  </verify>
  <done>Diagnostic messages render in a wrapping `.state-diagnostic-message` span; existing tests still pass.</done>
</task>

</tasks>

<success_criteria>
- Replay diagnostics list with >12 entries scrolls within the panel; surrounding inspector layout unaffected.
- Each diagnostic shows meta header + wrapping message; no horizontal overflow on long messages.
- Existing UI tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/14-hardening-state-diagnostics-scrolling-and-parsing-row-highli/14-01-SUMMARY.md`.
</output>
