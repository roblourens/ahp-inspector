---
phase: 14-hardening-state-diagnostics-scrolling-and-parsing-row-highli
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/timeline/EventRow.tsx
  - packages/ui/src/components/timeline/EventRow.columns.test.tsx
autonomous: true
requirements: [HARDEN-02]
must_haves:
  truths:
    - "When a row is selected and is also a search match and is also pair-highlighted, only the selected background is shown."
    - "Search-match rows that are NOT selected show a subtle bg tint without a competing outline; pair-highlight rows show their info bg."
    - "data-* attributes (`data-selected`, `data-search-match`, `data-pair-highlight`) remain stable for tests."
  artifacts:
    - path: packages/ui/src/components/timeline/EventRow.tsx
      provides: "Refactored row state precedence using a single `rowState` derivation; outline removed in favor of left rail accent."
  key_links:
    - from: "EventRow.tsx"
      to: "row state precedence (selected > pair > search)"
      via: "single derived bg variable"
      pattern: "rowState|background:"
---

<objective>
Clean up the visual layering of selected / pair-highlight / search-match in `EventRow` so only one background renders at a time and the search-match indicator no longer fights the selection outline.

Purpose: Today these three states overlap with mixed bg + outline rules, causing muddled appearance when a row is selected AND a search match.
Output: Deterministic precedence (selected > pair > search), no outline conflict.
</objective>

<context>
@.planning/phases/14-hardening-state-diagnostics-scrolling-and-parsing-row-highli/14-CONTEXT.md
@packages/ui/src/components/timeline/EventRow.tsx
@packages/ui/src/components/timeline/EventRow.columns.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Precedence-based row background</name>
  <files>packages/ui/src/components/timeline/EventRow.tsx</files>
  <action>
In the row's inline style:
1. Replace the nested ternary that selects `background` with a single derivation:
   ```ts
   const rowBg = isSelected
     ? "var(--row-selected-bg)"
     : pairHighlight
       ? "color-mix(in srgb, var(--color-info) 14%, transparent)"
       : isSearchMatch
         ? "color-mix(in srgb, var(--color-search-match-bg) 28%, transparent)"
         : "transparent";
   ```
   Keep using it as `background: rowBg`.
2. Remove the `outline` and `outlineOffset` lines that depend on `isSearchMatch`. Search-match indication moves to the rail: in the rail cell style, when `isSearchMatch &amp;&amp; !isSelected`, override the rail background to `var(--color-search-match-bg)` (full opacity) instead of `railColor(row, isSelected)`. When selected, rail uses `railColor(row, isSelected)` as today.
3. Keep all `data-*` attributes (`data-selected`, `data-search-match`, `data-pair-highlight`) unchanged so existing tests keep matching.
4. Leave `highlightMatches` (the `<mark>`-style inline match highlighting in the Summary cell) untouched.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui test -- EventRow.columns.test.tsx EventRow.orphan.test.tsx</automated>
  </verify>
  <done>Tests pass; outline rule removed; rail color reflects search-match when not selected.</done>
</task>

<task type="auto">
  <name>Task 2: Add precedence test</name>
  <files>packages/ui/src/components/timeline/EventRow.columns.test.tsx</files>
  <action>
Add one test: render an `EventRow` with `isSelected`, `isSearchMatch`, and `pairHighlight="request"` all set. Assert the row element has `data-selected="true"` AND `data-search-match="true"` AND `data-pair-highlight="request"`, AND its inline `background` style equals the value of `var(--row-selected-bg)` (read via `getComputedStyle(row).background` containing `var(--row-selected-bg)` or via the inline `style.background` string starting with `var(--row-selected-bg)`). Use whichever assertion matches the existing test patterns in this file.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui test -- EventRow.columns.test.tsx</automated>
  </verify>
  <done>New precedence test passes.</done>
</task>

</tasks>

<success_criteria>
- A row that is selected + pair-highlighted + search-match shows only the selected bg.
- No `outline` style is set on rows by `EventRow.tsx` for search match.
- All existing `EventRow.*.test.tsx` tests still pass.
</success_criteria>

<output>
After completion, create `14-02-SUMMARY.md` in the phase dir.
</output>
