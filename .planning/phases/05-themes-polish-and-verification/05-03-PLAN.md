---
phase: 05-themes-polish-and-verification
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
autonomous: true
requirements: [THEME-01, THEME-04, THEME-05, VERIFY-02]
files_modified:
  - packages/ui/src/theme/theme.ts
  - packages/ui/src/theme/theme.test.ts
  - packages/ui/src/main.tsx
  - packages/ui/src/components/shell/HeaderBar.tsx
  - packages/ui/src/components/shell/HeaderBar.test.tsx
  - packages/ui/src/state/persistence.test.ts
  - packages/ui/src/persistence/persist-effect.test.ts

must_haves:
  truths:
    - "Theme picker switches Dark, Light, and Hacker and persists the selected value globally."
    - "Invalid stored theme values fall back to dark."
    - "Storage read/write exceptions do not break the page."
    - "Theme remains global under localStorage['ahp-theme']; it is not moved into per-log preferences."
    - "Per-log preferences remain scoped by opaque logKey and do not persist log contents, paths, prompts, or raw payloads."
---

<objective>
Harden theme choice persistence and picker behavior.

Existing theme persistence works for the happy path. This plan adds robust coverage for all themes, invalid values, storage exceptions, keyboard interaction, and separation from per-log preferences.
</objective>

<context>
@.planning/phases/05-themes-polish-and-verification/05-UI-SPEC.md
@packages/ui/src/main.tsx
@packages/ui/src/components/shell/HeaderBar.tsx
@packages/ui/src/components/shell/HeaderBar.test.tsx
@packages/ui/src/state/persistence.ts
@packages/ui/src/persistence/persist-effect.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Centralize theme helpers</name>
  <files>packages/ui/src/theme/theme.ts, packages/ui/src/theme/theme.test.ts, packages/ui/src/main.tsx, packages/ui/src/components/shell/HeaderBar.tsx</files>
  <action>
Create `packages/ui/src/theme/theme.ts` exporting:
- `THEMES` with dark/light/hacker labels
- `ThemeId`
- `THEME_STORAGE_KEY = "ahp-theme"`
- `isThemeId(value)`
- `readStoredTheme(storage = localStorage)` with dark fallback for invalid/missing/throwing storage
- `applyTheme(theme, root = document.documentElement)`
- `persistTheme(theme, storage = localStorage)` that ignores storage write errors

Update `main.tsx` and `HeaderBar.tsx` to use these helpers and remove duplicate theme definitions.
  </action>
  <verify>
    <automated>pnpm -F @ahp-viewer/ui test src/theme/theme.test.ts src/components/shell/HeaderBar.test.tsx</automated>
  </verify>
  <done>
    - Shared theme helper exists and is used by startup and HeaderBar.
    - Missing, invalid, and throwing storage cases fall back to dark.
    - Applying a theme updates `document.documentElement[data-theme]`.
    - Tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Expand theme picker interaction coverage</name>
  <files>packages/ui/src/components/shell/HeaderBar.tsx, packages/ui/src/components/shell/HeaderBar.test.tsx</files>
  <action>
Update theme picker behavior/tests to satisfy UI-SPEC:
- Button accessible name: `Theme picker (Dark|Light|Hacker)`.
- Button title: `Theme: Dark|Light|Hacker`.
- Menu label: `Theme`.
- Options use `role="menuitemradio"` and current option has `aria-checked="true"`.
- Selecting each theme updates `data-theme`, writes `ahp-theme`, closes the menu, and returns focus to the picker button.
- Escape closes the menu and returns focus.
- Up/Down arrows move focus.
- Home/End jump to first/last option.
- Clicking the button again toggles the menu closed.
- Clicking outside closes the menu.
  </action>
  <verify>
    <automated>pnpm -F @ahp-viewer/ui test src/components/shell/HeaderBar.test.tsx</automated>
  </verify>
  <done>
    - Picker accessible names/titles/menu roles match UI-SPEC.
    - Dark, Light, and Hacker selection are covered by tests.
    - Escape, arrow navigation, Home/End, outside click, and button re-click close/toggle behavior are tested.
    - Focus returns to the picker button after close/selection.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Protect per-log persistence boundaries</name>
  <files>packages/ui/src/state/persistence.test.ts, packages/ui/src/persistence/persist-effect.test.ts, packages/ui/src/state/persistence.ts, packages/ui/src/persistence/persist-effect.ts</files>
  <action>
Add tests proving:
- Theme remains global under `localStorage["ahp-theme"]`.
- `ahp-log-prefs-v1` does not contain theme choice.
- Per-log preferences remain keyed by opaque `logKey`.
- No manual-open path, absolute path, raw payload, prompt, or log content is persisted.
  </action>
  <verify>
    <automated>pnpm -F @ahp-viewer/ui test src/state/persistence.test.ts src/persistence/persist-effect.test.ts</automated>
  </verify>
  <done>
    - Theme choice is absent from `ahp-log-prefs-v1`.
    - Per-log prefs stay keyed by opaque `logKey`.
    - Tests prove log content, raw payloads, prompts, manual paths, and absolute paths are not persisted.
    - Persistence tests pass.
  </done>
</task>

</tasks>

<verification>
Run:
1. `pnpm -F @ahp-viewer/ui test src/theme/theme.test.ts src/components/shell/HeaderBar.test.tsx`
2. `pnpm -F @ahp-viewer/ui test src/state/persistence.test.ts src/persistence/persist-effect.test.ts`
3. `pnpm -F @ahp-viewer/ui build`
</verification>

<success_criteria>
- Theme choice persists globally.
- All three themes are covered by tests.
- Invalid/throwing storage cases are safe.
- Per-log persistence boundary remains intact.
</success_criteria>

<commit_boundary>
Commit after this plan:
`feat(05-03): harden theme persistence and picker interactions`
</commit_boundary>

<output>
After completion, create `.planning/phases/05-themes-polish-and-verification/05-03-SUMMARY.md`.
</output>
