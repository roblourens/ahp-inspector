---
phase: 05-themes-polish-and-verification
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
autonomous: true
requirements: [VERIFY-02, THEME-01, THEME-02, THEME-03, THEME-04, THEME-05]
files_modified:
  - test/phase5-vertical-slice.test.ts
  - packages/ui/src/components/timeline/TimelineList.virt.test.tsx
  - packages/ui/src/components/detail/DetailPanel.test.tsx
  - packages/ui/src/components/filters/FilterBar.test.tsx
  - packages/ui/src/components/shell/AppShell.test.tsx
  - packages/ui/src/test-fixtures/phase5-log.ts
  - packages/ui/src/test-fixtures/phase5-log.test.ts

must_haves:
  truths:
    - "Representative fixtures cover timeline rendering, row selection, details, filtering/search, theme switching, empty states, and parse-error states."
    - "Integrated tests exercise realistic AHP JSONL data instead of tiny synthetic-only snippets."
    - "Tests cover all exact UI-SPEC copy for screen-level states."
    - "Phase 5 does not regress live-tail, switching logs, search/filtering, or detail loading flows from earlier phases."
---

<objective>
Add integrated UI coverage for VERIFY-02.

This plan turns Phase 5 polish into testable product behavior using representative fixtures and exact state-copy assertions.
</objective>

<context>
@.planning/phases/05-themes-polish-and-verification/05-VALIDATION.md
@.planning/phases/05-themes-polish-and-verification/05-UI-SPEC.md
@test/phase4-vertical-slice.test.ts
@packages/ui/src/components/shell/AppShell.test.tsx
@packages/ui/src/components/timeline/TimelineList.virt.test.tsx
@packages/ui/src/components/detail/DetailPanel.test.tsx
@packages/ui/src/components/filters/FilterBar.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Create representative Phase 5 fixture data</name>
  <files>packages/ui/src/test-fixtures/phase5-log.ts, packages/ui/src/test-fixtures/phase5-log.test.ts, test/phase5-vertical-slice.test.ts</files>
  <action>
Create or extend fixture helpers with a long realistic AHP log containing:
- requests, responses, notifications, actions, tool calls, auth requests, parse errors, and latency variations
- long action type strings
- multiple sessions and session IDs
- search-relevant messages and payload fields
- appended events for a mandatory follow-latest/new-events scenario

Do not include real user secrets, absolute machine paths, or real prompts.

Create `packages/ui/src/test-fixtures/phase5-log.test.ts` to parse every valid fixture line and assert expected message categories are present.
  </action>
  <verify>
    <automated>pnpm -F @ahp-viewer/ui test src/test-fixtures/phase5-log.test.ts</automated>
  </verify>
  <done>
    - Fixture helper exports a realistic base JSONL fixture and a separate appended event.
    - Fixture unit test parses every valid line and confirms expected message categories are present.
    - Fixture contains no real secrets or absolute local paths.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Cover timeline/search/filter/detail states</name>
  <files>packages/ui/src/components/timeline/TimelineList.virt.test.tsx, packages/ui/src/components/detail/DetailPanel.test.tsx, packages/ui/src/components/filters/FilterBar.test.tsx, packages/ui/src/components/shell/AppShell.test.tsx, packages/ui/src/test-fixtures/phase5-log.ts</files>
  <action>
Add/extend tests that verify:
- dense timeline rows do not wrap important columns
- column labels remain aligned with row cells
- search query filters/highlights expected realistic rows
- filter chips and dropdowns layer above the list and close when toggled
- selected row opens detail, and detail can switch Pretty/Raw
- detail loading/error/empty copy matches UI-SPEC
- no-results copy matches UI-SPEC
- parse-error-only copy matches UI-SPEC
  </action>
  <verify>
    <automated>pnpm -F @ahp-viewer/ui test src/components/timeline/TimelineList.virt.test.tsx src/components/detail/DetailPanel.test.tsx src/components/filters/FilterBar.test.tsx src/components/shell/AppShell.test.tsx</automated>
  </verify>
  <done>
    - Tests cover timeline rendering, column labels, no wrapping, search/filter, detail states, empty/no-results, and parse-error-only copy.
    - Pretty/Raw detail behavior remains covered.
    - Dropdown layering/toggle behavior is protected.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Add Phase 5 vertical slice test</name>
  <files>test/phase5-vertical-slice.test.ts, packages/ui/src/test-fixtures/phase5-log.ts</files>
  <action>
Create `test/phase5-vertical-slice.test.ts` that exercises the local server/UI path with representative data and proves:
- opening fixture log yields rows
- filtering/search changes visible results
- selecting a row retrieves detail
- switching themes updates `data-theme`
- per-log viewer preferences and global theme persistence remain separate
- known Phase 4 flows still work with the polished UI
- copied-fixture append scenario updates timeline/new-events/following behavior
  </action>
  <verify>
    <automated>pnpm vitest run test/phase5-vertical-slice.test.ts</automated>
  </verify>
  <done>
    - Opening, search/filtering, detail selection, theme switching, persistence separation, and append/follow behavior are covered.
    - Test uses a copied fixture path controlled by the test, not a real user log.
    - Vertical slice test passes.
  </done>
</task>

</tasks>

<verification>
Run:
1. `pnpm -F @ahp-viewer/ui test`
2. `pnpm vitest run test/phase5-vertical-slice.test.ts`
3. `pnpm test`
</verification>

<success_criteria>
- VERIFY-02 has automated integrated coverage.
- Exact UI-SPEC state copy is tested.
- Representative fixtures exercise real product flows.
- Earlier phase functionality remains green.
</success_criteria>

<commit_boundary>
Commit after this plan:
`test(05-04): add integrated phase 5 flow coverage`
</commit_boundary>

<output>
After completion, create `.planning/phases/05-themes-polish-and-verification/05-04-SUMMARY.md`.
</output>
