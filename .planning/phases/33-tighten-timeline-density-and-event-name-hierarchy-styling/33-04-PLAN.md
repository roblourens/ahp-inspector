---
phase: 33-tighten-timeline-density-and-event-name-hierarchy-styling
plan: 04
type: execute
wave: 4
depends_on: [33-02, 33-03]
files_modified:
  - e2e/phase33.spec.ts
  - screenshots/phase33/01-dark-density-desktop.png
  - screenshots/phase33/02-light-density-desktop.png
  - screenshots/phase33/03-hacker-density-desktop.png
autonomous: true
requirements: [PRIVACY-33-04, DENSITY-33-01, EVENTNAME-33-02, THEME-33-03]
must_haves:
  truths:
    - "A fixture-only browser workflow proves 24px timeline density and event-name prefix/leaf styling in dark, light, and hacker themes."
    - "The Phase 33 browser fixture uses only synthetic/temp JSONL data and no real local AHP logs."
    - "Before every saved screenshot, the E2E asserts no `/Users/`, `/home/`, or Windows absolute path text is present."
    - "Saved screenshots under `screenshots/phase33/` are reviewed for clipping, overlap, readability, prefix subtlety, and sensitive-content absence."
    - "No new network, dependency, server, host-adapter, persistence, protocol, Escape, or ARIA behavior is introduced by visual verification."
  artifacts:
    - path: "e2e/phase33.spec.ts"
      provides: "Synthetic all-theme browser proof for Phase 33 density and event-name hierarchy"
    - path: "screenshots/phase33/01-dark-density-desktop.png"
      provides: "Dark theme fixture-only visual evidence"
    - path: "screenshots/phase33/02-light-density-desktop.png"
      provides: "Light theme fixture-only visual evidence"
    - path: "screenshots/phase33/03-hacker-density-desktop.png"
      provides: "Hacker theme fixture-only visual evidence"
  key_links:
    - from: "e2e/phase33.spec.ts"
      to: "screenshots/phase33"
      via: "screenshots captured only after synthetic fixture, theme, geometry, and path-leak assertions"
      pattern: "SCREENSHOT_DIR|assertNoPathLeak|phase33"
    - from: "packages/ui/src/components/timeline/cells/EventNameLabel.tsx"
      to: "e2e/phase33.spec.ts"
      via: "browser fixture includes hierarchical labels exercised through normal row rendering"
      pattern: "foo/bar|foo/bar/baz"
    - from: "packages/ui/src/components/timeline/TimelineList.tsx"
      to: "e2e/phase33.spec.ts"
      via: "browser proof follows fast unit/style checks for 24px density"
      pattern: "24px|TimelineList.virt.test.tsx"
---

<objective>
Prove the Phase 33 visual contract in a real browser using synthetic fixtures only.

Purpose: The density and subtle prefix color are visual polish changes; component tests prove mechanics, but screenshots provide the reviewable all-theme evidence needed to decide whether it "still looks good."
Output: A Phase 33 Playwright spec plus fixture-only screenshots for dark, light, and hacker themes.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-RESEARCH.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-VALIDATION.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-PATTERNS.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-UI-SPEC.md
@e2e/phase31.spec.ts
@e2e/phase5.spec.ts
@packages/ui/src/test-fixtures/phase5-log.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixture-only browser proof for Phase 33 density and hierarchy</name>
  <files>e2e/phase33.spec.ts, screenshots/phase33/01-dark-density-desktop.png, screenshots/phase33/02-light-density-desktop.png, screenshots/phase33/03-hacker-density-desktop.png</files>
  <read_first>
    - e2e/phase31.spec.ts
    - e2e/phase5.spec.ts
    - packages/ui/src/test-fixtures/phase5-log.ts
    - packages/ui/src/components/timeline/EventRow.tsx
    - packages/ui/src/components/timeline/cells/EventNameLabel.tsx
    - packages/ui/src/styles/tokens.css
    - .planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-UI-SPEC.md
    - .planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-PATTERNS.md
  </read_first>
  <behavior>
    - The Playwright spec starts the local CLI with a temporary synthetic JSONL file and `--port 0 --no-open`.
    - The synthetic rows include event names `foo/bar`, `foo/bar/baz`, `initialize`, `/leading`, and `trailing/`.
    - The spec asserts event rows render with 24px row geometry or equivalent visible computed/DOM evidence after faster unit/style density checks have already passed.
    - The spec asserts prefix and leaf text are visible for hierarchical names.
    - Dark, light, and hacker screenshots are captured only after `html[data-theme]`, fast unit/style checks, and no-path-leak assertions pass.
  </behavior>
  <action>
Create `e2e/phase33.spec.ts` by reusing the local CLI harness, temp-directory cleanup, theme switching, and `assertNoPathLeak(page)` patterns from `e2e/phase31.spec.ts`. Write a temporary `phase33-browser-safe.jsonl` from repository-owned fixture content plus clearly synthetic rows that produce visible `foo/bar`, `foo/bar/baz`, `initialize`, `/leading`, and `trailing/` event labels in the timeline. Do not read `~/.vscode-oss-agents-dev`, `~/.vscode-oss-dev`, any `/Users/...` log, or any external URL.

Set `SCREENSHOT_DIR = resolve("screenshots/phase33")`. At desktop width, wait for timeline rows to render, assert no absolute path leaks, and assert visible text for `foo/bar` and `foo/bar/baz`. If component test ids are available in the browser, assert `event-name-prefix` and `event-name-leaf` exist; otherwise assert the full labels are visible and rely on component tests for internal split mechanics. Use locator bounding boxes or computed styles to verify event rows are compact with a 24px target. Capture `01-dark-density-desktop.png`, switch to Light and assert `html[data-theme="light"]` before `02-light-density-desktop.png`, then switch to Hacker and assert `html[data-theme="hacker"]` before `03-hacker-density-desktop.png`. Call `assertNoPathLeak(page)` immediately before each screenshot.

After the Playwright run writes screenshots, inspect all three files with the image viewer before completion. Reject any clipping, row overlap, unreadable prefix contrast, excessive prefix dimming, selected-row visual confusion, or sensitive text/path leakage.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ui/src/components/timeline/EventRow.columns.test.tsx packages/ui/src/components/timeline/TimelineList.virt.test.tsx packages/ui/src/styles/theme-tokens.test.ts packages/ui/src/styles/no-hex-in-components.test.ts &amp;&amp; pnpm --filter @ahp-inspector/ui build &amp;&amp; pnpm exec playwright test e2e/phase33.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/phase33.spec.ts` contains `SCREENSHOT_DIR = resolve("screenshots/phase33")`.
    - `e2e/phase33.spec.ts` contains `assertNoPathLeak` checks for `/Users/`, `/home/`, and Windows absolute paths.
    - `e2e/phase33.spec.ts` contains synthetic labels `foo/bar`, `foo/bar/baz`, `initialize`, `/leading`, and `trailing/`, and contains no real-log root such as `.vscode-oss-agents-dev`.
    - `screenshots/phase33/01-dark-density-desktop.png`, `screenshots/phase33/02-light-density-desktop.png`, and `screenshots/phase33/03-hacker-density-desktop.png` exist after the spec run.
    - Fast unit/style checks run before screenshot capture: `pnpm vitest run packages/ui/src/components/timeline/EventRow.columns.test.tsx packages/ui/src/components/timeline/TimelineList.virt.test.tsx packages/ui/src/styles/theme-tokens.test.ts packages/ui/src/styles/no-hex-in-components.test.ts` exits 0.
    - `pnpm --filter @ahp-inspector/ui build && pnpm exec playwright test e2e/phase33.spec.ts --project=chromium` exits 0.
    - Manual screenshot inspection confirms fixture-only content, no path leakage, no row overlap/clipping, and readable-but-subtle prefix styling in all themes.
  </acceptance_criteria>
  <done>Phase 33 visual changes are proven in all themes with fixture-only screenshots and privacy guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic fixture source -> local CLI/browser -> saved screenshots | Saved visual evidence could disclose sensitive content if real logs or local paths enter the harness. |
| Browser visual state -> committed screenshots | Theme contrast or geometry regressions may be missed without assertions and manual inspection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02 | Information Disclosure | `e2e/phase33.spec.ts` and `screenshots/phase33/*` | mitigate | Use only synthetic temp JSONL fixtures, reject `/Users/`, `/home/`, and Windows paths before every screenshot, and inspect images before committing. |
| T-33-03 | Denial of Service | Browser timeline rendering | mitigate | Run the existing UI build and browser spec after density changes; retain virtualizer/component tests from earlier plans. |
| T-33-04 | Information Disclosure / Usability | All-theme prefix contrast | mitigate | Capture dark, light, and hacker screenshots after theme assertions; manually inspect prefix readability and row density. |
| T-33-01 | Tampering / XSS | Log-derived event labels in browser | mitigate | Exercise synthetic slash labels through the normal React rendering path; component tests already assert no HTML injection. |
</threat_model>

<verification>
- Run `pnpm --filter @ahp-inspector/ui build && pnpm exec playwright test e2e/phase33.spec.ts --project=chromium`.
- Inspect all screenshots under `screenshots/phase33/` with the image viewer.
- Run final phase gate: `pnpm test && pnpm -F @ahp-inspector/ui build && pnpm typecheck && pnpm lint`.
</verification>

<success_criteria>
- Fixture-only Phase 33 E2E proves compact density and event-name hierarchy across dark, light, and hacker themes.
- No committed screenshot or test fixture contains real local log content or absolute paths.
- Full repository test, UI build, typecheck, and lint gates pass.
</success_criteria>

<source_coverage>
| Source | ID | Acceptance item | Task | Status |
|--------|----|-----------------|------|--------|
| RESEARCH | PRIVACY-33-04 | Screenshots use synthetic fixtures and contain no absolute path leakage | 1 | COVERED |
| UI-SPEC | Manual Verifications | Review density and prefix subtlety across all themes | 1 | COVERED |
| VALIDATION | 33-03-01 | Fixture-only screenshots with no absolute path leakage | 1 | COVERED |
| GOAL | - | App/timeline feels tighter and closer to DevTools while still polished/readable | 1 | COVERED |
</source_coverage>

<output>
After completion, create `.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-04-SUMMARY.md`.
</output>
