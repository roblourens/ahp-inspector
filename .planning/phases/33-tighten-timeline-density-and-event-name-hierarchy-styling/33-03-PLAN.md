---
phase: 33-tighten-timeline-density-and-event-name-hierarchy-styling
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified:
  - packages/ui/src/components/timeline/EventRow.tsx
  - packages/ui/src/components/timeline/cells/EventNameLabel.tsx
  - packages/ui/src/components/timeline/EventRow.columns.test.tsx
autonomous: true
requirements: [EVENTNAME-33-02]
must_haves:
  truths:
    - "`foo/bar` renders `foo/` with `var(--color-event-name-prefix)` and `bar` as the primary leaf."
    - "`foo/bar/baz` splits at the last slash as `foo/bar/` plus `baz`."
    - "No-slash, leading-slash-only, and trailing-slash-only labels render unchanged."
    - "Search highlighting still produces React `<mark>` nodes across prefix and leaf text."
    - "Event labels remain React text/spans only; no `dangerouslySetInnerHTML` is introduced."
    - "The full unsplit label remains available through the existing row title/accessibility contract."
  artifacts:
    - path: "packages/ui/src/components/timeline/cells/EventNameLabel.tsx"
      provides: "Safe hierarchical event-name renderer"
    - path: "packages/ui/src/components/timeline/EventRow.tsx"
      provides: "Integration of hierarchical label renderer in the primary event column"
    - path: "packages/ui/src/components/timeline/EventRow.columns.test.tsx"
      provides: "Prefix/leaf, search, and non-split regression coverage"
  key_links:
    - from: "packages/ui/src/styles/tokens.css"
      to: "packages/ui/src/components/timeline/cells/EventNameLabel.tsx"
      via: "prefix span consumes `--color-event-name-prefix` from plan 33-01"
      pattern: "--color-event-name-prefix"
    - from: "packages/ui/src/components/timeline/cells/EventNameLabel.tsx"
      to: "packages/ui/src/components/timeline/EventRow.tsx"
      via: "primary event label renderer replaces inline label text without changing row title/accessibility"
      pattern: "EventNameLabel|title={labelTitle}"
    - from: "packages/ui/src/components/timeline/EventRow.tsx"
      to: "packages/ui/src/components/timeline/EventRow.columns.test.tsx"
      via: "component tests prove split, non-split, and search-highlight behavior"
      pattern: "session/delta|session/tool/call|mark"
---

<objective>
Apply subtle event-name hierarchy styling without sacrificing safety, truncation, search highlighting, or row accessibility.

Purpose: Make protocol-like labels easier to scan by reducing emphasis on the namespace prefix while keeping it readable and preserving the event leaf as the focal text.
Output: A small safe event-label renderer, integrated in `EventRow`, with component regressions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-RESEARCH.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-VALIDATION.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-PATTERNS.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-UI-SPEC.md
@packages/ui/src/components/timeline/EventRow.tsx
@packages/ui/src/components/timeline/EventRow.columns.test.tsx
@packages/ui/src/components/timeline/cells/SummaryCell.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hierarchical event-name rendering tests</name>
  <files>packages/ui/src/components/timeline/EventRow.columns.test.tsx</files>
  <read_first>
    - packages/ui/src/components/timeline/EventRow.columns.test.tsx
    - packages/ui/src/components/timeline/EventRow.tsx
    - packages/ui/src/components/timeline/cells/SummaryCell.tsx
    - .planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-UI-SPEC.md
    - .planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-PATTERNS.md
  </read_first>
  <behavior>
    - `session/delta` renders a prefix span for `session/` and a leaf span for `delta`.
    - `session/tool/call` renders a prefix span for `session/tool/` and a leaf span for `call`.
    - Labels `initialize`, `/leading`, and `trailing/` render as unsplit text.
    - Search highlighting still renders `<mark>` when the query matches prefix, slash-adjacent text, or leaf.
    - The method cell title still contains the full unsplit label and action/method suffix.
  </behavior>
  <action>
Extend `EventRow.columns.test.tsx` before product changes. Use the existing typed `baseRow` fixture. Add tests for action rows with `actionType: "session/delta"` and `actionType: "session/tool/call"` that assert prefix and leaf elements are separately findable through test ids or data attributes, prefix text is exactly `session/` or `session/tool/`, leaf text is exactly `delta` or `call`, and the prefix element style contains `var(--color-event-name-prefix)`. Add tests for `method: "initialize"`, `actionType: "/leading"`, and `actionType: "trailing/"` that assert no prefix element is rendered and the visible text is unchanged. Add a search-query test that passes `searchQuery="delta"` or `searchQuery="session"` and asserts a `<mark>` element is present inside the event-name label. Preserve existing action primary label and long-label truncation tests.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ui/src/components/timeline/EventRow.columns.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/components/timeline/EventRow.columns.test.tsx` contains assertions for `session/delta`, `session/tool/call`, `initialize`, `/leading`, and `trailing/`.
    - The new tests assert prefix color style includes the exact token string `var(--color-event-name-prefix)`.
    - The new tests assert search highlighting by querying for a `mark` element or its text inside a `<mark>`.
    - `pnpm vitest run packages/ui/src/components/timeline/EventRow.columns.test.tsx` fails before implementation and exits 0 after Task 2.
  </acceptance_criteria>
  <done>The event-name hierarchy contract is locked by component tests before implementation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement safe EventNameLabel and integrate it into EventRow</name>
  <files>packages/ui/src/components/timeline/cells/EventNameLabel.tsx, packages/ui/src/components/timeline/EventRow.tsx</files>
  <read_first>
    - packages/ui/src/components/timeline/EventRow.tsx
    - packages/ui/src/components/timeline/cells/SummaryCell.tsx
    - packages/ui/src/components/timeline/EventRow.columns.test.tsx
    - packages/ui/src/styles/tokens.css
    - .planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-UI-SPEC.md
    - .planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-PATTERNS.md
  </read_first>
  <behavior>
    - The renderer splits only at the last slash when the slash is not at index `0` and not at `label.length - 1`.
    - Prefix and leaf are rendered as React text nodes/spans/marks.
    - Prefix uses `var(--color-event-name-prefix)`.
    - Leaf retains the existing event-name font weight and truncation behavior.
    - Search highlighting remains case-insensitive and works across the rendered prefix/leaf segments.
  </behavior>
  <action>
Create `packages/ui/src/components/timeline/cells/EventNameLabel.tsx`. Export a `splitHierarchicalEventName(label: string): { prefix: string; leaf: string } | null` helper that uses `const slash = label.lastIndexOf("/")` and returns null when `slash <= 0 || slash >= label.length - 1`. Export `EventNameLabel({ label, searchQuery }: { label: string; searchQuery: string })` that renders a truncating inline-block label. Reuse or move the existing safe search-highlighting logic from `EventRow.tsx` so text becomes React text spans and `<mark>` nodes, never HTML. When split is non-null, render a prefix span with `data-testid="event-name-prefix"` and `color: "var(--color-event-name-prefix)"`, then a leaf span with `data-testid="event-name-leaf"`. When split is null, render the highlighted full label with no prefix test id.

In `EventRow.tsx`, import `EventNameLabel`, remove or delegate the local `highlightMatches` only after preserving its behavior, and replace the primary label text body with `<EventNameLabel label={label} searchQuery={searchQuery} />`. Keep the surrounding method cell `title={labelTitle}`, flex gap, truncation container, `fontWeight: label ? 600 : 400`, selected/pair/search backgrounds, memoization, and `useCallback` unchanged. Do not use `dangerouslySetInnerHTML`, raw color literals, new dependencies, parser/core changes, store changes, or server/host changes.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ui/src/components/timeline/EventRow.columns.test.tsx packages/ui/src/styles/no-hex-in-components.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/components/timeline/cells/EventNameLabel.tsx` exports `splitHierarchicalEventName` and contains `lastIndexOf("/")`.
    - `packages/ui/src/components/timeline/cells/EventNameLabel.tsx` contains no `dangerouslySetInnerHTML`.
    - `packages/ui/src/components/timeline/cells/EventNameLabel.tsx` contains the exact token string `var(--color-event-name-prefix)`.
    - `packages/ui/src/components/timeline/EventRow.tsx` imports `EventNameLabel` and preserves `title={labelTitle}` in the method cell.
    - `pnpm vitest run packages/ui/src/components/timeline/EventRow.columns.test.tsx packages/ui/src/styles/no-hex-in-components.test.ts` exits 0.
  </acceptance_criteria>
  <done>Event names render with subtle readable hierarchy while preserving search, truncation, accessibility, and XSS safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Log-derived event labels -> React timeline rendering | Event labels are untrusted log content displayed in the browser. |
| Search query -> highlighted event-name fragments | User query text controls which substrings are wrapped in `<mark>`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering / Elevation of Privilege | `EventNameLabel` and `EventRow` | mitigate | Render labels only as React text nodes, spans, and marks; never use `dangerouslySetInnerHTML`; keep raw color literals out of components. |
| T-33-04 | Information Disclosure / Usability | Prefix readability across themes | mitigate | Use `var(--color-event-name-prefix)` from plan 33-01 and component tests that assert the token is applied. |
</threat_model>

<verification>
- Run `pnpm vitest run packages/ui/src/components/timeline/EventRow.columns.test.tsx packages/ui/src/styles/no-hex-in-components.test.ts`.
- Run Phase 33 quick check: `pnpm vitest run packages/ui/src/components/timeline/EventRow.columns.test.tsx packages/ui/src/components/timeline/TimelineList.virt.test.tsx packages/ui/src/styles/theme-tokens.test.ts packages/ui/src/styles/no-hex-in-components.test.ts`.
</verification>

<success_criteria>
- Slash hierarchy styling works for one-level and deeper event names using last-slash split.
- Non-hierarchical edge cases render unchanged.
- Search highlighting, title/accessibility, truncation, memoized row behavior, and XSS safety are preserved.
</success_criteria>

<source_coverage>
| Source | ID | Acceptance item | Task | Status |
|--------|----|-----------------|------|--------|
| RESEARCH | EVENTNAME-33-02 | `foo/bar` renders prefix and leaf with separate styling while preserving accessible label/title | 1, 2 | COVERED |
| UI-SPEC | Interaction Contract | Last-slash split, no split for edge cases, React text spans, preserved search highlighting | 1, 2 | COVERED |
| VALIDATION | 33-01-01 | Event labels render as React text spans, not HTML | 2 | COVERED |
</source_coverage>

<output>
After completion, create `.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-03-SUMMARY.md`.
</output>
