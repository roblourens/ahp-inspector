---
phase: 33-tighten-timeline-density-and-event-name-hierarchy-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/styles/tokens.css
  - packages/ui/src/styles/theme-tokens.test.ts
  - packages/ui/src/styles/no-hex-in-components.test.ts
autonomous: true
requirements: [THEME-33-03]
must_haves:
  truths:
    - "`--color-event-name-prefix` exists in the dark, light, and hacker theme blocks."
    - "`--color-event-name-prefix` is defined through existing text tokens and remains closer to `--color-text-muted` than `--color-text-subtle`."
    - "`--row-height` is changed from `28px` to `24px` in all theme blocks, with no global shrink of body, detail, picker, or filter typography."
    - "No React component receives a raw hex/rgb/hsl color literal for Phase 33 styling."
  artifacts:
    - path: "packages/ui/src/styles/tokens.css"
      provides: "Phase 33 row-height and event-name-prefix semantic tokens across all themes"
    - path: "packages/ui/src/styles/theme-tokens.test.ts"
      provides: "Required-token coverage for `--color-event-name-prefix`"
    - path: "packages/ui/src/styles/no-hex-in-components.test.ts"
      provides: "Existing raw component color guard remains green"
  key_links:
    - from: "packages/ui/src/styles/tokens.css"
      to: "packages/ui/src/components/timeline/cells/EventNameLabel.tsx"
      via: "`--color-event-name-prefix` consumed by the prefix span"
      pattern: "--color-event-name-prefix"
    - from: "packages/ui/src/styles/tokens.css"
      to: "packages/ui/src/components/timeline/TimelineList.tsx"
      via: "`--row-height: 24px` must agree with virtualizer row estimates"
      pattern: "--row-height|ITEM_HEIGHT"
    - from: "packages/ui/src/styles/theme-tokens.test.ts"
      to: "packages/ui/src/styles/tokens.css"
      via: "required-token coverage for dark/light/hacker theme blocks"
      pattern: "REQUIRED_THEME_TOKENS|blockFor"
---

<objective>
Add the theme and density token contract that all later Phase 33 UI work depends on.

Purpose: Establish `24px` timeline row density and a readable event-name prefix color in the design-token layer before components consume those values.
Output: Updated theme tokens plus style-guard coverage that prevents missing theme blocks or raw component colors.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-RESEARCH.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-VALIDATION.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-PATTERNS.md
@.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-UI-SPEC.md
@packages/ui/src/styles/tokens.css
@packages/ui/src/styles/theme-tokens.test.ts
@packages/ui/src/styles/no-hex-in-components.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 33 token coverage before changing theme tokens</name>
  <files>packages/ui/src/styles/theme-tokens.test.ts, packages/ui/src/styles/no-hex-in-components.test.ts</files>
  <read_first>
    - packages/ui/src/styles/theme-tokens.test.ts
    - packages/ui/src/styles/no-hex-in-components.test.ts
    - packages/ui/src/styles/tokens.css
    - .planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-UI-SPEC.md
    - .planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-PATTERNS.md
  </read_first>
  <behavior>
    - `theme-tokens.test.ts` fails until `--color-event-name-prefix` is present in the dark, light, and hacker theme blocks.
    - `no-hex-in-components.test.ts` continues to scan more than 20 source files and rejects raw hex/rgb/hsl literals outside tokenized styles.
  </behavior>
  <action>
Add `--color-event-name-prefix` to `REQUIRED_THEME_TOKENS` in `packages/ui/src/styles/theme-tokens.test.ts`. Do not weaken `blockFor`, the three theme selectors, or the missing-token assertion. Confirm `packages/ui/src/styles/no-hex-in-components.test.ts` still contains the non-trivial file-count assertion and the raw `hex`, `rgb`, and `hsl` patterns; do not add Phase 33 exceptions.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ui/src/styles/theme-tokens.test.ts packages/ui/src/styles/no-hex-in-components.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/styles/theme-tokens.test.ts` contains the exact string `--color-event-name-prefix` inside `REQUIRED_THEME_TOKENS`.
    - `packages/ui/src/styles/theme-tokens.test.ts` still checks selectors `:root,\n[data-theme="dark"]`, `[data-theme="light"]`, and `[data-theme="hacker"]`.
    - `packages/ui/src/styles/no-hex-in-components.test.ts` still contains `expect(walk(SRC_DIR).length).toBeGreaterThan(20)` or the current equivalent non-vacuous scan assertion.
    - `pnpm vitest run packages/ui/src/styles/theme-tokens.test.ts packages/ui/src/styles/no-hex-in-components.test.ts` initially reports the missing token before `tokens.css` is updated, then exits 0 after Task 2.
  </acceptance_criteria>
  <done>Style guards require the new semantic prefix token and preserve raw-color protection.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add row-height and event-name-prefix tokens in every theme</name>
  <files>packages/ui/src/styles/tokens.css</files>
  <read_first>
    - packages/ui/src/styles/tokens.css
    - packages/ui/src/styles/theme-tokens.test.ts
    - .planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-UI-SPEC.md
    - .planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-PATTERNS.md
  </read_first>
  <behavior>
    - Dark, light, and hacker themes all expose `--row-height: 24px`.
    - Dark, light, and hacker themes all expose `--color-event-name-prefix`.
    - The prefix color is token-composed and readable: `color-mix(in srgb, var(--color-text-muted) 70%, var(--color-text) 30%)`.
    - No body, detail, picker, filter, or app-wide typography tokens are reduced as part of this plan.
  </behavior>
  <action>
In `packages/ui/src/styles/tokens.css`, replace each theme block's `--row-height: 28px;` with `--row-height: 24px;`. Near the text tokens in each dark, light, and hacker theme block, add the exact declaration `--color-event-name-prefix: color-mix(in srgb, var(--color-text-muted) 70%, var(--color-text) 30%);`. Do not change `--text-body-size`, `--text-ui-size`, `--text-heading-size`, filter heights, picker spacing, detail-pane tokens, `--row-group-header-height`, or accent/destructive tokens.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ui/src/styles/theme-tokens.test.ts packages/ui/src/styles/no-hex-in-components.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/styles/tokens.css` contains exactly three `--row-height: 24px;` declarations and no `--row-height: 28px;` declarations.
    - `packages/ui/src/styles/tokens.css` contains exactly three `--color-event-name-prefix: color-mix(in srgb, var(--color-text-muted) 70%, var(--color-text) 30%);` declarations.
    - `git diff -- packages/ui/src/styles/tokens.css` shows no changes to `--text-body-size`, `--text-ui-size`, `--text-heading-size`, `--filter-bar-height`, `--filter-chips-height`, or `--row-group-header-height`.
    - `pnpm vitest run packages/ui/src/styles/theme-tokens.test.ts packages/ui/src/styles/no-hex-in-components.test.ts` exits 0.
  </acceptance_criteria>
  <done>All themes provide the Phase 33 density and prefix-color tokens without weakening token or color guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme token definitions -> timeline component styling | Component code will consume these values for log-derived event names and row geometry. |
| Theme blocks -> all-theme readability | A missing or too-faint token in one theme can make event prefixes unreadable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Information Disclosure / Usability | `tokens.css` theme blocks | mitigate | Add `--color-event-name-prefix` to all required theme blocks and require it in `theme-tokens.test.ts`; define it through text tokens closer to muted than subtle. |
| T-33-03 | Denial of Service | Row-height token consumers | mitigate | Establish one tokenized `24px` row-height value for later virtualizer alignment work; plan 33-02 updates component estimates/rendered heights together. |
| T-33-01 | Tampering / XSS | Future event-name rendering | mitigate | Keep colors tokenized in CSS and preserve `no-hex-in-components.test.ts`; later rendering plan uses React text nodes only. |
</threat_model>

<verification>
- Run `pnpm vitest run packages/ui/src/styles/theme-tokens.test.ts packages/ui/src/styles/no-hex-in-components.test.ts`.
- Inspect `packages/ui/src/styles/tokens.css` to confirm only row-height and prefix-token declarations changed in theme blocks.
</verification>

<success_criteria>
- Dark, light, and hacker themes all expose `--row-height: 24px` and `--color-event-name-prefix`.
- The prefix token is required by theme-token tests and no raw component color exceptions are added.
- No global non-timeline typography or filter/detail/picker layout token is shrunk.
</success_criteria>

<source_coverage>
| Source | ID | Acceptance item | Task | Status |
|--------|----|-----------------|------|--------|
| RESEARCH | THEME-33-03 | Prefix color token exists for dark/light/hacker and no raw component colors are introduced | 1, 2 | COVERED |
| UI-SPEC | Color | `--color-event-name-prefix` is subtly muted, readable, and not accent-colored | 2 | COVERED |
| VALIDATION | 33-01-03 | Theme token and raw-color guards cover Phase 33 color work | 1, 2 | COVERED |
</source_coverage>

<output>
After completion, create `.planning/phases/33-tighten-timeline-density-and-event-name-hierarchy-styling/33-01-SUMMARY.md`.
</output>
