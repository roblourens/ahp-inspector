---
phase: 34-rethink-search-result-navigation-and-focus-behavior
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - packages/ui/src/components/filters/SearchPopover.tsx
  - packages/ui/src/components/filters/SearchPopover.test.tsx
  - packages/ui/src/components/filters/FilterBar.tsx
  - packages/ui/src/components/filters/FilterBar.test.tsx
  - packages/ui/src/components/filters/SearchInputCore.tsx
autonomous: true
requirements: [D-07, D-10, D-11, D-12, D-13]
must_haves:
  truths:
    - "The find counter reports events as \"results\" (never \"match/matches\"), with singular/plural and truncation \"+\" handling (D-07)."
    - "The find popover is pinned above the desktop detail rail at Z.popover and is never covered by the non-modal rail (D-10)."
    - "Enter/Shift+Enter keeps focus in the find input; clicking prev/next keeps focus on the clicked button (D-11)."
    - "Pressing Cmd/Ctrl+F while find is already open refocuses the input and selects its current query (D-12)."
    - "SearchPopover no longer self-handles Escape; Escape close + row focus is owned by TimelineRegion (D-13 contract from Plan 02)."
  artifacts:
    - path: "packages/ui/src/components/filters/SearchPopover.tsx"
      provides: "results-wording counter, results aria-labels, no self Escape listener"
      contains: "results"
    - path: "packages/ui/src/components/filters/FilterBar.tsx"
      provides: "Cmd+F-while-open refocus+select query"
      contains: "select()"
  key_links:
    - from: "packages/ui/src/components/filters/FilterBar.tsx"
      to: "find input (searchPopoverInputRef)"
      via: "Cmd+F handler refocus + input.select()"
      pattern: "\\.select\\(\\)"
    - from: "packages/ui/src/components/filters/SearchPopover.tsx"
      to: "result counter copy"
      via: "results wording"
      pattern: "results"
---

<objective>
Make the find widget keyboard-continuous and event-oriented. Reword the result counter to event-oriented "results" language with singular/plural + truncation handling (D-07), reword prev/next/region aria-labels and the live-region announcement, confirm and lock the pinned-above-rail placement (D-10), keep focus in the input on Enter/Shift+Enter and on the clicked button on click (D-11), make a second Cmd/Ctrl+F refocus-and-select the query (D-12), and remove SearchPopover's own Escape listener so the authoritative Escape-close+row-focus (built in Plan 02's TimelineRegion) is the single handler (D-13).

Purpose: Implements the "find widget placement and focus" cluster (D-07, D-10, D-11, D-12) plus the SearchPopover half of D-13. Fixes RESEARCH Pitfalls 3 (counter wording) and 5 (Cmd+F swallowed).
Output: SearchPopover, FilterBar, SearchInputCore updates with extended tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-CONTEXT.md
@.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-UI-SPEC.md
@.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-RESEARCH.md

<interfaces>
<!-- SearchPopover.tsx current counter (lines ~50-58) — reword. -->
```tsx
// today: `${n} of ${count} ${count===1 ? "match" : "matches"}${truncated ? "+" : ""}`
// also the empty-counter branch uses "match"/"matches".
// Status color today references the NONEXISTENT token var(--color-danger) — fix to var(--color-destructive).
```
UI-SPEC Copywriting Contract (authoritative strings):
- active result: `{n} of {m} results` (e.g. `3 of 24 results`), append `+` when `searchTruncated`.
- before navigating: `{m} results`, append `+` when truncated.
- singular: `1 result` / `1 of 1 result`.
- searching: `Searching…`
- no matches (query present, 0 results): `No matching events`
- error: `Search failed: {searchError}` in `var(--color-destructive)`; empty error → `Search failed — check the server connection and try again`.
- prev/next aria-labels: `Previous result` / `Next result` (reword from "…search match").
- region: `role="region"` `aria-label="Find"` (reword from "Search popover"); keep `data-testid="search-popover"`.
- counter span: `role="status"` + `aria-atomic="true"` polite live region.

<!-- FilterBar.tsx current Cmd+F handler (lines ~117-133) early-returns when open. -->
```tsx
function handleKeyDown(e: KeyboardEvent) {
  if (isSearchPopoverOpen) { return; }   // ← change: refocus+select instead of bailing
  const isFindShortcut = (e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "f";
  if (e.key === "/" || isFindShortcut) { e.preventDefault(); openSearch(); }
}
```
`searchPopoverInputRef` (a `useRef<HTMLInputElement>`) is already passed to SearchPopover as `inputRef`.

<!-- SearchInputCore.tsx Enter handler (lines ~46-56) dispatches ahp-search-nav WITHOUT blur — keep that (D-11). -->
<!-- SearchPopover.tsx has its OWN document Escape listener (useEffect lines ~38-45) calling onClose — REMOVE it; Plan 02's TimelineRegion now owns Escape. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Event-oriented counter wording, results aria-labels, destructive color fix, remove SearchPopover Escape listener</name>
  <files>packages/ui/src/components/filters/SearchPopover.tsx, packages/ui/src/components/filters/SearchPopover.test.tsx</files>
  <read_first>
    - packages/ui/src/components/filters/SearchPopover.tsx (counter text ~lines 50-58, status color ~line "var(--color-danger)", the Escape useEffect ~lines 38-45, aria-labels on buttons + region)
    - packages/ui/src/components/filters/SearchPopover.test.tsx (existing status/nav tests)
    - .planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-UI-SPEC.md (Copywriting Contract — exact strings)
    - packages/ui/src/components/timeline/TimelineRegion.tsx (confirm Plan 02 added authoritative Escape close+focus before removing the popover's listener)
  </read_first>
  <action>
In `SearchPopover.tsx`:
1. Rewrite `searchStatusText` per the UI-SPEC Copywriting Contract:
   - searching → `"Searching…"`
   - error → `searchError ? \`Search failed: ${searchError}\` : "Search failed — check the server connection and try again"`
   - 0 results with a query present → `"No matching events"`
   - active (focusedSearchIndex !== null && >= 0) → `\`${(focusedSearchIndex + 1).toLocaleString()} of ${searchMatchCount.toLocaleString()} ${searchMatchCount === 1 ? "result" : "results"}${searchTruncated ? "+" : ""}\``
   - pre-navigation → `\`${searchMatchCount.toLocaleString()} ${searchMatchCount === 1 ? "result" : "results"}${searchTruncated ? "+" : ""}\``
   Remove every "match"/"matches" occurrence from this component.
2. Fix the status color: replace `var(--color-danger)` with `var(--color-destructive)`.
3. Make the status `<span>` a polite live region: add `role="status"` and `aria-atomic="true"` to the status text span (keep `data-testid="search-status"` on its container).
4. Reword aria-labels: previous button → `aria-label="Previous result"`, next button → `aria-label="Next result"`. Region wrapper → `aria-label="Find"` (keep `role="region"` and `data-testid="search-popover"`).
5. DELETE the `useEffect` that adds the document `keydown` Escape→`onClose` listener (lines ~38-45). Escape is now owned by TimelineRegion (Plan 02). Keep the `onClose` prop (still used by the trigger/X path).

Update `SearchPopover.test.tsx`: assert "results"/"result" wording for n-of-m, singular, pre-nav, and `+` truncation; assert `No matching events` and the error string render; assert error text uses the destructive color path (no `--color-danger`); assert aria-labels "Previous result"/"Next result" and region name "Find"; assert pressing Escape inside the popover no longer calls `onClose` directly (the component has no own listener).
  </action>
  <verify>
    <automated>pnpm vitest run packages/ui/src/components/filters/SearchPopover.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '"match"|"matches"' packages/ui/src/components/filters/SearchPopover.tsx` returns 0 (user-facing copy only — ignore identifiers like `searchMatchCount`/`hasSearchMatches`).
    - `grep -cE '"result"|"results"' packages/ui/src/components/filters/SearchPopover.tsx` returns 2 or more.
    - `grep -c "color-danger" packages/ui/src/components/filters/SearchPopover.tsx` returns 0.
    - `grep -c "var(--color-destructive)" packages/ui/src/components/filters/SearchPopover.tsx` returns 1 or more.
    - `grep -c "No matching events" packages/ui/src/components/filters/SearchPopover.tsx` returns 1.
    - `grep -c "Previous result\|Next result" packages/ui/src/components/filters/SearchPopover.tsx` returns 2.
    - `grep -c "aria-label=\"Find\"" packages/ui/src/components/filters/SearchPopover.tsx` returns 1.
    - `grep -c "role=\"status\"" packages/ui/src/components/filters/SearchPopover.tsx` returns 1.
    - `grep -c "Escape" packages/ui/src/components/filters/SearchPopover.tsx` returns 0 (listener removed).
    - `pnpm vitest run packages/ui/src/components/filters/SearchPopover.test.tsx` passes.
  </acceptance_criteria>
  <done>Counter and labels use event-oriented "results" language with a polite live region; destructive color corrected; SearchPopover no longer self-handles Escape.</done>
</task>

<task type="auto">
  <name>Task 2: Cmd+F-while-open refocuses + selects query (D-12); confirm pinned placement (D-10)</name>
  <files>packages/ui/src/components/filters/FilterBar.tsx, packages/ui/src/components/filters/FilterBar.test.tsx</files>
  <read_first>
    - packages/ui/src/components/filters/FilterBar.tsx (handleKeyDown ~lines 117-133; openSearch ~lines 108-114; searchPopoverInputRef; SearchPopover render block)
    - packages/ui/src/components/filters/FilterBar.test.tsx (existing Cmd+F / "/" tests)
    - packages/ui/src/styles/zLayers.ts (Z.popover = 1100 vs Z.drawer = 2010 — confirm popover is above the non-modal rail but below the modal drawer, per UI-SPEC)
    - .planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-RESEARCH.md (Pitfall 5: Cmd+F swallowed)
  </read_first>
  <action>
In `FilterBar.tsx` `handleKeyDown`: replace the `if (isSearchPopoverOpen) { return; }` early bail with refocus-and-select behavior when the find shortcut fires while open:
```tsx
const isFindShortcut = (e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "f";
if (isSearchPopoverOpen) {
  if (isFindShortcut) {
    e.preventDefault();                 // keep suppressing native find
    const input = searchPopoverInputRef.current;
    input?.focus();
    input?.select();                    // select current query for replace-typing (D-12)
  }
  return;                               // "/" while open does nothing special
}
if (e.key === "/" || isFindShortcut) { e.preventDefault(); openSearch(); }
```
Confirm (D-10) the SearchPopover is rendered inside FilterBar (it already is) at `zIndex: Z.popover` and the desktop detail rail in AppShell has no elevated z — no code change expected, but add a FilterBar test asserting the rendered popover container carries the popover z-layer so the contract is regression-locked.

Update `FilterBar.test.tsx`: with the popover open and the input holding a query, fire Cmd+F (metaKey) and assert `document.activeElement` is the find input and the input's selection spans the full value (`selectionStart === 0 && selectionEnd === value.length`); assert `e.preventDefault` semantics by confirming the popover stays open (does not toggle closed). Add the z-order assertion (popover container style zIndex === Z.popover).
  </action>
  <verify>
    <automated>pnpm vitest run packages/ui/src/components/filters/FilterBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.select()" packages/ui/src/components/filters/FilterBar.tsx` returns 1.
    - `grep -c "isSearchPopoverOpen" packages/ui/src/components/filters/FilterBar.tsx` returns 2 or more (handler still references it).
    - `grep -c "Z.popover" packages/ui/src/components/filters/SearchPopover.tsx` returns 1 (placement unchanged).
    - `grep -c "select()" packages/ui/src/components/filters/FilterBar.test.tsx` returns 1 or more.
    - `pnpm vitest run packages/ui/src/components/filters/FilterBar.test.tsx` passes the new Cmd+F-while-open + z-order cases.
  </acceptance_criteria>
  <done>A second Cmd/Ctrl+F refocuses and selects the query without closing find or invoking native find; pinned z-order is test-locked.</done>
</task>

<task type="auto">
  <name>Task 3: Focus retention on Enter/Shift+Enter and on prev/next click (D-11)</name>
  <files>packages/ui/src/components/filters/SearchInputCore.tsx, packages/ui/src/components/filters/SearchPopover.tsx, packages/ui/src/components/filters/SearchPopover.test.tsx</files>
  <read_first>
    - packages/ui/src/components/filters/SearchInputCore.tsx (Enter onKeyDown ~lines 46-56 — dispatches ahp-search-nav, must NOT blur)
    - packages/ui/src/components/filters/SearchPopover.tsx (prev/next buttons onClick ~lines 110-170)
    - .planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-UI-SPEC.md (Interaction Contract — Focus Behavior: input keeps focus on Enter; clicked button keeps focus)
  </read_first>
  <action>
D-11 input side: confirm `SearchInputCore` Enter handler dispatches `ahp-search-nav` and does NOT call `.blur()` or move focus (it currently does not). Leave focus in the input. No code change unless a blur is found; if found, remove it.

D-11 button side: ensure clicking prev/next leaves focus on the clicked button. The buttons currently dispatch via `onNavigate`. Browsers focus a `<button>` on `mousedown`/click by default, but selection re-renders can move focus. Make the intent explicit: in each prev/next `onClick`, after calling `onNavigate(...)`, call `(e.currentTarget as HTMLButtonElement).focus();` so a re-render cannot drop focus back to the input. Do not preventDefault the click.

Tests: in `SearchPopover.test.tsx` (or co-located), render with `hasSearchMatches` true, click the Next button, and assert `document.activeElement` is the Next button (D-11). In `SearchInputCore` coverage (add `SearchInputCore.test.tsx` if absent, else extend FilterBar/SearchPopover test), focus the input, fire Enter, and assert `document.activeElement` is still the input.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ui/src/components/filters/SearchPopover.test.tsx packages/ui/src/components/filters/FilterBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "currentTarget as HTMLButtonElement).focus()" packages/ui/src/components/filters/SearchPopover.tsx` returns 2 (prev + next).
    - `grep -c ".blur()" packages/ui/src/components/filters/SearchInputCore.tsx` returns 0.
    - `grep -c "activeElement" packages/ui/src/components/filters/SearchPopover.test.tsx` returns 1 or more.
    - `pnpm vitest run packages/ui/src/components/filters/SearchPopover.test.tsx packages/ui/src/components/filters/FilterBar.test.tsx` passes the focus-retention cases.
  </acceptance_criteria>
  <done>Enter/Shift+Enter keeps input focus; clicking prev/next keeps focus on the clicked button; tests assert document.activeElement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query → counter text | The query length/count is reflected in status copy; the query string itself is NOT interpolated into the counter (only counts and the static error template). |
| keyboard shortcut → focus | Cmd/Ctrl+F is intercepted to suppress native find; focus is moved within trusted in-app elements only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering/Elevation (XSS) | SearchPopover counter copy | mitigate | Counter renders counts and a static error template via React text children; the raw query is never injected into status markup. No `dangerouslySetInnerHTML`. |
| T-34-S3 | Usability/Focus theft | FilterBar Cmd+F + nav buttons | accept | Focus moves are confined to the find input and nav buttons; `e.preventDefault()` keeps native find suppressed. Locked by activeElement tests. |
</threat_model>

<verification>
- `pnpm vitest run packages/ui/src/components/filters/SearchPopover.test.tsx packages/ui/src/components/filters/FilterBar.test.tsx` green.
- `pnpm -r typecheck` green.
- `pnpm lint` clean (only theme tokens; no `--color-danger`).
</verification>

<success_criteria>
- Counter uses "results" language with correct singular/plural/truncation (D-07).
- Find widget pinned at Z.popover above the non-modal rail (D-10).
- Enter keeps input focus; click keeps button focus (D-11).
- Second Cmd/Ctrl+F refocuses + selects the query (D-12).
- SearchPopover no longer self-handles Escape (D-13 contract).
</success_criteria>

<output>
After completion, create `.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-03-SUMMARY.md`.
</output>
