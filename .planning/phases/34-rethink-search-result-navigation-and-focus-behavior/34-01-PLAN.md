---
phase: 34-rethink-search-result-navigation-and-focus-behavior
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/timeline/cells/highlight.tsx
  - packages/ui/src/components/timeline/cells/highlight.test.tsx
  - packages/ui/src/components/timeline/cells/EventNameLabel.tsx
  - e2e/phase34.spec.ts
autonomous: true
requirements: [D-06, D-08]
must_haves:
  truths:
    - "A single shared highlighter module exists and is the only place that turns a query into <mark> markup (D-08)."
    - "Query matching is literal, case-insensitive, non-overlapping, and ignores queries shorter than 2 chars (D-08, ReDoS-safe)."
    - "EventNameLabel renders identical highlight output after refactoring to consume the shared module (D-08 regression-safe)."
    - "A skipped e2e scaffold exists so the Wave-3 e2e plan has a test seam to fill in (D-06..D-13 scaffolding)."
  artifacts:
    - path: "packages/ui/src/components/timeline/cells/highlight.tsx"
      provides: "findMatchRanges + renderHighlightedSegment + HighlightedText, extracted from EventNameLabel"
      exports: ["findMatchRanges", "renderHighlightedSegment", "HighlightedText", "MatchRange"]
      min_lines: 40
    - path: "packages/ui/src/components/timeline/cells/highlight.test.tsx"
      provides: "Unit coverage for literal/case-insensitive/non-overlapping ranges and escaped React rendering"
      contains: "findMatchRanges"
    - path: "e2e/phase34.spec.ts"
      provides: "Playwright scaffold (test.describe + test.skip placeholders) filled in by Plan 05"
      contains: "phase34"
  key_links:
    - from: "packages/ui/src/components/timeline/cells/EventNameLabel.tsx"
      to: "packages/ui/src/components/timeline/cells/highlight.tsx"
      via: "import { findMatchRanges, renderHighlightedSegment }"
      pattern: "from \"./highlight"
---

<objective>
Establish the Wave-0 test scaffolding and the single shared query highlighter that every later wave depends on. Extract the battle-tested `findMatchRanges` + `renderHighlightedSegment` logic out of `EventNameLabel.tsx` into a reusable `cells/highlight.tsx` module (per RESEARCH "Don't Hand-Roll" / Pattern 3), add its unit test, refactor `EventNameLabel` to consume it with zero behavior change, and create the standalone e2e spec scaffold.

Purpose: D-08 highlighting in the detail views (Plan 04) and the timeline row must come from ONE implementation, never a reimplementation, and must be literal/non-regex (ReDoS-safe). This plan creates that single source of truth and the test seams the rest of the phase samples against.
Output: `highlight.tsx` (+ test), refactored `EventNameLabel.tsx`, `e2e/phase34.spec.ts` scaffold.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-CONTEXT.md
@.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-UI-SPEC.md
@.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-VALIDATION.md

<interfaces>
<!-- Contract this plan PRODUCES. Plan 04 imports HighlightedText; EventNameLabel imports findMatchRanges + renderHighlightedSegment. -->
New module `packages/ui/src/components/timeline/cells/highlight.tsx`:
```typescript
export interface MatchRange { start: number; end: number; }

// Literal, case-insensitive, non-overlapping. Returns [] when query.length < 2.
export function findMatchRanges(text: string, query: string): MatchRange[];

// Renders text[segmentStart..segmentEnd) wrapping any overlapping ranges in <mark>.
export function renderHighlightedSegment(
  label: string,
  segmentStart: number,
  segmentEnd: number,
  ranges: readonly MatchRange[],
): JSX.Element;

// Convenience whole-string highlighter for detail views (Plan 04).
export function HighlightedText({ text, query }: { text: string; query: string }): JSX.Element;
```

Existing source to MOVE (do not rewrite logic) — `packages/ui/src/components/timeline/cells/EventNameLabel.tsx` currently holds `MatchRange`, `findMatchRanges`, and `renderHighlightedSegment`. The `<mark>` element uses exactly:
```tsx
<mark style={{ background: "var(--color-search-match-bg)", color: "var(--color-search-match-fg)" }}>
```
e2e self-spawn pattern lives in `e2e/phase12.spec.ts` / `e2e/phase29.spec.ts`; the 5173-baseURL pattern lives in `e2e/phase14.spec.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared highlighter module + unit test, refactor EventNameLabel</name>
  <files>packages/ui/src/components/timeline/cells/highlight.tsx, packages/ui/src/components/timeline/cells/highlight.test.tsx, packages/ui/src/components/timeline/cells/EventNameLabel.tsx</files>
  <read_first>
    - packages/ui/src/components/timeline/cells/EventNameLabel.tsx (source of the logic being extracted)
    - .planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-UI-SPEC.md (§Interaction Contract — Query Highlighting: literal indexOf, min length 2, <mark> tokens, no dangerouslySetInnerHTML)
  </read_first>
  <behavior>
    - findMatchRanges("Hello world", "o") returns [] (query length < 2).
    - findMatchRanges("aXAxa", "xa") is case-insensitive → ranges at [1,3] only first... verify non-overlapping: "aaaa" with "aa" → [{0,2},{2,4}] (non-overlapping, advance past each match).
    - findMatchRanges("foobar", "zzz") returns [].
    - HighlightedText renders the query substring inside a <mark> element and the rest as plain text (assert getByText returns a MARK tagName for the matched slice).
    - HighlightedText with a payload containing "<script>" highlights it as escaped text — the rendered DOM contains the literal characters, never an injected element (query "script" → MARK around "script", no <script> node created).
  </behavior>
  <action>
Create `packages/ui/src/components/timeline/cells/highlight.tsx`. MOVE (cut, do not re-derive) the existing `MatchRange` interface, `findMatchRanges`, and `renderHighlightedSegment` functions verbatim from `EventNameLabel.tsx` and `export` all three plus `MatchRange`. Keep the `<mark style={{ background: "var(--color-search-match-bg)", color: "var(--color-search-match-fg)" }}>` markup EXACTLY (theme tokens, no hex). Add a convenience component:
```tsx
export function HighlightedText({ text, query }: { text: string; query: string }): JSX.Element {
  const ranges = findMatchRanges(text, query);
  return renderHighlightedSegment(text, 0, text.length, ranges);
}
```
Do NOT introduce any RegExp — matching stays `String.prototype.toLowerCase()` + `indexOf` (literal). Do NOT add `dangerouslySetInnerHTML`.

Refactor `EventNameLabel.tsx` to `import { findMatchRanges, renderHighlightedSegment } from "./highlight.js";` and delete its now-duplicated local definitions. Its JSX output must be byte-for-byte equivalent (same prefix/leaf segmentation, same `data-testid` values).

Create `packages/ui/src/components/timeline/cells/highlight.test.tsx` covering the behaviors above with `@testing-library/react` `render` + `screen`. Assert escaped rendering by querying for the matched text and checking `.closest("mark")` / `tagName === "MARK"`, and that no `<script>` element exists in the container.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ui/src/components/timeline/cells</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function findMatchRanges" packages/ui/src/components/timeline/cells/highlight.tsx` returns 1.
    - `grep -c "export function HighlightedText" packages/ui/src/components/timeline/cells/highlight.tsx` returns 1.
    - `grep -c "RegExp\|new RegExp\|dangerouslySetInnerHTML" packages/ui/src/components/timeline/cells/highlight.tsx` returns 0.
    - `grep -c "var(--color-search-match-bg)" packages/ui/src/components/timeline/cells/highlight.tsx` returns 1 or more.
    - `grep -c "function findMatchRanges" packages/ui/src/components/timeline/cells/EventNameLabel.tsx` returns 0 (logic moved out).
    - `grep -c "from \"./highlight" packages/ui/src/components/timeline/cells/EventNameLabel.tsx` returns 1.
    - `pnpm vitest run packages/ui/src/components/timeline/cells` passes.
  </acceptance_criteria>
  <done>The shared highlighter exists with passing unit tests, EventNameLabel consumes it, and no regex/innerHTML is introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Add e2e scaffold</name>
  <files>e2e/phase34.spec.ts</files>
  <read_first>
    - e2e/phase14.spec.ts (5173 baseURL pattern + filter-bar readiness wait)
    - e2e/phase29.spec.ts (self-spawn pattern that Plan 05 will adopt when filling this in)
  </read_first>
  <action>
Create the e2e scaffold `e2e/phase34.spec.ts` that compiles and is collected by Playwright but does not yet assert behavior. Use the `e2e/phase14.spec.ts` baseURL convention. Add a single `test.describe("phase34 find navigation", ...)` containing `test.skip(...)` placeholders named for the flows Plan 05 fills in: "uninterrupted Enter/Shift+Enter navigation (wide rail syncs)", "narrow viewport: search nav does not open drawer", "repeated Cmd+F refocuses and selects query", "Escape closes find and focuses current row", "query highlighted in detail content". Reference `SCREENSHOT_DIR = resolve("screenshots/phase34")` (do not create screenshots yet). Add a top-of-file comment: screenshots MUST come from `test/fixtures/*.jsonl` only (copilot-instructions privacy constraint). Do NOT import any test helper that another plan creates — this scaffold must stand alone.
  </action>
  <verify>
    <automated>pnpm exec tsc -p packages/ui/tsconfig.json --noEmit && pnpm exec playwright test e2e/phase34.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `test -f e2e/phase34.spec.ts` succeeds.
    - `grep -c "test.skip" e2e/phase34.spec.ts` returns 5 or more.
    - `grep -c "screenshots/phase34" e2e/phase34.spec.ts` returns 1 or more.
    - `grep -c "test-fixtures/viewport" e2e/phase34.spec.ts` returns 0 (no cross-plan import).
    - `pnpm exec playwright test e2e/phase34.spec.ts --list` lists the 5 skipped tests without a collection error.
  </acceptance_criteria>
  <done>The skipped e2e scaffold exists, typechecks, and is collected by Playwright; Plan 05 will fill it in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query → highlighter | The find query is untrusted text used to compute match ranges over arbitrary log payloads. |
| log payload → rendered DOM | Arbitrary JSONL content (may contain `<script>`, secrets) is rendered with `<mark>` highlights. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02 | DoS (ReDoS) | findMatchRanges (highlight.tsx) | mitigate | Matching uses literal `toLowerCase()` + `indexOf`; NO `RegExp` is compiled from the query. Asserted by grep gate (regex count == 0) and unit test on long inputs. |
| T-34-01 | Tampering/Elevation (XSS) | HighlightedText / renderHighlightedSegment | mitigate | Highlight wraps only escaped React text children in `<mark>`; NO `dangerouslySetInnerHTML`. Unit test asserts a `<script>`-containing payload renders as inert text, never an injected node. |
</threat_model>

<verification>
- `pnpm vitest run packages/ui/src/components/timeline/cells` green.
- `pnpm -r typecheck` green (no broken EventNameLabel imports).
- `pnpm lint` clean (biome, no-hex-in-components passes — only tokens used).
- `pnpm exec playwright test e2e/phase34.spec.ts --list` collects without error.
</verification>

<success_criteria>
- One shared highlighter module is the sole producer of `<mark>` markup; EventNameLabel consumes it with identical output.
- No regex compiled from the query anywhere in highlight.tsx (T-34-02).
- A `<script>`-bearing payload is proven inert by unit test (T-34-01).
- Skipped e2e scaffold exists for downstream waves (the narrow-screen viewport helper is owned by Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-01-SUMMARY.md`.
</output>
