---
phase: 34-rethink-search-result-navigation-and-focus-behavior
plan: 05
type: execute
wave: 3
depends_on: ["01", "02", "03", "04"]
files_modified:
  - test/fixtures/phase34-find-nav.jsonl
  - e2e/phase34.spec.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13]
must_haves:
  truths:
    - "An end-to-end Playwright spec self-spawns the CLI on a synthetic fixture and exercises the full find-and-navigate flow (D-01..D-13)."
    - "At desktop width, repeated Enter/Shift+Enter advances results, the rail syncs, and focus never leaves the find input (D-01, D-05, D-11)."
    - "At narrow width, search navigation advances results without opening the detail drawer, while an explicit row click does open it (D-02, D-04)."
    - "Escape closes find, preserves the query/results, and focuses the current matching row; a second Cmd/Ctrl+F refocuses and selects the query (D-12, D-13)."
    - "The active query is visibly highlighted in the matching row and detail content; the counter reads event-oriented \"results\" (D-07, D-08)."
    - "Committed screenshots are produced from a synthetic fixture only and contain no real filesystem paths (privacy)."
  artifacts:
    - path: "test/fixtures/phase34-find-nav.jsonl"
      provides: "Committed synthetic fixture: ≥3 events sharing the literal token \"telemetry\", one event whose match lives only in a nested/collapsed JSON branch (D-09 reveal); no real paths/secrets"
      contains: "telemetry"
    - path: "e2e/phase34.spec.ts"
      provides: "Full Playwright coverage of D-01..D-13 with committed screenshot evidence"
      contains: "phase34"
  key_links:
    - from: "e2e/phase34.spec.ts"
      to: "screenshots/phase34"
      via: "page.screenshot path under SCREENSHOT_DIR"
      pattern: "screenshots/phase34"
---

<objective>
Replace the Wave-0 skipped scaffold (`e2e/phase34.spec.ts` from Plan 01) with a full Playwright spec that proves the whole phase end-to-end against a synthetic, committed-safe fixture, following the self-spawn pattern in `e2e/phase29.spec.ts`. Capture committed screenshot evidence under `screenshots/phase34/` from fixture data only, and assert no real filesystem paths leak into the UI.

Purpose: Holistic verification of D-01..D-13 in a real browser at both desktop (≥1400px) and narrow (<1400px) widths — the integration gate before `/gsd-verify-work`.
Output: implemented `e2e/phase34.spec.ts` + `screenshots/phase34/*.png`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-CONTEXT.md
@.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-UI-SPEC.md
@.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-VALIDATION.md

<interfaces>
<!-- Self-spawn analog: e2e/phase29.spec.ts — spawnCli / waitForPort / killCli / assertNoPathLeak / mkdtemp fixture. -->
<!-- DETAIL_DESKTOP_BREAKPOINT = 1400px. Desktop: page.setViewportSize({width:1440,...}); narrow: {width:1366,...}. -->
<!-- Stable testids/labels available after Plans 01-04:
     - filter bar row filter input: getByLabel("Filter rows")
     - find popover container: getByTestId("search-popover"), region aria-label "Find"
     - find input: getByLabel("Search all events")
     - status counter: getByTestId("search-status") — text "{n} of {m} results"
     - prev/next buttons: getByRole("button", { name: "Previous result" | "Next result" })
     - timeline rows: getByTestId("row-{idx}")
     - desktop rail: getByTestId("detail-panel-wrapper"); narrow drawer: getByTestId("detail-drawer")
     - detail content: getByTestId("detail-panel"), raw view getByTestId("raw-json-view"), pretty getByTestId("pretty-json-view")
     - <mark> highlight: page.locator("mark") inside the matching row / detail. -->
<!-- Open find via keyboard: page.keyboard.press("ControlOrMeta+f"). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desktop + narrow navigation, focus, and drawer behavior</name>
  <files>e2e/phase34.spec.ts</files>
  <read_first>
    - e2e/phase29.spec.ts (spawnCli, waitForPort, killCli, assertNoPathLeak, ControlOrMeta+f usage)
    - e2e/phase34.spec.ts (Plan 01 scaffold to replace)
    - test/fixtures/phase34-find-nav.jsonl (the committed synthetic fixture this spec spawns the CLI on; shared token "telemetry", nested-only match for D-09)
    - .planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-UI-SPEC.md (Interaction Contract tables — desktop rail sync vs narrow drawer suppression; focus behavior)
  </read_first>
  <action>
Rewrite `e2e/phase34.spec.ts` using the `e2e/phase29.spec.ts` self-spawn pattern, but spawn the CLI on the **committed** synthetic fixture `resolve("test/fixtures/phase34-find-nav.jsonl")` (per copilot-instructions: committed screenshots derive from `test/fixtures/*.jsonl` only — do NOT build the fixture from `phase5-log.ts` or `mkdtemp`). The fixture contains ≥3 events sharing the literal token `telemetry`, with one event (`workspace/executeCommand` / `diagnostics.run`) whose only `telemetry` occurrence lives in a nested `params.config.options.flags.telemetryMode` branch that the Pretty tree collapses by default — this exercises D-09 reveal. Spawn with `[resolve("test/fixtures/phase34-find-nav.jsonl"), "--port", "0", "--no-open"]`, `waitForPort`, and `killCli` in `afterAll`. `SCREENSHOT_DIR = resolve("screenshots/phase34")`, `mkdir` it in `beforeAll`. The search query for every flow is the literal `telemetry`. NEVER read real logs — committed fixture only.

Implement (remove `test.skip`):
1. **Desktop nav + focus (D-01, D-05, D-06, D-11):** `setViewportSize({width:1440,height:900})`, goto, wait for `row-0`. Press `ControlOrMeta+f`; expect `search-popover` visible and the find input focused. Type the shared token; wait for `search-status` to show `of` + `results`. Press `Enter` → expect the counter index advances (`1 of N results` → `2 of N results`) and the find input is still `document.activeElement` (assert via `page.evaluate(() => document.activeElement?.getAttribute("aria-label"))` === `"Search all events"`). Expect `detail-panel-wrapper` (desktop rail) visible and reflecting the selected event. Press `Shift+Enter` → index decrements, focus still in input.
2. **Narrow drawer suppression (D-02, D-04):** `setViewportSize({width:1366,height:900})`, reload. Open find, type token, press `Enter` a few times → expect `detail-drawer` is NOT visible (search nav does not open drawer) while a row is selected. Then explicitly click a `row-{idx}` → expect `detail-drawer` becomes visible (D-04). Close the drawer.
3. **Escape preserves + focuses row (D-03, D-13):** back at narrow or desktop, open find, type token, navigate, press `Escape` → `search-popover` hidden, the find input value is preserved on reopen (results not cleared), and `document.activeElement` is the matching `row-{idx}` (assert the active element's data-testid starts with `row-`).
Call `assertNoPathLeak(page)` at least once. Screenshot key states to `SCREENSHOT_DIR` (e.g. `01-desktop-nav.png`, `02-narrow-no-drawer.png`).
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/phase34.spec.ts -g "navigation"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "telemetry" test/fixtures/phase34-find-nav.jsonl` returns 3 or more (shared token across events).
    - `grep -cE "/Users/|/home/|C:\\\\" test/fixtures/phase34-find-nav.jsonl` returns 0 (no real path leak in the committed fixture).
    - `grep -c "test/fixtures/phase34-find-nav.jsonl" e2e/phase34.spec.ts` returns 1 or more (spec spawns CLI on the committed fixture).
    - `grep -c "phase5-log\|mkdtemp" e2e/phase34.spec.ts` returns 0 (fixture is the committed file, not constructed).
    - `grep -c "spawnCli\|waitForPort" e2e/phase34.spec.ts` returns 2 or more.
    - `grep -c "setViewportSize" e2e/phase34.spec.ts` returns 2 or more (desktop + narrow).
    - `grep -c "detail-drawer" e2e/phase34.spec.ts` returns 2 or more (suppression + explicit-open).
    - `grep -c "assertNoPathLeak" e2e/phase34.spec.ts` returns 1 or more.
    - `grep -c "test.skip" e2e/phase34.spec.ts` returns 0 (scaffold filled in).
    - `pnpm exec playwright test e2e/phase34.spec.ts -g "navigation"` passes.
  </acceptance_criteria>
  <done>Desktop nav keeps input focus + syncs rail; narrow nav suppresses the drawer while explicit clicks open it; Escape preserves results and focuses the row; no path leak.</done>
</task>

<task type="auto">
  <name>Task 2: Cmd+F refocus, counter wording, highlight evidence + screenshots</name>
  <files>e2e/phase34.spec.ts, screenshots/phase34/</files>
  <read_first>
    - e2e/phase34.spec.ts (Task 1 scaffolding + helpers)
    - .planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-UI-SPEC.md (Copywriting Contract — "{n} of {m} results"; Query Highlighting)
    - .github/copilot-instructions.md (committed screenshots from fixtures only, under screenshots/<phase>/)
  </read_first>
  <action>
Add tests to `e2e/phase34.spec.ts`:
1. **Repeated Cmd+F (D-12):** open find, type the token, press `ControlOrMeta+f` again → expect the find input is focused and its full value is selected (assert via `page.evaluate(() => { const el = document.activeElement as HTMLInputElement; return el.selectionStart === 0 && el.selectionEnd === el.value.length; })` is true) and `search-popover` stays visible.
2. **Counter wording (D-07):** assert `search-status` text matches `/\d+ of \d+ results?\+?/` after navigating and `/^\d+ results?\+?$/` before navigating; assert it never contains the word "match".
3. **Highlight evidence — Raw `<mark>` (D-08):** with the query `telemetry` active and an event selected, switch to the Raw tab and expect at least one `mark` element inside `getByTestId("detail-panel")`, AND at least one `mark` inside the selected timeline row (`getByTestId("row-...")`). Screenshot `03-highlight-detail.png`.
4. **Highlight evidence — Pretty CSS Custom Highlight + nested reveal (D-08, D-09):** select the `workspace/executeCommand` / `diagnostics.run` event whose only `telemetry` match lives in the nested `params.config.options.flags.telemetryMode` branch, switch to the Pretty tab (`getByTestId("pretty-json-view")`). Assert the CSS Custom Highlight registry is active in the real browser: `expect(await page.evaluate(() => CSS.highlights?.has("ahp-search-match"))).toBe(true)` (Playwright/Chromium supports the CSS Custom Highlight API). Assert the nested branch was auto-revealed so the matched key `telemetryMode` is visible (`await expect(page.getByTestId("pretty-json-view").getByText("telemetryMode")).toBeVisible()`). Screenshot `05-pretty-highlight-reveal.png`.
5. Capture a final `04-find-widget-pinned.png` at desktop width showing the find popover open above the detail rail (D-10 evidence). All screenshots land in `screenshots/phase34/` from the committed synthetic fixture only.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/phase34.spec.ts && ls screenshots/phase34/*.png</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "selectionEnd" e2e/phase34.spec.ts` returns 1 or more (Cmd+F select assertion).
    - `grep -cE "results\?|of \\\\d\+ results" e2e/phase34.spec.ts` returns 1 or more (counter wording assertion).
    - `grep -c "locator(\"mark\")\|\"mark\"" e2e/phase34.spec.ts` returns 1 or more (highlight assertion).
    - `grep -c "CSS.highlights?.has(\"ahp-search-match\")" e2e/phase34.spec.ts` returns 1 (Pretty-tab CSS Custom Highlight verified in-browser).
    - `grep -c "telemetryMode" e2e/phase34.spec.ts` returns 1 or more (D-09 nested-branch reveal asserted).
    - `grep -c "screenshots/phase34" e2e/phase34.spec.ts` returns 1 or more.
    - `pnpm exec playwright test e2e/phase34.spec.ts` passes all (non-skipped) tests.
    - `ls screenshots/phase34/*.png` lists at least 3 committed screenshots.
  </acceptance_criteria>
  <done>Cmd+F refocus+select, event-oriented counter wording, and visible highlight are proven in-browser; committed fixture-only screenshots exist under screenshots/phase34/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture log → committed screenshot | E2E captures UI rendered from a log; committed images must never contain real session content or filesystem paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Information Disclosure | e2e screenshots under screenshots/phase34 | mitigate | Spec spawns the CLI on the committed synthetic fixture `test/fixtures/phase34-find-nav.jsonl` (per copilot-instructions; never real `~/.vscode-oss-agents-dev/logs`). The fixture greps clean of `/Users/`, `/home/`, `C:\`. `assertNoPathLeak(page)` asserts no `/Users/`, `/home/`, or `C:\` strings appear in the rendered UI before screenshots are taken. |
| T-34-01 | Tampering/Elevation (XSS) | highlighted detail content (e2e) | mitigate | E2E confirms highlights render as `<mark>` text nodes (no injected script executes); complements the unit-level inert-payload assertions in Plans 01/04. |
</threat_model>

<verification>
- `pnpm exec playwright test e2e/phase34.spec.ts` green (all D-01..D-13 flows).
- Committed screenshots exist under `screenshots/phase34/` and pass the path-leak guard.
- Full-suite gate before `/gsd-verify-work`: `pnpm test && pnpm -r typecheck && pnpm lint && pnpm e2e -- e2e/phase34.spec.ts`.
</verification>

<success_criteria>
- One Playwright spec covers desktop + narrow navigation, focus retention, drawer suppression vs explicit open, Escape, Cmd+F, counter wording, and highlight (D-01..D-13).
- Screenshots are fixture-derived and path-leak-free (T-34-03).
</success_criteria>

<output>
After completion, create `.planning/phases/34-rethink-search-result-navigation-and-focus-behavior/34-05-SUMMARY.md`.
</output>
