---
phase: 22-improve-large-log-loading-and-high-throughput-live-tail-perf
plan: 06
type: execute
wave: 5
depends_on: ["22-04"]
files_modified:
  - packages/ui/src/persistence/persist-effect.ts
  - packages/ui/src/persistence/persist-effect.test.ts
  - packages/ui/src/components/timeline/TimelineList.tsx
  - packages/ui/src/components/timeline/TimelineList.virt.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Per-log preference hydration waits for explicit baseline lifecycle completion instead of firing on the first progressive row, avoiding avoidable responsiveness regressions per D-02."
    - "A user inspecting already-loaded rows is not pulled forward by later baseline chunks, preserving viewport stability per D-05."
    - "Tail-follow remains smooth only while the user is actually following the latest row, per D-07."
  artifacts:
    - path: "packages/ui/src/persistence/persist-effect.ts"
      provides: "Baseline-complete-driven persistence hydration"
    - path: "packages/ui/src/components/timeline/TimelineList.tsx"
      provides: "Viewport ownership rules under progressive load and live follow"
  key_links:
    - from: "packages/ui/src/persistence/persist-effect.ts"
      to: "packages/ui/src/state/store.ts"
      via: "explicit load lifecycle completion from Plan 22-04"
      pattern: "load|baseline|complete"
    - from: "packages/ui/src/components/timeline/TimelineList.tsx"
      to: "requestAnimationFrame scroll scheduling"
      via: "follow-tail gated scroll effects"
      pattern: "followTailRef|requestAnimationFrame"
---

<objective>
Close the two lifecycle regressions progressive rows would otherwise create: premature persistence hydration and viewport motion that fights active inspection.

Purpose: Satisfy D-02, D-05, and D-07 while preserving earlier Phase 4 persistence and tail-follow behavior.
Output: Baseline-complete persistence hydration and virtualized timeline tests proving stable off-tail inspection plus smooth real follow-tail updates.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-RESEARCH.md
@packages/ui/src/persistence/persist-effect.ts
@packages/ui/src/components/timeline/TimelineList.tsx
@.planning/milestones/v1.0-phases/04-live-tail-discovery-and-persistence/04-06-SUMMARY.md
@.planning/quick/20260508-tail-follow-and-picker-polish/SUMMARY.md

<interfaces>
`usePersistEffect()` currently hydrates when `rows.length` transitions `0 -> N`; progressive rows make that trigger too early. Plan 22-04 introduces an explicit loading lifecycle in the store; hydrate against that lifecycle instead.
`TimelineList` already gates auto-scroll on `followTailRef`, disables follow when selection scrolls an off-screen row, and schedules bottom scrolling with `requestAnimationFrame`. Extend tests/logic around those existing rules; do not move viewport policy server-side.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Re-key preference hydration to explicit baseline completion</name>
  <files>packages/ui/src/persistence/persist-effect.ts, packages/ui/src/persistence/persist-effect.test.ts</files>
  <read_first>
    - packages/ui/src/persistence/persist-effect.ts
    - packages/ui/src/persistence/persist-effect.test.ts
    - packages/ui/src/state/store.ts
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-RESEARCH.md
  </read_first>
  <behavior>
    - A first progressive row does not hydrate stored filters/grouping/search/selection yet.
    - Explicit baseline completion hydrates once for the current `logKey`, with the existing selected-index bounds check preserved.
    - Log-key switch and debounced save behavior remain intact.
  </behavior>
  <action>
    Refactor `usePersistEffect()` away from the historical `rows.length` `0 -> N` snapshot-end heuristic and onto the explicit baseline-complete store lifecycle delivered by Plan 22-04. Preserve synchronous pending-save flush on log-key change, quota-disable behavior, per-log reset behavior, and selected-index bounds checking against currently available rows at hydration time. Update persistence tests to prove hydration does not happen on partial first rows and does happen exactly once when baseline completion flips.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/persistence/persist-effect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "rowsLen > 0 && prevLen === 0" packages/ui/src/persistence/persist-effect.ts` returns no matches.
    - `grep -n "baseline\\|complete\\|partial" packages/ui/src/persistence/persist-effect.ts packages/ui/src/persistence/persist-effect.test.ts` finds lifecycle-driven hydration logic and assertions.
    - The targeted persistence test command exits 0.
  </acceptance_criteria>
  <done>Preferences hydrate on truthful baseline completion instead of first-row visibility, preserving Phase 4 persistence behavior under progressive loading.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard stable inspection viewport and smooth true tail-follow</name>
  <files>packages/ui/src/components/timeline/TimelineList.tsx, packages/ui/src/components/timeline/TimelineList.virt.test.tsx</files>
  <read_first>
    - packages/ui/src/components/timeline/TimelineList.tsx
    - packages/ui/src/components/timeline/TimelineList.virt.test.tsx
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
    - .planning/quick/20260508-tail-follow-and-picker-polish/SUMMARY.md
  </read_first>
  <behavior>
    - Progressive row additions do not bottom-scroll the timeline after user inspection disables follow-tail.
    - While the user is parked at the actual bottom, appended row batches still scroll smoothly on the existing rAF boundary.
    - Selecting or keyboard-jumping to an already-visible row remains stable and does not induce tail-follow again.
  </behavior>
  <action>
    Extend `TimelineList` behavior/tests only as needed to make D-05 and D-07 explicit under progressive row count changes. Keep the current browser-local `followTailRef`, bottom-threshold logic, and rAF scroll scheduling as the implementation base. Add virtualization tests that model item-count growth while off-tail and at-tail, asserting `scrollTop`/scroll calls remain stable or advance only in the correct follow state. Avoid broad styling or grouping changes.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/components/timeline/TimelineList.virt.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "progressive\\|follow\\|stable\\|bottom" packages/ui/src/components/timeline/TimelineList.virt.test.tsx` finds new viewport/follow coverage.
    - `grep -n "followTailRef\\|requestAnimationFrame" packages/ui/src/components/timeline/TimelineList.tsx` confirms viewport policy remains browser-local and scheduled.
    - The targeted virtualized timeline test command exits 0.
  </acceptance_criteria>
  <done>Partial-load row growth respects active inspection, while actual tail-follow stays smooth and remains a local timeline policy.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| streamed row growth -> persisted per-log UI state | Premature lifecycle assumptions can restore stale view state against incomplete rows. |
| streamed row growth -> virtualized timeline scroll state | High-volume row updates can wrest viewport ownership away from a user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06-01 | T | `usePersistEffect()` hydration trigger | mitigate | Hydrate only from explicit baseline completion, not first-row visibility, and keep index bounds checks. |
| T-22-06-02 | D | `TimelineList` follow-scroll effects | mitigate | Gate scroll-to-bottom on follow state and test off-tail progressive growth so interaction is not repeatedly displaced. |
</threat_model>

<verification>
Run `pnpm exec vitest run packages/ui/src/persistence/persist-effect.test.ts packages/ui/src/components/timeline/TimelineList.virt.test.tsx`.
</verification>

<success_criteria>
- Progressive loading no longer trips persistence hydration early.
- Stable inspection and tail-follow behavior are separately tested under row-count growth.
- Existing Phase 4 persistence/tail-follow design remains recognizable and intact.
</success_criteria>

<output>
After completion, create `.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-06-SUMMARY.md`.
</output>
