---
phase: 22-improve-large-log-loading-and-high-throughput-live-tail-perf
plan: 04
type: execute
wave: 4
depends_on: ["22-03"]
files_modified:
  - packages/ui/src/state/store.ts
  - packages/ui/src/state/store.test.ts
  - packages/ui/src/transport/sse-client.ts
  - packages/ui/src/transport/sse-client.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Snapshot rows become visible before snapshot-end per D-01 and remain normal selectable rows for later UI consumers per D-04."
    - "Live append and patch bursts are drained in bounded scheduled batches rather than forcing one immediate full-store write per frame, preserving interaction responsiveness per D-02 and D-06 while rows-first metadata settlement remains valid per D-09."
    - "The UI store tracks explicit baseline load and transport backlog state for rendering by later plans, including the compact lag cue required by D-08."
  artifacts:
    - path: "packages/ui/src/state/store.ts"
      provides: "Rows/progress/backlog state and bounded batch actions"
    - path: "packages/ui/src/transport/sse-client.ts"
      provides: "Progressive snapshot/live drain scheduler over EventSource frames"
  key_links:
    - from: "packages/ui/src/transport/sse-client.ts"
      to: "packages/ui/src/state/store.ts"
      via: "queued snapshot/append/patch/progress drains"
      pattern: "requestAnimationFrame|schedule.*drain|drain"
---

<objective>
Turn the browser stream bridge from single-baseline buffering into progressive, bounded store publication with explicit load/backlog state.

Purpose: Satisfy D-01, D-02, D-04, D-06, D-08, and D-09 at the browser state boundary before adding presentational UI.
Output: Store actions/state for loading and backlog plus an SSE client scheduler that publishes rows and delayed metadata responsively.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-RESEARCH.md
@packages/ui/src/state/store.ts
@packages/ui/src/transport/sse-client.ts
@packages/ui/src/transport/sse-client.test.ts

<interfaces>
Plan 22-03 extends the server stream with compact progress/backlog frames beside existing snapshot/append/patch frames. The client must parse those frames into store state without putting progress metadata inside `EventRow`.
The existing store has `rows`, live-pause `pendingBuffer`, `pendingNewCount`, and append/patch actions. Preserve manual pause semantics while adding performance-driven backlog state separately.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add explicit load/backlog state and batch-friendly store actions</name>
  <files>packages/ui/src/state/store.ts, packages/ui/src/state/store.test.ts</files>
  <read_first>
    - packages/ui/src/state/store.ts
    - packages/ui/src/state/store.test.ts
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-RESEARCH.md
  </read_first>
  <behavior>
    - Store state represents baseline loading/completion, loaded rows/bytes, optional percent, and separate stream backlog counts.
    - Visible row publication can append snapshot chunks progressively without abusing `setRows()` as a final-only signal.
    - Manual live-pause `pendingBuffer` behavior stays separate from performance backlog state.
  </behavior>
  <action>
    Extend `AppStoreState` with a typed load lifecycle and a compact transport backlog shape consumed by later UI work. Add batch-oriented store actions for progressive snapshot row commits, progress updates, backlog updates, and completion/reset while preserving existing `appendRows`, `applyPatch`, live-pause buffering, rotation reset, and log-switch reset semantics. Keep meta counts correct but avoid adding row-count-based percentage fabrication. Add focused store tests covering progressive rows, progress percentage optionality, backlog separation from `pendingNewCount`, and reset behavior.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/state/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "load.*progress\\|baseline\\|backlog" packages/ui/src/state/store.ts packages/ui/src/state/store.test.ts` finds the new explicit lifecycle state and tests.
    - `grep -n "pendingNewCount" packages/ui/src/state/store.test.ts` still finds manual live-pause coverage, demonstrating backlog state did not replace it.
    - The targeted store test command exits 0.
  </acceptance_criteria>
  <done>The store can represent progressive baseline loading and temporary transport lag independently from the existing manual live-pause feature.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace hidden snapshot buffering with a scheduled progressive SSE drain</name>
  <files>packages/ui/src/transport/sse-client.ts, packages/ui/src/transport/sse-client.test.ts</files>
  <read_first>
    - packages/ui/src/transport/sse-client.ts
    - packages/ui/src/transport/sse-client.test.ts
    - packages/ui/src/state/store.ts
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-RESEARCH.md
  </read_first>
  <behavior>
    - Snapshot chunks become store-visible before `snapshot-end` per D-01/D-04.
    - Append, patch, progress, and backlog frames drain in deterministic bounded publication steps around render-friendly scheduling.
    - Snapshot completion still flips connection to connected and closes/reset paths clear queued work.
  </behavior>
  <action>
    Refactor `connectLogStream()` to stop concatenating an invisible `snapshotRows` array until `snapshot-end`. Queue incoming snapshot, append, patch, progress, and backlog frames, then drain them through a bounded scheduler using `requestAnimationFrame` or an equivalent interaction-friendly browser boundary documented in research. User-visible actions remain urgent; only streamed publication is batched. Update FakeEventSource tests so rows MUST appear mid-snapshot, progress/backlog frames map to store state, burst frames coalesce without reordering appends before relevant patches, and rotation/log-reset/close clear pending drain state.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/transport/sse-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MUST NOT appear" packages/ui/src/transport/sse-client.test.ts` returns no matches; the obsolete hidden-baseline contract is removed.
    - `grep -n "requestAnimationFrame\\|schedule.*drain\\|drain" packages/ui/src/transport/sse-client.ts` finds the bounded scheduler implementation.
    - `grep -n "progress\\|backlog\\|mid-snapshot" packages/ui/src/transport/sse-client.test.ts` finds new event-to-store assertions.
    - The targeted SSE client test command exits 0.
  </acceptance_criteria>
  <done>The browser stream bridge publishes useful rows during baseline load, handles progress/backlog frames, and smooths live bursts without losing transport ordering.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local SSE EventSource -> browser state store | Malformed or high-volume frames can trigger expensive state churn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | D | SSE client drain queue | mitigate | Drain bounded queued frames on render-friendly scheduling, clear queues on reset/close, and surface backlog state instead of hiding lag. |
| T-22-04-02 | T | Progress parsing | mitigate | Accept typed optional percentages only from progress frames; do not synthesize browser-side percentages from row counts. |
| T-22-04-03 | D | Store pause/backlog separation | mitigate | Keep manual live-pause buffers independent from performance backlog counters so one lag source cannot corrupt the other control flow. |
</threat_model>

<verification>
Run `pnpm exec vitest run packages/ui/src/state/store.test.ts packages/ui/src/transport/sse-client.test.ts`. Confirm the hidden-baseline negative string is gone and new progress/backlog/mid-snapshot tests exist.
</verification>

<success_criteria>
- UI state becomes useful during baseline loading.
- Burst publication is scheduled and bounded rather than one synchronous mutation per frame.
- The store exposes enough lifecycle state for presentational progress/backlog surfaces without guessing.
</success_criteria>

<output>
After completion, create `.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-04-SUMMARY.md`.
</output>
