---
phase: 22-improve-large-log-loading-and-high-throughput-live-tail-perf
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - packages/server/src/app-state.ts
  - packages/server/src/app-state.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "AppState emits the same append and patch semantics without scanning every earlier projected row after each append, protecting responsiveness during heavy live bursts per D-06."
    - "Timeout flush patches update only rows reported by the correlator's changed-index contract."
    - "Large append sequences retain compact row payloads and avoid replay/state inflation while allowing derived metadata to settle after row visibility per D-09."
  artifacts:
    - path: "packages/server/src/app-state.ts"
      provides: "Targeted patch projection based on changed indexes"
    - path: "packages/server/src/app-state.test.ts"
      provides: "Regression/perf-shaped coverage for late-pair patches and bounded patch work"
  key_links:
    - from: "packages/server/src/app-state.ts"
      to: "packages/core/src/correlator.ts"
      via: "changed-index drain after store append and after flush"
      pattern: "drainChangedIndexes|takeChangedIndexes"
---

<objective>
Replace AppState's broad historical patch rediscovery loops with targeted changed-index projection while preserving the existing append-then-patch transport contract.

Purpose: Deliver the server-side performance win identified by research before progressive transport increases publication frequency.
Output: AppState patch generation that scales with changed rows, plus tests guarding late-pair, timeout, and compact-payload behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-RESEARCH.md
@packages/server/src/app-state.ts
@packages/server/src/app-state.test.ts
@packages/core/src/correlator.ts

<interfaces>
`AppState` currently emits `append` rows for newly projected rows and `patch` updates shaped as `{ idx, status, latencyMs, latencyBand, summary?, pairIdx }`.
Plan 22-01 provides a correlator changed-index drain method. Consume that method here after append correlation runs and after `runFlush()` calls; preserve the existing `SsePayload` patch shape.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Project append-time patches from correlator changed indexes</name>
  <files>packages/server/src/app-state.ts, packages/server/src/app-state.test.ts</files>
  <read_first>
    - packages/server/src/app-state.ts
    - packages/server/src/app-state.test.ts
    - packages/core/src/correlator.ts
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-RESEARCH.md
  </read_first>
  <behavior>
    - A late response still emits an append for the response row and a patch for the prior request row.
    - A duplicate/displaced correlation path patches only affected prior rows.
    - AppState no longer loops from `0` to `range.from` to discover changed rows.
  </behavior>
  <action>
    Refactor the store subscription in `createAppState()` so new rows are projected exactly as today, then prior-row patch updates are built only from the correlator changed-index drain API delivered by Plan 22-01. Reuse a small local helper for projecting patch updates if it removes duplication with flush handling, but keep `EventRow` payloads and `SsePayload` shapes unchanged. Preserve row summaries and `pairIdx` metadata on patch updates. This task implements the server half of D-06 and D-09 by making delayed pair/status metadata cheap to settle.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/server/src/app-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "drainChangedIndexes\\|takeChangedIndexes" packages/server/src/app-state.ts` finds correlator changed-index consumption.
    - `grep -n "for (let i = 0; i < range.from; i++)" packages/server/src/app-state.ts` returns no matches.
    - Existing late-response assertions in `packages/server/src/app-state.test.ts` remain green, and new coverage asserts affected patch indexes without expecting unrelated history patches.
  </acceptance_criteria>
  <done>Append-time patch generation is keyed by exact changed indexes, late-pair behavior is unchanged to consumers, and the old history scan is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound flush-time patch work and add a perf-shaped regression guard</name>
  <files>packages/server/src/app-state.ts, packages/server/src/app-state.test.ts</files>
  <read_first>
    - packages/server/src/app-state.ts
    - packages/server/src/app-state.test.ts
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-VALIDATION.md
  </read_first>
  <behavior>
    - `runFlush()` patches timed-out pending rows without iterating over every stored row.
    - A fixture-like burst with many unaffected rows produces patch updates only for changed timeout/pair rows.
    - Snapshot and SSE row payloads remain free of replay resources, diagnostics, intents, cache, and state fields.
  </behavior>
  <action>
    Update `runFlush()` to consume the same changed-index drain contract instead of scanning `store.size()`. Add focused app-state coverage that creates a meaningful number of earlier unaffected rows, triggers one late pair or timeout, and asserts emitted patch count/index locality rather than relying on wall-clock microbenchmarks. Keep the existing no-replay-fields checks in place and extend them only where the new test path needs them.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/server/src/app-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "for (let i = 0; i < store.size(); i++)" packages/server/src/app-state.ts` returns no matches.
    - `grep -n "unaffected\\|bounded\\|patch count\\|patches" packages/server/src/app-state.test.ts` finds the new bounded-work regression coverage.
    - The targeted app-state test command exits 0.
  </acceptance_criteria>
  <done>Both append-time and flush-time patch production scale with changed rows, and tests guard the behavior that research identified as the primary server bottleneck.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed log traffic -> server projection fanout | Untrusted event volume and key patterns can force derived metadata updates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | D | `AppState` append/flush patch projection | mitigate | Consume exact changed indexes instead of rescanning historical rows on each append or flush. |
| T-22-02-02 | I | `SsePayload` rows/patches | mitigate | Preserve compact row/patch contracts and keep replay/detail state lazy, verified by existing no-replay-field assertions. |
</threat_model>

<verification>
Run `pnpm exec vitest run packages/server/src/app-state.test.ts`. Confirm both legacy full-scan loop strings are absent and targeted patch locality assertions are present.
</verification>

<success_criteria>
- AppState no longer performs the two history-wide patch rediscovery loops called out by research.
- Patch payloads remain semantically compatible with the current SSE/UI consumers.
- Tests encode behavior, not fragile elapsed-time thresholds.
</success_criteria>

<output>
After completion, create `.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-02-SUMMARY.md`.
</output>
