---
phase: 22-improve-large-log-loading-and-high-throughput-live-tail-perf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/correlator.ts
  - packages/core/src/correlator.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Correlation work exposes the exact historical row indexes whose pair/status metadata changed, supporting responsive metadata settlement during bursts per D-06 and D-09."
    - "Late responses, displaced requests/responses, and timeout flushes all report changed rows without requiring callers to inspect every prior row, preserving the rows-first derived-metadata path required by D-09."
  artifacts:
    - path: "packages/core/src/correlator.ts"
      provides: "Changed-index drain API beside the existing pair/status indexes"
    - path: "packages/core/src/correlator.test.ts"
      provides: "Unit coverage for changed-index reporting across pair, orphan, reset, and flush paths"
  key_links:
    - from: "packages/core/src/correlator.ts"
      to: "packages/server/src/app-state.ts"
      via: "public changed-index drain method consumed by Plan 22-02"
      pattern: "drainChangedIndexes|takeChangedIndexes"
---

<objective>
Expose precise changed-row locality from `Correlator` so Phase 22 can stop rediscovering pair/status patches by scanning projected history.

Purpose: Remove the root algorithmic pressure identified in research before changing transport or browser cadence.
Output: A tested correlator contract that returns only changed historical indexes after pairing, displacement, timeout flush, and reset flows.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-RESEARCH.md
@packages/core/src/correlator.ts
@packages/core/src/correlator.test.ts

<interfaces>
Current `Correlator` exposes `pairOf(idx)`, `latencyOf(idx)`, `statusOf(idx)`, `flush(nowMs, timeoutMs)`, and `reset()` while mutating `pairIdx`, `latencyMs`, and `status` arrays from `EventStore` append notifications.
Plan 22-02 will consume a new drain-style method from this class; the method name may be `drainChangedIndexes()` or an equivalent precise drain name, but it must return exact row indexes and clear its pending changed set after read.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add changed-index tracking for pairing and displacement mutations</name>
  <files>packages/core/src/correlator.ts, packages/core/src/correlator.test.ts</files>
  <read_first>
    - packages/core/src/correlator.ts
    - packages/core/src/correlator.test.ts
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-RESEARCH.md
  </read_first>
  <behavior>
    - Pairing a late response reports the earlier request index and any other already-visible row whose derived metadata changed.
    - Displacing a duplicate pending request or early response reports the displaced row index once.
    - Draining changed indexes returns a stable deduplicated set and leaves a second drain empty until another mutation happens.
  </behavior>
  <action>
    Implement an internal changed-index accumulator in `Correlator` and expose a public drain-style API for callers. Record indexes at the actual mutation sites in `#pair`, `#onRequest`, and `#onResponse`; do not infer changes by comparing full status arrays after the fact. Keep all existing JSON-RPC-safe correlation key behavior intact, preserve the current `pairOf`/`latencyOf`/`statusOf` API, and keep notifications/actions out of the correlation path as they are today. This is the core prerequisite for D-06 and D-09: rows can publish quickly while derived metadata settles through targeted patches.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/core/src/correlator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "drainChangedIndexes\\|takeChangedIndexes" packages/core/src/correlator.ts` prints the public drain API.
    - `grep -n "late response\\|displaced\\|changed" packages/core/src/correlator.test.ts` finds new assertions that exercise targeted indexes, not whole-array snapshots only.
    - The targeted correlator test command exits 0.
  </acceptance_criteria>
  <done>`Correlator` reports exact changed indexes for pair and displacement mutations, tests prove drain semantics, and no existing correlation result contract regresses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover timeout flush and reset semantics for the changed-index drain</name>
  <files>packages/core/src/correlator.ts, packages/core/src/correlator.test.ts</files>
  <read_first>
    - packages/core/src/correlator.ts
    - packages/core/src/correlator.test.ts
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-VALIDATION.md
  </read_first>
  <behavior>
    - `flush()` reports only requests whose status flips from pending to unmatched.
    - `reset()` clears pending changed-index state so rows from a rotated log cannot leak into a replacement log.
    - Repeated flushes without new timeouts produce no changed indexes.
  </behavior>
  <action>
    Extend the changed-index accumulator to the `flush()` unmatched-status transition and clear it inside `reset()`. Keep `flush()` O(pendingRequests) as documented; do not introduce a full store-size pass in core. Add tests that first drain append-time changes, then flush to isolate timeout-created changes, and finally reset before asserting the drain is empty.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/core/src/correlator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "reset.*changed\\|changed.*reset\\|flush" packages/core/src/correlator.test.ts` finds reset/flush coverage for the new contract.
    - `grep -n "for (let i = 0; i < this.#store" packages/core/src/correlator.ts` returns no matches; flush must remain pending-map bounded.
    - The targeted correlator test command exits 0.
  </acceptance_criteria>
  <done>Timeout and rotation/reset paths participate in changed-index reporting without growing the correlator into a history scanner.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed JSONL event -> correlator state | Untrusted or malformed log contents drive correlation-key churn and status mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | D | `Correlator` changed-index accumulator | mitigate | Use a deduplicated drain set and clear it after consumption so bursts cannot replay stale indexes indefinitely. |
| T-22-01-02 | T | `Correlator.reset()` | mitigate | Reset the changed-index accumulator with pending correlation maps so rotated-log indexes never patch replacement-log rows. |
</threat_model>

<verification>
Run `pnpm exec vitest run packages/core/src/correlator.test.ts`. Confirm the public changed-index drain API exists and that new tests cover pair, displacement, flush, and reset paths.
</verification>

<success_criteria>
- The correlator owns changed-row locality at mutation time.
- AppState no longer needs to discover affected indexes by inspecting correlator arrays in later plans.
- Drain semantics are deterministic, deduplicated, and reset-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-01-SUMMARY.md`.
</output>
