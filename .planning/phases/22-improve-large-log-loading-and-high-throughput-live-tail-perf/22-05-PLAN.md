---
phase: 22-improve-large-log-loading-and-high-throughput-live-tail-perf
plan: 05
type: execute
wave: 5
depends_on: ["22-04"]
files_modified:
  - packages/ui/src/components/states/LoadingState.tsx
  - packages/ui/src/components/states/states.test.tsx
  - packages/ui/src/components/shell/StreamBacklogPill.tsx
  - packages/ui/src/components/shell/StreamBacklogPill.test.tsx
  - packages/ui/src/components/timeline/TimelineRegion.tsx
  - packages/ui/src/components/timeline/TimelineRegion.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users see trustworthy percentage loading when available, and an honest indeterminate/loading-count state when it is not, per D-03."
    - "Already-loaded rows stay visible per D-01 and inspectable per D-04 while loading status remains present."
    - "Buffered live-tail lag is visible through a compact backlog cue distinct from manual pause/resume UI, per D-08."
  artifacts:
    - path: "packages/ui/src/components/states/LoadingState.tsx"
      provides: "Progress-aware initial empty loading presentation"
    - path: "packages/ui/src/components/shell/StreamBacklogPill.tsx"
      provides: "Compact transport backlog cue"
    - path: "packages/ui/src/components/timeline/TimelineRegion.tsx"
      provides: "Rows-plus-status composition during partial load and bursts"
  key_links:
    - from: "packages/ui/src/components/timeline/TimelineRegion.tsx"
      to: "packages/ui/src/state/store.ts"
      via: "load progress and backlog selectors"
      pattern: "load|progress|backlog"
---

<objective>
Render truthful progressive-loading and stream-backlog signals without taking the timeline away once useful rows exist.

Purpose: Convert the load/backlog state from Plan 22-04 into the specific user experience locked by D-01, D-03, D-04, and D-08.
Output: Progress-aware loading presentation, compact backlog pill, and TimelineRegion composition/tests for partial-load visibility.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-RESEARCH.md
@packages/ui/src/components/states/LoadingState.tsx
@packages/ui/src/components/timeline/TimelineRegion.tsx
@packages/ui/src/components/shell/NewEventsPill.tsx

<interfaces>
Plan 22-04 adds store state for baseline progress and performance backlog. `TimelineRegion` currently swaps the whole timeline to `LoadingState` only when connecting with zero rows, and it separately mounts `NewEventsPill` for manual live-pause backlog.
Keep those concepts distinct: `StreamBacklogPill` is for performance-driven temporary lag, `NewEventsPill` remains the click-to-resume manual pause affordance.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make loading copy truthful for percentage and indeterminate progress</name>
  <files>packages/ui/src/components/states/LoadingState.tsx, packages/ui/src/components/states/states.test.tsx</files>
  <read_first>
    - packages/ui/src/components/states/LoadingState.tsx
    - packages/ui/src/components/states/states.test.tsx
    - packages/ui/src/state/store.ts
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
  </read_first>
  <behavior>
    - LoadingState shows a percentage only when store progress includes a trustworthy percent.
    - Without a percentage, loading remains honest through row/byte or generic progress copy, not a fabricated numeric estimate.
    - Basename-only filename display is preserved.
  </behavior>
  <action>
    Extend `LoadingState` props to render the Plan 22-04 progress data while the timeline has no visible rows yet. Keep the existing spinner/filename posture but add percentage/count presentation that follows D-03 exactly: render percent when provided, omit it when not. Do not show absolute paths, technical implementation prose, or noisy feature explanation text. Add component tests for percentage present, percentage absent, and filename rendering.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/components/states/states.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "percent\\|%" packages/ui/src/components/states/LoadingState.tsx packages/ui/src/components/states/states.test.tsx` finds optional percentage rendering and tests.
    - `grep -n "Reading" packages/ui/src/components/states/LoadingState.tsx` confirms the existing basename-oriented loading copy remains anchored.
    - The targeted state component test command exits 0.
  </acceptance_criteria>
  <done>Empty-timeline loading communicates only trustworthy progress and remains consistent with existing local/private file display rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add compact stream backlog cue for performance-driven lag</name>
  <files>packages/ui/src/components/shell/StreamBacklogPill.tsx, packages/ui/src/components/shell/StreamBacklogPill.test.tsx</files>
  <read_first>
    - packages/ui/src/components/shell/NewEventsPill.tsx
    - packages/ui/src/components/shell/NewEventsPill.test.tsx
    - packages/ui/src/state/store.ts
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
  </read_first>
  <behavior>
    - A compact cue reports queued/buffered stream work while the viewer is briefly behind.
    - The cue is informational, not a resume button, and does not reuse manual live-pause semantics.
    - Large counts compact sensibly without causing width/layout thrash.
  </behavior>
  <action>
    Create a small `StreamBacklogPill` presentational component that follows existing tokenized pill styling patterns from `NewEventsPill` but communicates performance backlog only. Use the Plan 22-04 backlog shape and copy that stays compact, e.g. queued event count or a bounded `99+` count as existing pill precedent suggests. Do not conflate it with `pendingNewCount`, and do not add click behavior unless the store contract explicitly calls for one. Add focused UI tests for singular/plural or compact-count formatting and passive semantics.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/components/shell/StreamBacklogPill.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function StreamBacklogPill" packages/ui/src/components/shell/StreamBacklogPill.tsx` finds the new component.
    - `grep -n "pendingNewCount" packages/ui/src/components/shell/StreamBacklogPill.tsx` returns no matches.
    - The targeted backlog pill test command exits 0.
  </acceptance_criteria>
  <done>The UI has a compact, tokenized, non-interactive backlog status component dedicated to performance batching lag.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep TimelineRegion visible during partial loads and mount progress/backlog status</name>
  <files>packages/ui/src/components/timeline/TimelineRegion.tsx, packages/ui/src/components/timeline/TimelineRegion.test.tsx</files>
  <read_first>
    - packages/ui/src/components/timeline/TimelineRegion.tsx
    - packages/ui/src/components/timeline/TimelineRegion.test.tsx
    - packages/ui/src/components/states/LoadingState.tsx
    - packages/ui/src/components/shell/StreamBacklogPill.tsx
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-RESEARCH.md
  </read_first>
  <behavior>
    - With zero rows, TimelineRegion renders the progress-aware LoadingState.
    - Once rows exist, TimelineList remains mounted while load progress/backlog status is shown nearby, so selection/detail flows can operate before completion.
    - Existing filters/search controls remain usable over currently visible rows; the persistent loading cue makes partial-data context explicit without inventing a new search subsystem.
  </behavior>
  <action>
    Wire Plan 22-04 store progress/backlog fields into `TimelineRegion`. Keep the full-page `LoadingState` only for the zero-row loading case; when rows exist and baseline loading continues, keep `TimelineList` mounted and render a restrained inline progress status plus `StreamBacklogPill` when performance backlog is nonzero. Preserve `NewEventsPill` for manual paused-follow behavior. Tests must assert row-region visibility during loading, backlog pill separation from pause pill, and no regression to empty/no-results/disconnected conditions. This locks in D-01, D-04, D-08, and the planner discretion choice that current filters/search remain available over loaded rows while loading status exposes incompleteness.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/components/timeline/TimelineRegion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "StreamBacklogPill\\|LoadingState" packages/ui/src/components/timeline/TimelineRegion.tsx` finds both composition points.
    - `grep -n "partial\\|progress\\|backlog" packages/ui/src/components/timeline/TimelineRegion.test.tsx` finds coverage for the new partial-load/status contract.
    - The targeted TimelineRegion test command exits 0.
  </acceptance_criteria>
  <done>Users see rows as soon as practical, keep using row/search/filter inspection surfaces while more rows load, and receive visible compact progress/backlog cues.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser store progress/backlog state -> visible UI copy | Status text must not leak paths or misrepresent progress precision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05-01 | I | `LoadingState` and backlog status copy | mitigate | Render basename-safe existing filename only and keep progress payloads to counts/percentages, never paths or raw event content. |
| T-22-05-02 | T | Percentage presentation | mitigate | Render percentage only when the store receives an explicit server-derived percent; otherwise show honest non-percent progress. |
| T-22-05-03 | D | Backlog cue layout | mitigate | Use compact bounded count formatting and existing fixed pill styling conventions to avoid status copy resizing the timeline unpredictably. |
</threat_model>

<verification>
Run `pnpm exec vitest run packages/ui/src/components/states/states.test.tsx packages/ui/src/components/shell/StreamBacklogPill.test.tsx packages/ui/src/components/timeline/TimelineRegion.test.tsx`.
</verification>

<success_criteria>
- Progress presentation never lies.
- Loaded rows remain the primary experience during ongoing baseline work.
- Burst lag is visible and distinct from manual live pause.
</success_criteria>

<output>
After completion, create `.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-05-SUMMARY.md`.
</output>
