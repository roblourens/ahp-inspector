---
phase: 23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - packages/ui/src/styles/tokens.css
  - packages/ui/src/styles/global.css
  - packages/ui/src/styles/theme-tokens.test.ts
  - packages/ui/src/styles/reduced-motion-css.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Hacker's default identity becomes a bold whole-screen curved-glass CRT stack with dark tube framing, strong scanlines, glass shading, fine noise, phosphor glow, and edge-weighted RGB fringe per D-03, D-04, D-05: full analog stack, D-06: stylized-first but usable, D-07: edge-weighted chromatic/RGB separation, D-08, and D-14."
    - "The CRT surface feels ambient and restless through low-level drift plus occasional noticeable signal beats, without creating hover/scroll/focus-reactive effects per D-09, D-10, and D-12."
    - "`prefers-reduced-motion: reduce` keeps the static Hacker CRT identity while removing Phase 23 drift, jitter, and glitch animations per D-11."
    - "D-15: Hacker ships as one bold curated CRT treatment with no intensity slider, preset, or added control surface."
  artifacts:
    - path: "packages/ui/src/styles/tokens.css"
      provides: "Exact Hacker CRT tuning tokens with neutral Dark/Light defaults"
    - path: "packages/ui/src/styles/global.css"
      provides: "Tube frame, glass/noise/fringe overlays, signal-beat keyframes, and reduced-motion static fallback"
    - path: "packages/ui/src/styles/theme-tokens.test.ts"
      provides: "Static token contract for Hacker CRT values and Dark/Light neutral scope"
    - path: "packages/ui/src/styles/reduced-motion-css.test.ts"
      provides: "Animation-removal contract for the new CRT motion stack"
  key_links:
    - from: "packages/ui/src/styles/tokens.css"
      to: "packages/ui/src/styles/global.css"
      via: "named `--effect-crt-*` values consumed by Hacker-only CSS"
      pattern: "--effect-crt-(warp|frame|fringe|glitch|glass|jitter)"
    - from: "packages/ui/src/styles/global.css"
      to: "prefers-reduced-motion: reduce"
      via: "Phase 23 animation selectors zeroed to `animation: none`"
      pattern: "ahp-crt-(surface-drift|signal-beat|fringe-jitter)"
---

<objective>
Turn the placement proof into the curated Hacker CRT identity the user approved.

Purpose: D-03 through D-15, the UI-SPEC compositing contract, and the validation strategy all require concrete effect values, visible analog layers, and a static reduced-motion identity without new control surfaces.
Output: Hacker-only tokens, whole-screen analog compositing CSS, and CSS/token tests that encode the new contract.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
@.planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-CONTEXT.md
@.planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-RESEARCH.md
@.planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-PATTERNS.md
@.planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-UI-SPEC.md
@.planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-VALIDATION.md
@packages/ui/src/styles/tokens.css
@packages/ui/src/styles/global.css
@packages/ui/src/styles/theme-tokens.test.ts
@packages/ui/src/styles/reduced-motion-css.test.ts

<interfaces>
`tokens.css` already neutralizes Phase 5 effect tokens in Dark/Light and sets Hacker-only effect values in `[data-theme="hacker"]`; Phase 23 must follow that three-block pattern.
`global.css` already owns Hacker `body::before`, `body::after`, root glow, `ahp-scanline-drift`, `ahp-crt-pulse`, and a reduced-motion media block. Phase 23 replaces the restrained Hacker layer contract with explicit CRT selectors and keyframes while keeping decorative surfaces pointer-transparent.
The old `theme-tokens.test.ts` opacity caps (`0.14`, `0.1`, `0.06`) describe pre-Phase-23 restraint and must be replaced with assertions for the approved aggressive token values below.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish exact Hacker CRT tokens and token contract tests</name>
  <files>packages/ui/src/styles/tokens.css, packages/ui/src/styles/theme-tokens.test.ts</files>
  <read_first>
    - packages/ui/src/styles/tokens.css
    - packages/ui/src/styles/theme-tokens.test.ts
    - .planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-UI-SPEC.md
    - .planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-CONTEXT.md
  </read_first>
  <behavior>
    - Hacker exposes an exact CRT tuning surface, and every token is present in the shared token inventory.
    - Dark and Light set the new opacity/displacement/intensity tokens to neutral values so the CRT system cannot leak outside Hacker.
    - The old restrained-opacity ceiling assertions disappear, replaced by exact Phase 23 aggressive-value checks.
  </behavior>
  <action>
    Extend all three theme blocks with the Phase 23 CRT tokens. In Hacker, set exact values: `--effect-scanline-opacity: 0.22`, `--effect-grid-opacity: 0.12`, `--effect-noise-opacity: 0.085`, `--effect-crt-glass-opacity: 0.72`, `--effect-crt-frame-opacity: 0.94`, `--effect-crt-fringe-opacity: 0.34`, `--effect-crt-fringe-spread: 18px`, `--effect-crt-vignette-stop: 64%`, `--effect-crt-signal-jitter: 1.5px`, and `--effect-crt-glitch-boost: 1.08`. Preserve existing Hacker glow tokens and palette. In root/dark and light, define the same new tokens with neutral values: all opacity/spread/jitter tokens `0`, vignette stop `100%`, and glitch boost `1`. Update `REQUIRED_THEME_TOKENS` to include every `--effect-crt-*` token. Replace the stale Phase 5 ceiling test with exact `toBe(...)` assertions for Hacker `0.22`, `0.12`, `0.085`, `0.72`, `0.94`, `0.34`, `18`, `64`, `1.5`, and `1.08`, plus checks that Dark and Light keep `--effect-crt-frame-opacity: 0` and `--effect-crt-glitch-boost: 1`. Do not alter Dark/Light color palette tokens, add copy, or introduce configuration-token branches for presets.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/styles/theme-tokens.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n -- '--effect-crt-glass-opacity: 0.72\|--effect-crt-frame-opacity: 0.94\|--effect-crt-fringe-opacity: 0.34\|--effect-crt-fringe-spread: 18px\|--effect-crt-vignette-stop: 64%\|--effect-crt-signal-jitter: 1.5px\|--effect-crt-glitch-boost: 1.08' packages/ui/src/styles/tokens.css` finds the Hacker values.
    - `grep -n -- '--effect-crt-glass-opacity\|--effect-crt-frame-opacity\|--effect-crt-fringe-opacity\|--effect-crt-fringe-spread\|--effect-crt-vignette-stop\|--effect-crt-signal-jitter\|--effect-crt-glitch-boost' packages/ui/src/styles/theme-tokens.test.ts` finds the new contract inventory/assertions.
    - `grep -n 'toBeLessThanOrEqual(0.14)\|toBeLessThanOrEqual(0.1)\|toBeLessThanOrEqual(0.06)' packages/ui/src/styles/theme-tokens.test.ts` returns no matches.
    - The focused theme token test command exits 0.
  </acceptance_criteria>
  <done>Phase 23 has one exact Hacker CRT tuning contract and explicit neutral Dark/Light defaults, encoded by unit tests rather than prose.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the tube-frame analog stack and static reduced-motion fallback</name>
  <files>packages/ui/src/styles/global.css, packages/ui/src/styles/reduced-motion-css.test.ts</files>
  <read_first>
    - packages/ui/src/styles/global.css
    - packages/ui/src/styles/reduced-motion-css.test.ts
    - .planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-RESEARCH.md
    - .planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-UI-SPEC.md
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
  </read_first>
  <behavior>
    - Hacker CSS visibly layers curved-screen enclosure, denser scanlines, glass/vignette shading, fine noise, and edge-weighted cyan/magenta fringe around the filtered display surface.
    - Motion remains ambient: `ahp-crt-surface-drift`, `ahp-crt-signal-beat`, and `ahp-crt-fringe-jitter` animate Hacker's surface/overlays on fixed cadences, not on hover, scrolling, focus, or selection events.
    - Reduced motion keeps the static Hacker selectors and tokenized opacity/frame values while setting Phase 23 animations and transitions to `none`.
  </behavior>
  <action>
    Evolve `global.css` around the existing Hacker section. Keep decorative layers `pointer-events: none`. Add `[data-theme="hacker"] .crt-display-surface` framing styles with `position: relative`, `overflow: clip`, `border-radius: 42px`, `box-shadow: inset 0 0 0 2px color-mix(in srgb, var(--color-accent) 26%, transparent), inset 0 0 140px color-mix(in srgb, var(--color-bg) 96%, transparent), 0 0 0 18px color-mix(in srgb, var(--color-bg) 92%, transparent)`, and `animation: ahp-crt-surface-drift 11s ease-in-out infinite`. Upgrade `body::before` to use 3px scanline spacing and `opacity: var(--effect-scanline-opacity)`. Upgrade `body::after` to combine the existing grid with a radial glass vignette stopping at `var(--effect-crt-vignette-stop)` plus a noise-like fine gradient, and animate it with `ahp-crt-signal-beat 7.2s steps(1, end) infinite`. Add pseudo-elements on `.crt-display-surface::before` and `::after`: `::before` is a dark tube/glass rim layer using `opacity: var(--effect-crt-frame-opacity)` and the tokenized glass opacity; `::after` creates edge-weighted fringe with cyan and magenta `radial-gradient`/`linear-gradient` edges, `mix-blend-mode: screen`, `opacity: var(--effect-crt-fringe-opacity)`, and `animation: ahp-crt-fringe-jitter 9s steps(2, end) infinite`. Define keyframes named exactly `ahp-crt-surface-drift`, `ahp-crt-signal-beat`, and `ahp-crt-fringe-jitter`; signal-beat must include an obvious jolt at `43%` using `transform: translateX(var(--effect-crt-signal-jitter)) scaleY(var(--effect-crt-glitch-boost))`. Extend the existing reduced-motion block so `body::before`, `body::after`, `.crt-display-surface`, `.crt-display-surface::before`, `.crt-display-surface::after`, `#root`, and selected rows have `animation: none` and `transition: none`. Do not add selectors reacting to `:hover`, scroll state, or focus for CRT animation.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/styles/reduced-motion-css.test.ts packages/ui/src/styles/theme-tokens.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'border-radius: 42px\|0 0 0 18px\|ahp-crt-surface-drift 11s\|ahp-crt-signal-beat 7.2s\|ahp-crt-fringe-jitter 9s' packages/ui/src/styles/global.css` finds the framed surface and ambient cadences.
    - `grep -n 'var(--effect-crt-frame-opacity)\|var(--effect-crt-fringe-opacity)\|var(--effect-crt-vignette-stop)\|var(--effect-crt-glitch-boost)' packages/ui/src/styles/global.css` finds token consumption.
    - `grep -n '43%\|translateX(var(--effect-crt-signal-jitter)) scaleY(var(--effect-crt-glitch-boost))' packages/ui/src/styles/global.css` finds the intentional occasional jolt.
    - `grep -n 'ahp-crt-surface-drift\|ahp-crt-signal-beat\|ahp-crt-fringe-jitter\|crt-display-surface::before\|crt-display-surface::after' packages/ui/src/styles/reduced-motion-css.test.ts` finds the reduced-motion coverage updates.
    - `grep -n 'crt.*:hover\|crt.*:focus\|crt.*scroll' packages/ui/src/styles/global.css` returns no matches.
    - The focused reduced-motion/token test command exits 0.
  </acceptance_criteria>
  <done>Hacker ships one aggressive analog visual system with static CRT identity under reduced motion and without a new interaction-reactive effects subsystem.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hacker effect tokens -> Dark/Light visual baseline | New global CSS/token names must not alter non-Hacker themes. |
| Animated CRT overlays -> Phase 22 responsiveness posture | Ambient visual motion must not become a new per-interaction or per-scroll work loop. |
| Decorative overlays -> usable controls | Tube/glass/fringe layers are visual only and must not consume pointer events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | T | `tokens.css` theme blocks | mitigate | Define every `--effect-crt-*` token in Dark/Light with neutral values and assert exact Hacker/non-Hacker contracts in `theme-tokens.test.ts`. |
| T-23-02-02 | D | CRT animation stack in `global.css` | mitigate | Keep cadence CSS-only, use fixed keyframes instead of JS scheduling, and remove every Phase 23 animation under `prefers-reduced-motion: reduce`. |
| T-23-02-03 | D | `.crt-display-surface::{before,after}` visual overlays | mitigate | Require `pointer-events: none` overlay selectors and preserve existing product control selectors unchanged. |
</threat_model>

<verification>
Run `pnpm exec vitest run packages/ui/src/styles/theme-tokens.test.ts packages/ui/src/styles/reduced-motion-css.test.ts packages/ui/src/theme/theme.test.ts`.
</verification>

<success_criteria>
- Hacker alone owns the tokenized CRT frame, glass, fringe, noise, and motion identity.
- The CSS includes exact frame, fringe, cadence, and signal-jolt values rather than leaving intensity unspecified.
- Reduced-motion tests prove Phase 23 animations are removed while the static visual identity stays represented by the Hacker selector/token system.
</success_criteria>

<output>
After completion, create `.planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-02-SUMMARY.md`.
</output>