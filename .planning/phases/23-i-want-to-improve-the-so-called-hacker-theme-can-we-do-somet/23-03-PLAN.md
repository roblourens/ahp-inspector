---
phase: 23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - e2e/phase23.spec.ts
  - packages/extension/src/__test__/webviewHtml.test.ts
  - screenshots/phase23/
autonomous: false
requirements: []
must_haves:
  truths:
    - "Fixture-only screenshots under `screenshots/phase23/` document the finished Hacker CRT at desktop, drawer, narrow-edge, ultrawide, and reduced-motion static states."
    - "Browser automation proves the finished CRT still supports the critical theme menu, timeline, search, drawer, picker, and no-path-leak flows after full analog layers land."
    - "The extension webview path proves D-16 by keeping local-only shared UI asset delivery and by inspecting the copied extension UI bundle for the same CRT display surface, SVG displacement filter, and Hacker compositing hooks exercised in standalone."
    - "The validation run includes the Phase 22 long fixture posture so CRT motion does not casually regress large-log/loading responsiveness expectations."
  artifacts:
    - path: "e2e/phase23.spec.ts"
      provides: "Completed fixture screenshot, reduced-motion, interaction, privacy, and responsiveness browser scenario"
    - path: "packages/extension/src/__test__/webviewHtml.test.ts"
      provides: "Webview HTML/CSP locality assertion plus extension UI bundle inspection for the real CRT SVG/filter/compositing surface"
    - path: "screenshots/phase23/"
      provides: "Committed fixture-only review screenshots"
  key_links:
    - from: "e2e/phase23.spec.ts"
      to: "screenshots/phase23/"
      via: "named screenshot outputs from safe fixture pages"
      pattern: "screenshots/phase23"
    - from: "packages/extension/src/__test__/webviewHtml.test.ts"
      to: "renderWebviewHtml + packages/extension/ui-dist/assets/*"
      via: "assert webview HTML stays local and the copied extension UI JS/CSS bundles retain the CRT display wrapper, SVG displacement filter, and Hacker compositing rule"
      pattern: "stylesheetUri|crt-display-surface|ahp-crt-warp|feDisplacementMap|filter"
---

<objective>
Turn the CRT implementation into reviewable fixture evidence and parity/responsiveness gates.

Purpose: D-13 through D-16, the UI-SPEC verification evidence contract, and Phase 22's responsiveness posture require browser-level proof after the visual stack is complete.
Output: Expanded Phase 23 Playwright coverage, safe saved screenshots, a focused webview parity contract test that inspects the copied CRT bundle surface, and a human review checkpoint for the final aesthetic/pointer-trust judgment.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
@.planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-06-SUMMARY.md
@.planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-CONTEXT.md
@.planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-RESEARCH.md
@.planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-UI-SPEC.md
@.planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-VALIDATION.md
@e2e/phase23.spec.ts
@packages/extension/src/__test__/webviewHtml.test.ts
@packages/extension/src/webviewHtml.ts

<interfaces>
Phase 23 verification must save screenshots only under `screenshots/phase23/` and only from committed `test/fixtures/*.jsonl` data, per repo instructions.
`webviewHtml.test.ts` already asserts CSP/connect-src and stylesheet/script URL behavior around `renderWebviewHtml`; Phase 23 should keep that local-asset guard and, per the resolved research parity gate, inspect the copied `packages/extension/ui-dist/assets/*` bundles after build for the CRT SVG/filter/compositing contract rather than introducing a separate extension visual runtime.
Phase 22's completion summary states viewport ownership and progressive-loading posture are lifecycle-tested. Phase 23's browser evidence should use `test/fixtures/long-realistic-ahp.jsonl` for a bounded long-fixture smoke, not invent a new performance architecture.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Expand Phase 23 fixture screenshots, reduced-motion proof, and long-fixture smoke</name>
  <files>e2e/phase23.spec.ts, screenshots/phase23/</files>
  <read_first>
    - e2e/phase23.spec.ts
    - e2e/phase5.spec.ts
    - .planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-UI-SPEC.md
    - .planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-VALIDATION.md
    - .planning/phases/22-improve-large-log-loading-and-high-throughput-live-tail-perf/22-CONTEXT.md
  </read_first>
  <behavior>
    - A named fast Playwright smoke path proves Hacker CRT activation, computed filter presence, ordinary clickability, and Dark-mode escape without waiting for screenshot capture.
    - Saved screenshots come from committed fixture JSONL only and land under exactly `screenshots/phase23/`.
    - The screenshot matrix captures `01-hacker-desktop-crt.png`, `02-hacker-drawer-crt.png`, `03-hacker-narrow-edge-crt.png`, `04-hacker-ultrawide-crt.png`, and `05-hacker-reduced-motion-static.png`.
    - Reduced-motion emulation proves Hacker remains selected and CRT selectors/tokens remain present while CSS animation on `.crt-display-surface` computes to `none`.
    - A long-fixture browser smoke opens `test/fixtures/long-realistic-ahp.jsonl`, waits for `[data-testid="row-0"]`, opens one row detail, and leaves `[data-testid="status-bar"]` visible without browser page errors.
  </behavior>
  <action>
    Extend `e2e/phase23.spec.ts` after the placement test from Plan 23-01. Preserve or split out a test title containing exactly `CRT smoke` that runs the fixture-backed activation and ordinary-click essentials without screenshot writes: Hacker selection, computed `ahp-crt-warp` filter presence, row/search/picker interaction, drawer close, and Dark-mode escape. This is the quick iteration signal. Create `SCREENSHOT_DIR = resolve("screenshots/phase23")` and ensure the directory exists with `mkdir(..., { recursive: true })`. Continue using `test/fixtures/phase4.1-real-shapes.safe.jsonl` for the heavier evidence test and visible CRT screenshot states: 1440x900 Hacker desktop full shell, 1366x768 detail drawer, 768x900 narrow edge-sensitive layout, and 2560x1440 ultrawide. Emulate reduced motion with Playwright `page.emulateMedia({ reducedMotion: "reduce" })`, keep Hacker selected, assert `getComputedStyle(document.querySelector('[data-testid="crt-display-surface"]')).animationName === "none"`, then save `05-hacker-reduced-motion-static.png`. Preserve/assert no-path-leak checks against `/Users/`, `/home/`, and Windows drive patterns. Add a second CLI-backed test or serial subflow opening committed `test/fixtures/long-realistic-ahp.jsonl`; assert `row-0`, detail panel/drawer as appropriate, and `status-bar` visibility, and collect page errors so the test fails on runtime exceptions. Do not save screenshots outside `screenshots/phase23/`, do not open real `~/.vscode-*` logs, and do not use external URL fixtures.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/phase23.spec.ts --grep "CRT smoke" && pnpm exec playwright test e2e/phase23.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'screenshots/phase23\|01-hacker-desktop-crt.png\|02-hacker-drawer-crt.png\|03-hacker-narrow-edge-crt.png\|04-hacker-ultrawide-crt.png\|05-hacker-reduced-motion-static.png' e2e/phase23.spec.ts` finds every required saved screenshot path.
    - `grep -n 'CRT smoke\|phase4.1-real-shapes.safe.jsonl\|long-realistic-ahp.jsonl\|reducedMotion: "reduce"\|animationName.*none\|status-bar' e2e/phase23.spec.ts` finds quick-smoke naming, fixture, comfort, and Phase 22 posture assertions.
    - `grep -n '/Users/\|/home/\|A-Za-z' e2e/phase23.spec.ts` finds retained no-path-leak guards.
    - `find screenshots/phase23 -maxdepth 1 -name '*.png' | wc -l` prints at least `5` after the Playwright command.
    - The quick `--grep "CRT smoke"` Playwright command exits 0 before the full screenshot/evidence command exits 0.
  </acceptance_criteria>
  <done>The finished CRT stack has a fast fixture smoke signal plus fixture-only screenshot evidence, reduced-motion static proof, privacy guards, and a bounded long-fixture browser smoke.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove extension webview CRT surface parity through local HTML and copied bundle inspection</name>
  <files>packages/extension/src/__test__/webviewHtml.test.ts</files>
  <read_first>
    - packages/extension/src/__test__/webviewHtml.test.ts
    - packages/extension/src/webviewHtml.ts
    - .planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-CONTEXT.md
    - .planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-UI-SPEC.md
  </read_first>
  <behavior>
    - Extension HTML keeps exactly the supplied local `stylesheetUri`, which is the shared UI CSS delivery path for the Hacker CRT.
    - Rendered webview HTML contains no remote `http://` or `https://` asset/filter URL beyond the existing explicit localhost loopback test case.
    - After the extension copies the UI bundle, the copied `packages/extension/ui-dist/assets/` CSS proves the Hacker compositing route still names `.crt-display-surface` and `ahp-crt-warp`, and the copied JS proves the mounted shared UI bundle still carries `crt-filter-defs`, `crt-display-surface`, `ahp-crt-warp`, and `feDisplacementMap`.
    - Existing CSP/connect-src/api-base XSS assertions remain intact.
  </behavior>
  <action>
    Add one focused HTML-locality test to `webviewHtml.test.ts` using `baseOpts` that renders default webview HTML and asserts the emitted `<link>`/stylesheet reference contains exactly `vscode-webview://abc/main.css`. In the same test, assert the default HTML does not match remote URL substrings `https://` or `http://` and does not contain `url(https://` or `url(http://`; this guards against an external CRT texture/filter-map path while leaving the already-separate localhost `loopbackOrigin` test untouched. Add a second parity test in the same file that asynchronously reads the built copied assets from `packages/extension/ui-dist/assets/` after `pnpm -F @ahp-inspector/ui build && pnpm -F @ahp-inspector/extension build`: locate the emitted CSS and JS files by extension, then assert the CSS includes the Hacker compositing route for `.crt-display-surface` plus `ahp-crt-warp`, and assert the JS includes `crt-filter-defs`, `crt-display-surface`, `ahp-crt-warp`, and `feDisplacementMap`. This directly inspects the SVG/filter/compositing surface that the extension webview actually receives through its copied bundle, satisfying D-16's resolved parity gate alongside the standalone rendered Playwright proof from Plan 23-01/Task 1. Keep `renderWebviewHtml` production code unchanged unless the test reveals a genuine current parity violation; do not create a forked webview-only CRT implementation.
  </action>
  <verify>
    <automated>pnpm -F @ahp-inspector/ui build && pnpm -F @ahp-inspector/extension build && pnpm exec vitest run packages/extension/src/__test__/webviewHtml.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'vscode-webview://abc/main.css\|url(https://\|url(http://\|https://\|http://' packages/extension/src/__test__/webviewHtml.test.ts` finds the new stylesheet/locality assertions and the pre-existing localhost-specific lines.
    - `grep -n 'ui-dist/assets\|crt-filter-defs\|crt-display-surface\|ahp-crt-warp\|feDisplacementMap' packages/extension/src/__test__/webviewHtml.test.ts` finds the extension bundle parity hook for the actual CRT SVG/filter/compositing surface.
    - `grep -n 'renderWebviewHtml\|loopbackOrigin widens connect-src CSP\|apiBaseUrl XSS payload is escaped' packages/extension/src/__test__/webviewHtml.test.ts` confirms the old security tests remain present.
    - The focused extension webview test command exits 0.
  </acceptance_criteria>
  <done>The extension webview parity contract proves both local asset delivery and the copied bundle's real CRT SVG/filter/compositing surface, with no planned outbound CRT asset dependency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review fixture CRT intensity and pointer honesty</name>
  <files>screenshots/phase23/</files>
  <read_first>
    - screenshots/phase23/01-hacker-desktop-crt.png
    - screenshots/phase23/02-hacker-drawer-crt.png
    - screenshots/phase23/03-hacker-narrow-edge-crt.png
    - screenshots/phase23/04-hacker-ultrawide-crt.png
    - screenshots/phase23/05-hacker-reduced-motion-static.png
    - .planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-UI-SPEC.md
  </read_first>
  <action>
    Pause after automated verification and ask for a fixture-only visual sign-off. The review scope is exact: the saved screenshots must visibly show full-screen curved CRT treatment, dark tube edge/frame, scan/noise/glass/fringe material identity, reduced-motion static Hacker continuity, and no incoherent clipping at drawer/narrow/ultrawide edges; then manually try Theme picker, a visible timeline row, search field, and drawer close once in the running fixture app to judge whether visual displacement feels honest enough for use.
  </action>
  <what-built>Phase 23 fast CRT smoke, fixture screenshot matrix, interaction automation, reduced-motion static CRT proof, long-fixture smoke, and extension webview copied-bundle CRT parity test.</what-built>
  <how-to-verify>
    1. Review `screenshots/phase23/01-hacker-desktop-crt.png` through `05-hacker-reduced-motion-static.png` and confirm the specific CRT material cues above are visibly present.
    2. In the fixture browser flow created by `pnpm exec playwright test e2e/phase23.spec.ts --headed` or the executor's equivalent open fixture app, use Theme picker, click one row, focus the search field, and close the responsive drawer.
    3. Confirm the warp is aggressive but controls do not look materially detached from their click targets.
  </how-to-verify>
  <verify>
    <automated>pnpm exec playwright test e2e/phase23.spec.ts --grep "CRT smoke" && pnpm exec playwright test e2e/phase23.spec.ts && pnpm -F @ahp-inspector/ui build && pnpm -F @ahp-inspector/extension build && pnpm exec vitest run packages/extension/src/__test__/webviewHtml.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `find screenshots/phase23 -maxdepth 1 -name '*.png' | wc -l` prints at least `5` before review begins.
    - `grep -n '01-hacker-desktop-crt.png\|05-hacker-reduced-motion-static.png' e2e/phase23.spec.ts` finds the reviewed evidence producers.
    - Human resume signal is exactly `approved` or a concrete list of screenshot/pointer issues to fix.
  </acceptance_criteria>
  <done>Fixture screenshots and live fixture interaction receive explicit human approval, or the checkpoint returns concrete CRT intensity, clipping, or pointer-honesty revisions for the executor to address.</done>
  <resume-signal>Type `approved` or describe the exact CRT intensity, clipping, or pointer-honesty issues that should be revised.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Saved verification evidence -> local log privacy | Screenshots must never be captured from real user AHP logs. |
| Shared UI CSS/SVG -> extension webview HTML/copied bundle | CRT delivery must keep the existing local bundled stylesheet route, not introduce remote assets, and the extension's copied bundle must retain the same CRT surface contract. |
| Visual approval -> product usability | Automated clickability is necessary but not sufficient to judge obvious paint/hit target divergence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | I | `screenshots/phase23/` evidence | mitigate | Drive every saved screenshot from committed `test/fixtures/*.jsonl`, assert path-leak patterns remain absent, and forbid real `~/.vscode-*` log paths in the E2E task. |
| T-23-03-02 | I / T | Webview CRT asset delivery and parity | mitigate | Test the existing `vscode-webview://abc/main.css` local stylesheet path, reject remote URL/filter-map strings in default rendered HTML, build the extension bundle, and inspect copied JS/CSS for `crt-filter-defs`, `crt-display-surface`, `ahp-crt-warp`, `feDisplacementMap`, and the Hacker compositing rule. |
| T-23-03-03 | D | Final CRT warp usability | mitigate | Combine ordinary-click browser interactions with a blocking human screenshot/pointer-honesty review before declaring the phase visually approved. |
</threat_model>

<verification>
Run `pnpm exec playwright test e2e/phase23.spec.ts --grep "CRT smoke"` for the quick browser signal, then run `pnpm -F @ahp-inspector/ui test && pnpm exec playwright test e2e/phase23.spec.ts && pnpm -F @ahp-inspector/ui build && pnpm -F @ahp-inspector/extension build && pnpm exec vitest run packages/extension/src/__test__/webviewHtml.test.ts` for the full evidence/parity gate.
</verification>

<success_criteria>
- Five named safe-fixture CRT screenshots exist in `screenshots/phase23/` and include reduced-motion evidence.
- The completed Phase 23 E2E provides a fast CRT smoke result first, then validates interaction, privacy, screenshot matrix, and bounded long-fixture posture.
- Extension HTML parity stays tied to the shared bundled local stylesheet path, and the copied extension UI bundle proves the CRT SVG/filter/compositing surface arrives in the webview delivery path with no remote CRT asset escape hatch.
- Human verification explicitly approves or records concrete visual/pointer issues after seeing the fixture evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/23-i-want-to-improve-the-so-called-hacker-theme-can-we-do-somet/23-03-SUMMARY.md`.
</output>