{
  "description": "session/configChanged with schema only replaces schema and leaves values untouched",
  "reducer": "session",
  "initial": {
    "summary": {
      "resource": "copilot:/test",
      "provider": "copilot",
      "title": "Test",
      "status": 1,
      "createdAt": 1000,
      "modifiedAt": 2000
    },
    "config": {
      "schema": {
        "type": "object",
        "properties": {
          "target": { "type": "string", "title": "Target", "enum": ["workspace", "worktree"] }
        }
      },
      "values": { "target": "worktree" }
    },
    "lifecycle": "ready",
    "turns": []
  },
  "actions": [
    {
      "type": "session/configChanged",
      "session": "copilot:/test",
      "schema": {
        "type": "object",
        "properties": {
          "target": { "type": "string", "title": "Target", "enum": ["workspace", "worktree"] },
          "baseBranch": { "type": "string", "title": "Base Branch", "enum": ["main", "develop"] }
        },
        "required": ["baseBranch"]
      }
    }
  ],
  "expected": {
    "summary": {
      "resource": "copilot:/test",
      "provider": "copilot",
      "title": "Test",
      "status": 1,
      "createdAt": 1000,
      "modifiedAt": 9999
    },
    "config": {
      "schema": {
        "type": "object",
        "properties": {
          "target": { "type": "string", "title": "Target", "enum": ["workspace", "worktree"] },
          "baseBranch": { "type": "string", "title": "Base Branch", "enum": ["main", "develop"] }
        },
        "required": ["baseBranch"]
      },
      "values": { "target": "worktree" }
    },
    "lifecycle": "ready",
    "turns": []
  }
}
