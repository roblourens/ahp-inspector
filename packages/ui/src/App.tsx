import type { JSX } from "react";

// Plan 02-02 Task 1: shell components and store wiring not yet mounted.
// Replaced with <AppShell /> in Task 2 once shell components exist.
// This stub keeps the build green between Task 1 and Task 2 commits.
export function App(): JSX.Element {
  return <div data-testid="app-root">AHP Log Viewer</div>;
}
