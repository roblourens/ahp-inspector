/**
 * Detail component barrel export (Plan 03-04).
 *
 * Only DetailPanel is exported — sub-components are internal to this directory.
 * AppShell imports DetailPanel from here.
 */
export { DetailPanel } from "./DetailPanel.js";
