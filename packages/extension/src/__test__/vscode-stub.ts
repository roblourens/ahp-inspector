// Stub `vscode` module used by Vitest when resolving the extension package's
// imports. Real behavior is supplied per-test via `vi.mock("vscode", ...)`.
// This file exists only so Vite's import resolver can find a target.
export {};
